// FLOW: CommentList — issue comment list (stub for Task 1, full implementation in Task 3)
"use client";

import { observer } from "mobx-react";

type Props = {
  issueId: string;
  workspaceId: string;
  projectId: string;
};

export const CommentList = observer(function CommentList(_props: Props) {
  return null;
});
