// FLOW: ActivityLog — issue activity log (stub for Task 1, full implementation in Task 4)
"use client";

import { observer } from "mobx-react";

type Props = {
  issueId: string;
};

export const ActivityLog = observer(function ActivityLog(_props: Props) {
  return null;
});
