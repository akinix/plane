// FLOW: CommentInput — issue comment creation input (stub for Task 1, full implementation in Task 3)
"use client";

import { observer } from "mobx-react";

type Props = {
  issueId: string;
};

export const CommentInput = observer(function CommentInput(_props: Props) {
  return null;
});
