// Intentionally empty: Project.Contracts is NOT a module (no FshModule attribute).
// Contracts projects expose DTOs/interfaces for cross-module consumption only.
// Compare with Modules.Project/AssemblyInfo.cs which DOES register FshModule — that is the
// implementation module, not its Contracts twin.
