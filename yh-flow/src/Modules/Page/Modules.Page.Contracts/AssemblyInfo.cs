// Intentionally empty: Page.Contracts is NOT a module (no FshModule attribute).
// Contracts projects expose DTOs/interfaces for cross-module consumption only.
// Compare with Modules.Page/AssemblyInfo.cs which DOES register FshModule — that is the
// implementation module, not its Contracts twin.