---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified: []
autonomous: false
requirements:
  - REQ-1.2
  - REQ-1.3
  - REQ-1.4
  - NFR-1
  - NFR-2

must_haves:
  truths:
    - "DbMigrator 成功执行所有模块的迁移，包括 APIToken 和 OAuthProviderSettings 新表（D-12, D-13, D-14, D-15）"
    - "完整解决方案编译零错误零警告"
    - "所有 Phase 1 测试通过（认证、格式适配、OAuth 框架）"
    - "API 服务可启动并响应健康检查"
  artifacts:
    - path: "yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Identity/"
      provides: "EF Core 迁移文件（包含 APIToken 和 OAuthProviderSettings 表）"
  key_links:
    - from: "DbMigrator"
      to: "IdentityDbContext"
      via: "IdentityDbInitializer.MigrateAsync"
      pattern: "MigrateAsync"
    - from: "迁移文件"
      to: "APITokenConfiguration"
      via: "EF Core model snapshot"
      pattern: "ApiTokens"
    - from: "迁移文件"
      to: "OAuthProviderSettingsConfiguration"
      via: "EF Core model snapshot"
      pattern: "OAuthProviderSettings"
---

<objective>
[BLOCKING] 执行数据库 Schema Push，将所有新增实体（APIToken、OAuthProviderSettings）的迁移应用到 PostgreSQL 数据库。然后执行完整 Phase 1 验证。

此任务是 Phase 1 的必需步骤 — 根据 schema_push_requirement，没有数据库迁移，类型检查会通过但产生假阳性验证状态（类型来自 DbContext 配置而非实际数据库）。

Purpose: 确保所有 EF Core 实体配置正确映射到数据库表，迁移管道端到端工作。
Output: 数据库迁移已应用 + 全阶段验证通过 + Phase 1 完成。
</output>
</objective>

<execution_context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Data/IdentityDbContext.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Data/Configurations/APITokenConfiguration.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Data/Configurations/OAuthProviderSettingsConfiguration.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 EF Core 迁移</name>
  <files>
    yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/
  </files>
  <read_first>
    yh-flow/src/Modules/Identity/Modules.Identity/Data/IdentityDbContext.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Data/Configurations/APITokenConfiguration.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Data/Configurations/OAuthProviderSettingsConfiguration.cs,
    yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
  </read_first>
  <behavior>
    - 编译验证: dotnet build 整个解决方案零错误
    - 迁移文件包含 APIToken 表的创建
    - 迁移文件包含 OAuthProviderSettings 表的创建
  </behavior>
  <action>
    1. 执行完整解决方案编译: `dotnet build yh-flow/YH.Flow.slnx` — 确认零错误
    2. 检查是否存在待生成的迁移: 查看 `yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/` 目录下是否有包含 APIToken 和 OAuthProviderSettings 的迁移文件
    3. 如果没有迁移文件，使用 dotnet ef 工具生成:
       ```
       cd yh-flow
       dotnet ef migrations add AddAPITokenAndOAuthProviderSettings \
         --project src/Host/YH.Flow.Migrations.PostgreSQL/ \
         --startup-project src/Host/YH.Flow.Api/ \
         --context IdentityDbContext
       ```
    4. 检查生成的迁移文件确认:
       - 包含 `migrationBuilder.CreateTable` 用于 `ApiTokens` 表（在 `identity` schema 下）
       - 包含 `migrationBuilder.CreateTable` 用于 `OAuthProviderSettings` 表（在 `identity` schema 下）
       - TokenHash 列有唯一索引
       - ProviderName 列有唯一索引
       - APIToken 表包含 TenantId 列（IHasTenant 多租户隔离）
       - OAuthProviderSettings 表不包含 TenantId 列（IGlobalEntity）
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build YH.Flow.slnx && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build yh-flow/YH.Flow.slnx` 零错误零警告
    - 迁移文件存在于 `yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/` 目录
    - 迁移文件中包含 `identity."ApiTokens"` 表的 CREATE 语句
    - 迁移文件中包含 `identity."OAuthProviderSettings"` 表的 CREATE 语句
    - APIToken 表包含 TenantId 列（多租户隔离）
    - OAuthProviderSettings 表不包含 TenantId 列（全局实体）
    - TokenHash 列有唯一索引
    - ProviderName 列有唯一索引
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] 执行数据库迁移（Schema Push）</name>
  <what-built>
    Plan 01-04 已完成所有代码实现:
    - 3 个认证方案注册（JWT + API Key + Session Cookie + PolicyScheme 路由器）
    - 2 个新领域实体（APIToken + OAuthProviderSettings）+ EF 配置
    - OAuth Provider 框架（IOAuthProvider + Registry + SettingsService）
    - Plane 兼容认证端点（/auth/sign-in, sign-up, sign-out, me）
    - OAuth 发起/回调端点（/auth/oauth/{provider}, callback）
    - API Token CRUD 端点（/auth/api-tokens）
    - OAuth Provider 管理端点
    - Plane 格式适配（错误格式 + 分页格式）
    - EF Core 迁移文件已生成

    现在需要通过 DbMigrator 将迁移应用到数据库。
  </what-built>
  <how-to-verify>
    **前提条件:** Docker/Aspire 基础设施运行中（PostgreSQL 容器可达）。

    **执行步骤:**
    1. 确保 Aspire 基础设施已启动（PostgreSQL 容器运行中）:
       ```bash
       cd yh-flow && dotnet run --project src/Host/YH.Flow.AppHost/
       ```
       或者如果 PostgreSQL 已通过其他方式运行，确保连接字符串可达。

    2. 运行 DbMigrator:
       ```bash
       cd yh-flow && dotnet run --project src/Host/YH.Flow.DbMigrator/
       ```
       DbMigrator 启动时自动执行所有模块迁移（非交互式，无提示）。

    3. 验证迁移输出:
       - 控制台应显示 `[tenant-catalog] already at head` 或 `applying N migration(s)`
       - 控制台应显示 `[root] migrating...` 和 `[root] done`
       - 控制台应显示 `[migrator] finished successfully.`

    4. （可选）直接验证数据库:
       ```bash
       # 连接到 PostgreSQL 检查表是否存在
       psql -h localhost -U postgres -d yhflow-db -c "\dt identity.*"
       # 应看到 identity."ApiTokens" 和 identity."OAuthProviderSettings"
       ```

    **预期结果:** DbMigrator 退出码 0，迁移成功应用。
  </how-to-verify>
  <resume-signal>迁移完成后，输入 "migrated" 继续验证。</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 全阶段验证 — 编译 + 测试套件</name>
  <files>
    N/A — 纯验证任务，无文件修改
  </files>
  <read_first>
    yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
  </read_first>
  <behavior>
    - 完整解决方案编译零错误
    - 所有 Identity.Tests 通过
    - 所有 Architecture.Tests 通过
    - 所有其他模块测试通过
  </behavior>
  <action>
    1. 执行完整解决方案编译:
       ```bash
       cd yh-flow && dotnet build YH.Flow.slnx
       ```
       预期: 0 errors, 0 warnings

    2. 执行所有测试:
       ```bash
       cd yh-flow && dotnet test src/Tests/ --no-build
       ```
       预期: 所有测试通过（包括新增的 ApiKeyAuthentication, SessionCookieAuthentication, MultiSchemeSelector, APIToken, OAuthProviderSettings, OAuthProvider, PlaneSignIn, OAuthInitiate, ApiTokenService, PlaneFormat 测试）

    3. 执行架构测试（如存在）:
       ```bash
       cd yh-flow && dotnet test src/Tests/Architecture.Tests/ --no-build
       ```
       预期: 架构测试通过（模块边界、依赖规则等）

    4. 验证关键决策覆盖:
       - D-01: 多 Scheme 自动协商 — 检查 PolicyScheme 注册
       - D-02: 双发 JWT + Cookie — 检查 PlaneSignInEndpoint
       - D-03: API Key 数据库存储 + 多 Key — 检查 APIToken 实体 + ApiTokenService
       - D-04: 复用 FSH 端点 + 适配格式 — 检查 /auth/* 端点
       - D-05: Phase 1 OAuth 框架 — 检查 IOAuthProvider + Registry
       - D-07: OAuth Provider 数据库动态管理 — 检查 OAuthProviderSettings + CRUD
       - D-09: FSH 四层限流 — 检查 RateLimitingOptions.Enabled = true
       - D-12: 模块独立 Schema — 检查 IdentityModuleConstants.SchemaName = "identity"

    5. 验证 T1.11 — Scalar/OpenAPI 文档可达性:
       ```bash
       cd yh-flow && dotnet run --project src/Host/YH.Flow.Api/ --no-build &
       sleep 10
       curl -sf http://localhost:5000/scalar/v1 -o /dev/null && echo "Scalar UI: OK" || echo "Scalar UI: FAIL"
       curl -sf http://localhost:5000/swagger/v1/swagger.json -o /dev/null && echo "OpenAPI JSON: OK" || echo "OpenAPI JSON: FAIL"
       kill %1 2>/dev/null
       ```
       预期: Scalar UI 和 OpenAPI JSON 端点均可达

    6. 验证 T1.12 — Serilog 结构化日志 + OpenTelemetry:
       - 检查 `Program.cs` 或 `appsettings.json` 中 Serilog 配置存在（WriteTo.Console / WriteTo.OpenTelemetry 等）
       - 检查 OpenTelemetry 服务注册: `services.AddOpenTelemetry()` 包含 tracing + metrics
       - 启动 API 并发送一个请求，确认控制台输出结构化日志（JSON 格式，非纯文本）
       - 确认 `X-Request-Id` 或 `traceId` 字段出现在日志中

    7. 验证 Security Headers 配置:
       - 检查 `Program.cs` 中是否配置了安全 Headers 中间件（如 NetEscapades.AspNetCore.SecurityHeaders 或自定义中间件）
       - 启动 API 并发送请求，检查响应 Headers 包含:
         ```bash
         curl -sI http://localhost:5000/health | grep -iE "X-Content-Type-Options|X-Frame-Options|Content-Security-Policy|Strict-Transport-Security|Referrer-Policy"
         ```
       - 预期: 至少包含 `X-Content-Type-Options: nosniff` 和 `X-Frame-Options: DENY`
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build YH.Flow.slnx && dotnet test src/Tests/ --no-build --verbosity normal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build yh-flow/YH.Flow.slnx` 输出 "Build succeeded" 且 0 errors 0 warnings
    - `dotnet test yh-flow/src/Tests/` 所有测试通过（Failed: 0）
    - 测试覆盖:
      - ApiKeyAuthentication 测试全部通过
      - SessionCookieAuthentication 测试全部通过
      - MultiSchemeSelector 测试全部通过
      - APIToken 领域测试全部通过
      - OAuthProviderSettings 领域测试全部通过
      - OAuthProvider 框架测试全部通过
      - PlaneSignIn 测试全部通过
      - ApiTokenService 测试全部通过
      - PlaneFormat 适配测试全部通过
    - 架构测试通过（模块边界、依赖规则）
    - T1.11: Scalar UI (`/scalar/v1`) 和 OpenAPI JSON (`/swagger/v1/swagger.json`) 端点可达
    - T1.12: Serilog 结构化日志配置存在，OpenTelemetry tracing + metrics 已注册
    - Security Headers: 响应至少包含 `X-Content-Type-Options: nosniff` 和 `X-Frame-Options: DENY`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DbMigrator → PostgreSQL | 迁移使用 elevated-DDL 连接字符串执行，需确保连接字符串权限正确 |
| 迁移文件 → Schema | 迁移文件定义了表的 Schema（identity），确保与 EF Configuration 一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Tampering | Migration files | accept | 迁移文件由 EF Core 工具生成，在 Git 版本控制下，不会被外部篡改 |
| T-01-21 | Information Disclosure | Database connection string | mitigate | DbMigrator 使用环境变量或配置文件中的连接字符串，不硬编码在源码中 |
| T-01-SC | Tampering | NuGet package supply chain | accept | Phase 1 不安装新 NuGet 包，全部使用 Phase 0 已验证的包 |
</threat_model>

<verification>
- `dotnet build yh-flow/YH.Flow.slnx` 零错误零警告
- `dotnet test yh-flow/src/Tests/` 所有测试通过
- DbMigrator 成功执行（退出码 0）
- 数据库中存在 `identity."ApiTokens"` 和 `identity."OAuthProviderSettings"` 表
- 所有 15 个 LOCKED 决策（D-01 ~ D-15）均有对应实现
</verification>

<success_criteria>
- 数据库迁移成功应用（DbMigrator 退出码 0）
- 完整解决方案编译零错误零警告
- 所有 Phase 1 测试通过
- 架构测试通过
- Phase 1 目标达成: 认证体系（JWT + API Key + Session Cookie + OAuth 框架）、API 基础设施（异常处理、分页、CORS、Rate Limiting）、数据库迁移管道、多租户基础全部就绪
</success_criteria>

<output>
创建 `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
