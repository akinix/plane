---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/IOAuthProvider.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthProviderRegistry.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthInitiateEndpoint.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthCallbackEndpoint.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/AuthEndpoints.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignIn/PlaneSignInEndpoint.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignUp/PlaneSignUpEndpoint.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignOut/PlaneSignOutEndpoint.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/Me/PlaneMeEndpoint.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/Services/OAuthProviderSettingsService.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/OAuth/OAuthInitiateQuery.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/OAuth/OAuthCallbackCommand.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/Auth/PlaneSignInCommand.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/Auth/PlaneSignUpCommand.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/PlaneAuthResponse.cs
  - yh-flow/src/Tests/Identity.Tests/Authorization/OAuthProviderFrameworkTests.cs
autonomous: true
requirements:
  - REQ-1.3

must_haves:
  truths:
    - "IOAuthProvider 接口定义 ProviderName, GetAuthUrl, ExchangeCodeAsync 三个成员"
    - "OAuthProviderRegistry 从数据库加载启用的 Provider 配置到内存缓存"
    - "GET /auth/oauth/{provider} 重定向到 Provider 的 OAuth 授权 URL"
    - "POST /auth/oauth/{provider}/callback 处理回调，双发 JWT + Session Cookie（D-02, D-06）"
    - "POST /auth/sign-in 复用 FSH GenerateTokenCommand 并双发 JWT + Cookie（D-02, D-04）"
    - "POST /auth/sign-up 复用 FSH RegisterUserCommand 并自动登录（D-04）"
    - "POST /auth/sign-out 清除 Session Cookie + 撤销 RefreshToken（D-04）"
    - "GET /auth/me 返回当前用户信息（D-04）"
    - "认证端点使用 .RequireRateLimiting(\"auth\") 策略（D-09）"
  artifacts:
    - path: "yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/IOAuthProvider.cs"
      provides: "OAuth Provider 抽象接口"
      contains: "interface IOAuthProvider"
    - path: "yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthProviderRegistry.cs"
      provides: "OAuth Provider 注册表"
      contains: "class OAuthProviderRegistry"
    - path: "yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/AuthEndpoints.cs"
      provides: "Plane 兼容认证端点路由注册"
      contains: "MapPlaneAuthEndpoints"
    - path: "yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignIn/PlaneSignInEndpoint.cs"
      provides: "Plane 兼容登录端点"
      contains: "PlaneSignInEndpoint"
  key_links:
    - from: "PlaneSignInEndpoint"
      to: "GenerateTokenCommand"
      via: "复用 FSH Identity 认证逻辑"
      pattern: "IMediator.*Send.*GenerateTokenCommand"
    - from: "OAuthCallbackEndpoint"
      to: "OAuthProviderRegistry"
      via: "获取 Provider 实例处理 code 交换"
      pattern: "OAuthProviderRegistry.*GetProvider"
    - from: "AuthEndpoints"
      to: "IdentityModule.MapEndpoints"
      via: "endpoints.MapPlaneAuthEndpoints()"
      pattern: "MapPlaneAuthEndpoints"
    - from: "PlaneSignInEndpoint"
      to: "Session Cookie"
      via: "双发 JWT + Cookie (D-02)"
      pattern: "SignInAsync|Cookie.*SignIn"
---

<objective>
搭建 OAuth Provider 框架（D-05 仅抽象不实现具体 Provider）并创建 Plane 兼容认证端点（D-04）。包括 IOAuthProvider 抽象接口、OAuthProviderRegistry 注册表、OAuth 发起/回调端点，以及 /auth/sign-in、/auth/sign-up、/auth/sign-out、/auth/me 四个认证端点。

Purpose: 这是 Phase 1 认证体系的功能层——将 Plan 01 的多 Scheme 基础设施和 Plan 02 的实体通过具体端点暴露给客户端。OAuth 框架为 Phase 9 具体 Provider 实现预留扩展点。
Output: OAuth 框架 + 4 个 Plane 认证端点 + 双发 JWT+Cookie 逻辑 + IdentityModule 端点注册。
</objective>

<execution_context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Tokens/TokenGeneration/GenerateTokenEndpoint.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Tokens/TokenGeneration/GenerateTokenCommandHandler.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/RegisterUser/RegisterUserEndpoint.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Sessions/RevokeSession/RevokeSessionEndpoint.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/GetUserProfile/GetMeEndpoint.cs
@yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/ITokenService.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Authorization/ApiKey/ApiKeyAuthenticationDefaults.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Authorization/SessionCookie/SessionCookieAuthenticationDefaults.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Domain/OAuthProviderSettings.cs
@yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/IOAuthProviderSettingsService.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OAuth Provider 框架（接口 + 注册表 + 服务）</name>
  <files>
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/IOAuthProvider.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthUserInfo.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthProviderRegistry.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Services/OAuthProviderSettingsService.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs,
    yh-flow/src/Tests/Identity.Tests/Authorization/OAuthProviderFrameworkTests.cs
  </files>
  <read_first>
    yh-flow/src/Modules/Identity/Modules.Identity/Domain/OAuthProviderSettings.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/IOAuthProviderSettingsService.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/OAuthProviderSettingsDto.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Services/TokenService.cs
  </read_first>
  <behavior>
    - Test 1: IOAuthProvider 接口包含 ProviderName 属性、GetAuthUrl 方法、ExchangeCodeAsync 方法
    - Test 2: OAuthProviderRegistry.GetAvailableProviders() 返回空列表（Phase 1 无具体 Provider）
    - Test 3: OAuthProviderRegistry.GetProvider("github") 返回 null（Phase 1 未注册）
    - Test 4: OAuthProviderRegistry.InitializeAsync 从 IOAuthProviderSettingsService 加载配置
    - Test 5: OAuthProviderSettingsService.GetAllAsync 从 DbContext 查询所有配置
    - Test 6: OAuthProviderSettingsService.GetAllEnabledAsync 仅返回 Enabled=true 的配置
    - Test 7: OAuthProviderSettingsService.GetByProviderNameAsync 按 ProviderName 查询
  </behavior>
  <action>
    **创建 IOAuthProvider.cs:** 在 `Features/v1/OAuth/` 下。定义 OAuth Provider 抽象接口（D-05）：
    ```
    public interface IOAuthProvider
    {
        string ProviderName { get; }
        string GetAuthUrl(string state, string callbackUrl);
        Task<OAuthUserInfo> ExchangeCodeAsync(string code, string callbackUrl, CancellationToken ct);
    }
    ```

    **创建 OAuthUserInfo.cs:** 在 `Features/v1/OAuth/` 下：
    ```
    public sealed record OAuthUserInfo(
        string ProviderName,
        string ProviderId,
        string Email,
        string? Avatar,
        string? FirstName,
        string? LastName);
    ```

    **创建 OAuthProviderRegistry.cs:** 在 `Features/v1/OAuth/` 下。Singleton 服务：
    - 依赖注入: `IOAuthProviderSettingsService`, `IServiceScopeFactory`（用于创建 scope 查 DB）
    - `ConcurrentDictionary<string, OAuthProviderSettingsDto> _cachedSettings` — 内存缓存
    - `InitializeAsync(CancellationToken)` — 从 DB 加载所有 Enabled 配置到缓存
    - `GetProvider(string name)` — Phase 1 始终返回 null（无具体 Provider 实现）。Phase 9 将根据 ProviderName 实例化具体 Provider
    - `GetAvailableProviders()` — 返回缓存中的 Provider 名称列表
    - `GetSettings(string providerName)` — 返回缓存的配置（供端点使用）
    - `RefreshCacheAsync(CancellationToken)` — 重新从 DB 加载（配置变更时调用）

    **创建 OAuthProviderSettingsService.cs:** 在 `Services/` 下。实现 `IOAuthProviderSettingsService` 接口：
    - 依赖注入: `IdentityDbContext`, `ILogger<OAuthProviderSettingsService>`
    - 实现所有接口方法: GetAllAsync, GetAllEnabledAsync, GetByProviderNameAsync, CreateAsync, UpdateAsync, ToggleEnabledAsync, DeleteAsync
    - 每个写操作后调用 `OAuthProviderRegistry.RefreshCacheAsync()` 刷新缓存
    - 所有方法传递 CancellationToken（黄金法则 #7）

    **修改 IdentityModule.cs (ConfigureServices):** 添加服务注册：
    - `services.AddSingleton<OAuthProviderRegistry>();`
    - `services.AddScoped<IOAuthProviderSettingsService, OAuthProviderSettingsService>();`

    **测试文件:** `Tests/Identity.Tests/Authorization/OAuthProviderFrameworkTests.cs` — 使用 NSubstitute mock IOAuthProviderSettingsService 和 IdentityDbContext，验证 Registry 和 Service 行为。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Identity.Tests/ --filter "OAuthProvider" --no-restore && dotnet build src/Modules/Identity/Modules.Identity/ --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `IOAuthProvider` 接口定义 `ProviderName`, `GetAuthUrl(string state, string callbackUrl)`, `ExchangeCodeAsync(string code, string callbackUrl, CancellationToken ct)`
    - `OAuthProviderRegistry` 注册为 Singleton（`services.AddSingleton<OAuthProviderRegistry>()`）
    - `OAuthProviderRegistry.GetProvider()` 在 Phase 1 始终返回 null
    - `OAuthProviderRegistry.GetAvailableProviders()` 返回从 DB 加载的 Enabled 配置名称
    - `OAuthProviderSettingsService` 实现 `IOAuthProviderSettingsService` 接口的所有方法
    - 写操作后调用 `OAuthProviderRegistry.RefreshCacheAsync()`
    - `dotnet test src/Tests/Identity.Tests/ --filter "OAuthProvider"` 全部通过
    - `dotnet build src/Modules/Identity/Modules.Identity/` 零错误
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plane 兼容认证端点（/auth/sign-in, sign-up, sign-out, me）</name>
  <files>
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/AuthEndpoints.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignIn/PlaneSignInEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignUp/PlaneSignUpEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/SignOut/PlaneSignOutEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/Me/PlaneMeEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/Auth/PlaneSignInCommand.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/Auth/PlaneSignUpCommand.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/PlaneAuthResponse.cs,
    yh-flow/src/Tests/Identity.Tests/Handlers/PlaneSignInCommandHandlerTests.cs
  </files>
  <read_first>
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Tokens/TokenGeneration/GenerateTokenEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Tokens/TokenGeneration/GenerateTokenCommandHandler.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/RegisterUser/RegisterUserEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Sessions/RevokeSession/RevokeSessionEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/GetUserProfile/GetMeEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/Tokens/TokenGeneration/GenerateTokenCommand.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/TokenResponse.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/ITokenService.cs
  </read_first>
  <behavior>
    - Test 1: PlaneSignInCommandHandler 复用 GenerateTokenCommand 的验证逻辑
    - Test 2: 登录成功后响应包含 access_token, refresh_token, access_token_expires_at, user（Plane 格式）
    - Test 3: 登录成功后同时设置 Session Cookie（.YHFlow.Session）（D-02）
    - Test 4: 无效凭证返回 401 + Plane 错误格式 { error, error_code }
    - Test 5: PlaneSignUpCommandHandler 注册后自动登录（返回 token + cookie）
    - Test 6: PlaneSignOutEndpoint 清除 Session Cookie + 撤销 refresh token
    - Test 7: PlaneMeEndpoint 返回当前用户信息
  </behavior>
  <action>
    **创建 PlaneAuthResponse.cs:** 在 `Contracts/DTOs/` 下。Plane 兼容认证响应格式：
    ```
    public sealed record PlaneAuthResponse(
        string AccessToken,
        string RefreshToken,
        DateTime AccessTokenExpiresAt,
        PlaneUserProfile User);

    public sealed record PlaneUserProfile(
        string Id,
        string Email,
        string? FirstName,
        string? LastName,
        string? Avatar,
        bool IsEmailVerified);
    ```
    JSON 序列化时使用 snake_case（通过 `JsonPropertyName` 属性或全局 camelCase 策略）。

    **创建 PlaneSignInCommand.cs:** 在 `Contracts/v1/Auth/` 下：
    ```
    public record PlaneSignInCommand(string Email, string Password) : ICommand<PlaneAuthResponse>;
    ```

    **创建 PlaneSignInEndpoint.cs:** 在 `Features/v1/Auth/SignIn/` 下。参照 `GenerateTokenEndpoint.cs` 模式：
    - `MapPost("/auth/sign-in", ...)`
    - Handler 内部：复用 FSH `IIdentityService.ValidateCredentialsAsync` 验证凭证
    - 成功后：通过 `ITokenService.IssueAsync` 生成 JWT，同时通过 `HttpContext.SignInAsync(CookieSchemeName, claimsPrincipal)` 设置 Session Cookie（D-02 双发）
    - 返回 `PlaneAuthResponse`
    - `.AllowAnonymous().RequireRateLimiting("auth")`
    - 认证端点使用 `[AllowAnonymous]`，但需要在 Cookie 认证方案中排除（Cookie scheme 仅对有 Cookie 的请求生效）

    **创建 PlaneSignUpCommand.cs:** 在 `Contracts/v1/Auth/` 下：
    ```
    public record PlaneSignUpCommand(string Email, string Password, string? FirstName, string? LastName) : ICommand<PlaneAuthResponse>;
    ```

    **创建 PlaneSignUpEndpoint.cs:** 在 `Features/v1/Auth/SignUp/` 下。参照 `RegisterUserEndpoint.cs`：
    - `MapPost("/auth/sign-up", ...)`
    - 复用 FSH `IUserRegistrationService` 创建用户
    - 创建成功后自动登录（同 sign-in 的双发逻辑）
    - 返回 `PlaneAuthResponse`
    - `.AllowAnonymous().RequireRateLimiting("auth")`

    **创建 PlaneSignOutEndpoint.cs:** 在 `Features/v1/Auth/SignOut/` 下：
    - `MapPost("/auth/sign-out", ...)`
    - `HttpContext.SignOutAsync(CookieSchemeName)` 清除 Cookie
    - 复用 FSH RevokeSession 逻辑撤销 refresh token
    - `.RequireAuthenticatedUser()`

    **创建 PlaneMeEndpoint.cs:** 在 `Features/v1/Auth/Me/` 下。参照 `GetMeEndpoint.cs`：
    - `MapGet("/auth/me", ...)`
    - 从 `ICurrentUser` 获取用户信息
    - 返回 `PlaneUserProfile`
    - `.RequireAuthenticatedUser()`

    **创建 AuthEndpoints.cs:** 在 `Features/v1/Auth/` 下。路由注册扩展方法：
    ```
    internal static class AuthEndpoints
    {
        public static void MapPlaneAuthEndpoints(this IEndpointRouteBuilder endpoints)
        {
            var auth = endpoints.MapGroup("/auth").WithTags("Authentication");
            auth.MapPlaneSignInEndpoint().AllowAnonymous().RequireRateLimiting("auth");
            auth.MapPlaneSignUpEndpoint().AllowAnonymous().RequireRateLimiting("auth");
            auth.MapPlaneSignOutEndpoint().RequireAuthenticatedUser();
            auth.MapPlaneMeEndpoint().RequireAuthenticatedUser();
        }
    }
    ```

    **测试文件:** `Tests/Identity.Tests/Handlers/PlaneSignInCommandHandlerTests.cs` — 使用 NSubstitute mock IIdentityService 和 ITokenService，验证登录流程。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Identity.Tests/ --filter "PlaneSignIn" --no-restore && dotnet build src/Modules/Identity/Modules.Identity/ --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `POST /auth/sign-in` 端点存在且标记 `[AllowAnonymous]` 和 `RequireRateLimiting("auth")`
    - `POST /auth/sign-up` 端点存在且标记 `[AllowAnonymous]` 和 `RequireRateLimiting("auth")`
    - `POST /auth/sign-out` 端点存在且要求认证
    - `GET /auth/me` 端点存在且要求认证
    - 登录成功响应格式为 `PlaneAuthResponse`（包含 AccessToken, RefreshToken, AccessTokenExpiresAt, User）
    - 登录成功后调用 `HttpContext.SignInAsync("SessionCookie", principal)` 设置 Cookie（D-02）
    - `AuthEndpoints.MapPlaneAuthEndpoints` 注册 `/auth` 路由组
    - `dotnet test src/Tests/Identity.Tests/ --filter "PlaneSignIn"` 全部通过
    - `dotnet build src/Modules/Identity/Modules.Identity/` 零错误
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OAuth 发起/回调端点 + IdentityModule 端点注册</name>
  <files>
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthInitiateEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthCallbackEndpoint.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/OAuth/OAuthCallbackCommand.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/AuthEndpoints.cs,
    yh-flow/src/Tests/Identity.Tests/Handlers/OAuthInitiateEndpointTests.cs
  </files>
  <read_first>
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/IOAuthProvider.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/OAuth/OAuthProviderRegistry.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Auth/AuthEndpoints.cs,
    yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/PlaneAuthResponse.cs,
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/ITokenService.cs
  </read_first>
  <behavior>
    - Test 1: GET /auth/oauth/{provider} — 未知 provider 返回 400
    - Test 2: GET /auth/oauth/{provider} — provider 存在但未启用返回 400
    - Test 3: GET /auth/oauth/{provider} — provider 存在且启用时返回 302 重定向到 auth URL
    - Test 4: POST /auth/oauth/{provider}/callback — 无 code 参数返回 400
    - Test 5: POST /auth/oauth/{provider}/callback — Phase 1 GetProvider 返回 null，返回 501 Not Implemented
    - Test 6: AuthEndpoints 注册了 /auth/oauth/{provider} 和 /auth/oauth/{provider}/callback 路由
    - Test 7: IdentityModule.MapEndpoints 调用了 endpoints.MapPlaneAuthEndpoints()
  </behavior>
  <action>
    **创建 OAuthCallbackCommand.cs:** 在 `Contracts/v1/OAuth/` 下：
    ```
    public record OAuthCallbackCommand(string Code, string? State, string? RedirectUri) : ICommand<PlaneAuthResponse>;
    ```

    **创建 OAuthInitiateEndpoint.cs:** 在 `Features/v1/OAuth/` 下：
    - `MapGet("/auth/oauth/{provider}", ...)`
    - 从 `OAuthProviderRegistry` 获取 Provider 配置
    - Provider 不存在或未启用 → 返回 400 `ProblemDetails`
    - Phase 1: `GetProvider(name)` 返回 null → 返回 501 `StatusCodeProblemDetails("OAuth provider not yet implemented in Phase 1")`
    - 如果未来 Provider 可用：生成 state → 构建 authUrl → 302 重定向
    - `.AllowAnonymous().RequireRateLimiting("auth")`

    **创建 OAuthCallbackEndpoint.cs:** 在 `Features/v1/OAuth/` 下：
    - `MapPost("/auth/oauth/{provider}/callback", ...)` 或 `MapGet` 取决于 Provider 回调方式
    - Phase 1: `GetProvider(name)` 返回 null → 返回 501
    - 未来实现逻辑（记录为注释）：
      1. ExchangeCodeAsync 获取 OAuthUserInfo
      2. 查找或创建用户（AutoCreateAccount 逻辑）
      3. 双发 JWT + Cookie（D-02, D-06）
      4. 重定向到前端 URL（`?token=xxx&refresh_token=yyy`）
    - `.AllowAnonymous().RequireRateLimiting("auth")`

    **修改 AuthEndpoints.cs:** 在 `MapPlaneAuthEndpoints` 中添加 OAuth 路由：
    ```
    auth.MapOAuthInitiateEndpoint().AllowAnonymous().RequireRateLimiting("auth");
    auth.MapOAuthCallbackEndpoint().AllowAnonymous().RequireRateLimiting("auth");
    ```

    **修改 IdentityModule.cs (MapEndpoints):** 在现有 `group.MapXxxEndpoint()` 调用之后，添加：
    ```
    // Plane-compatible auth endpoints (/auth/*)
    endpoints.MapPlaneAuthEndpoints();
    ```

    **测试文件:** `Tests/Identity.Tests/Handlers/OAuthInitiateEndpointTests.cs` — 使用 NSubstitute mock OAuthProviderRegistry，验证路由逻辑。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Identity.Tests/ --filter "OAuthInitiate" --no-restore && dotnet build src/Modules/Identity/Modules.Identity/ --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `GET /auth/oauth/{provider}` 端点存在且标记 `[AllowAnonymous]`
    - `POST /auth/oauth/{provider}/callback` 端点存在且标记 `[AllowAnonymous]`
    - Phase 1 中两个 OAuth 端点对未知/未实现 Provider 返回 501（不崩溃）
    - `IdentityModule.MapEndpoints` 包含 `endpoints.MapPlaneAuthEndpoints()` 调用
    - `AuthEndpoints.MapPlaneAuthEndpoints` 注册了 sign-in, sign-up, sign-out, me, oauth/{provider}, oauth/{provider}/callback 六个路由
    - 所有认证端点使用 `.RequireRateLimiting("auth")`（匿名端点）
    - `dotnet build src/Modules/Identity/Modules.Identity/` 零错误
    - `dotnet test src/Tests/Identity.Tests/ --filter "OAuthInitiate"` 全部通过
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth Provider → Callback Endpoint | 外部 OAuth Provider 重定向回 callback URL，code 参数不可信 |
| /auth/sign-in → Identity Service | 用户凭证通过 FSH Identity 验证，不自行处理密码比对 |
| Session Cookie → Client | Cookie 通过 HttpOnly + Secure + SameSite 保护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | OAuth Callback | mitigate | ASP.NET Core OAuth 内置 state 参数验证防 CSRF；Phase 1 端点返回 501 不处理实际回调 |
| T-01-11 | Spoofing | /auth/sign-in | mitigate | 凭证通过 FSH IIdentityService.ValidateCredentialsAsync 验证（使用 ASP.NET Identity PasswordHasher），不自行比对密码 |
| T-01-12 | Denial of Service | /auth/* endpoints | mitigate | 所有匿名端点使用 `.RequireRateLimiting("auth")`（10/60s per D-09）；ASP.NET Identity 账户锁定 5 次/15 分钟 |
| T-01-13 | Information Disclosure | PlaneAuthResponse | mitigate | 响应中不包含密码、内部 ID 等敏感信息；PlaneUserProfile 仅暴露必要字段 |
| T-01-14 | Spoofing | Session Cookie | mitigate | Cookie 配置 HttpOnly=true（防 XSS 窃取）、SecurePolicy=Always（仅 HTTPS）、SameSite=Lax（防 CSRF） |
</threat_model>

<verification>
- `dotnet build yh-flow/src/Modules/Identity/Modules.Identity/` 零错误零警告
- `dotnet test yh-flow/src/Tests/Identity.Tests/` 所有新增测试通过
- `IdentityModule.MapEndpoints` 包含 `endpoints.MapPlaneAuthEndpoints()`
- `/auth/sign-in`, `/auth/sign-up`, `/auth/sign-out`, `/auth/me`, `/auth/oauth/{provider}`, `/auth/oauth/{provider}/callback` 六个端点全部注册
- OAuth 端点在 Phase 1 对未实现的 Provider 返回 501
</verification>

<success_criteria>
- OAuth Provider 框架搭建完毕（IOAuthProvider + Registry + SettingsService）
- Phase 1 无具体 Provider 实现，框架可正确返回 501
- 4 个 Plane 认证端点（sign-in, sign-up, sign-out, me）工作正常
- 登录成功后双发 JWT + Session Cookie（D-02）
- 认证端点全部使用 "auth" Rate Limiting 策略
- IdentityModule 端点注册完整
</success_criteria>

<output>
创建 `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
