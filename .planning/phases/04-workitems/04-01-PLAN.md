---
phase: 04-workitems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Contracts project
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/Modules.WorkItems.Contracts.csproj
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/AssemblyInfo.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/WorkItemsConstants.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/StateDto.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/LabelDto.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/EstimateDto.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/EstimatePointDto.cs
  # Module project
  - src/Modules/WorkItems/Modules.WorkItems/Modules.WorkItems.csproj
  - src/Modules/WorkItems/Modules.WorkItems/AssemblyInfo.cs
  - src/Modules/WorkItems/Modules.WorkItems/WorkItemsModuleConstants.cs
  - src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/StateGroup.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/State.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/Label.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/Estimate.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/EstimatePoint.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/StateConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/LabelConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/EstimateConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/EstimatePointConfiguration.cs
  # Test project
  - src/Tests/WorkItems.Tests/WorkItems.Tests.csproj
  - src/Tests/WorkItems.Tests/GlobalUsings.cs
  - src/Tests/WorkItems.Tests/Usings.cs
  # Host wiring
  - src/YH.Flow.slnx
  - src/Host/YH.Flow.Api/Program.cs
  - src/Host/YH.Flow.DbMigrator/Program.cs
  - src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj
  # Migration files (generated by ef)
  - src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/YYYYMMDDHHMMSS_InitialWorkItems.cs
  - src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/YYYYMMDDHHMMSS_InitialWorkItems.Designer.cs
  - src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/WorkItemsDbContextModelSnapshot.cs
autonomous: true
requirements: [REQ-4.1, REQ-4.2, REQ-4.7]

must_haves:
  truths:
    - "WorkItems contracts and module projects compile independently"
    - "State entity implements IHasTenant with 5 Group enum values (Backlog/Unstarted/Started/Completed/Cancelled)"
    - "Label entity exists with Name, Color, ParentId supporting hierarchy"
    - "Estimate entity exists with EstimatePoint child entities"
    - "WorkItemsDbContext exists in yhschema.WorkItems schema with tenant isolation"
    - "EF migration creates States, Labels, Estimates, EstimatePoints tables"
    - "WorkItemsModule registered in host with Order 260"
    - "Test project compiles and references the module"
  artifacts:
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/State.cs"
      provides: "State entity with 5-group enum"
      contains: "StateGroup"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/Label.cs"
      provides: "Label entity with hierarchy support"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/Estimate.cs"
      provides: "Estimate aggregate"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs"
      provides: "WorkItems-scoped DbContext"
      contains: "yhschema.WorkItems"
    - path: "src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs"
      provides: "Module entry point with Order 260"
      contains: "IModule"
  key_links:
    - from: "src/Host/YH.Flow.Api/Program.cs"
      to: "Modules.WorkItems.WorkItemsModule"
      via: "FshModule auto-discovery"
      pattern: "WorkItemsModule"
    - from: "src/Host/YH.Flow.DbMigrator/Program.cs"
      to: "Modules.WorkItems.WorkItemsModule"
      via: "FshModule auto-discovery"
      pattern: "WorkItemsModule"
    - from: "Data/Configurations/StateConfiguration.cs"
      to: "TenantId"
      via: "BaseDbContext ApplyTenantIsolationByDefault"
      pattern: "HasFilter"
---

<objective>
**Phase 4 Wave 1 — Scaffold + Base Domain:** Create the WorkItems module project structure, contracts DTOs, test project, base domain entities (State, Label, Estimate, EstimatePoint), WorkItemsDbContext with EF configurations, and EF migration.

**Purpose:** Establish the full module skeleton and data model for all WorkItems entities. After this wave, the WorkItems module compiles with 0 errors and the base schema is migrated.

**Output:** Modules.WorkItems.Contracts + Modules.WorkItems + WorkItems.Tests projects; State/Label/Estimate/EstimatePoint domain entities; WorkItemsDbContext with 4 EF configurations; EF migration creating base tables in yhschema.WorkItems schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-workitems/04-CONTEXT.md
@.planning/phases/04-workitems/04-RESEARCH.md

# Phase 3 reference patterns (identical structure)

@yh-flow/src/Modules/Project/Modules.Project/ProjectModule.cs
@yh-flow/src/Modules/Project/Modules.Project/ProjectModuleConstants.cs
@yh-flow/src/Modules/Project/Modules.Project/AssemblyInfo.cs
@yh-flow/src/Modules/Project/Modules.Project/Domain/Project.cs
@yh-flow/src/Modules/Project/Modules.Project/Data/ProjectDbContext.cs
@yh-flow/src/Modules/Project/Modules.Project/Data/Configurations/ProjectConfiguration.cs
@yh-flow/src/Modules/Project/Modules.Project.Contracts/Constants/ProjectConstants.cs
@yh-flow/src/Modules/Project/Modules.Project.Contracts/DTOs/ProjectDto.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/Modules.Workspace.Contracts.csproj
@yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj
@yh-flow/src/YH.Flow.slnx
@yh-flow/src/Host/YH.Flow.Api/Program.cs
@yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
@yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Contracts project with DTOs and constants, Module project with skeleton, Test project, and host wiring</name>
  <files>
    src/Modules/WorkItems/Modules.WorkItems.Contracts/Modules.WorkItems.Contracts.csproj
    src/Modules/WorkItems/Modules.WorkItems.Contracts/AssemblyInfo.cs
    src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/WorkItemsConstants.cs
    src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/StateDto.cs
    src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/LabelDto.cs
    src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/EstimateDto.cs
    src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/EstimatePointDto.cs
    src/Modules/WorkItems/Modules.WorkItems/Modules.WorkItems.csproj
    src/Modules/WorkItems/Modules.WorkItems/AssemblyInfo.cs
    src/Modules/WorkItems/Modules.WorkItems/WorkItemsModuleConstants.cs
    src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
    src/Tests/WorkItems.Tests/WorkItems.Tests.csproj
    src/Tests/WorkItems.Tests/GlobalUsings.cs
    src/Tests/WorkItems.Tests/Usings.cs
    src/YH.Flow.slnx
    src/Host/YH.Flow.Api/Program.cs
    src/Host/YH.Flow.DbMigrator/Program.cs
    src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj
  </files>
  <read_first>
    @yh-flow/src/Modules/Project/Modules.Project.Contracts/Constants/ProjectConstants.cs
    @yh-flow/src/Modules/Project/Modules.Project.Contracts/DTOs/ProjectDto.cs
    @yh-flow/src/Modules/Project/Modules.Project/Modules.Project.csproj
    @yh-flow/src/Modules/Project/Modules.Project/AssemblyInfo.cs
    @yh-flow/src/Modules/Project/Modules.Project/ProjectModuleConstants.cs
    @yh-flow/src/Modules/Project/Modules.Project/ProjectModule.cs
    @yh-flow/src/Tests/Project.Tests/Project.Tests.csproj
    @yh-flow/src/Tests/Project.Tests/GlobalUsings.cs
    @yh-flow/src/Tests/Project.Tests/Usings.cs
    @yh-flow/src/YH.Flow.slnx
    @yh-flow/src/Host/YH.Flow.Api/Program.cs
    @yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
    @yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj
  </read_first>
  <action>
    Create the full project scaffold following Phase 3 Project module pattern exactly:

    **Modules.WorkItems.Contracts.csproj** — Follow Modules.Project.Contracts.csproj: RootNamespace YH.Modules.WorkItems.Contracts, NoWarn S2094;CA1010, FrameworkReference Microsoft.AspNetCore.App, ProjectReference to Modules.Identity.Contracts. No FshModule attribute.

    **AssemblyInfo.cs** (Contracts) — Intentionally empty comment file, same as Workspace.Contracts AssemblyInfo.

    **WorkItemsConstants.cs** — sealed class under YH.Modules.WorkItems.Contracts namespace: NameMaxLength (255), DescriptionMaxLength (10000), ColorMaxLength (7), PriorityMaxLength (10), IdentifierMaxLength (12), DefaultSortOrder (65535.0), ForbiddenCharsPattern regex from ProjectConstants.

    **StateDto.cs** — class under YH.Modules.WorkItems.Contracts.DTOs: Id (Guid), Name (string), Color (string?), Group (int — StateGroup int value), ProjectId (Guid), IsDefault (bool), SortOrder (double). JsonPropertyName attributes for Plane snake_case naming (created_at, updated_at).

    **LabelDto.cs** — Id (Guid), Name (string), Color (string?), ParentId (Guid?), ProjectId (Guid), SortOrder (double), CreatedAt, UpdatedAt.

    **EstimateDto.cs** — Id (Guid), Name (string), Type (string — "points"/"categories"), ProjectId (Guid), IsLastUsed (bool), EstimatePoints (List of EstimatePointDto?), CreatedAt, UpdatedAt.

    **EstimatePointDto.cs** — Id (Guid), EstimateId (Guid), Key (int), Value (string), SortOrder (double), CreatedAt, UpdatedAt.

    **Modules.WorkItems.csproj** — Follow Modules.Project.csproj: RootNamespace YH.Modules.WorkItems, same NoWarn list. FrameworkReference Microsoft.AspNetCore.App. ProjectReferences to BuildingBlocks (Caching, Core, Persistence, Web), Modules.WorkItems.Contracts, Modules.Identity.Contracts, Modules.Workspace.Contracts, Modules.Project.Contracts. NO direct Finbuckle references.

    **AssemblyInfo.cs** — [assembly: FshModule(typeof(YH.Modules.WorkItems.WorkItemsModule), 260)] — Order 260 per CONTEXT decision (after Project 250).

    **WorkItemsModuleConstants.cs** — implement IModuleConstants: ModuleId = "WorkItems", ModuleName = "WorkItems", ApiPrefix = "work-items". SchemaName = "yhschema.WorkItems" as const string.

    **WorkItemsModule.cs** — bare skeleton implementing IModule: ConfigureServices has TODO for AddHeroDbContext<WorkItemsDbContext> + health check. ConfigureMiddleware empty. MapEndpoints has TODO for work-items route group with ApiVersionSet.

    **WorkItems.Tests.csproj** — Follow Project.Tests.csproj: net10.0, IsPackable/Publishable false, same NoWarn. PackageReferences: coverlet.collector, Microsoft.NET.Test.Sdk, xunit, xunit.runner.visualstudio, Shouldly, AutoFixture, NSubstitute, Microsoft.EntityFrameworkCore.InMemory. ProjectReferences: Modules.WorkItems, Modules.WorkItems.Contracts.

    **GlobalUsings.cs** — global using Shouldly; global using Xunit;

    **Usings.cs** — Empty file.

    **slnx** — Add Folder /Modules/WorkItems/ with both csprojs, and Folder /Tests/ with WorkItems.Tests.csproj. Follow existing slnx XML format.

    **Program.cs** (Api) — Add typeof(YH.Modules.WorkItems.WorkItemsModule).Assembly to moduleAssemblies array. Add typeof(YH.Modules.WorkItems.Contracts.WorkItemsConstants) to Mediator o.Assemblies list.

    **Program.cs** (DbMigrator) — Same changes as API: moduleAssemblies + Mediator assemblies.

    **Migration csproj** — Add ProjectReference to ..\..\Modules\WorkItems\Modules.WorkItems\Modules.WorkItems.csproj. Add Folder include "WorkItems\".

    After writing all files, run dotnet build src/YH.Flow.slnx --nologo to verify all projects compile together.

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | findstr /V "warning CS" | findstr /C:"error" /C:"Error"</automated>
  </verify>
  <done>All new projects (Contracts, Module, Tests) compile with 0 errors. WorkItemsModule discovered via FshModule at Order 260. slnx updated with WorkItems folder entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create base domain entities (State + StateGroup enum, Label, Estimate, EstimatePoint) and WorkItemsDbContext with EF configurations</name>
  <files>
    src/Modules/WorkItems/Modules.WorkItems/Domain/StateGroup.cs
    src/Modules/WorkItems/Modules.WorkItems/Domain/State.cs
    src/Modules/WorkItems/Modules.WorkItems/Domain/Label.cs
    src/Modules/WorkItems/Modules.WorkItems/Domain/Estimate.cs
    src/Modules/WorkItems/Modules.WorkItems/Domain/EstimatePoint.cs
    src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
    src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/StateConfiguration.cs
    src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/LabelConfiguration.cs
    src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/EstimateConfiguration.cs
    src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/EstimatePointConfiguration.cs
  </files>
  <read_first>
    @yh-flow/src/Modules/Project/Modules.Project/Domain/Project.cs
    @yh-flow/src/Modules/Project/Modules.Project/Domain/ProjectMember.cs
    @yh-flow/src/Modules/Project/Modules.Project/Data/ProjectDbContext.cs
    @yh-flow/src/Modules/Project/Modules.Project/Data/Configurations/ProjectConfiguration.cs
  </read_first>
  <action>
    Create domain entities and data layer under YH.Modules.WorkItems.Domain and YH.Modules.WorkItems.Data namespaces:

    **StateGroup.cs** — Simple public enum:
    - Backlog = 0, Unstarted = 1, Started = 2, Completed = 3, Cancelled = 4

    **State.cs** — IHasTenant, ISoftDeletable, IAuditableEntity:
    - Id (Guid), Name (string, max 255), Color (string?, max 7), Group (StateGroup), ProjectId (Guid), IsDefault (bool), SortOrder (double, default 65535.0)
    - TenantId (string), audit fields (CreatedOnUtc, CreatedBy, LastModifiedOnUtc, LastModifiedBy), soft-delete (IsDeleted, DeletedOnUtc, DeletedBy)
    - Private empty ctor for EF. Static factory Create(name, color, group, projectId, isDefault, sortOrder). Method Update(name?, color?, sortOrder?) that updates LastModifiedOnUtc. Method SoftDelete(DateTimeOffset now).
    - Validation: name required non-empty. projectId required non-empty Guid. Group validated via switch.

    **Label.cs** — IHasTenant, ISoftDeletable, IAuditableEntity:
    - Id (Guid), Name (string, max 255), Color (string?, max 7), ParentId (Guid? — self-ref FK for hierarchy), ProjectId (Guid), Description (string?, max 1000), SortOrder (double, default 65535.0)
    - IHasTenant + audit + soft-delete same as State
    - Static factory Create(name, color, projectId, parentId?, sortOrder?). Method Update(name?, color?, parentId?, description?, sortOrder?). SoftDelete(DateTimeOffset now).

    **Estimate.cs** — IHasTenant, ISoftDeletable, IAuditableEntity (per Plane estimate.py):
    - Id (Guid), Name (string, max 255), Type (string — "points" or "categories"), ProjectId (Guid), IsLastUsed (bool)
    - Collection: List of EstimatePoint (EF navigation, private field, public IReadOnlyList accessor)
    - Entity framework initialization: private List&lt;EstimatePoint&gt; _estimatePoints = []
    - Static factory Create(name, type, projectId). Method Update(name?, type?). Method AddPoint(EstimatePoint point). Method RemovePoint(Guid pointId). Method MarkAsLastUsed(). SoftDelete(DateTimeOffset now).

    **EstimatePoint.cs** — IHasTenant, IAuditableEntity (NOT soft-deletable — cascade via Estimate):
    - Id (Guid), EstimateId (Guid — FK), Key (int, sort key), Value (string, display "1"/"2"/"S"/"M"/"L"), SortOrder (double defaults 65535), Estimate (navigation)
    - audit fields only (no ISoftDeletable)
    - Static factory Create(estimateId, key, value, sortOrder). Method UpdateValue(string value, int? key).

    **WorkItemsDbContext.cs** — Follow ProjectDbContext pattern EXACTLY (per RESEARCH Pitfall 8):
    - Derive from BaseDbContext with same constructor signature (IMultiTenantContextAccessor&lt;AppTenantInfo&gt;, DbContextOptions, IOptions&lt;DatabaseOptions&gt;, IHostEnvironment)
    - DbSets: States, Labels, Estimates, EstimatePoints, Issues (TODO), IssueAssignees (TODO), IssueLabels (TODO), IssueLinks (TODO), IssueComments (TODO), IssueActivities (TODO), IntakeIssues (TODO)
    - OnModelCreating: ApplyConfigurationsFromAssembly FIRST, then base.OnModelCreating LAST (critical order per RESEARCH Pitfall 6/8)
    - Schema via WorkItemsModuleConstants.SchemaName

    **StateConfiguration.cs** — IEntityTypeConfiguration&lt;State&gt;:
    - ToTable "States", schema WorkItemsModuleConstants.SchemaName. Key Id.
    - Name: IsRequired, HasMaxLength(255). Color: HasMaxLength(7). Group: HasConversion&lt;int&gt;. SortOrder: HasDefaultValue(65535.0).
    - Unique index on (TenantId, ProjectId, Name) — state names unique per project. HasFilter("[DeletedOnUtc] IS NULL").
    - Query index on (ProjectId, Group) for state listing by group.
    - Query index on (ProjectId, IsDefault) for default state lookup.
    - Audit columns: CreatedOnUtc IsRequired, CreatedBy/LastModifiedBy/DeletedBy HasMaxLength(450).

    **LabelConfiguration.cs** — IEntityTypeConfiguration&lt;Label&gt;:
    - ToTable "Labels", schema WorkItemsModuleConstants.SchemaName. Key Id.
    - Name: IsRequired, HasMaxLength(255). Color: HasMaxLength(7). ParentId: optional (no FK cascade — self-referencing FK with OnDelete(SetNull) to prevent orphan issues). Description: HasMaxLength(1000). SortOrder: HasDefaultValue(65535.0).
    - Unique index on (TenantId, ProjectId, Name) — label names unique per project. HasFilter("[DeletedOnUtc] IS NULL").
    - Index on (ProjectId, ParentId) for hierarchical queries.

    **EstimateConfiguration.cs** — ToTable "Estimates", schema. Key Id.
    - Name: IsRequired, HasMaxLength(255). Type: IsRequired, HasMaxLength(20). ProjectId: IsRequired.
    - HasMany(e => e.EstimatePoints).WithOne(ep => ep.Estimate).HasForeignKey(ep => ep.EstimateId).OnDelete(DeleteBehavior.Cascade).
    - Index on (TenantId, ProjectId). Index on (ProjectId, IsLastUsed).

    **EstimatePointConfiguration.cs** — ToTable "EstimatePoints", schema. Key Id.
    - EstimateId: IsRequired. Key: IsRequired. Value: IsRequired, HasMaxLength(50). SortOrder: HasDefaultValue(65535.0).
    - Unique index on (EstimateId, Key). Index on (EstimateId, SortOrder).

    Build after creation: dotnet build src/Modules/WorkItems/Modules.WorkItems/Modules.WorkItems.csproj

  </action>
  <verify>
    <automated>dotnet build src/Modules/WorkItems/Modules.WorkItems/Modules.WorkItems.csproj --nologo 2>&1 | findstr /V "warning CS" | findstr /C:"error" /C:"Error"</automated>
  </verify>
  <done>All 5 domain entities + WorkItemsDbContext + 4 configurations compile with 0 errors. StateGroup enum has 5 values. State/Label have IHasTenant. Estimate has EstimatePoint collection with cascade delete.</done>
</task>

<task type="auto">
  <name>Task 3: Complete WorkItemsModule registration with DbContext + health check, generate EF migration, verify full solution build</name>
  <files>
    src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
    src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/YYYYMMDDHHMMSS_InitialWorkItems.cs
    src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/YYYYMMDDHHMMSS_InitialWorkItems.Designer.cs
    src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/WorkItemsDbContextModelSnapshot.cs
  </files>
  <read_first>
    @yh-flow/src/Modules/Project/Modules.Project/ProjectModule.cs
  </read_first>
  <action>
    Complete WorkItemsModule and generate migration:

    **WorkItemsModule.cs** — Update skeleton with real services:
    - ConfigureServices: builder.Services.AddHeroDbContext&lt;WorkItemsDbContext&gt;(); builder.Services.AddHealthChecks().AddDbContextCheck&lt;WorkItemsDbContext&gt;(name: "db:work-items", failureStatus: HealthStatus.Unhealthy);
    - ConfigureMiddleware: empty (no middleware needed)
    - MapEndpoints: Create apiVersionSet (NewApiVersionSet with ApiVersion(1) + ReportApiVersions + Build). Create route group: endpoints.MapGroup("api/v{version:apiVersion}/workspaces/{slug}/projects/{projectId}/work-items").WithTags("WorkItems").WithApiVersionSet(apiVersionSet). Add TODO comments for each endpoint group to be wired in subsequent waves.

    **Migration generation:**
    1. Ensure solution builds: dotnet build src/YH.Flow.slnx --nologo
    2. From yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/ directory, generate:
       dotnet ef migrations add InitialWorkItems --context WorkItemsDbContext --output-dir WorkItems --startup-project ../YH.Flow.DbMigrator --project ../YH.Flow.Migrations.PostgreSQL.csproj
    3. Verify migration files created in WorkItems/ directory with tables: States, Labels, Estimates, EstimatePoints in yhschema.WorkItems schema
    4. Verify migration includes: TenantId columns, unique indexes with and without HasFilter, foreign key EstimatePoints→Estimates with cascade delete, Label self-referencing ParentId FK

    The migration must produce correct column types matching configurations: Name as varchar(255), Color as varchar(7), StateGroup as integer, SortOrder as double precision with default 65535.0, TenantId as text (Finbuckle default).

  </action>
  <verify>
    <automated>dir "src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/*InitialWorkItems*.cs" 2>&1</automated>
  </verify>
  <done>WorkItemsModule registers WorkItemsDbContext + health check. MapEndpoints defines route group with TODO endpoints. EF migration InitialWorkItems creates States, Labels, Estimates, EstimatePoints tables in yhschema.WorkItems schema. Full solution builds with 0 errors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                          |
| ---------------------------- | -------------------------------------------------------------------- |
| WorkItems tenant isolation   | All WorkItems entities scoped per-workspace via IHasTenant           |
| Module registration boundary | Order 260 ensures Project module (250) DbContext is registered first |

## STRIDE Threat Register

| Threat ID       | Category               | Component             | Disposition | Mitigation Plan                                                                                |
| --------------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-4-scaffold-01 | Information Disclosure | WorkItems.IHasTenant  | mitigate    | All entities implement IHasTenant (not IGlobalEntity) — BaseDbContext auto-filters by TenantId |
| T-4-scaffold-02 | Tampering              | State unique name     | mitigate    | Conditional unique index (TenantId, ProjectId, Name) with HasFilter("[DeletedOnUtc] IS NULL")  |
| T-4-scaffold-03 | Tampering              | Label unique name     | mitigate    | Conditional unique index (TenantId, ProjectId, Name) with HasFilter per same pattern           |
| T-4-scaffold-04 | Tampering              | OnModelCreating order | mitigate    | ApplyConfigurationsFromAssembly FIRST, base.OnModelCreating LAST per RESEARCH Pitfall 8        |
| T-4-scaffold-SC | Tampering              | Package installs      | accept      | No new packages in this wave — all dependencies already locked in Phase 1/2/3                  |

</threat_model>

<verification>
1. dotnet build src/YH.Flow.slnx --nologo — 0 errors, 0 warnings
2. Migration files exist in src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/
3. Verify migration creates 4 tables (States, Labels, Estimates, EstimatePoints) in yhschema.WorkItems schema
4. dotnet test src/Tests/WorkItems.Tests --nologo — test project builds (0 tests for now)
</verification>

<success_criteria>

- [ ] Modules.WorkItems.Contracts + Modules.WorkItems + WorkItems.Tests all compile with 0 errors
- [ ] State entity with StateGroup enum (5 values) implements IHasTenant, ISoftDeletable
- [ ] Label entity implements IHasTenant with ParentId self-ref
- [ ] Estimate entity with EstimatePoint child collection (cascade delete)
- [ ] WorkItemsDbContext in yhschema.WorkItems schema with ApplyConfigurations FIRST pattern
- [ ] WorkItemsModule registered with Order 260, DbContext + health check wired
- [ ] EF migration InitialWorkItems creates all 4 base tables with correct schema
- [ ] Solution builds with 0 errors
- [ ] Test project compiles and references WorkItems module
      </success_criteria>

<output>
Create `.planning/phases/04-workitems/04-01-SUMMARY.md` when done
</output>
