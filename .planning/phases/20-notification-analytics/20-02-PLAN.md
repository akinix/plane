---
phase: 20-notification-analytics
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - yh-flow/clients/web/src/lib/mock-data.ts
  - yh-flow/clients/web/app/store/analytics.store.ts
  - yh-flow/clients/web/app/store/root.store.ts
  - yh-flow/clients/web/app/store/types.ts
  - yh-flow/clients/web/src/lib/services/analytics.service.ts
  - yh-flow/clients/web/src/lib/hooks/use-analytics.ts
  - yh-flow/clients/web/src/lib/hooks/index.ts
  - yh-flow/clients/web/app/components/analytics/insight-card.tsx
  - yh-flow/clients/web/app/components/analytics/bar-chart.tsx
  - yh-flow/clients/web/app/components/analytics/area-chart.tsx
  - yh-flow/clients/web/app/components/analytics/radar-chart.tsx
  - yh-flow/clients/web/app/components/analytics/overview-tab.tsx
  - yh-flow/clients/web/app/components/analytics/work-items-tab.tsx
  - yh-flow/clients/web/app/components/analytics/data-table.tsx
  - yh-flow/clients/web/app/components/analytics/filter-bar.tsx
  - yh-flow/clients/web/app/components/analytics/analytics-empty-state.tsx
  - yh-flow/clients/web/app/components/analytics/analytics-skeleton.tsx
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/workspaces/[workspaceId]/analytics/page.tsx
autonomous: true
requirements: [ANAL-01, ANAL-02, ANAL-03]
user_setup: []

must_haves:
  truths:
    - "用户可在工作区分析仪表板看到 Issue 统计图表"
    - "概览 Tab 显示 4 个 Insight 卡片（总 Issue 数/已完成/进行中/积压）"
    - "概览 Tab 显示项目对比雷达图"
    - "工作项 Tab 显示创建 vs 完成趋势面积图"
    - "工作项 Tab 显示条形图（可按 X/Y/group-by 配置）"
    - "工作项 Tab 显示数据表，可按数量排序"
    - "用户可点击'导出 CSV'按钮下载分析数据"
    - "用户可通过项目筛选器选择特定项目查看分析"
    - "加载中显示骨架屏，无数据时显示空状态"
  artifacts:
    - path: "yh-flow/clients/web/src/lib/mock-data.ts"
      provides: "MOCK_ANALYTICS_OVERVIEW + MOCK_ANALYTICS_TREND + MOCK_ANALYTICS_BAR mock data"
      min_lines: 40
    - path: "yh-flow/clients/web/app/store/analytics.store.ts"
      provides: "AnalyticsStore (MobX UI state for activeTab, selectedProjectIds, dateRange)"
      min_lines: 50
    - path: "yh-flow/clients/web/src/lib/services/analytics.service.ts"
      provides: "AnalyticsService class with mock fetch overview/trend/bar data"
      min_lines: 40
    - path: "yh-flow/clients/web/src/lib/hooks/use-analytics.ts"
      provides: "useAnalyticsOverview, useAnalyticsTrend, useAnalyticsExport hooks"
      min_lines: 50
    - path: "yh-flow/clients/web/app/components/analytics/insight-card.tsx"
      provides: "Stat card with title, count value, optional trend indicator"
      min_lines: 25
    - path: "yh-flow/clients/web/app/components/analytics/bar-chart.tsx"
      provides: "Recharts BarChart wrapper per Plane chart type patterns"
      min_lines: 35
    - path: "yh-flow/clients/web/app/components/analytics/area-chart.tsx"
      provides: "Recharts AreaChart wrapper per Plane chart type patterns"
      min_lines: 35
    - path: "yh-flow/clients/web/app/components/analytics/radar-chart.tsx"
      provides: "Recharts RadarChart wrapper per Plane chart type patterns"
      min_lines: 35
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/analytics/page.tsx"
      provides: "Analytics dashboard page with tabs, charts, filter bar"
      min_lines: 60
  key_links:
    - from: "analytics/page.tsx"
      to: "overview-tab.tsx + work-items-tab.tsx"
      via: "conditional render based on activeTab from AnalyticsStore"
      pattern: "activeTab"
    - from: "overview-tab.tsx"
      to: "insight-card.tsx + radar-chart.tsx"
      via: "composes InsightCard grid + RadarChart"
      pattern: "InsightCard|AnalyticsRadarChart"
    - from: "work-items-tab.tsx"
      to: "area-chart.tsx + bar-chart.tsx + data-table.tsx"
      via: "composes area chart, bar chart, data table"
      pattern: "AnalyticsAreaChart|AnalyticsBarChart|DataTable"
    - from: "data-table.tsx"
      to: "export-to-csv package"
      via: "exportCSV utility triggers file download"
      pattern: "exportCSV"
    - from: "routes.ts"
      to: "analytics/page.tsx"
      via: "route import"
      pattern: "workspaces/:workspaceId/analytics"
---

<objective>
创建分析仪表板（Analytics Dashboard）的完整数据层和 UI 层。

**Purpose:** 实现 ANAL-01（工作区分析仪表板）、ANAL-02（项目级别分析图表）、ANAL-03（CSV 导出）三个需求。per D-P20-05 分析在 workspace 级别展示，per D-P20-02 使用 recharts（已通过 @plane/propel/charts 封装），per D-P20-03 使用 export-to-csv。

**Output:** 分析数据层（mock data + MobX store + service + TanStack Query hooks）+ 图表组件（Bar/Area/RadarChart 封装）+ 分析 UI 组件（OverviewTab/WorkItemsTab/InsightCard/DataTable/FilterBar/Empty/Skeleton）+ 分析仪表板页面 + 路由集成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-notification-analytics/20-CONTEXT.md
@.planning/phases/20-notification-analytics/20-UI-SPEC.md

# Existing patterns

@yh-flow/clients/web/app/store/notification.store.ts
@yh-flow/clients/web/src/lib/hooks/use-notifications.ts
@yh-flow/clients/web/src/lib/mock-data.ts
@yh-flow/clients/web/src/lib/types/analytics.ts
@yh-flow/clients/web/src/lib/types/charts/index.ts
@yh-flow/clients/web/src/lib/types/charts/common.ts
@yh-flow/clients/web/app/routes.ts
@yh-flow/clients/web/app/store/root.store.ts
@yh-flow/clients/web/app/store/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 分析数据层 — Mock 数据 + MobX Store + Service + Hooks</name>
  <files>
    yh-flow/clients/web/src/lib/mock-data.ts
    yh-flow/clients/web/app/store/analytics.store.ts
    yh-flow/clients/web/app/store/root.store.ts
    yh-flow/clients/web/app/store/types.ts
    yh-flow/clients/web/src/lib/services/analytics.service.ts
    yh-flow/clients/web/src/lib/hooks/use-analytics.ts
    yh-flow/clients/web/src/lib/hooks/index.ts
  </files>
  <action>
    1. 扩展 `src/lib/mock-data.ts`：
       - 在文件顶部 imports 区添加 import type { TChart } from "@plane/types" 和其他需要的类型（如 TChartDatum, IChartResponse）— 检查 @plane/types 中是否有这些导出；目前 `src/lib/types/charts/common.ts` 中已有 TChart, TChartDatum，优先从本地类型导入
       - 末尾添加分析 mock 数据：
         - `MOCK_ANALYTICS_OVERVIEW: TChart` = 4 个 key metrics: total_issues(128), completed(72), in_progress(31), backlog(25) + schema
         - `MOCK_ANALYTICS_TREND: TChartDatum[]` = 6 个月的月度数据（1月-6月），每个点有 key(日期), name(月份), created(创建数), resolved(完成数)
         - `MOCK_ANALYTICS_BAR: TChart` = 按优先级的分布: urgent(8), high(35), medium(42), low(18), none(25) + schema

    2. 创建 `app/store/analytics.store.ts`（per D-P20-06）：
       - 遵循 PageStore 模式（makeObservable + observable + action）
       - State: activeTab ("overview" | "work-items"), selectedProjectIds (string[], default []), dateRange (string, default "last-30-days")
       - Actions: setActiveTab, setSelectedProjectIds, setDateRange, reset
       - export interface IAnalyticsStore 和 class AnalyticsStore

    3. 创建 `src/lib/services/analytics.service.ts`：
       - class AnalyticsService
       - getOverview(workspaceId: string): 模拟延迟 300ms 返回 MOCK_ANALYTICS_OVERVIEW
       - getTrend(workspaceId: string): 模拟延迟 300ms 返回 MOCK_ANALYTICS_TREND
       - getBarData(workspaceId: string): 模拟延迟 300ms 返回 MOCK_ANALYTICS_BAR
       - export default new AnalyticsService()

    4. 创建 `src/lib/hooks/use-analytics.ts`：
       - 遵循 use-notifications.ts 模式
       - useAnalyticsOverview(workspaceId: string) → useQuery<TChart>, queryKey: ["analytics-overview", workspaceId]
       - useAnalyticsTrend(workspaceId: string) → useQuery<TChartDatum[]>, queryKey: ["analytics-trend", workspaceId]
       - useAnalyticsExport(workspaceId: string) → 返回一个函数 (params?: TAnalyticsFilterParams) => void，该函数:
         - 使用 export-to-csv 包的 generateCsv 和 download 方法
         - per D-P20-03 和 UI-SPEC Data Contract
         - 从 MOCK_ANALYTICS_OVERVIEW 或当前表格数据生成 CSV
         - 文件名: `analytics-export-{Date.now()}.csv`
         - 列: 类别 | 数量 | 占比（数据表列按 UI-SPEC Copywriting 定义: 类别, 数量, 占比）

    5. 注册到 `app/store/root.store.ts`：
       - 导入 AnalyticsStore 和 IAnalyticsStore
       - 在 ICoreRootStore interface 添加 `analytics: IAnalyticsStore;`
       - 在 CoreRootStore class 添加 analytics 属性和 new AnalyticsStore()

    6. 注册到 `app/store/types.ts`：
       - 导入并 re-export IAnalyticsStore

  </action>
  <verify>
    <automated>grep -c "MOCK_ANALYTICS_OVERVIEW" yh-flow/clients/web/src/lib/mock-data.ts</automated>
  </verify>
  <done>
    - 分析 mock 数据在 mock-data.ts 中可访问
    - AnalyticsStore 在 root.store.ts 中注册为 `store.analytics`
    - useAnalyticsOverview / useAnalyticsTrend / useAnalyticsExport 三个 hooks 创建完毕
    - 数据层可在无后端时正常工作（mock data）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 分析图表组件 + UI 组件</name>
  <files>
    yh-flow/clients/web/app/components/analytics/insight-card.tsx
    yh-flow/clients/web/app/components/analytics/bar-chart.tsx
    yh-flow/clients/web/app/components/analytics/area-chart.tsx
    yh-flow/clients/web/app/components/analytics/radar-chart.tsx
    yh-flow/clients/web/app/components/analytics/data-table.tsx
    yh-flow/clients/web/app/components/analytics/filter-bar.tsx
    yh-flow/clients/web/app/components/analytics/analytics-empty-state.tsx
    yh-flow/clients/web/app/components/analytics/analytics-skeleton.tsx
  </files>
  <action>
    按照 20-UI-SPEC.md 规范创建以下组件：

    1. `app/components/analytics/insight-card.tsx`：
       - 接收 props: `title: string`, `value: number | string`, `icon?: React.ReactNode`, `trend?: { direction: "up" | "down" | "neutral", percentage: number }`
       - 使用 Card 组件包裹（从 src/lib/ui/card/ 导入）
       - 显示 label（text-13, text-custom-text-300）+ value（text-2xl font-semibold）
       - 可选趋势指示器（TrendingUp/down icon + 百分比）

    2. `app/components/analytics/bar-chart.tsx`（AnalyticsBarChart）：
       - 接收 props: `data: TChartDatum[]`, `xKey: string`, `yKey: string`, `groupBy?: string`, `height?: number`(default 300)
       - 使用 recharts 的 ResponsiveContainer + BarChart + Bar + XAxis + YAxis + Tooltip + CartesianGrid
       - 从 @plane/constants 的 CHART_COLOR_PALETTES 中按 D-P20-02 引用颜色（或使用简单的硬编码 palette 作为 fallback）
       - 中文轴标签: X 轴 label 传参, Y 轴 "数量"
       - 遵循 UI-SPEC Chart Colors 规则, 默认使用 'horizon' palette

    3. `app/components/analytics/area-chart.tsx`（AnalyticsAreaChart）：
       - 接收 props: `data: TChartDatum[]`, `xKey: string`, `areas: { key: string, label: string, color: string }[]`, `height?: number`(default 300)
       - 使用 recharts 的 ResponsiveContainer + AreaChart + Area + XAxis + YAxis + Tooltip + CartesianGrid
       - 两个 area line: "已创建" (蓝色) 和 "已完成" (绿色) — 按 UI-SPEC Work Items Tab 规范
       - 中文轴标签: X 轴 "日期", Y 轴 "数量"
       - 遵循 'earthen' palette

    4. `app/components/analytics/radar-chart.tsx`（AnalyticsRadarChart）：
       - 接收 props: `data: TChartDatum[]`, `radars: { key: string, name: string, fill: string, stroke: string }[]`, `angleKey: string`, `height?: number`(default 300)
       - 使用 recharts 的 ResponsiveContainer + RadarChart + Radar + PolarGrid + PolarAngleAxis + Tooltip
       - 遵循 'modern' palette per UI-SPEC Chart Colors

    5. `app/components/analytics/data-table.tsx`：
       - 接收 props: `data: TChartDatum[]`, `columns: { key: string, label: string, sortable?: boolean }[]`
       - 使用已有 Table 组件（src/lib/ui/tables/）
       - 默认按 count 降序排列
       - 点击列头切换排序（asc/desc）
       - CSV 导出按钮（Download icon from lucide-react），位置在表格右上角
       - 导出时使用 useAnalyticsExport hook 或直接调用 export-to-csv
       - 数据列: 类别 | 数量 | 占比（中文 label）

    6. `app/components/analytics/filter-bar.tsx`：
       - 项目筛选下拉框（使用已有的 `CustomSearchSelect` 或 Dropdown 组件）
       - 日期范围选择（预设: 昨天 / 最近7天 / 最近30天 / 最近3个月）
       - 刷新按钮（RefreshCw icon）
       - 已选筛选条件显示为 tags（x 可移除）

    7. `app/components/analytics/analytics-empty-state.tsx`：
       - 居中显示
       - BarChart3 icon (size-12, text-custom-text-300)
       - "暂无分析数据" heading
       - "选择项目后查看分析图表" body text

    8. `app/components/analytics/analytics-skeleton.tsx`：
       - 4 个 Card skeleton (w-full h-24) 在 grid 中
       - 2 个 Chart skeleton (w-full h-80)
       - 使用 Loader 组件

    所有组件使用中文文本，严格遵循 UI-SPEC Typography 系统和 Color 系统。
    在 `app/components/analytics/` 创建 index.ts 导出所有组件。

    注意：图表组件需要 import from `recharts`。per D-P20-02，recharts 已在 Plan 20-01 的 Task 1 中安装。
    使用 `"use client"` 指令（如果 React Router 需要，因为 recharts 需要浏览器 DOM）。

  </action>
  <verify>
    <automated>grep -c "export.*AnalyticsBarChart" yh-flow/clients/web/app/components/analytics/bar-chart.tsx</automated>
  </verify>
  <done>
    - 8 个分析相关 UI 组件创建完成
    - 图表组件使用 recharts，无 TypeScript 编译错误
    - 所有中文文本符合 UI-SPEC Copywriting Contract
    - 组件可在 import 时被正确引用
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 分析仪表板页面 + 路由 + Tab 集成</name>
  <files>
    yh-flow/clients/web/app/routes.ts
    yh-flow/clients/web/app/components/analytics/overview-tab.tsx
    yh-flow/clients/web/app/components/analytics/work-items-tab.tsx
    yh-flow/clients/web/app/workspaces/[workspaceId]/analytics/page.tsx
  </files>
  <action>
    1. 路由注册 `app/routes.ts`：
       - 在 workspace layout 块内，在 Notification 路由之后添加（per UI-SPEC Route Registration）：
         ```
         // ANALYTICS ROUTES per D-P20-05
         route(
           "workspaces/:workspaceId/analytics",
           "app/workspaces/[workspaceId]/analytics/page.tsx"
         ),
         ```

    2. `app/components/analytics/overview-tab.tsx`：
       - 使用 useAnalyticsOverview hook 获取概览数据
       - 顶部: Insight Cards 4 列网格（grid-cols-4, 小于1024px 时 grid-cols-2 通过 Tailwind responsive class）
         - 4 个 InsightCard: "总 Issue 数" (128), "已完成" (72), "进行中" (31), "积压" (25)
         - 使用 useMemo 处理数据，用 useStore() 无交互（仅 UI store）
       - 中部: RadarChart（项目对比）
         - 横轴: 项目名
         - 指标: 完成数, 进行中, 积压
       - 底部: 活跃项目列表（简化: 项目名 + emoji + 完成百分比）

    3. `app/components/analytics/work-items-tab.tsx`：
       - 使用 useAnalyticsTrend 和 useAnalyticsBar hook 获取数据
       - 顶部: AreaChart（创建 vs 解决趋势）
         - X 轴: 月份（key = 日期列名, 从 mock 数据的 key 或 name 映射）
         - Y 轴: 数量
         - 两条 area: "已创建" (蓝色 #3B82F6), "已完成" (绿色 #10B981)
       - 中部: BarChart（自定义洞察）
         - X 轴: 优先级分布
         - Y 轴: 数量
         - 使用配置的 X/Y 选择（简化: 直接展示优先级分布数据）
       - 底部: DataTable（展示 BarChart 对应的表格数据）

    4. 分析仪表板页面 `app/workspaces/[workspaceId]/analytics/page.tsx`：
       - 使用 ContentWrapper 包裹
       - max-w-6xl mx-auto 限制最大宽度
       - Header:
         - "分析" 标题 (h1, text-2xl font-semibold)
         - FilterBar 组件（项目选择器 + 日期范围 + 刷新）
       - Tabs: "概览" | "工作项"（使用 Tabs 组件从 src/lib/ui/tabs/ 导入，或简单 button-based tab）
         - 使用 AnalyticsStore.activeTab 控制当前 Tab
       - Tab 内容切换:
         - "概览" → OverviewTab
         - "工作项" → WorkItemsTab
       - 使用 useStore() 获取 workspace 信息
       - 整体加载 → AnalyticsSkeleton
       - 整体空数据 → AnalyticsEmptyState
       - 整体错误 → 内联 error banner "加载分析数据失败，请重试"

    注意：由于 overview-tab.tsx 和 work-items-tab.tsx 内部也都各自使用 useQuery hooks，页面级别的 loading/error 状态和子组件的 loading 状态可以并存。推荐做法：page 组件用 `isLoading || isPending` 判断总 loading，子组件独立处理自己的骨架屏。

  </action>
  <verify>
    <automated>grep -c "workspaces/:workspaceId/analytics" yh-flow/clients/web/app/routes.ts</automated>
  </verify>
  <done>
    - 分析仪表板可通过 `/workspaces/:workspaceId/analytics` 访问
    - 概览 Tab 显示 4 个 Insight 卡片 + RadarChart
    - 工作项 Tab 显示 AreaChart + BarChart + DataTable
    - CSV 导出按钮可触发下载
    - 项目筛选器可用
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                           |
| --------------- | --------------------------------------------------------------------- |
| client→recharts | Chart rendering library — client-side only, no data exfiltration risk |

## STRIDE Threat Register

| Threat ID | Category | Component                  | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | -------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-20-02   | Spoofing | CSV export (export-to-csv) | accept      | CSV export generates data from in-memory mock data only — no user upload, no injection surface for real data until live API is connected. [ASSUMED] |

</threat_model>

<verification>
1. 分析页面路由可正常访问
2. 概览 Tab: 4 个 InsightCard + RadarChart 正常渲染
3. 工作项 Tab: AreaChart + BarChart + DataTable 正常渲染
4. CSV 导出按钮触发文件下载
5. 项目筛选器切换后影响图表数据
6. Loading skeleton 和 empty state 正常显示
</verification>

<success_criteria>

- [ ] 分析数据层完整：MOCK_ANALYTICS + AnalyticsStore + AnalyticsService + useAnalytics hooks
- [ ] 图表组件完整：AnalyticsBarChart + AnalyticsAreaChart + AnalyticsRadarChart — 使用 recharts
- [ ] 分析 UI 组件完整：InsightCard + DataTable + FilterBar + OverviewTab + WorkItemsTab + EmptyState + Skeleton
- [ ] 分析仪表板页面含 Tab 切换、项目筛选、日期范围、CSV 导出
- [ ] 所有中文文本符合 UI-SPEC Copywriting Contract
      </success_criteria>

<output>
Create `.planning/phases/20-notification-analytics/20-02-SUMMARY.md` when done
</output>
