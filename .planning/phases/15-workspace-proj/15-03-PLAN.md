---
phase: 15-workspace-proj
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/workspaces/[workspaceId]/page.tsx
  - yh-flow/clients/web/app/workspaces/[workspaceId]/settings/page.tsx
  - yh-flow/clients/web/app/workspaces/[workspaceId]/members/page.tsx
  - yh-flow/clients/web/app/workspaces/create/page.tsx
  - yh-flow/clients/web/app/components/workspace/dashboard/project-card.tsx
  - yh-flow/clients/web/app/components/workspace/dashboard/project-grid.tsx
  - yh-flow/clients/web/app/components/workspace/dashboard/activity-list.tsx
  - yh-flow/clients/web/app/components/workspace/dashboard/index.ts
  - yh-flow/clients/web/app/components/workspace/settings/workspace-settings-form.tsx
  - yh-flow/clients/web/app/components/workspace/settings/index.ts
  - yh-flow/clients/web/app/components/workspace/members/member-list.tsx
  - yh-flow/clients/web/app/components/workspace/members/member-role-dropdown.tsx
  - yh-flow/clients/web/app/components/workspace/members/index.ts
  - yh-flow/clients/web/app/components/workspace/create-workspace-form.tsx
  - yh-flow/clients/web/app/components/workspace/index.ts
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04]
user_setup: []

must_haves:
  truths:
    - "Workspace dashboard shows project card grid at top and recent activity list below (D-P15-09)"
    - "Project cards show project name, identifier, emoji logo, and member count"
    - "Activity list shows recent changes with actor name, action, and timestamp"
    - "Workspace settings page allows editing name and description"
    - "Workspace settings page allows changing logo via EmojiPicker"
    - "Workspace members page shows member list with avatar, name, email, role"
    - "Member role can be changed via dropdown (ADMIN/MEMBER/GUEST)"
    - "Create workspace page is a standalone page (no sidebar) with name, slug, description fields and emoji logo selector"
    - "Workspace switching via sidebar dropdown navigates to selected workspace dashboard (WORK-02)"
  artifacts:
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/page.tsx"
      provides: "Workspace dashboard page (project grid + activity list)"
      min_lines: 30
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/settings/page.tsx"
      provides: "Workspace settings page (name, description, logo)"
      min_lines: 20
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/members/page.tsx"
      provides: "Workspace member list page"
      min_lines: 20
    - path: "yh-flow/clients/web/app/components/workspace/dashboard/project-card.tsx"
      provides: "Project card for dashboard grid"
      exports: ["ProjectCard"]
    - path: "yh-flow/clients/web/app/components/workspace/dashboard/activity-list.tsx"
      provides: "Recent activity list component"
      exports: ["ActivityList"]
    - path: "yh-flow/clients/web/app/components/workspace/settings/workspace-settings-form.tsx"
      provides: "Workspace settings form with name, description, logo"
      exports: ["WorkspaceSettingsForm"]
  key_links:
    - from: "app/workspaces/[workspaceId]/page.tsx"
      to: "app/components/workspace/dashboard/project-grid.tsx + activity-list.tsx"
      via: "composition"
      pattern: "ProjectGrid|ActivityList"
    - from: "app/components/workspace/settings/workspace-settings-form.tsx"
      to: "src/lib/ui/emoji-picker/emoji-picker.tsx"
      via: "EmojiPicker import for logo selector"
      pattern: "EmojiPicker"
    - from: "app/routes.ts"
      to: "app/workspaces/create/page.tsx + app/workspaces/[workspaceId]/page.tsx"
      via: "route registration added by this plan"
      pattern: "workspaces/create|workspaces/:workspaceId"
---

<objective>
实现工作区功能页面：dashboard（项目概览 + 最近活动）、设置（名称/描述/Logo 编辑）、成员管理（列表/角色变更）、创建工作区流程。同时将当前 plan 的工作区页面注册到 routes.ts 中。

Purpose: 满足 WORK-01~04 需求，让用户可以全面管理工作区。
Output: 四个工作区功能页面 + routes.ts 中工作区路由注册，使用 mock 数据运行。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-workspace-proj/15-CONTEXT.md

# 基础设施（Plan 1 产出）

@yh-flow/clients/web/src/lib/mock-data.ts
@yh-flow/clients/web/src/lib/hooks/use-workspaces.ts
@yh-flow/clients/web/src/lib/hooks/use-projects.ts
@yh-flow/clients/web/src/lib/hooks/use-members.ts
@yh-flow/clients/web/app/store/workspace.store.ts
@yh-flow/clients/web/app/store/project.store.ts

# 布局（Plan 2 产出）

@yh-flow/clients/web/app/layouts/workspace-layout.tsx
@yh-flow/clients/web/app/routes.ts

# 类型参考

@yh-flow/clients/web/src/lib/types/workspace.ts — IWorkspace, IWorkspaceMember, EUserWorkspaceRoles
@yh-flow/clients/web/src/lib/types/project/projects.ts — IProject
@yh-flow/clients/web/src/lib/types/activity.ts — TProjectBaseActivity
@yh-flow/clients/web/src/lib/types/enums.ts — EUserPermissions

# Plane 参考

@apps/web/core/components/workspace/create-workspace-form.tsx
@apps/web/core/components/workspace/logo.tsx
@apps/web/core/components/workspace/settings/workspace-details.tsx
@apps/web/core/components/workspace/settings/members-list.tsx
@apps/web/core/components/workspace/settings/member-columns.tsx
@apps/web/core/components/project/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注册工作区路由 + 创建工作区 Dashboard（项目卡片网格 + 活动列表）</name>
  <files>
    yh-flow/clients/web/app/routes.ts
    yh-flow/clients/web/app/workspaces/[workspaceId]/page.tsx
    yh-flow/clients/web/app/components/workspace/dashboard/project-card.tsx
    yh-flow/clients/web/app/components/workspace/dashboard/project-grid.tsx
    yh-flow/clients/web/app/components/workspace/dashboard/activity-list.tsx
    yh-flow/clients/web/app/components/workspace/dashboard/index.ts
  </files>
  <read_first>
    yh-flow/clients/web/app/routes.ts — 当前路由配置（含 auth routes + workspace layout wrapper from 15-02）
    yh-flow/clients/web/src/lib/hooks/use-projects.ts — useProjects(workspaceId)
    yh-flow/clients/web/src/lib/mock-data.ts — MOCK_ACTIVITIES 格式
    yh-flow/clients/web/src/lib/types/project/projects.ts — IProject, IProjectLite, TLogoProps
    yh-flow/clients/web/src/lib/types/activity.ts — TProjectBaseActivity
  </read_first>
  <action>
    1. 更新 `app/routes.ts`，在 workspace-layout 的 children 数组中添加工作区页面路由，以及在 layout 外部添加创建工作区独立路由：
    ```
    {
      layout: "app/layouts/workspace-layout.tsx",
      children: [
        { index: true, file: "app/workspace-redirect/page.tsx" },
        // ↓ 以下为此 plan 添加的路由
        { path: "workspaces/:workspaceId", file: "app/workspaces/[workspaceId]/page.tsx" },
        {
          path: "workspaces/:workspaceId/settings/*",
          file: "app/workspaces/[workspaceId]/settings/page.tsx",
        },
        {
          path: "workspaces/:workspaceId/members/*",
          file: "app/workspaces/[workspaceId]/members/page.tsx",
        },
      ],
    },
    // 创建工作区页面在 layout 外部注册——此时用户没有工作区，不该显示 sidebar
    { path: "workspaces/create", file: "app/workspaces/create/page.tsx" },
    ```
    - `workspaces/create` 放在 layout 外部（不是 workspace-layout 的 children），确保创建页面没有侧边栏/顶栏
    - 保持 auth routes 和 "/" 根路由不动

    2. 创建 `app/components/workspace/dashboard/project-card.tsx`：
    - 组件名：`ProjectCard`
    - Props: `{ project: IProject; onClick?: () => void }`
    - 显示：Emoji logo（使用 fromCodePoint 渲染 TLogoProps.emoji.value）、项目名称、identifier、成员数（如果有）
    - 使用 @plane/ui 的 Card 组件（或自实现卡片：rounded-lg border p-4 hover:shadow transition）
    - 点击卡片 → onClick 或 navigate 到 `/workspaces/:workspaceId/projects/:projectId`

    3. 创建 `app/components/workspace/dashboard/project-grid.tsx`：
    - 组件名：`ProjectGrid`
    - Props: `{ workspaceId: string }`
    - 使用 useProjects(workspaceId) 获取项目列表
    - 渲染为 2-3 列响应式网格（grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4）
    - 每个项目渲染为 ProjectCard
    - 空状态：显示 "暂无项目" + 创建项目按钮（空状态设计属于 Claude's Discretion 范围）

    4. 创建 `app/components/workspace/dashboard/activity-list.tsx`：
    - 组件名：`ActivityList`
    - Props: `{ activities: TProjectBaseActivity[] }`（先传 props，后续用 hook）
    - 从 MOCK_ACTIVITIES 取当前 workspace 的活动（按 project 匹配或全部显示）
    - 每个活动项：icon/avatar + 描述文字 + "几分钟前" 相对时间
    - 使用 date-fns 的 formatDistanceToNow 或手动计算时间差
    - 列表：flex col 布局，项目之间用 border-b 分隔

    5. 创建 `app/components/workspace/dashboard/index.ts` 作为 barrel export。

    6. 创建 `app/workspaces/[workspaceId]/page.tsx`：
    - 使用 useParams() 获取 workspaceId
    - 使用 useWorkspace(workspaceId) 获取工作区详情
    - 上方展示：工作区名称 + 描述
    - 中间展示 ProjectGrid(workspaceId)
    - 下方展示 ActivityList(activities from MOCK_ACTIVITIES)
    - 使用 AuthenticationWrapper(pageType=EPageTypes.AUTHENTICATED)

  </action>
  <acceptance_criteria>
    - `app/routes.ts` 包含 workspace dashboard/settings/members 路由在 layout 内，create 路由在 layout 外
    - `app/workspaces/[workspaceId]/page.tsx` 渲染工作区名称 + ProjectGrid + ActivityList
    - ProjectCard 显示项目 emoji logo、名称、identifier
    - ProjectGrid 以响应式网格布局展示项目
    - ActivityList 显示活动记录列表
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Workspace routes registered in routes.ts. Workspace dashboard renders project card grid + activity list. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: 创建工作区设置 + 创建页面（含 Emoji logo 选择）</name>
  <files>
    yh-flow/clients/web/app/workspaces/[workspaceId]/settings/page.tsx
    yh-flow/clients/web/app/components/workspace/settings/workspace-settings-form.tsx
    yh-flow/clients/web/app/components/workspace/settings/index.ts
    yh-flow/clients/web/app/workspaces/create/page.tsx
    yh-flow/clients/web/app/components/workspace/create-workspace-form.tsx
    yh-flow/clients/web/app/components/workspace/index.ts
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/ui/emoji-picker/emoji-picker.tsx — EmojiPicker 组件 API
    yh-flow/clients/web/src/lib/types/workspace.ts — IWorkspace name/slug/logo_url 字段
    yh-flow/clients/web/app/store/workspace.store.ts — setCurrentWorkspace 方法
    apps/web/core/components/workspace/create-workspace-form.tsx — Plane 创建工作区表单模式
  </read_first>
  <action>
    1. 创建 `app/components/workspace/settings/workspace-settings-form.tsx`（per WORK-03）：
    - 组件名：`WorkspaceSettingsForm`
    - Props: `{ workspaceId: string }`
    - 使用 useWorkspace(workspaceId) 获取当前工作区数据作为默认值
    - 表单字段（mock 模式，修改仅更新本地状态）：
      - Logo 区域：显示当前 emoji 图标，点击使用 EmojiPicker 选择新 emoji（per D-P15-10）
      - 名称：text input，显示当前 workspace.name
      - 描述：textarea（虽然 IWorkspace 没有 description 字段，使用 url 字段替代展示或添加 description 状态）
      - Slug：disabled text input（仅显示，不可修改）
    - 保存按钮：点击显示 toast 提示 "设置已保存"（mock 模式）
    - 使用 lucide-react 的 Save 图标 + 按钮

    2. 创建 `app/workspaces/[workspaceId]/settings/page.tsx`：
    - 页面标题 "工作区设置"
    - 渲染 WorkspaceSettingsForm

    3. 创建 `app/components/workspace/create-workspace-form.tsx`（per WORK-01 创建工作区流程）：
    - 组件名：`CreateWorkspaceForm`
    - 表单字段：
      - Emoji logo selector（使用 EmojiPicker）
      - 名称（text input，必填）
      - Slug（自动根据名称生成，可编辑）
      - 描述（textarea，非必填）
      - 组织规模（select/input，非必填）
    - 提交按钮 "创建工作区"
    - 提交后：mock 模式，显示成功 toast，将新工作区信息输出到 console，重定向到新工作区

    4. 创建 `app/workspaces/create/page.tsx`（per WORK-01）：
    - 这是一个独立页面，不在 workspace-layout 内部——用户没有工作区时不应看到 sidebar
    - 居中布局（flex items-center justify-center min-h-screen）
    - 标题 "创建工作区"
    - 渲染 CreateWorkspaceForm
    - 此路由已在 Task 1 中注册为 layout 外部的独立路由 `workspaces/create`

    5. 创建 barrel exports。

  </action>
  <acceptance_criteria>
    - `WorkspaceSettingsForm` 显示名称、描述、logo 编辑字段，EmojiPicker 可用于更换 logo
    - `CreateWorkspaceForm` 包含 emoji logo 选择器 + 名称 + slug + 描述字段
    - 创建工作区页面可通过 `/workspaces/create` 访问，且没有侧边栏/顶栏
    - 设置页面可通过 `/workspaces/:workspaceId/settings` 访问
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Workspace settings form with name, description, emoji logo and standalone create workspace form render correctly. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: 创建工作区成员管理页面</name>
  <files>
    yh-flow/clients/web/app/workspaces/[workspaceId]/members/page.tsx
    yh-flow/clients/web/app/components/workspace/members/member-list.tsx
    yh-flow/clients/web/app/components/workspace/members/member-role-dropdown.tsx
    yh-flow/clients/web/app/components/workspace/members/index.ts
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-members.ts — useMembers(workspaceId)
    yh-flow/clients/web/src/lib/mock-data.ts — MOCK_MEMBERS 结构
    yh-flow/clients/web/src/lib/types/workspace.ts — IWorkspaceMember, EUserWorkspaceRoles
    yh-flow/clients/web/src/lib/types/enums.ts — EUserPermissions
  </read_first>
  <action>
    1. 创建 `app/components/workspace/members/member-role-dropdown.tsx`（per WORK-04 角色变更）：
    - 组件名：`MemberRoleDropdown`
    - Props: `{ currentRole: EUserWorkspaceRoles | TUserPermissions; onRoleChange: (newRole: EUserWorkspaceRoles) => void }`
    - 显示当前角色名（Admin / Member / Guest）
    - 下拉菜单显示三个选项：Admin (20), Member (15), Guest (5)
    - 使用 headlessui Menu 组件
    - mock 模式：选择新角色后调用 onRoleChange 回调

    2. 创建 `app/components/workspace/members/member-list.tsx`：
    - 组件名：`MemberList`
    - Props: `{ workspaceId: string }`
    - 使用 useMembers(workspaceId) 获取成员列表
    - 表格/列表布局，每行显示：
      - 用户头像（avatar_url 或首字母）
      - 用户名（display_name）
      - 邮箱（email）
      - 角色（MemberRoleDropdown 组件）
      - 加入日期（joining_date 格式化）
    - 空状态（mock：不太可能出现，但处理）

    3. 创建 `app/workspaces/[workspaceId]/members/page.tsx`：
    - 页面标题 "工作区成员"
    - 渲染 MemberList(workspaceId)

    4. 创建 barrel exports。

  </action>
  <acceptance_criteria>
    - `MemberRoleDropdown` 显示当前角色，点击可切换 Admin/Member/Guest
    - `MemberList` 渲染成员表格，每行包含头像、姓名、邮箱、角色下拉、加入日期
    - 成员页面可通过 `/workspaces/:workspaceId/members` 访问
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Member list with avatar, name, email, role dropdown renders. Role change triggers callback. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                   |
| --------------------- | ------------------------------------------------------------- |
| client-side mock data | All workspace data is client-side mock, no server interaction |

## STRIDE Threat Register

| Threat ID | Category  | Component                        | Disposition | Mitigation Plan                                                |
| --------- | --------- | -------------------------------- | ----------- | -------------------------------------------------------------- |
| T-15-05   | Tampering | WorkspaceSettingsForm name input | mitigate    | Validate name max 100 chars client-side; reject empty name     |
| T-15-06   | Tampering | EmojiPicker text input           | mitigate    | Sanitize input (no HTML, max 50 chars)                         |
| T-15-07   | Spoofing  | MemberRoleDropdown               | accept      | Mock data only; real role enforcement requires backend Phase 2 |

</threat_model>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] routes.ts contains workspace routes (dashboard, settings, members inside layout; create outside)
- [ ] Dashboard route renders project cards and activity list
- [ ] Settings form loads with current workspace values
- [ ] Member list shows all members with role dropdown
- [ ] Create workspace form is standalone (no sidebar) at /workspaces/create
</verification>

<success_criteria>

1. Workspace dashboard shows project card grid + activity list per D-P15-09
2. Workspace settings page allows editing name, description, and emoji logo per WORK-03
3. Workspace members page shows list with role change dropdown per WORK-04
4. Create workspace page is a standalone page (no sidebar) with emoji logo selector per WORK-01 creation flow
5. All workspace routes registered in routes.ts (dashboard/settings/members inside layout, create outside)
6. All TypeScript compiles without errors
   </success_criteria>

<output>
Create `.planning/phases/15-workspace-proj/15-03-SUMMARY.md` when done
</output>
