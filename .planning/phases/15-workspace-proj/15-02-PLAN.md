---
phase: 15-workspace-proj
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/layouts/workspace-layout.tsx
  - yh-flow/clients/web/app/workspace-redirect/page.tsx
  - yh-flow/clients/web/app/components/sidebar/workspace-sidebar.tsx
  - yh-flow/clients/web/app/components/sidebar/workspace-switcher.tsx
  - yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx
  - yh-flow/clients/web/app/components/navigation/top-bar.tsx
  - yh-flow/clients/web/app/components/navigation/user-dropdown.tsx
  - yh-flow/clients/web/app/components/navigation/index.ts
  - yh-flow/clients/web/app/components/sidebar/index.ts
autonomous: true
requirements: [UI-01, UI-02]
user_setup: []

must_haves:
  truths:
    - "Sidebar shows workspace switcher at top with current workspace name and expand arrow (D-P15-03)"
    - "Sidebar shows tree structure: workspace > projects > project views, current item highlighted (D-P15-05)"
    - "Sidebar can be collapsed to icons only (D-P15-06)"
    - "Top bar shows page title on left, search icon + notification bell + user avatar dropdown on right (D-P15-07)"
    - "User avatar dropdown shows settings and sign out options"
    - "Unauthenticated users are redirected to sign-in (reusing AuthenticationWrapper)"
    - "No workspace → redirect to create workspace; has workspace → redirect to first workspace dashboard (D-P15-04)"
    - "Routes follow /workspaces/:workspaceId/ pattern (D-P15-02)"
  artifacts:
    - path: "yh-flow/clients/web/app/layouts/workspace-layout.tsx"
      provides: "Main layout with sidebar + topbar + Outlet content area"
      min_lines: 60
    - path: "yh-flow/clients/web/app/components/sidebar/workspace-sidebar.tsx"
      provides: "Sidebar container with switcher and tree"
      exports: ["WorkspaceSidebar"]
    - path: "yh-flow/clients/web/app/components/sidebar/workspace-switcher.tsx"
      provides: "Workspace selector dropdown at sidebar top"
      exports: ["WorkspaceSwitcher"]
    - path: "yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx"
      provides: "Tree navigation: workspace > projects > views"
      exports: ["SidebarTree"]
    - path: "yh-flow/clients/web/app/components/navigation/top-bar.tsx"
      provides: "Top navigation bar with title, search, notification, avatar"
      exports: ["TopBar"]
  key_links:
    - from: "app/components/sidebar/workspace-sidebar.tsx"
      to: "app/components/sidebar/workspace-switcher.tsx + sidebar-tree.tsx"
      via: "composition"
      pattern: "WorkspaceSwitcher|SidebarTree"
    - from: "app/routes.ts"
      to: "app/layouts/workspace-layout.tsx"
      via: "layout key in route config"
      pattern: "workspace-layout"
---

<objective>
实现工作区布局结构：左侧边栏（含工作区切换下拉 + 树形导航，可折叠）+ 顶部导航栏 + 路由体系。这是 Phase 15 的 UI 骨架，所有工作区和项目页面将在此布局中渲染。

Purpose: 建立 Plane 风格的侧边栏 + 主内容区布局（D-P15-01），配置完整的 React Router 路由结构（D-P15-02）。
Output: 可导航的 workspace layout 页面，侧边栏可折叠/展开，顶栏显示用户信息。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-workspace-proj/15-CONTEXT.md

# 基础设施（Plan 1 产出）

@yh-flow/clients/web/src/lib/mock-data.ts
@yh-flow/clients/web/src/lib/hooks/use-workspaces.ts
@yh-flow/clients/web/src/lib/hooks/use-projects.ts
@yh-flow/clients/web/app/store/workspace.store.ts
@yh-flow/clients/web/app/store/project.store.ts

# 现有代码参考

@yh-flow/clients/web/app/routes.ts
@yh-flow/clients/web/app/root.tsx
@yh-flow/clients/web/app/provider.tsx
@yh-flow/clients/web/app/lib/wrappers/authentication-wrapper.tsx
@yh-flow/clients/web/app/lib/store-context.tsx
@yh-flow/clients/web/app/page.tsx
@yh-flow/clients/web/src/lib/types/workspace.ts
@yh-flow/clients/web/src/lib/types/project/projects.ts

# Plane 参考

@apps/web/core/components/sidebar/sidebar-wrapper.tsx
@apps/web/core/components/sidebar/sidebar-navigation.tsx
@apps/web/core/components/sidebar/sidebar-item.tsx
@apps/web/core/components/navigation/project-header.tsx
@apps/web/core/components/workspace/logo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 配置路由结构 + 创建 workspace 布局和重定向页面</name>
  <files>
    yh-flow/clients/web/app/routes.ts
    yh-flow/clients/web/app/layouts/workspace-layout.tsx
    yh-flow/clients/web/app/workspace-redirect/page.tsx
  </files>
  <read_first>
    yh-flow/clients/web/app/routes.ts — 现有路由配置格式（flat RouteConfig）
    yh-flow/clients/web/app/root.tsx — 根布局（main + Outlet 结构，需确认是否要改）
    yh-flow/clients/web/app/lib/wrappers/authentication-wrapper.tsx — 路由保护组件模式
  </read_first>
  <action>
    1. 更新 `app/routes.ts`（per D-P15-02 路由格式）：
    - 保留现有 auth 路由（sign-in, sign-up, forgot-password, reset-password）
    - 用 layout route 包裹 workspace 相关路由：
      ```
      {
        layout: "app/layouts/workspace-layout.tsx",
        children: [
          { index: true, file: "app/workspace-redirect/page.tsx" }, // / → redirect
          { path: "workspaces/:workspaceId", file: "app/workspaces/[workspaceId]/page.tsx" }, // dashboard
          { path: "workspaces/:workspaceId/settings/*", file: "app/workspaces/[workspaceId]/settings/page.tsx" },
          { path: "workspaces/:workspaceId/members/*", file: "app/workspaces/[workspaceId]/members/page.tsx" },
          { path: "workspaces/:workspaceId/projects/*", file: "app/workspaces/[workspaceId]/projects/page.tsx" },
          {
            path: "workspaces/:workspaceId/projects/:projectId",
            children: [
              { index: true, file: "app/workspaces/[workspaceId]/projects/[projectId]/page.tsx" },
              { path: "settings", file: "app/workspaces/[workspaceId]/projects/[projectId]/settings/page.tsx" },
              { path: "members", file: "app/workspaces/[workspaceId]/projects/[projectId]/members/page.tsx" },
            ],
          },
        ],
      }
      ```
    - 注意 layout 内的 index route 会匹配 "/" 路径，auth 路由放在 layout 外面以确保不被布局包裹

    2. 创建 `app/layouts/workspace-layout.tsx`：
    - 使用 AuthenticationWrapper(pageType=EPageTypes.AUTHENTICATED) 包装整个布局
    - 导入 WorkspaceSidebar 和 TopBar 组件（将在 Task 2 和 Task 3 创建）
    - 布局结构（per D-P15-01）：flex row，左边 sidebar，右侧 flex col 包含 topbar + main content
    - Sidebar 宽度：展开 250px，折叠 60px（per Plane SIDEBAR_WIDTH）
    - 从 useStore() 读取 sidebarCollapsed 状态
    - 内容区使用 flex-1 overflow-auto，渲染 `<Outlet />`
    - 在 layout 加载时，调用 useCurrentWorkspace() 预取当前工作区数据

    3. 创建 `app/workspace-redirect/page.tsx`（per D-P15-04）：
    - 使用 TanStack Query 的 useWorkspaces() 获取工作区列表
    - 如果 loading → 显示 spinner
    - 如果 workspaces.length === 0 → 使用 useNavigate 跳到创建页面（/workspaces/create）
    - 如果 has workspaces → navigate 到 `/workspaces/${workspaces[0].id}`
    - 此页面没有实际 UI，只是逻辑重定向

  </action>
  <acceptance_criteria>
    - `app/routes.ts` 包含 layout 路由指向 workspace-layout.tsx，children 包含 workspace 和 project routes
    - `app/layouts/workspace-layout.tsx` 包含 AuthenticationWrapper + flex row layout + Sidebar + TopBar + Outlet
    - `app/workspace-redirect/page.tsx` 根据 workspace 数量重定向到第一个 workspace 或创建页面
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Route config includes workspace/project routes with layout. Redirect logic works per D-P15-04. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: 创建侧边栏组件（切换器 + 树形导航 + 折叠功能）</name>
  <files>
    yh-flow/clients/web/app/components/sidebar/workspace-sidebar.tsx
    yh-flow/clients/web/app/components/sidebar/workspace-switcher.tsx
    yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx
    yh-flow/clients/web/app/components/sidebar/index.ts
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-workspaces.ts — useWorkspaces 等 hooks
    yh-flow/clients/web/src/lib/hooks/use-projects.ts — useProjects hook
    yh-flow/clients/web/app/store/workspace.store.ts — WorkspaceStore interface
    yh-flow/clients/web/app/store/project.store.ts — ProjectStore interface
    yh-flow/clients/web/src/lib/types/workspace.ts — IWorkspace, IWorkspaceLite
    yh-flow/clients/web/src/lib/types/project/projects.ts — IProject, IPartialProject
    apps/web/core/components/sidebar/sidebar-navigation.tsx — Plane 侧边栏导航项 UI 模式
    apps/web/core/components/sidebar/sidebar-item.tsx — Plane 侧边栏 item 样式
  </read_first>
  <action>
    1. 创建 `app/components/sidebar/workspace-switcher.tsx`（per D-P15-03）：
    - 组件名：`WorkspaceSwitcher`
    - 显示当前工作区名称 + 展开箭头（ChevronDown 图标 from lucide-react）
    - 点击展开一个下拉面板，列出所有工作区（useWorkspaces() hook）
    - 每个工作区显示名 + 点表示当前选中
    - 点击工作区 → store.setCurrentWorkspace(id) + navigate 到对应 workspace dashboard
    - 底部有 "创建工作区" 按钮 → navigate 到创建工作区页面
    - 样式保持 Plane 风格：bg-custom-sidebar-background-100, text-custom-sidebar-text-100

    2. 创建 `app/components/sidebar/sidebar-tree.tsx`（per D-P15-05）：
    - 组件名：`SidebarTree`
    - 使用 useProjects(workspaceId) 获取当前 workspace 的项目列表
    - 使用 store.project.expandedProjectIds 控制项目展开/折叠
    - 树形结构：
      - 当前工作区节点（可展开→列出项目）
        - 每个项目节点（可展开→项目内视图：Issues, Cycles, Modules, Pages, Views）
          - 视图项（当前仅显示占位文本，per D-P15-11 项目内视图后续 phase 实现）
    - 当前所在位置高亮：使用 useLocation() 获取当前 path，匹配 `workspaces/:wsId/projects/:projId` 中的 projId
    - 导航：点击项目 → navigate 到项目 dashboard；点击视图 → placeholder navigate
    - 展开/折叠：点击箭头或项目名左侧的 ChevronRight/ChevronDown 图标切换

    3. 创建 `app/components/sidebar/workspace-sidebar.tsx`：
    - 组件名：`WorkspaceSidebar`
    - 包含 WorkspaceSwitcher（顶部）+ SidebarTree（scrollable 列表）
    - 底部有一个折叠/展开按钮（使用 PanelLeftClose/PanelLeftOpen 图标 from lucide-react）
    - 宽度由 store.workspace.sidebarCollapsed 控制（展开: w-64, 折叠: w-14）
    - 折叠时：仅显示工作区首字母图标作为 switcher，tree 隐藏，hover 可展开预览（可选）
    - 使用 cn() 实现过渡动画：transition-all duration-300
    - 使用 Tailwind CSS 4 的 dark: 变体配合 data-theme 属性
    - 背景色：bg-custom-sidebar-background-100

    4. 创建 `app/components/sidebar/index.ts` 以 barrel export 所有 sidebar 组件。

  </action>
  <acceptance_criteria>
    - `WorkspaceSwitcher` 显示当前工作区名称，点击展开下拉列出所有工作区
    - `SidebarTree` 显示 workspace > projects > views 树形结构，当前项高亮
    - `WorkspaceSidebar` 整合 switcher + tree + collapse button，可折叠/展开
    - 所有组件使用 `observer` (mobx-react) 包裹以响应 store 变化
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Sidebar renders workspace switcher at top, tree navigation in middle, collapse button at bottom. Collapse/expand works. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: 创建顶部导航栏 + 用户下拉菜单</name>
  <files>
    yh-flow/clients/web/app/components/navigation/top-bar.tsx
    yh-flow/clients/web/app/components/navigation/user-dropdown.tsx
    yh-flow/clients/web/app/components/navigation/index.ts
  </files>
  <read_first>
    yh-flow/clients/web/app/store/root.store.ts — useStore 获取 auth store
    yh-flow/clients/web/app/store/auth.store.ts — currentUser, signOut 方法
    yh-flow/clients/web/app/page.tsx — 现有 signOut 按钮模式
    apps/web/core/components/navigation/project-header.tsx — Plane 顶栏项目模式
  </read_first>
  <action>
    1. 创建 `app/components/navigation/top-bar.tsx`（per D-P15-07）：
    - 组件名：`TopBar`
    - 布局：flex row items-center justify-between，高度 h-12，border-b border-custom-border-200
    - 左侧：
      - 面包屑或当前页面标题（从 route 上下文中提取，简化版：接收 title prop 或从 location path 推断）
      - 使用 useLocation().pathname 判断当前是 workspace 还是 project 层级，对应显示名称
    - 右侧（flex row items-center gap-2）：
      - 搜索图标（Search from lucide-react）→ 搜索入口（后续 phase 实现搜索功能，目前仅显示图标）
      - 通知铃铛（Bell from lucide-react）→ 通知入口（后续 phase 实现，目前仅显示图标和未读数徽标）
      - UserDropdown 组件
    - 背景色：bg-custom-sidebar-background-100 或 bg-custom-background-90

    2. 创建 `app/components/navigation/user-dropdown.tsx`：
    - 组件名：`UserDropdown`
    - 触发器：用户头像（如果 currentUser.avatar_url 存在）或首字母圆形图标
    - 使用 lucide-react 的 User 图标作为缺省头像
    - 使用 @radix-ui/react-dropdown-menu 或简单 click-toggle 弹出下拉
    - 下拉内容：
      - 用户信息（display_name, email）
      - 分隔线
      - 设置（Settings icon）→ navigate 到设置页（后续 phase 实现）
      - 退出登录（LogOut icon）→ 调用 auth.signOut() → navigate /auth/sign-in
    - 使用 headlessui 的 Menu 组件（已在依赖中，@headlessui/react）或自实现

    3. 创建 `app/components/navigation/index.ts` 以 barrel export。

  </action>
  <acceptance_criteria>
    - `TopBar` 显示左侧标题 + 右侧搜索/通知图标 + UserDropdown
    - `UserDropdown` 点击显示用户信息和退出登录选项
    - 退出登录调用 auth.signOut() 并跳转到 /auth/sign-in
    - 组件使用 `observer` 包裹
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TopBar renders with title, search icon, notification bell, and user avatar dropdown. Sign-out works. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                  |
| --------------------- | ---------------------------------------------------------------------------- |
| client-side rendering | Layout and navigation components render in browser, no data crossing network |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                          |
| --------- | ---------------------- | ----------------- | ----------- | ------------------------------------------------------------------------ |
| T-15-03   | Spoofing               | UserDropdown      | mitigate    | Display only currentUser.display_name from AuthStore; no editable fields |
| T-15-04   | Information Disclosure | Sidebar tree URLs | accept      | Workspace/project IDs in URL are already visible in address bar          |

</threat_model>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] Route config loads workspace layout for /workspaces/*
- [ ] Sidebar collapses/expands correctly
- [ ] TopBar renders with search, notification, user menu
</verification>

<success_criteria>

1. Routes follow /workspaces/:workspaceId/ pattern per D-P15-02
2. Sidebar has workspace switcher at top, tree navigation, collapse/expand per D-P15-03/05/06
3. Top bar shows title + search icon + notification bell + user avatar per D-P15-07
4. No workspace → create redirect; has workspace → first dashboard redirect per D-P15-04
5. All TypeScript compiles without errors
6. Layout matches Plane style: bg-custom-sidebar-background-100, text-custom-sidebar-text-100
   </success_criteria>

<output>
Create `.planning/phases/15-workspace-proj/15-02-SUMMARY.md` when done
</output>
