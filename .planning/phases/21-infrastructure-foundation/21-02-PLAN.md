---
phase: 21-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yh-flow/clients/web/src/lib/services/query-keys.ts
autonomous: true
requirements:
  - INFRA-03
user_setup: []

must_haves:
  truths:
    - "每个模块的 TanStack Query 缓存键均使用集中式 query-keys.ts 工厂函数，无临时字符串拼写"
    - "TypeScript 编译通过，所有键工厂函数有正确的泛型参数类型"
  artifacts:
    - path: "yh-flow/clients/web/src/lib/services/query-keys.ts"
      provides: "集中式查询键工厂，覆盖所有模块"
      contains: "all factories: workspace, project, member, issue, state, label, comment, cycle, module, page, view, notification, analytics"
  key_links:
    - from: "query-keys.ts"
      to: "所有 hooks/*.ts"
      via: "hooks 导入 queryKey 工厂并调用"
      pattern: "import.*queryKeys.*from"
---

<objective>
创建集中式查询键工厂 `query-keys.ts`，统一管理所有模块的 TanStack Query 缓存键。

Purpose: 消除 hooks 中的内联字符串键（如 `["workspaces"]`、`["issues", projectId, filters]`），用类型安全的工厂函数替代。这使得键的命名规范统一、跨模块的键失效更加精确、键变更时 IDE 重构支持。

Output:

- 创建 `src/lib/services/query-keys.ts`，包含所有业务模块的查询键工厂函数
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-infrastructure-foundation/21-CONTEXT.md
@yh-flow/clients/web/src/lib/hooks/use-workspaces.ts
@yh-flow/clients/web/src/lib/hooks/use-projects.ts
@yh-flow/clients/web/src/lib/hooks/use-members.ts
@yh-flow/clients/web/src/lib/hooks/use-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-comments.ts
@yh-flow/clients/web/src/lib/hooks/use-cycles.ts
@yh-flow/clients/web/src/lib/hooks/use-modules.ts
@yh-flow/clients/web/src/lib/hooks/use-pages.ts
@yh-flow/clients/web/src/lib/hooks/use-views.ts
@yh-flow/clients/web/src/lib/hooks/use-notifications.ts
@yh-flow/clients/web/src/lib/hooks/use-analytics.ts
@yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-module-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-issue-views.ts
@yh-flow/clients/web/src/lib/hooks/use-page-mutations.ts
@yh-flow/clients/web/src/lib/hooks/use-view-mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 query-keys.ts 集中式查询键工厂</name>
  <files>
    yh-flow/clients/web/src/lib/services/query-keys.ts
  </files>

<read_first>
@yh-flow/clients/web/src/lib/hooks/use-workspaces.ts
@yh-flow/clients/web/src/lib/hooks/use-projects.ts
@yh-flow/clients/web/src/lib/hooks/use-members.ts
@yh-flow/clients/web/src/lib/hooks/use-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-comments.ts
@yh-flow/clients/web/src/lib/hooks/use-cycles.ts
@yh-flow/clients/web/src/lib/hooks/use-modules.ts
@yh-flow/clients/web/src/lib/hooks/use-pages.ts
@yh-flow/clients/web/src/lib/hooks/use-views.ts
@yh-flow/clients/web/src/lib/hooks/use-notifications.ts
@yh-flow/clients/web/src/lib/hooks/use-analytics.ts
@yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-module-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-issue-views.ts
@yh-flow/clients/web/src/lib/hooks/use-page-mutations.ts
@yh-flow/clients/web/src/lib/hooks/use-view-mutations.ts
</read_first>

  <action>
    在 `src/lib/services/query-keys.ts` 中创建集中式查询键工厂。使用 TanStack Query v5 的 `readonly [...unknown[]]` 模式（每个工厂函数返回 `readonly [string, ...unknown[]]` 元组）。

    **结构约定：**
    - 每个模块一个命名空间对象（如 `workspaceKeys`、`issueKeys`）
    - 每个工厂函数以模块名称前缀开头，作为键的根部分
    - 所有工厂函数导出在一个 `export const queryKeys = { ... }` 对象中
    - 每个工厂函数返回 `as const` 确保类型安全

    **模块覆盖清单（共 13 组）：**

    1. **workspaceKeys**：
       - `all()` → `["workspaces"]` as const（列表键，用于 invalidate）
       - `detail(id: string)` → `["workspaces", id]` as const
       - `current()` → `["workspaces", "current"]` as const

    2. **projectKeys**：
       - `all()` → `["projects"]` as const
       - `list(workspaceId: string)` → `["projects", workspaceId]` as const
       - `detail(workspaceId: string, projectId: string)` → `["projects", workspaceId, projectId]` as const

    3. **memberKeys**：
       - `all()` → `["members"]` as const
       - `list(workspaceId: string)` → `["members", workspaceId]` as const

    4. **issueKeys**：
       - `all()` → `["issues"]` as const
       - `list(projectId: string, filters?: Record<string, unknown>)` → `["issues", projectId, filters]` as const
       - `detail(projectId: string, issueId: string)` → `["issues", projectId, issueId]` as const

    5. **stateKeys**：
       - `all()` → `["states"]` as const
       - `list(projectId: string)` → `["states", projectId]` as const

    6. **labelKeys**：
       - `all()` → `["labels"]` as const
       - `list(projectId: string)` → `["labels", projectId]` as const

    7. **commentKeys**：
       - `all()` → `["comments"]` as const
       - `list(issueId: string)` → `["comments", issueId]` as const

    8. **cycleKeys**：
       - `all()` → `["cycles"]` as const
       - `list(projectId: string)` → `["cycles", projectId]` as const
       - `detail(projectId: string, cycleId: string)` → `["cycles", projectId, cycleId]` as const
       - `progress(projectId: string, cycleId: string)` → `["cycles", projectId, cycleId, "progress"]` as const

    9. **moduleKeys**：
       - `all()` → `["modules"]` as const
       - `list(projectId: string)` → `["modules", projectId]` as const
       - `detail(projectId: string, moduleId: string)` → `["modules", projectId, moduleId]` as const
       - `links(moduleId: string)` → `["modules", moduleId, "links"]` as const

    10. **pageKeys**：
        - `all()` → `["pages"]` as const
        - `list(workspaceId: string)` → `["pages", workspaceId]` as const
        - `detail(workspaceId: string, pageId: string)` → `["pages", workspaceId, pageId]` as const
        - `archived(workspaceId: string)` → `["pages", "archived", workspaceId]` as const

    11. **viewKeys**：
        - `all()` → `["views"]` as const
        - `list(projectId: string)` → `["views", projectId]` as const
        - `detail(viewId: string)` → `["views", viewId]` as const

    12. **notificationKeys**：
        - `all()` → `["notifications"]` as const
        - `list(workspaceId: string)` → `["workspace-notifications", workspaceId]` as const
        - `unreadCount(workspaceId: string)` → `["workspace-unread-count", workspaceId]` as const

    13. **analyticsKeys**：
        - `all()` → `["analytics"]` as const
        - `dashboard(workspaceId: string)` → `["analytics", workspaceId]` as const

    **导出结构：**
    ```
    export const queryKeys = {
      workspace: workspaceKeys,
      project: projectKeys,
      member: memberKeys,
      issue: issueKeys,
      state: stateKeys,
      label: labelKeys,
      comment: commentKeys,
      cycle: cycleKeys,
      module: moduleKeys,
      page: pageKeys,
      view: viewKeys,
      notification: notificationKeys,
      analytics: analyticsKeys,
    } as const;
    ```

    **多导出方式**：除了 `queryKeys` 命名空间对象外，同时单个导出每个命名空间对象（`export const workspaceKeys = { ... }`），方便 hooks 直接导入特定模块的工厂。

    注意：`as const` 在 TypeScript 中会将元组标记为 `readonly`，这完全兼容 TanStack Query 的 `QueryKey` 类型（`ReadonlyArray<unknown>`）。

  </action>

  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>

  <done>
    - `query-keys.ts` 文件存在
    - 包含全部 13 个模块的键工厂（workspace / project / member / issue / state / label / comment / cycle / module / page / view / notification / analytics）
    - 导出 `queryKeys` 命名空间对象
    - 每个模块工厂同时单独导出
    - 所有工厂函数返回 `as const` 只读元组
    - TypeScript 编译通过
    - 键命名与现有 hooks 中的字符串键完全兼容
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

无信任边界——查询键工厂是纯数据结构生成，不涉及用户输入或网络通信。
</threat_model>

<verification>
1. TypeScript 编译：`cd yh-flow/clients/web && npx tsc --noEmit --pretty`
2. 验证所有 13 个模块的工厂函数签出正确的键结构
</verification>

<success_criteria>

1. `query-keys.ts` 创建完成，包含 13 个模块的所有键工厂
2. 每个工厂函数返回 `as const` 只读元组
3. TypeScript 编译无错误
4. 键命名与现有 hooks 的字符串键模式兼容
   </success_criteria>

<output>
创建 `.planning/phases/21-infrastructure-foundation/21-02-SUMMARY.md` 当完成
</output>
