---
phase: 18-cycle-module
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - yh-flow/clients/web/app/components/cycles/cycles-view.tsx
  - yh-flow/clients/web/app/components/cycles/cycles-view-header.tsx
  - yh-flow/clients/web/app/components/cycles/form.tsx
  - yh-flow/clients/web/app/components/cycles/modal.tsx
  - yh-flow/clients/web/app/components/cycles/delete-modal.tsx
  - yh-flow/clients/web/app/components/cycles/transfer-issues-modal.tsx
  - yh-flow/clients/web/app/components/cycles/cycle-peek-overview.tsx
  - yh-flow/clients/web/app/components/cycles/list/root.tsx
  - yh-flow/clients/web/app/components/cycles/list/cycles-list-item.tsx
  - yh-flow/clients/web/app/components/cycles/list/cycles-list-map.tsx
  - yh-flow/clients/web/app/components/cycles/list/cycle-list-group-header.tsx
  - yh-flow/clients/web/app/components/cycles/list/cycle-list-item-action.tsx
  - yh-flow/clients/web/app/components/cycles/list/cycle-list-project-group-header.tsx
  - yh-flow/clients/web/app/components/cycles/active-cycle/progress.tsx
  - yh-flow/clients/web/app/components/cycles/active-cycle/cycle-stats.tsx
  - yh-flow/clients/web/app/components/cycles/active-cycle/productivity.tsx
  - yh-flow/clients/web/app/components/cycles/analytics-sidebar/root.tsx
  - yh-flow/clients/web/app/components/cycles/analytics-sidebar/burndown-chart.tsx
  - yh-flow/clients/web/app/components/cycles/analytics-sidebar/sidebar-details.tsx
  - yh-flow/clients/web/app/components/cycles/analytics-sidebar/sidebar-header.tsx
  - yh-flow/clients/web/app/components/cycles/analytics-sidebar/issue-progress.tsx
  - yh-flow/clients/web/app/components/cycles/analytics-sidebar/progress-stats.tsx
  - yh-flow/clients/web/app/components/cycles/dropdowns/estimate-type-dropdown.tsx
  - yh-flow/clients/web/app/components/cycles/quick-actions.tsx
  - yh-flow/clients/web/app/components/cycles/transfer-issues.tsx
files_created:
  - app/components/cycles/
  - app/components/cycles/list/
  - app/components/cycles/active-cycle/
  - app/components/cycles/analytics-sidebar/
  - app/components/cycles/dropdowns/
autonomous: true
requirements: [CYCLE-01, CYCLE-02, CYCLE-03, CYCLE-04]

must_haves:
  truths:
    - "CyclesView 组件包含 3 个 Tab（活跃/已完成/全部）通过 CycleStore.activeTab 切换"
    - "CyclesViewHeader 显示"创建周期"按钮 + 视图切换"
    - "Cycle 列表项显示：名称、日期范围、进度条、Issue 计数、状态点"
    - "Cycle 创建通过 Modal + CycleForm（名称、起止日期、描述）"
    - "Cycle 详情页显示进度条、统计、Burndown 图表"
    - "Burndown 图表使用 SVG Fork Plane 组件（D-P18-08），含理想线（虚线灰）与实际线（accent 蓝）"
    - "Cycle 看板复用 Phase 16 KanbanView 通过 cycleId 过滤（D-P18-09）"
    - "Cycle 删除弹窗含 Issue 转移选项（TransferModal）"
    - "所有组件使用中文文案，遵循 18-UI-SPEC Copywriting Contract"
  artifacts:
    - path: "yh-flow/clients/web/app/components/cycles/"
      provides: "完整 Cycle 组件目录"
    - path: "yh-flow/clients/web/app/components/cycles/cycles-view.tsx"
      provides: "Cycle 主视图容器（Tab 切换）"
    - path: "yh-flow/clients/web/app/components/cycles/analytics-sidebar/burndown-chart.tsx"
      provides: "SVG Burndown 图表（D-P18-08）"
  key_links:
    - from: "cycles-view.tsx"
      to: "@tanstack/react-query, @/store/root.store, @/components/cycles/*, @/components/issues/kanban-view, @/components/issues/list-view"
      via: "imports (useQuery, useStore, CycleForm, KanbanView, IssueListView)"
    - from: "burndown-chart.tsx"
      to: "@tanstack/react-query"
      via: "useStore, progress data from hooks"
    - from: "form.tsx"
      to: "@tanstack/react-query, @/../src/lib/hooks/use-cycles"
      via: "useCycleMutations"
---

<objective>
从 Plane apps/web/core/components/cycles/ Fork Cycle 相关 UI 组件到 yh-flow。适配 yh-flow 的导入路径（替换 @/components/cycles/ 为 ./，替换 @plane/* 为本地等价）。

目的：实现 CYCLE-01（Cycle 列表）、CYCLE-02（Cycle 详情）、CYCLE-03（Cycle Issue 管理）、CYCLE-04（创建/编辑/删除 Cycle）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-cycle-module/18-CONTEXT.md — D-P18-01~09
@.planning/phases/18-cycle-module/18-UI-SPEC.md — Full component inventory, copywriting, visual contract

# Plane source to fork

@apps/web/core/components/cycles/cycles-view.tsx
@apps/web/core/components/cycles/cycles-view-header.tsx
@apps/web/core/components/cycles/form.tsx
@apps/web/core/components/cycles/modal.tsx
@apps/web/core/components/cycles/delete-modal.tsx
@apps/web/core/components/cycles/transfer-issues-modal.tsx
@apps/web/core/components/cycles/cycle-peek-overview.tsx
@apps/web/core/components/cycles/list/
@apps/web/core/components/cycles/active-cycle/
@apps/web/core/components/cycles/analytics-sidebar/
@apps/web/core/components/cycles/dropdowns/
@apps/web/core/components/cycles/quick-actions.tsx
@apps/web/core/components/cycles/transfer-issues.tsx

# yh-flow patterns

@yh-flow/clients/web/app/components/issues/list-view.tsx — existing list pattern
@yh-flow/clients/web/app/components/issues/kanban-view.tsx — view container pattern (reusable as Cycle board)
@yh-flow/clients/web/src/lib/hooks/use-cycles.ts — data hooks (from Plan 18-01)
@yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts — cycle issue hooks (from Plan 18-01)
@yh-flow/clients/web/app/store/cycle.store.ts — cycle UI store (from Plan 18-01)
@yh-flow/clients/web/src/lib/mock-data.ts — mock data
@yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx — reusable filter component (D-P18-06)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fork Cycle 列表组件</name>
  <files>
    - yh-flow/clients/web/app/components/cycles/cycles-view.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/cycles-view-header.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/list/root.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/list/cycles-list-item.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/list/cycles-list-map.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/list/cycle-list-group-header.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/list/cycle-list-item-action.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/list/cycle-list-project-group-header.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/quick-actions.tsx (CREATE)
  </files>
  <read_first>
    apps/web/core/components/cycles/cycles-view.tsx — Plane cycles-view.tsx main container
    apps/web/core/components/cycles/cycles-view-header.tsx — Plane header with create button + view toggle
    apps/web/core/components/cycles/list/ — Plane list directory
    apps/web/core/components/cycles/quick-actions.tsx — quick action buttons
  </read_first>
  <action>
    从 Plane apps/web/core/components/cycles/ 复制组件到 app/components/cycles/，遵循以下适配规则：

    1. 创建 cycles-view.tsx（由 Plane cycles-view.tsx Fork 适配）：
       - Props: `{ workspaceId: string; projectId: string }`
       - 使用 useCycles(projectId) 获取所有 Cycle
       - 使用 useStore().cycle 管理 activeTab/selectedCycleId/viewLayout
       - 渲染 CyclesViewHeader（传递 projectId + 创建按钮回调）
       - 根据 activeTab（"active"/"completed"/"all"）过滤 Cycle 列表：
         - active: status === "current"
         - completed: status === "completed"
         - all: 全部
       - Tab 切换通过 CycleStore.setActiveTab()
       - 每个 Tab 有独立的空状态文案（参考 18-UI-SPEC Copywriting Contract）
       - 内容区渲染 CycleListMap（Tab 过滤后的数据）
       - 加载中显示 skeleton（3-4 个占位卡片）
       - 空状态：无 Cycles 时显示居中空状态（"暂无活跃周期"等）
       - 错误状态：显示重试按钮
       - 标记：`// FLOW: Forked from Plane cycles/cycles-view.tsx`

    2. 创建 cycles-view-header.tsx（由 Plane cycles-view-header.tsx Fork）：
       - 显示页面标题"周期" + "创建周期"按钮
       - "创建周期"按钮 → 调用 CycleStore.openCycleModal("create")
       - 视图切换按钮（列表/看板）：调用 CycleStore.setViewLayout()
       - 布局：flex row，左侧标题，右侧创建按钮 + 视图切换
       - 使用 FilterBar 组件（传入 showGroupBy=false, showSorting=true per D-P18-06）
       - 标记：`// FLOW: Forked from Plane cycles/cycles-view-header.tsx`

    3. 创建 list/root.tsx — Cycle 列表根容器：
       - 接收 cycles: ICycle[] 数组
       - 使用 @hello-pangea/dnd DragDropContext 包装列表（支持拖拽排序）
       - 渲染 CyclesListMap 子组件
       - 标记：`// FLOW: Forked from Plane cycles/list/root.tsx`

    4. 创建 list/cycles-list-item.tsx — Cycle 列表项卡片：
       - 显示：Cycle 名称（semibold 16px）、日期范围（text-xs text-secondary）、进度条（8px 高 accent 蓝填充）、Issue 计数（已完成/总数）、状态指示点（活跃周期显示绿色圆点）
       - 单击 → 导航到 `/workspaces/:ws/projects/:pid/cycles/:cycleId`
       - 标记：`// FLOW: Forked from Plane cycles/list/cycles-list-item.tsx`

    5. 创建 list/cycles-list-map.tsx — Cycle 列表映射组件：
       - 按状态组合分组（current/upcoming/completed/draft）
       - 每个分组渲染 CycleListGroupHeader + cycles-list-item 列表
       - 标记：`// FLOW: Forked from Plane cycles/list/cycles-list-map.tsx`

    6. 创建 list/cycle-list-group-header.tsx — 分组标题：
       - 显示分组名称（"活跃周期"/"即将开始"/"已完成周期"）
       - 显示该组 Cycle 数量
       - 标记：`// FLOW: Forked from Plane cycles/list/cycle-list-group-header.tsx`

    7. 创建 list/cycle-list-item-action.tsx — 操作按钮：
       - 编辑/删除操作按钮
       - 标记：`// FLOW: Forked from Plane cycles/list/cycle-list-item-action.tsx`

    8. 创建 list/cycle-list-project-group-header.tsx — 项目分组标题：
       - Cycle 按项目分组时的标题（多项目视图）
       - 标记：`// FLOW: Forked from Plane cycles/list/cycle-list-project-group-header.tsx`

    9. 创建 quick-actions.tsx — Cycle 快速操作：
       - 编辑/删除等快捷操作菜单
       - 标记：`// FLOW: Forked from Plane cycles/quick-actions.tsx`

    导入路径适配规则（同 Plan 17-02/03/04）：
    - `@/components/cycles/` → `./`（同级引用）
    - `@/components/cycles/list/` → `./list/`
    - `@plane/types` → 保持 `@plane/types`
    - `@plane/ui` → 保持 `@plane/ui`（组件库已 Fork 在 Phase 14）
    - `@plane/utils` → 保持 `@plane/utils`
    - Plane 内部 hooks → yh-flow 等价替换（useCycles, useCycleDetail, useStore）
    - Plane `observable` store imports → yh-flow `useStore().cycle.*`
    - 所有修改用 `// FLOW:` 标记

    中文文案使用 18-UI-SPEC 定义的 Copywriting Contract。Tab 名称为"活跃周期"/"已完成周期"/"全部周期"；空状态"暂无活跃周期"/"暂无已完成周期"。

  </action>
  <acceptance_criteria>
    1. cycles-view.tsx 导出 CyclesView — 含 3 Tab 切换
    2. cycles-view-header.tsx 导出 CyclesViewHeader — 含创建按钮 + FilterBar + 视图切换
    3. list/ 目录包含 6 个组件文件（root, list-item, list-map, group-header, item-action, project-group-header）
    4. quick-actions.tsx 导出 CycleQuickActions
    5. 所有组件使用中文文案
    6. npx tsc --noEmit 不新增 Cycle 组件相关错误
  </acceptance_criteria>
  <verify>
    <automated>ls app/components/cycles/list/*.tsx | wc -l</automated>
    <automated>grep -q "CyclesView" app/components/cycles/cycles-view.tsx</automated>
    <automated>grep -q "CyclesViewHeader" app/components/cycles/cycles-view-header.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep "cycles" | grep -c "error" || echo "no new cycle errors"</automated>
  </verify>
  <done>
    * Cycle 列表组件（CyclesView, CyclesViewHeader, list/*, quick-actions）完成
    * 3 Tab 切换（活跃/已完成/全部）
    * 空/加载/错误状态处理
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fork Cycle 创建/编辑表单 + 弹窗/删除/转移</name>
  <files>
    - yh-flow/clients/web/app/components/cycles/form.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/modal.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/delete-modal.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/transfer-issues-modal.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/transfer-issues.tsx (CREATE)
  </files>
  <read_first>
    apps/web/core/components/cycles/form.tsx — Plane CycleForm
    apps/web/core/components/cycles/modal.tsx — Plane CycleModal
    apps/web/core/components/cycles/delete-modal.tsx — Plane CycleDeleteModal
    apps/web/core/components/cycles/transfer-issues-modal.tsx — Plane transfer modal
    apps/web/core/components/cycles/transfer-issues.tsx — Plane transfer component
  </read_first>
  <action>
    1. 创建 form.tsx（Cycle 表单，由 Plane cycles/form.tsx Fork）：
       - Props: `{ onClose?: () => void; workspaceId: string; projectId: string; data?: ICycle }`
       - 模式：创建模式 (data 为空) | 编辑模式 (data 不为空)
       - 字段：名称（text input）、描述（textarea）、开始日期（date input）、结束日期（date input）
       - 默认值（创建模式）：开始日期 = 今天，结束日期 = 今天+30天
       - 验证：名称必填，结束日期 >= 开始日期
       - 提交：调用 useCycleMutations().createCycle.mutate 或 updateCycle.mutate
       - 提交成功后：关闭弹窗 + invalidateQueries(["cycles"])
       - 标记：`// FLOW: Forked from Plane cycles/form.tsx`

    2. 创建 modal.tsx（Cycle 弹窗包装，由 Plane cycles/modal.tsx Fork）：
       - 使用 @plane/ui Modal 组件包装 CycleForm
       - 标题：创建模式→"创建周期"，编辑模式→"编辑周期"
       - 通过 CycleStore.cycleModalOpen 控制可见性
       - 通过 CycleStore.cycleModalMode 决定模式
       - 标记：`// FLOW: Forked from Plane cycles/modal.tsx`

    3. 创建 delete-modal.tsx（删除确认弹窗，由 Plane cycles/delete-modal.tsx Fork）：
       - Props: `{ isOpen: boolean; onClose: () => void; cycle: ICycle; projectId: string }`
       - 确认文案："确定删除此周期？周期内的 Issue 不会被删除。"
       - 按钮：取消（@plane/ui Button variant="neutral-primary"）| 删除（bg-danger-primary）
       - 删除操作后：若 cycle 有关联 Issue → 显示 TransferModal
       - 调用 useCycleMutations().deleteCycle.mutate(cycleId)
       - 标记：`// FLOW: Forked from Plane cycles/delete-modal.tsx`

    4. 创建 transfer-issues-modal.tsx（Issue 转移弹窗，由 Plane cycles/transfer-issues-modal.tsx Fork）：
       - Props: `{ isOpen: boolean; onClose: () => void; fromCycleId: string; projectId: string }`
       - 内容：下拉选择目标 Cycle（排除当前 Cycle）
       - 说明文字："周期中的 Issue 将转移到："
       - 提交：调用 useTransferCycleIssues().mutate({ fromCycleId, toCycleId })
       - 标记：`// FLOW: Forked from Plane cycles/transfer-issues-modal.tsx`

    5. 创建 transfer-issues.tsx（转移逻辑组件，由 Plane cycles/transfer-issues.tsx Fork）：
       - 独立的 Issue 转移逻辑（不含 UI），供 delete-modal 复用
       - 标记：`// FLOW: Forked from Plane cycles/transfer-issues.tsx`

  </action>
  <acceptance_criteria>
    1. form.tsx 导出 CycleForm — 创建/编辑模式，名称/描述/起止日期字段
    2. modal.tsx 导出 CycleModal — 弹窗包装 CycleForm
    3. delete-modal.tsx 导出 CycleDeleteModal — 删除确认 + Issue 转移选项
    4. transfer-issues-modal.tsx 导出 CycleTransferModal — Cycle 选择转移
    5. 遵循 D-P18-07（创建弹窗轻量，详情页完整编辑）
    6. npx tsc --noEmit 不新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CycleForm" app/components/cycles/form.tsx</automated>
    <automated>grep -q "CycleModal" app/components/cycles/modal.tsx</automated>
    <automated>grep -q "CycleDeleteModal" app/components/cycles/delete-modal.tsx</automated>
    <automated>grep -q "CycleTransferModal" app/components/cycles/transfer-issues-modal.tsx</automated>
  </verify>
  <done>
    * CycleForm / CycleModal / CycleDeleteModal / CycleTransferModal 完成
    * 创建弹窗 + 编辑模式（D-P18-07）实现
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fork Cycle 详情组件（active-cycle/ + cycle-peek-overview）</name>
  <files>
    - yh-flow/clients/web/app/components/cycles/active-cycle/progress.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/active-cycle/cycle-stats.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/active-cycle/productivity.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/cycle-peek-overview.tsx (CREATE)
  </files>
  <read_first>
    apps/web/core/components/cycles/active-cycle/ — Plane active cycle detail
    apps/web/core/components/cycles/active-cycle/progress.tsx
    apps/web/core/components/cycles/active-cycle/cycle-stats.tsx
    apps/web/core/components/cycles/active-cycle/productivity.tsx
    apps/web/core/components/cycles/cycle-peek-overview.tsx
  </read_first>
  <action>
    1. 创建 active-cycle/progress.tsx（Cycle 进度条，由 Plane cycles/active-cycle/progress.tsx Fork）：
       - Props: `{ cycle: ICycle }`
       - 进度百分比 = completed_issues / total_issues（避免除以 0）
       - 渲染：8px 高 progress bar + 百分比文字
       - 使用 @plane/ui ProgressBar 组件
       - 标记：`// FLOW: Forked from Plane cycles/active-cycle/progress.tsx`

    2. 创建 active-cycle/cycle-stats.tsx（统计概览，由 Plane cycles/active-cycle/cycle-stats.tsx Fork）：
       - 显示：已完成 Issue 数、Issue 总数、开始日期、结束日期、剩余天数
       - 使用 text-xs text-secondary 标签、text-sm text-primary 数值
       - 标记：`// FLOW: Forked from Plane cycles/active-cycle/cycle-stats.tsx`

    3. 创建 active-cycle/productivity.tsx（生产力指标，由 Plane cycles/active-cycle/productivity.tsx Fork）：
       - Props: `{ cycle: ICycle }`
       - 显示：每天完成的 Issue 数、平均 Issues/天
       - 标记：`// FLOW: Forked from Plane cycles/active-cycle/productivity.tsx`

    4. 创建 cycle-peek-overview.tsx（Cycle 详情弹窗/概览，由 Plane cycles/cycle-peek-overview.tsx Fork）：
       - Props: `{ cycleId: string; projectId: string; isOpen: boolean; onClose: () => void }`
       - 使用 useCycleDetail(projectId, cycleId) 获取数据
       - 显示：Cycle 名称、日期范围、进度条、描述、Issue 计数
       - 使用 CycleStats + Productivity 子组件
       - 标记：`// FLOW: Forked from Plane cycles/cycle-peek-overview.tsx`

  </action>
  <acceptance_criteria>
    1. active-cycle/ 目录包含 3 个组件
    2. cycle-peek-overview.tsx 导出 CyclePeekOverview
    3. 进度条使用 @plane/ui ProgressBar
    4. npx tsc --noEmit 不新增错误
  </acceptance_criteria>
  <verify>
    <automated>ls app/components/cycles/active-cycle/*.tsx | wc -l</automated>
    <automated>grep -q "CyclePeekOverview" app/components/cycles/cycle-peek-overview.tsx</automated>
  </verify>
  <done>
    * Cycle 详情组件（progress, stats, productivity, peek-overview）完成
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fork Burndown 图表（analytics-sidebar/ + dropdowns/）</name>
  <files>
    - yh-flow/clients/web/app/components/cycles/analytics-sidebar/root.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/analytics-sidebar/burndown-chart.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/analytics-sidebar/sidebar-details.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/analytics-sidebar/sidebar-header.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/analytics-sidebar/issue-progress.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/analytics-sidebar/progress-stats.tsx (CREATE)
    - yh-flow/clients/web/app/components/cycles/dropdowns/estimate-type-dropdown.tsx (CREATE)
  </files>
  <read_first>
    apps/web/core/components/cycles/analytics-sidebar/ — Plane burndown + analytics
    apps/web/core/components/cycles/analytics-sidebar/root.tsx
    apps/web/core/components/cycles/dropdowns/estimate-type-dropdown.tsx
  </read_first>
  <action>
    从 Plane apps/web/core/components/cycles/analytics-sidebar/ 复制并适配：

    1. 创建 analytics-sidebar/root.tsx（分析侧栏容器）：
       - Props: `{ cycleId: string; projectId: string }`
       - 使用 useCycleProgress(projectId, cycleId) 获取进度数据
       - 布局：垂直 flex，包含 SidebarHeader + BurndownChart + IssueProgress + ProgressStats
       - 标记：`// FLOW: Forked from Plane cycles/analytics-sidebar/root.tsx`

    2. 创建 analytics-sidebar/burndown-chart.tsx — **核心 Burndown SVG 组件**（D-P18-08）：
       - Fork Plane Burndown 实现（SVG 渲染）
       - Props: `{ progress: TProgressChartData[]; startDate: string; endDate: string }`
       - 若 Plane 组件过于耦合 API 格式，降级为手写 SVG：
         - SVG viewBox 尺寸 (400 x 200)，padding 12px inner / 24px outer
         - X 轴：从 startDate 到 endDate 的天数，显示 5~7 个日期标签（text-xs）
         - Y 轴：Issue 数量（0 到 total_issues），5 个刻度
         - 理想线（ideal line）：从左上到右下的虚线（stroke-dasharray, text-tertiary gray）
         - 实际线（actual line）：基于实际完成数据的折线（accent blue）
         - 网格背景线（浅 gray 虚线）
         - 图表标题："Burndown 图"
       - 优先尝试 Fork Plane 算法；若不能工作则回退手写 SVG
       - 标记：`// FLOW: Forked from Plane cycles/analytics-sidebar/ | SVG burndown per D-P18-08`

    3. 创建 analytics-sidebar/sidebar-details.tsx（侧栏详情）：
       - Cycle 详细信息展示（日期、状态等）
       - 标记：`// FLOW: Forked from Plane cycles/analytics-sidebar/sidebar-details.tsx`

    4. 创建 analytics-sidebar/sidebar-header.tsx（侧栏标题）：
       - "周期统计"标题
       - Estimate type 选择器（Issue 计数/Story Points）
       - 标记：`// FLOW: Forked from Plane cycles/analytics-sidebar/sidebar-header.tsx`

    5. 创建 analytics-sidebar/issue-progress.tsx（Issue 进度详情）：
       - 分组显示 backlog/unstarted/started/completed/cancelled 各状态的 Issue 数量
       - 标记：`// FLOW: Forked from Plane cycles/analytics-sidebar/issue-progress.tsx`

    6. 创建 analytics-sidebar/progress-stats.tsx（进度统计数据）：
       - 显示 pending/completed/total 的数值和百分比
       - 标记：`// FLOW: Forked from Plane cycles/analytics-sidebar/progress-stats.tsx`

    7. 创建 dropdowns/estimate-type-dropdown.tsx（估算类型下拉）：
       - 切换 Issue 计数 / Story Points
       - 标记：`// FLOW: Forked from Plane cycles/dropdowns/estimate-type-dropdown.tsx`

  </action>
  <acceptance_criteria>
    1. analytics-sidebar/ 目录包含 6 个组件文件
    2. burndown-chart.tsx 导出 BurndownChart — SVG 渲染（Fork 或手写）
    3. BurndownChart 显示理想线 + 实际线 + 坐标轴
    4. dropdowns/ 目录包含 estimate-type-dropdown.tsx
    5. npx tsc --noEmit 不新增错误
  </acceptance_criteria>
  <verify>
    <automated>ls app/components/cycles/analytics-sidebar/*.tsx | wc -l</automated>
    <automated>grep -q "<svg\|viewBox" app/components/cycles/analytics-sidebar/burndown-chart.tsx</automated>
    <automated>grep -q "BurndownChart" app/components/cycles/analytics-sidebar/burndown-chart.tsx</automated>
  </verify>
  <done>
    * Burndown SVG 图表完成（D-P18-08）
    * analytics-sidebar 目录完成（6 个组件）
    * dropdowns 目录完成
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID  | Category        | Component                             | Disposition | Mitigation Plan                                               |
| ---------- | --------------- | ------------------------------------- | ----------- | ------------------------------------------------------------- |
| T-18-CY-01 | DoS             | Burndown SVG large dataset            | mitigate    | SVG 最大渲染 60 个数据点（2 个月 daily），超出则聚合为周数据  |
| T-18-CY-02 | Tampering       | CycleForm date validation             | mitigate    | form.tsx 中 end_date >= start_date 验证，不满足时禁用提交按钮 |
| T-18-CY-03 | Info Disclosure | transfer-issues-modal 所有 Cycle 列表 | accept      | Mock 阶段所有数据可见，生产 API 接权限过滤                    |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
ls app/components/cycles/ | head -20
ls app/components/cycles/list/ | wc -l
ls app/components/cycles/active-cycle/ | wc -l
ls app/components/cycles/analytics-sidebar/ | wc -l
grep -q "CyclesView" app/components/cycles/cycles-view.tsx
grep -q "CycleForm" app/components/cycles/form.tsx
grep -q "BurndownChart" app/components/cycles/analytics-sidebar/burndown-chart.tsx
```
</verification>

<success_criteria>

- [ ] Cycle 列表组件（CyclesView + CyclesViewHeader + list/\* + quick-actions）
- [ ] Cycle 创建/编辑（CycleForm + CycleModal）
- [ ] Cycle 删除 + Issue 转移（CycleDeleteModal + CycleTransferModal + transfer-issues）
- [ ] Cycle 详情概览（active-cycle/\* + cycle-peek-overview）
- [ ] Burndown SVG 图表（analytics-sidebar/\* + burndown-chart.tsx）
- [ ] Cycle 下拉选择器（dropdowns/estimate-type-dropdown）
- [ ] 所有中文文案遵循 18-UI-SPEC
- [ ] npx tsc --noEmit 无新增错误

</success_criteria>

<output>
创建 `.planning/phases/18-cycle-module/18-02-SUMMARY.md`
</output>
