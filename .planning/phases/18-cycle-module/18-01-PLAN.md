---
phase: 18-cycle-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yh-flow/clients/web/src/lib/mock-data.ts
  - yh-flow/clients/web/app/store/root.store.ts
  - yh-flow/clients/web/app/lib/store-context.tsx
  - yh-flow/clients/web/app/store/types.ts
files_created:
  - yh-flow/clients/web/src/lib/hooks/use-cycles.ts
  - yh-flow/clients/web/src/lib/hooks/use-modules.ts
  - yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts
  - yh-flow/clients/web/src/lib/hooks/use-module-issues.ts
  - yh-flow/clients/web/app/store/cycle.store.ts
  - yh-flow/clients/web/app/store/module.store.ts
autonomous: true
requirements: [CYCLE-01, CYCLE-02, CYCLE-03, CYCLE-04, MODU-01, MODU-02, MODU-03]

must_haves:
  truths:
    - "TanStack Query hooks 提供 Cycle/Module CRUD mock 数据查询"
    - "CycleStore 管理 Cycle UI 状态（activeTab, selectedCycleId, viewLayout）"
    - "ModuleStore 管理 Module UI 状态（selectedModuleId, filterState）"
    - "mock-data.ts 扩展包含 5+ 个 mock Cycle 和 4+ 个 mock Module"
    - "Cycle / Module mock 数据与现有 Issue mock 数据通过 cycle_id/module_ids 关联"
    - "root.store.ts 注册 CycleStore + ModuleStore"
    - "store-context.tsx 的 StoreProvider 传递新 store"
  artifacts:
    - path: "yh-flow/clients/web/src/lib/hooks/use-cycles.ts"
      provides: "useCycles, useCycleDetail, useCycleMutations hooks"
    - path: "yh-flow/clients/web/src/lib/hooks/use-modules.ts"
      provides: "useModules, useModuleDetail, useModuleMutations hooks"
    - path: "yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts"
      provides: "useCycleIssues — issues filtered by cycleId"
    - path: "yh-flow/clients/web/src/lib/hooks/use-module-issues.ts"
      provides: "useModuleIssues — issues filtered by moduleId"
    - path: "yh-flow/clients/web/app/store/cycle.store.ts"
      provides: "CycleStore (UI state only)"
    - path: "yh-flow/clients/web/app/store/module.store.ts"
      provides: "ModuleStore (UI state only)"
  key_links:
    - from: "use-cycles.ts"
      to: "@plane/types, ../mock-data"
      via: "ICycle, TCycleGroups, MOCK_CYCLES"
    - from: "use-cycles.ts"
      to: "@tanstack/react-query"
      via: "useQuery, useMutation"
    - from: "cycle.store.ts"
      to: "mobx"
      via: "makeObservable, observable, action"
    - from: "cycle.store.ts"
      to: "@/components/issues/filters/types"
      via: "TViewLayout"
    - from: "module.store.ts"
      to: "mobx, @/components/issues/filters/types"
      via: "mobx observable, TFilterCriteria"
    - from: "root.store.ts"
      to: "./cycle.store, ./module.store"
      via: "creates CycleStore, ModuleStore instances"
---

<objective>
创建 Phase 18（周期 & 模块）的数据层基础设施。包括 TanStack Query hooks（useCycles, useModules, useCycleIssues, useModuleIssues）、MobX UI stores（CycleStore, ModuleStore），以及 mock 数据扩展。

目的：为 Cycle/Module 组件层提供完整的数据获取和 UI 状态管理能力。延用 Phase 16 的 D-P16-02 混合状态管理模式（TanStack Query 存数据，MobX 存 UI 状态）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-cycle-module/18-CONTEXT.md — D-P18-04, D-P18-05
@.planning/phases/18-cycle-module/18-UI-SPEC.md — Component inventory for hooks/stores

# Plane types for Cycle/Module

@packages/types/src/cycle/cycle.ts — ICycle, TCycleGroups, TCycleProgress, TProgressSnapshot
@packages/types/src/module/modules.ts — IModule, TModuleStatus, ModuleLink
@packages/types/src/issues.ts — IIssueCycle, IIssueModule, ILinkDetails
@packages/constants/src/cycle.ts — cycle status helpers
@packages/constants/src/module.ts — module status helpers

# Existing patterns to follow

@yh-flow/clients/web/src/lib/hooks/use-issues.ts — TanStack Query + mock data pattern
@yh-flow/clients/web/app/store/issue.store.ts — MobX UI store pattern
@yh-flow/clients/web/app/store/root.store.ts — Root store registration pattern
@yh-flow/clients/web/app/store/types.ts — Store type interface pattern
@yh-flow/clients/web/src/lib/mock-data.ts — Existing mock data structure
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 mock-data.ts 扩展 Cycle 和 Module 数据</name>
  <files>
    - yh-flow/clients/web/src/lib/mock-data.ts (MODIFY)
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/mock-data.ts — full file to find insertion point near end (after MOCK_ISSUES)
    packages/types/src/cycle/cycle.ts — ICycle interface fields
    packages/types/src/module/modules.ts — IModule interface fields
  </read_first>
  <action>
    在 mock-data.ts 末尾（MOCK_ISSUES 之后、export 之前）插入两组 mock 数据：

    1. `MOCK_CYCLES: ICycle[]` — 至少 5 个 Cycle，跨 proj-1（3个）和 proj-2（2个）：
       - 当前活跃周期（current）1个：start_date = 今天-14天，end_date = 今天+14天
       - 即将开始周期（upcoming）1个：start_date = 今天+15天
       - 已完成周期（completed）2个：含 progress_snapshot 数据
       - 草稿周期（draft）1个：start_date/end_date 均为 null
       - 每个 Cycle 包含：id, name, description, start_date, end_date, status(TCycleGroups), progress_snapshot（含 total_issues, completed_issues, backlog_issues 等）, sort_order, project_id, workspace_id, created_by, created_at, updated_at, assigned_to_ids, owned_by_id
       - 使用 date-fns/addDays 或手写相对于 now 的时间计算

    2. `MOCK_MODULES: IModule[]` — 至少 4 个 Module，跨 proj-1（3个）和 proj-2（1个）：
       - 不同状态（TModuleStatus）：planned, in-progress, completed, cancelled
       - 每个 Module 包含：id, name, description, status, start_date, target_date, completed_issues, backlog_issues, started_issues, unstarted_issues, cancelled_issues, total_issues, member_ids, lead_id, sort_order, project_id, workspace_id, created_by, created_at, updated_at

    3. 更新部分现有 MOCK_ISSUES 设置 cycle_id 和 module_ids：
       - issue-1 → cycle_id = "cycle-active-1" (currently null)
       - issue-2 → cycle_id = "cycle-active-1" (currently null)
       - issue-5 → cycle_id = "cycle-active-1" (currently null)
       - issue-8 → cycle_id = "cycle-upcoming-1" (currently null)
       - issue-7 → module_ids = ["mod-proj1-1", "mod-proj1-2"] (currently null)
       - issue-4 → module_ids = ["mod-proj1-1"] (currently null)
       - issue-9 → module_ids = ["mod-proj2-1"] (currently null)
       - 其他 issue 保持 null

    4. 导出类型声明：在文件末尾 export { MOCK_CYCLES, MOCK_MODULES };

    遵循现有 mock-data.ts 风格：每个字段添加注释说明，数据使用中文语境（cycle 名称如 "Sprint 14"、"迭代 15"等；模块名称如 "用户认证模块"、"UI 重构"等）。

  </action>
  <acceptance_criteria>
    1. MOCK_CYCLES 数组包含 5+ 个 Cycle，覆盖 current/upcoming/completed/draft 状态
    2. MOCK_MODULES 数组包含 4+ 个 Module，覆盖 planned/in-progress/completed/cancelled 状态
    3. 至少 3 个 Issue 关联了 cycle_id
    4. 至少 3 个 Issue 关联了 module_ids
    5. TypeScript 编译不报新类型错误（ICycle/IModule 来自 @plane/types）
  </acceptance_criteria>
  <verify>
    <automated>grep -q "MOCK_CYCLES" yh-flow/clients/web/src/lib/mock-data.ts</automated>
    <automated>grep -q "MOCK_MODULES" yh-flow/clients/web/src/lib/mock-data.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "mock-data.ts.*error" || echo "no errors"</automated>
  </verify>
  <done>
    * mock-data.ts 扩展完成
    * MOCK_CYCLES (5+) + MOCK_MODULES (4+) 已添加
    * Issue 已关联 cycle_id 和 module_ids
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 use-cycles.ts — Cycle TanStack Query hooks</name>
  <files>
    - yh-flow/clients/web/src/lib/hooks/use-cycles.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts — TanStack Query hook pattern (useQuery, useMutation with mock data)
    packages/types/src/cycle/cycle.ts — ICycle, TCycleGroups, TCycleProgress types
  </read_first>
  <action>
    按照 use-issues.ts 的模式创建 use-cycles.ts：

    1. `useCycles(projectId: string)` — 返回项目下所有 Cycles：
       - queryKey: ["cycles", projectId]
       - queryFn: await delay(250) → MOCK_CYCLES.filter(c => c.project_id === projectId)
       - enabled: !!projectId
       - 返回类型: useQuery&lt;ICycle[]&gt;

    2. `useCycleDetail(projectId: string, cycleId: string)` — 返回单个 Cycle 详情：
       - queryKey: ["cycles", projectId, cycleId]
       - queryFn: await delay(200) → MOCK_CYCLES.find(c => c.id === cycleId && c.project_id === projectId)
       - enabled: !!projectId && !!cycleId

    3. `useCycleMutations()` — Cycle CRUD mutation hooks：
       - queryClient.useQueryClient()
       - `createCycle` — mutationFn: async (data: Partial&lt;ICycle&gt;) => 创建新 Cycle（mock ID + sort_order）, push to MOCK_CYCLES, return it
       - `updateCycle` — mutationFn: async ({ cycleId, data }: { cycleId: string; data: Partial&lt;ICycle&gt; }) => 找到并更新 MOCK_CYCLES 中的条目
       - `deleteCycle` — mutationFn: async (cycleId: string) => 从 MOCK_CYCLES 中 splice 移除
       - onSuccess 分别: invalidateQueries(["cycles"])

    4. `useCycleProgress(projectId: string, cycleId: string)` — 进度数据：
       - queryKey: ["cycles", projectId, cycleId, "progress"]
       - queryFn: 返回 TProgressSnapshot 数据
       - enabled: !!cycleId

  </action>
  <acceptance_criteria>
    1. useCycles export — 接收 projectId 返回 ICycle[] 类型的 useQuery
    2. useCycleDetail export — 接收 projectId + cycleId 返回单 Cycle
    3. useCycleMutations export — 含 createCycle/updateCycle/deleteCycle
    4. 所有 hook 延用 use-issues.ts 的 delay + MOCK_* 模式
    5. 导入 @plane/types 的 ICycle, TCycleGroups, TCycleProgress
    6. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useCycles" yh-flow/clients/web/src/lib/hooks/use-cycles.ts</automated>
    <automated>grep -q "useCycleDetail" yh-flow/clients/web/src/lib/hooks/use-cycles.ts</automated>
    <automated>grep -q "useCycleMutations" yh-flow/clients/web/src/lib/hooks/use-cycles.ts</automated>
    <automated>grep -q "MOCK_CYCLES" yh-flow/clients/web/src/lib/hooks/use-cycles.ts</automated>
  </verify>
  <done>
    * useCycles, useCycleDetail, useCycleMutations hooks 完成
    * TanStack Query mock data 模式一致
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 创建 use-modules.ts — Module TanStack Query hooks</name>
  <files>
    - yh-flow/clients/web/src/lib/hooks/use-modules.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts — TanStack Query hook pattern
    packages/types/src/module/modules.ts — IModule, TModuleStatus types
  </read_first>
  <action>
    按照 use-issues.ts 的模式创建 use-modules.ts：

    1. `useModules(projectId: string)` — 返回项目下所有 Modules：
       - queryKey: ["modules", projectId]
       - queryFn: await delay(250) → MOCK_MODULES.filter(m => m.project_id === projectId)
       - enabled: !!projectId

    2. `useModuleDetail(projectId: string, moduleId: string)` — 返回单个 Module：
       - queryKey: ["modules", projectId, moduleId]
       - queryFn: await delay(200) → MOCK_MODULES.find(m => m.id === moduleId && m.project_id === projectId)
       - enabled: !!projectId && !!moduleId

    3. `useModuleMutations()` — Module CRUD mutation hooks：
       - createModule / updateModule / deleteModule
       - 对应 MOCK_MODULES 数组操作 + invalidateQueries(["modules"])
       - 与 useCycleMutations 结构一致

    4. `useModuleLinkMutations(moduleId: string)` — Module 链接管理：
       - addLink — 向 IModule.link_module[] 添加 ILinkDetails
       - updateLink — 更新链接 title/url
       - removeLink — 移除链接
       - invalidateQueries(["modules"])

  </action>
  <acceptance_criteria>
    1. useModules export — 接收 projectId 返回 IModule[] 类型的 useQuery
    2. useModuleDetail export — 接收 projectId + moduleId 返回单 Module
    3. useModuleMutations export — 含 createModule/updateModule/deleteModule
    4. useModuleLinkMutations export — 含 addLink/updateLink/removeLink
    5. 导入 @plane/types 的 IModule, TModuleStatus, ModuleLink, ILinkDetails
    6. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useModules" yh-flow/clients/web/src/lib/hooks/use-modules.ts</automated>
    <automated>grep -q "useModuleDetail" yh-flow/clients/web/src/lib/hooks/use-modules.ts</automated>
    <automated>grep -q "useModuleMutations" yh-flow/clients/web/src/lib/hooks/use-modules.ts</automated>
    <automated>grep -q "useModuleLinkMutations" yh-flow/clients/web/src/lib/hooks/use-modules.ts</automated>
  </verify>
  <done>
    * useModules, useModuleDetail, useModuleMutations, useModuleLinkMutations hooks 完成
    * TanStack Query mock data 模式一致
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 创建 use-cycle-issues.ts — 按 Cycle 过滤 Issue hooks</name>
  <files>
    - yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts — filter pattern
    packages/types/src/issues.ts — IIssueCycle type
  </read_first>
  <action>
    创建 use-cycle-issues.ts：

    1. `useCycleIssues(projectId: string, cycleId: string)`：
       - queryKey: ["cycles", projectId, cycleId, "issues"]
       - queryFn: await delay(200) → MOCK_ISSUES.filter(i => i.project_id === projectId && i.cycle_id === cycleId)
       - enabled: !!projectId && !!cycleId
       - 返回类型: useQuery&lt;TIssue[]&gt;

    2. `useAddIssueToCycle()` — 将 Issue 分配到 Cycle 的 mutation：
       - mutationFn: async ({ issueId, cycleId }: { issueId: string; cycleId: string }) => 在 MOCK_ISSUES 中找到 issue，设置 i.cycle_id = cycleId
       - invalidateQueries(["cycles"]), invalidateQueries(["issues"])

    3. `useRemoveIssueFromCycle()` — 从 Cycle 中移除 Issue 的 mutation：
       - mutationFn: async (issueId: string) => 在 MOCK_ISSUES 中找到 issue，设置 i.cycle_id = null
       - 乐观移除（乐观更新 with onMutate）+ invalidateQueries

    4. `useTransferCycleIssues()` — 将 Cycle 中所有 Issue 转移到另一个 Cycle：
       - mutationFn: async ({ fromCycleId, toCycleId }: { fromCycleId: string; toCycleId: string }) => 将所有 cycle_id === fromCycleId 的 Issue 改为 toCycleId
       - invalidateQueries(["cycles"]), invalidateQueries(["issues"])

  </action>
  <acceptance_criteria>
    1. useCycleIssues export — 按 cycleId 过滤 MOCK_ISSUES
    2. useAddIssueToCycle export — 设置 issue.cycle_id
    3. useRemoveIssueFromCycle export — 设置 issue.cycle_id = null（乐观更新）
    4. useTransferCycleIssues export — 批量转移 Issue 到目标 Cycle
    5. 导入 @plane/types 的 TIssue
    6. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useCycleIssues" yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts</automated>
    <automated>grep -q "useAddIssueToCycle" yh-flow/clients/web/src/lib/hooks/use-cycle-issues.ts</automated>
  </verify>
  <done>
    * useCycleIssues, useAddIssueToCycle, useRemoveIssueFromCycle, useTransferCycleIssues 完成
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: 创建 use-module-issues.ts — 按 Module 过滤 Issue hooks</name>
  <files>
    - yh-flow/clients/web/src/lib/hooks/use-module-issues.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts — filter pattern
    packages/types/src/issues.ts — IIssueModule type
  </read_first>
  <action>
    创建 use-module-issues.ts（与 use-cycle-issues 对称）：

    1. `useModuleIssues(projectId: string, moduleId: string)`：
       - queryKey: ["modules", projectId, moduleId, "issues"]
       - queryFn: await delay(200) → MOCK_ISSUES.filter(i => i.project_id === projectId && i.module_ids?.includes(moduleId))
       - enabled: !!projectId && !!moduleId
       - 返回类型: useQuery&lt;TIssue[]&gt;

    2. `useAddIssueToModule()` — 将 Issue 关联到 Module 的 mutation：
       - mutationFn: async ({ issueId, moduleId }: { issueId: string; moduleId: string }) => 在 MOCK_ISSUES 中追加 module_ids 数组

    3. `useRemoveIssueFromModule()` — 从 Module 中移除 Issue 的 mutation：
       - mutationFn: async ({ issueId, moduleId }: { issueId: string; moduleId: string }) => 从 MOCK_ISSUES 的 module_ids 中移除 moduleId
       - 乐观更新模式

  </action>
  <acceptance_criteria>
    1. useModuleIssues export — 按 moduleId 过滤 MOCK_ISSUES（检查 module_ids.includes）
    2. useAddIssueToModule export — 向 issue.module_ids 追加 moduleId
    3. useRemoveIssueFromModule export — 从 issue.module_ids 移除 moduleId（乐观更新）
    4. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useModuleIssues" yh-flow/clients/web/src/lib/hooks/use-module-issues.ts</automated>
    <automated>grep -q "useAddIssueToModule" yh-flow/clients/web/src/lib/hooks/use-module-issues.ts</automated>
  </verify>
  <done>
    * useModuleIssues, useAddIssueToModule, useRemoveIssueFromModule 完成
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: 创建 CycleStore (MobX UI Store)</name>
  <files>
    - yh-flow/clients/web/app/store/cycle.store.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/app/store/issue.store.ts — MobX store pattern (observable, action, makeObservable)
    yh-flow/clients/web/app/store/types.ts — Store type interface pattern
    packages/types/src/cycle/cycle.ts — ICycle, TCycleGroups
    packages/types/src/issues.ts — IIssueCycle
  </read_first>
  <action>
    创建 cycle.store.ts — Cycle UI 状态 MobX Store（仅 UI 状态，不放 Issue 数据 per D-P18-04）：

    1. Interface `ICycleStore`:
       - activeTab: "active" | "completed" | "all" — Cycle 列表 Tab 状态（D-P18-03）
       - selectedCycleId: string | null — 当前选中的 Cycle ID
       - viewLayout: "list" | "board" — Cycle 详情页视图切换
       - cycleModalOpen: boolean — 创建弹窗状态
       - cycleModalMode: "create" | "edit" — 弹窗模式
       - cycleDeleting: boolean — 删除确认状态
       - transferModalOpen: boolean — Issue 转移弹窗状态

       Actions:
       - setActiveTab(tab) — 切换活跃/已完成/全部 Tab
       - setSelectedCycleId(id) — 选中 Cycle
       - setViewLayout(layout) — 切换列表/看板视图
       - openCycleModal(mode) / closeCycleModal()
       - setCycleDeleting(state)
       - setTransferModalOpen(state)
       - reset()

    2. Class `CycleStore implements ICycleStore`:
       - makeObservable 所有字段和 action
       - 默认值：activeTab="active", selectedCycleId=null, viewLayout="list", cycleModalOpen=false, cycleDeleting=false, transferModalOpen=false

  </action>
  <acceptance_criteria>
    1. ICycleStore 接口导出，包含所有字段和 action 类型签名
    2. CycleStore 类导出，使用 makeObservable 注册 observable/action
    3. 默认值与设计一致
    4. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ICycleStore" yh-flow/clients/web/app/store/cycle.store.ts</automated>
    <automated>grep -q "class CycleStore" yh-flow/clients/web/app/store/cycle.store.ts</automated>
  </verify>
  <done>
    * CycleStore (ICycleStore interface + class) 完成
    * MobX observable/action 模式遵循 Phase 16 惯例
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: 创建 ModuleStore (MobX UI Store)</name>
  <files>
    - yh-flow/clients/web/app/store/module.store.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/app/store/issue.store.ts — MobX store pattern
    yh-flow/clients/web/app/store/types.ts — Store type interface pattern
    packages/types/src/module/modules.ts — IModule, TModuleStatus
  </read_first>
  <action>
    创建 module.store.ts — Module UI 状态 MobX Store：

    1. Interface `IModuleStore`:
       - selectedModuleId: string | null
       - moduleModalOpen: boolean
       - moduleModalMode: "create" | "edit"
       - moduleDeleting: boolean
       - activeView: "list" | "gantt"
       - filters: { status?: TModuleStatus[]; searchQuery?: string }

       Actions:
       - setSelectedModuleId(id)
       - openModuleModal(mode) / closeModuleModal()
       - setModuleDeleting(state)
       - setActiveView(view)
       - setFilters(filters)
       - reset()

    2. Class `ModuleStore implements IModuleStore`

  </action>
  <acceptance_criteria>
    1. IModuleStore 接口导出，包含所有字段和 action 类型签名
    2. ModuleStore 类导出，使用 makeObservable
    3. 默认值与设计一致
    4. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "IModuleStore" yh-flow/clients/web/app/store/module.store.ts</automated>
    <automated>grep -q "class ModuleStore" yh-flow/clients/web/app/store/module.store.ts</automated>
  </verify>
  <done>
    * ModuleStore (IModuleStore interface + class) 完成
    * MobX observable/action 模式遵循 Phase 16 惯例
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: 注册 CycleStore + ModuleStore 到 root.store.ts 和 store-context.tsx</name>
  <files>
    - yh-flow/clients/web/app/store/root.store.ts (MODIFY)
    - yh-flow/clients/web/app/store/types.ts (MODIFY)
  </files>
  <read_first>
    yh-flow/clients/web/app/store/root.store.ts — existing store registration pattern
    yh-flow/clients/web/app/store/types.ts — existing type exports
  </read_first>
  <action>
    修改 root.store.ts：
    1. 添加导入：`import { CycleStore } from "./cycle.store";` + `import { ModuleStore } from "./module.store";`
    2. ICoreRootStore 接口添加：`cycle: ICycleStore;` 和 `module: IModuleStore;`
    3. 导入类型：`import type { ICycleStore } from "./cycle.store";` + `import type { IModuleStore } from "./module.store";`
    4. CoreRootStore 类添加：`cycle: ICycleStore;` + `module: IModuleStore;`
    5. 构造函数中添加实例化：`this.cycle = new CycleStore();` + `this.module = new ModuleStore();`

    修改 types.ts：
    1. 添加：`export type { ICycleStore } from "./cycle.store";` + `export type { IModuleStore } from "./module.store";`
    2. 更新现有 type exports 行（如果已有同类模式）

  </action>
  <acceptance_criteria>
    1. ICoreRootStore 包含 cycle: ICycleStore 和 module: IModuleStore
    2. CoreRootStore 构造函数创建 CycleStore 和 ModuleStore 实例
    3. types.ts 导出 ICycleStore 和 IModuleStore
    4. 通过 `useStore().cycle.activeTab` 和 `useStore().module.selectedModuleId` 可访问
    5. TypeScript 编译通过
  </acceptance_criteria>
  <verify>
    <automated>grep -q "cycle.*ICycleStore" yh-flow/clients/web/app/store/root.store.ts</automated>
    <automated>grep -q "module.*IModuleStore" yh-flow/clients/web/app/store/root.store.ts</automated>
    <automated>grep -q "new CycleStore" yh-flow/clients/web/app/store/root.store.ts</automated>
    <automated>grep -q "new ModuleStore" yh-flow/clients/web/app/store/root.store.ts</automated>
  </verify>
  <done>
    * CycleStore + ModuleStore 注册到 root.store.ts
    * types.ts 导出新接口
    * `useStore().cycle` 和 `useStore().module` 可用
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID  | Category        | Component                          | Disposition | Mitigation Plan                                                      |
| ---------- | --------------- | ---------------------------------- | ----------- | -------------------------------------------------------------------- |
| T-18-DL-01 | Spoofing        | Mock data                          | accept      | Mock 阶段无认证风险，API 对接后替换                                  |
| T-18-DL-02 | Tampering       | useCycleMutations 修改 MOCK_CYCLES | mitigate    | 直接数组操作不涉及状态同步；后续 API 对接后 switch to real API calls |
| T-18-DL-03 | Info Disclosure | CycleStore/ModuleStore UI 状态     | accept      | 仅 UI 状态（Tab/Modal），不含敏感数据                                |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
grep -q "MOCK_CYCLES" src/lib/mock-data.ts && echo "mock cycles ok"
grep -q "MOCK_MODULES" src/lib/mock-data.ts && echo "mock modules ok"
grep -q "useCycles" src/lib/hooks/use-cycles.ts && echo "useCycles ok"
grep -q "useModules" src/lib/hooks/use-modules.ts && echo "useModules ok"
grep -q "useStore()\.cycle" app/store/root.store.ts && echo "cycle store registered"
grep -q "useStore()\.module" app/store/root.store.ts && echo "module store registered"
```
</verification>

<success_criteria>

- [ ] mock-data.ts 扩展（5+ cycles, 4+ modules, issue 关联）
- [ ] use-cycles.ts (useCycles, useCycleDetail, useCycleMutations)
- [ ] use-modules.ts (useModules, useModuleDetail, useModuleMutations, useModuleLinkMutations)
- [ ] use-cycle-issues.ts (useCycleIssues, useAddIssueToCycle, useRemoveIssueFromCycle, useTransferCycleIssues)
- [ ] use-module-issues.ts (useModuleIssues, useAddIssueToModule, useRemoveIssueFromModule)
- [ ] cycle.store.ts (CycleStore with ICycleStore)
- [ ] module.store.ts (ModuleStore with IModuleStore)
- [ ] root.store.ts 注册 + types.ts 导出
- [ ] npx tsc --noEmit 无新增错误

</success_criteria>

<output>
创建 `.planning/phases/18-cycle-module/18-01-SUMMARY.md`
</output>
