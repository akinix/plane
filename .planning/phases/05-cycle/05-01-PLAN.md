---
phase: 05-cycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/CycleConstants.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleDto.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleProgressDto.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/Cycle.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/CycleIssue.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleIssueConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
  - src/Modules/WorkItems/Modules.WorkItems/Services/IBurndownCalculator.cs
  - src/Modules/WorkItems/Modules.WorkItems/Services/BurndownCalculator.cs
  - src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
  - src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/*_AddCycles.cs
  - src/Tests/WorkItems.Tests/Domain/CycleDomainTests.cs
  - src/Tests/WorkItems.Tests/Domain/CycleIssueDomainTests.cs
  - src/Tests/WorkItems.Tests/TestData/TestCycleFactory.cs
autonomous: true
requirements: [REQ-5.1, REQ-5.2]
user_setup: []
must_haves:
  truths:
    - "Cycle entity exists with all Plane-compatible fields (16 fields) and factory methods (Create, AssignSortOrder, UpdateRestricted, Archive, Unarchive, FreezeSnapshot, SoftDelete)"
    - "CycleIssue bridge entity exists with soft-delete support (IHasTenant + ISoftDeletable)"
    - "WorkItemsDbContext exposes Cycles and CycleIssues DbSet properties"
    - "EF migration AddCycles creates Cycles and CycleIssues tables in yhschema.WorkItems schema with correct indexes"
    - "Cycle response DTO defines all annotated fields (total_issues, completed_issues, status, assignee_ids, etc.)"
    - "BurndownCalculator service implements CalculateCompletionChartAsync and BuildSnapshotAsync methods"
    - "IBurndownCalculator service registered in WorkItemsModule.ConfigureServices"
    - "Cycle route group stub exists in WorkItemsModule.MapEndpoints"
    - "dotnet build src/YH.Flow.slnx produces 0 errors"
  artifacts:
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/Cycle.cs"
      provides: "Cycle domain entity with IHasTenant, ISoftDeletable, IAuditableEntity"
      min_lines: 130
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/CycleIssue.cs"
      provides: "CycleIssue bridge entity with IHasTenant, ISoftDeletable"
      min_lines: 50
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleConfiguration.cs"
      provides: "EF Core configuration for Cycle entity"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleIssueConfiguration.cs"
      provides: "EF Core configuration for CycleIssue with conditional unique index"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs"
      provides: "Cycles and CycleIssues DbSet declarations in existing DbContext"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Services/IBurndownCalculator.cs"
      provides: "Burndown calculator interface"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Services/BurndownCalculator.cs"
      provides: "Burndown calculator implementation"
    - path: "src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleDto.cs"
      provides: "Cycle response DTO with all annotation fields"
  key_links:
    - from: "WorkItemsDbContext"
      to: "CycleConfiguration + CycleIssueConfiguration"
      via: "ApplyConfigurationsFromAssembly in OnModelCreating"
    - from: "WorkItemsModule"
      to: "IBurndownCalculator"
      via: "AddScoped registration in ConfigureServices"
    - from: "CycleIssueConfiguration"
      to: "WorkItemsModuleConstants.SchemaName"
      via: 'ToTable("CycleIssues", WorkItemsModuleConstants.SchemaName)'
---

<objective>
创建 Cycle 和 CycleIssue 领域实体、EF 配置、迁移、Contracts DTO 和 Burndown 计算服务。

Purpose: 为 Phase 5 所有 Cycle CRUD 和 Cycle-Issue 关联端点提供实体基础和持久化能力。Cycle 纳入 WorkItemsDbContext（yhschema.WorkItems schema），与 Issue 共享同一个 DbContext（per D-01）。
Output: Cycle.cs, CycleIssue.cs, EF 配置, AddCycles 迁移, Contracts DTOs, BurndownCalculator 服务, 测试脚手架
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workitems/04-01-SUMMARY.md
@.planning/phases/05-cycle/05-CONTEXT.md
@.planning/phases/05-cycle/05-RESEARCH.md
@.planning/phases/05-cycle/05-PATTERNS.md

# Existing reference files (absolute paths):

@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/WorkItemsModuleConstants.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/Issue.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/IssueAssignee.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/IssueAssigneeConfiguration.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/StateDto.cs
@D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/TestFactories.cs
@D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/WorkItemsTestFixture.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CycleContracts (DTOs + Constants) + BurndownCalculator service + WorkItemsModule service registration</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/StateDto.cs — 用于 DTO 命名/JsonPropertyName/namespace 模式参考
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/WorkItemsConstants.cs — 常量类 namespace/结构参考
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Services/IssueSequenceService.cs — 服务接口+实现模式参考
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs — ConfigureServices 注册模式参考
  </read_first>
  <files>
    NEW: src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/CycleConstants.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleDto.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleProgressDto.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Services/IBurndownCalculator.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Services/BurndownCalculator.cs
    MODIFIED: src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
  </files>
  <action>
    创建以下文件，所有 namespace 严格遵循已有模式：

    **CycleConstants.cs** (`YH.Modules.WorkItems.Contracts.Constants`):
    - `NameMaxLength = 255`, `DescriptionMaxLength = 10000`, `DefaultSortOrder = 65535.0`, `DefaultTimezone = "UTC"`, `SortOrderStep = 10000`

    **CycleDto.cs** (`YH.Modules.WorkItems.Contracts.DTOs`):
    - 字段集: Id(Guid), Name(string), Description(string?), StartDate(DateTimeOffset?), EndDate(DateTimeOffset?), SortOrder(double, default 65535.0), ExternalSource(string?), ExternalId(string?), ProgressSnapshot(string?), ArchivedAt(DateTimeOffset?), LogoProps(string?), Timezone(string, default "UTC"), Version(int, default 1)
    - 注释字段（查询时计算，非存储字段）: Status(string), TotalIssues(int), CompletedIssues(int), CancelledIssues(int), StartedIssues(int), UnstartedIssues(int), BacklogIssues(int), IsFavorite(bool, default false, Phase 13 stub), AssigneeIds(List&lt;string&gt;, default [], Phase 13 stub)
    - JSON 序列化: `[JsonPropertyName("created_at")]` on CreatedAt(DateTimeOffset), `[JsonPropertyName("updated_at")]` on UpdatedAt(DateTimeOffset?)
    - 不使用 IReadOnlyCollection — 使用 List 或数组，与 StateDto 模式一致

    **CycleProgressDto.cs** (`YH.Modules.WorkItems.Contracts.DTOs`):
    - `public Dictionary&lt;string, int?&gt; CompletionChart { get; init; } = [];`

    **IBurndownCalculator.cs** (`YH.Modules.WorkItems.Services`):
    - 接口方法:
      - `Task&lt;Dictionary&lt;string, int?&gt;&gt; CalculateCompletionChartAsync(Guid cycleId, string type, CancellationToken cancellationToken)` — type: "issues" | "points"
      - `Task&lt;CycleProgressSnapshot&gt; BuildSnapshotAsync(Guid cycleId, CancellationToken cancellationToken)`
    - 创建 CycleProgressSnapshot DTO 作为嵌套 record: `public sealed record CycleProgressSnapshot(int TotalIssues, int CompletedIssues, int CancelledIssues, int StartedIssues, int UnstartedIssues, int BacklogIssues, Dictionary&lt;string, int?&gt; CompletionChart);`

    **BurndownCalculator.cs** (`YH.Modules.WorkItems.Services`):
    - 构造注入 `WorkItemsDbContext _db`
    - `CalculateCompletionChartAsync`: 根据 05-RESEARCH.md Pattern 4 算法实现
      1. 加载 Cycle, 检查 StartDate/EndDate 非空
      2. 生成日期序列: `Enumerable.Range(0, days+1).Select(d => start.Date.AddDays(d))`
      3. type=="points" 模式: `_db.CycleIssues.Where(ci => ci.CycleId == cycleId && !ci.IsDeleted).Join(_db.Issues.Where(i => i.CompletedAt != null), ci => ci.IssueId, i => i.Id, (ci,i) => i.CompletedAt.Value.Date).GroupBy(d => d).Select(g => new { Date = g.Key, Count = g.Count() })` 然后连线 EstimatePoint 聚合求和
      4. type=="issues" 模式: 同上但 Count() 不对 estimate 求和
      5. 累积减法: 对每个日期计算 total - cumulative_completed, 未来日期 -> null
    - `BuildSnapshotAsync`: 查询 Cycle 的 Issue 分布统计, 调用 CalculateCompletionChartAsync, 组装 CycleProgressSnapshot

    **WorkItemsModule.cs** 修改:
    - 在 `ConfigureServices` 中添加: `builder.Services.AddScoped&lt;IBurndownCalculator, BurndownCalculator&gt;();`
    - 在 `MapEndpoints` 中添加 cycle 路由组桩代码（TODO 注释，实际端点在 05-02/05-03 接入）:
      ```csharp
      // Cycle route group (Phase 5) — endpoints registered in plans 05-02 and 05-03
      var cycles = endpoints
          .MapGroup("api/v{version:apiVersion}/workspaces/{slug}/projects/{projectId}/cycles")
          .WithTags("Cycles")
          .WithApiVersionSet(apiVersionSet);
      ```
    - 添加 using 导入: `using YH.Modules.WorkItems.Services;`

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error" -NotMatch</automated>
  </verify>
  <done>
    - CycleConstants.cs, CycleDto.cs, CycleProgressDto.cs, IBurndownCalculator.cs, BurndownCalculator.cs 文件存在
    - WorkItemsModule.cs 包含 IBurndownCalculator DI 注册和 cycles 路由组桩代码
    - 构建 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Cycle and CycleIssue domain entities + EF configurations + WorkItemsDbContext update</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/Issue.cs — 完整领域实体模式（IHasTenant + ISoftDeletable + IAuditableEntity + 私有构造函数 + 工厂方法）
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/IssueAssignee.cs — 桥接实体模式（IHasTenant，无软删除）
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/IssueAssigneeConfiguration.cs — HasFilter 条件唯一索引模式
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/IssueConfiguration.cs — 索引模式（IX_Project_SortOrder）
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs — 现有 DbSet 声明和 OnModelCreating 顺序（CRITICAL）
  </read_first>
  <files>
    NEW: src/Modules/WorkItems/Modules.WorkItems/Domain/Cycle.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Domain/CycleIssue.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleConfiguration.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleIssueConfiguration.cs
    MODIFIED: src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
  </files>
  <action>
    创建领域实体和 EF 配置，严格遵循 Phase 4 Issue 实体模式。

    **Cycle.cs** (`YH.Modules.WorkItems.Domain`):
    - 实现 `IHasTenant`, `ISoftDeletable`, `IAuditableEntity`（不和 Issue 一样用 IHasDomainEvents — Cycle 不需要活动日志）
    - 字段（私有 setter，符合 Domain 模式）:
      - Id(Guid), Name(string), Description(string, default ""), StartDate(DateTimeOffset?), EndDate(DateTimeOffset?)
      - SortOrder(double, default 65535.0), ExternalSource(string?), ExternalId(string?), ProgressSnapshot(string?), ArchivedAt(DateTimeOffset?), LogoProps(string?), Timezone(string, default "UTC"), Version(int, default 1)
      - ProjectId(Guid) — 标量 FK，无导航属性（跨模块：Project 在 Modules.Project）
    - IHasTenant: `public string TenantId { get; private set; } = default!;`
    - IAuditableEntity: CreatedOnUtc, CreatedBy, LastModifiedOnUtc, LastModifiedBy
    - ISoftDeletable: IsDeleted, DeletedOnUtc, DeletedBy
    - 私有构造函数 `private Cycle() { }` — EF Core
    - **工厂方法 `Create`**:
      - `public static Cycle Create(string name, Guid projectId, DateTimeOffset? startDate = null, DateTimeOffset? endDate = null, string? description = null, string? timezone = null)`
      - 验证: name 非空, 且 startDate/endDate 必须同时为 null 或同时非空（per D-03 / CONTEXT.md "Claude's Discretion" 日期校验: `(startDate is null) != (endDate is null)` → throw）
    - **方法**:
      - `AssignSortOrder(double? smallestExistingSortOrder)`: null → 65535.0, 有值 → value - 10000（per Claude's Discretion sort_order 规则）
      - `Update(string? name, string? description, DateTimeOffset? startDate, DateTimeOffset? endDate, double? sortOrder)`: 非 COMPLETED Cycle 的全字段更新，PATCH 语义（null 跳过）
      - `UpdateRestricted(string? name, string? description, double? sortOrder)`: COMPLETED Cycle 受限更新（per D-03: 只允许修改 name/description/sort_order）
      - `Archive()`: `ArchivedAt = DateTimeOffset.UtcNow`
      - `Unarchive()`: `ArchivedAt = null`
      - `FreezeSnapshot(string snapshotJson)`: `ProgressSnapshot = snapshotJson`
      - `SoftDelete(DateTimeOffset now)`: 标准软删除模式，幂等

    **CycleIssue.cs** (`YH.Modules.WorkItems.Domain`):
    - 实现 `IHasTenant`, `ISoftDeletable`
    - 字段: Id(Guid), IssueId(Guid), CycleId(Guid), CreatedOnUtc(DateTimeOffset)
    - IHasTenant: `public string TenantId { get; private set; } = default!;`
    - ISoftDeletable: IsDeleted, DeletedOnUtc, DeletedBy
    - 私有构造函数
    - **工厂方法**: `public static CycleIssue Create(Guid issueId, Guid cycleId)` — 验证非空 Guid

    **CycleConfiguration.cs** (`YH.Modules.WorkItems.Data.Configurations`):
    - `ToTable("Cycles", WorkItemsModuleConstants.SchemaName)`
    - `HasKey(x => x.Id)`
    - 属性配置: Name(IsRequired, HasMaxLength(255)), SortOrder(IsRequired, HasDefaultValue(65535.0)), ExternalSource(HasMaxLength(255)), ExternalId(HasMaxLength(255)), Timezone(IsRequired, HasMaxLength(255), HasDefaultValue("UTC")), Version(IsRequired, HasDefaultValue(1)), ProgressSnapshot/LegoProps 为 nvarchar(max) 无配置
    - 索引:
      - `HasIndex(x => new { x.ProjectId, x.SortOrder }).HasDatabaseName("IX_Cycles_Project_SortOrder")`
      - `HasIndex(x => new { x.TenantId, x.IsDeleted, x.ProjectId }).HasDatabaseName("IX_Cycles_Tenant_Deleted_Project")`

    **CycleIssueConfiguration.cs** (`YH.Modules.WorkItems.Data.Configurations`):
    - `ToTable("CycleIssues", WorkItemsModuleConstants.SchemaName)`
    - `HasKey(x => x.Id)`
    - 属性: IssueId(IsRequired), CycleId(IsRequired)
    - **条件唯一索引**（per D-03 + CycleIssue 桥接表规则）:
      ```csharp
      builder.HasIndex(x => new { x.TenantId, x.IssueId, x.CycleId })
          .IsUnique()
          .HasDatabaseName("IX_CycleIssues_Tenant_Issue_Cycle")
          .HasFilter("[DeletedOnUtc] IS NULL");
      ```
    - 注意: Finbuckle 的 AdjustUniqueIndexes 会自动将 TenantId 添加到索引列

    **WorkItemsDbContext.cs** 修改:
    - 添加两个 DbSet 属性（在 IssueActivities 和 IntakeIssues 之后）:
      ```csharp
      /// <summary>Cycles table (Phase 5).</summary>
      public DbSet<Cycle> Cycles => Set<Cycle>();

      /// <summary>Cycle-Issue M2M through table (Phase 5).</summary>
      public DbSet<CycleIssue> CycleIssues => Set<CycleIssue>();
      ```
    - 不需要修改 OnModelCreating — ApplyConfigurationsFromAssembly 会自动发现 CycleConfiguration/CycleIssueConfiguration
    - 添加 using 导入: `using YH.Modules.WorkItems.Domain;`（如果尚不存在）

    **注意**: 不修改任何现有实体或配置，只新增 Cycle/CycleIssue 相关文件。

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error" -NotMatch</automated>
  </verify>
  <done>
    - Cycle.cs, CycleIssue.cs, CycleConfiguration.cs, CycleIssueConfiguration.cs 文件存在
    - WorkItemsDbContext 包含 Cycles 和 CycleIssues DbSet 属性
    - 构建 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate AddCycles EF migration + Create test scaffolds (TestCycleFactory + Domain tests)</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/TestFactories.cs — 测试工厂模式（TestStateFactory）
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/WorkItemsTestFixture.cs — InMemory DbContext fixture
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/Domain/StateTests.cs — 领域单元测试模式
    @D:/github/akinix-plane/yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/20260624073603_AddCommentsActivitiesIntake.cs — 迁移文件命名和结构参考
  </read_first>
  <files>
    NEW: src/Tests/WorkItems.Tests/Domain/CycleDomainTests.cs
    NEW: src/Tests/WorkItems.Tests/Domain/CycleIssueDomainTests.cs
    NEW: src/Tests/WorkItems.Tests/TestData/TestCycleFactory.cs
    NEW: src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/*_AddCycles.cs
  </files>
  <action>
    生成 EF 迁移并创建测试文件。

    **EF 迁移生成**:
    - 在 yh-flow 目录执行（使用 PowerShell）:
      ```powershell
      dotnet ef migrations add AddCycles --project src/Host/YH.Flow.Migrations.PostgreSQL --startup-project src/Host/YH.Flow.Api --output-dir WorkItems
      ```
    - 如果遇到 "already exists" 错误，先移除重复迁移
    - 迁移应在 `yhschema.WorkItems` 创建 "Cycles" 和 "CycleIssues" 表
    - 验证迁移生成的 Up() 和 Down() 包含正确的列和索引
    - **CRITICAL**: 迁移生成后执行 `dotnet build src/YH.Flow.slnx --nologo` 验证

    **TestCycleFactory.cs** (`YH.Tests.WorkItems.TestData`):
    - namespace `YH.Tests.WorkItems.TestData`
    - `internal static class TestCycleFactory`
      - `CreateValid(string name = "Sprint 1", Guid? projectId = null, DateTimeOffset? startDate = null, DateTimeOffset? endDate = null)`: 默认日期为 now 到 now+14天
      - `CreateDraft(string name = "Draft Cycle", Guid? projectId = null)`: 两端 date 为 null
      - `CreateCompleted(string name = "Past Sprint", Guid? projectId = null)`: endDate = now - 1天
      - `CreateUpcoming(string name = "Next Sprint", Guid? projectId = null)`: startDate = now + 7天
    - `internal static class TestCycleIssueFactory`
      - `CreateValid(Guid issueId, Guid cycleId)`: 委托 CycleIssue.Create

    **CycleDomainTests.cs** (`YH.Tests.WorkItems.Domain`):
    - namespace `YH.Tests.WorkItems.Domain`
    - 测试用例（使用 Shouldly + Xunit，[Fact] 属性）:
      - `Create_WithValidArgs_SetsProperties`: 验证 Name/ProjectId/SortOrder/IsDeleted
      - `Create_WithBothDatesNull_CreatesDraft`: StartDate/EndDate 为 null
      - `Create_WithBothDatesSet_CreatesActiveCycle`: StartDate/EndDate 非 null
      - `Create_WithOnlyOneDate_Throws`: startDate!=null但endDate==null → ArgumentException
      - `Create_WithEmptyName_Throws`: name == "" → ArgumentException
      - `AssignSortOrder_WithExistingValue_CalculatesCorrectly`: 传入 10000 → 得到 -90000? 哦不对: min existing SortOrder - 10000. 如果 min=65535, assignSortOrder(65535) → SortOrder = 65535 - 10000 = -3465. 不对，Plane 的算法是 min sort_order - 10000. 如果 min=0, then -10000. 如果 min 不存在 (null), then 65535.0.
      所以正确的测试: `cycle.AssignSortOrder(65535.0)` → `cycle.SortOrder.ShouldBe(65535.0 - 10000)` 即 -3465.0 或 -10000.0? 公式是 `value - 10000`，如果 value 是 65535.0，结果是 55535.0。
      重新看 AssignSortOrder: `sortOrder = smallestExistingSortOrder.HasValue ? smallestExistingSortOrder.Value - 10000 : 65535.0`
      所以传入 65535.0 → 结果是 55535.0。
      - `AssignSortOrder_WithNoExisting_DefaultsTo65535`: 传入 null → SortOrder = 65535.0
      - `Archive_SetsArchivedAt`: Archive() → ArchivedAt 非 null
      - `Unarchive_ClearsArchivedAt`: Archive() → Unarchive() → ArchivedAt 为 null
      - `FreezeSnapshot_SetsProgressSnapshot`: FreezeSnapshot("{}") → ProgressSnapshot == "{}"
      - `UpdateRestricted_ChangesAllowedFields`: UpdateRestricted(name:"new", description:"desc", sortOrder:1.0) → Name/Description/SortOrder 改变
      - `UpdateRestricted_WithNullParams_DoesNotChange`: UpdateRestricted() 无参 → 字段不变
      - `SoftDelete_SetsDeletedFlag`: SoftDelete → IsDeleted=true
      - `SoftDelete_IsIdempotent`: 首次删除后再次 SoftDelete 不改变 DeletedOnUtc

    **CycleIssueDomainTests.cs** (`YH.Tests.WorkItems.Domain`):
    - namespace `YH.Tests.WorkItems.Domain`
    - 测试用例:
      - `Create_WithValidArgs_SetsProperties`: IssueId/CycleId 匹配
      - `Create_WithEmptyIssueId_Throws`: Guid.Empty → ArgumentException
      - `Create_WithEmptyCycleId_Throws`: Guid.Empty → ArgumentException
      - `SoftDelete_SetsDeletedFlag`
      - `IsDeleted_DefaultIsFalse`

  </action>
  <verify>
    <automated>dotnet test --no-restore --filter "Category=" 2>&1 | Select-String "Failed" -NotMatch; dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String "error" -NotMatch</automated>
  </verify>
  <done>
    - 迁移 AddCycles 生成，在 yhschema.WorkItems 创建 Cycles 和 CycleIssues 表
    - TestCycleFactory.cs 和 CycleDomainTests.cs, CycleIssueDomainTests.cs 文件存在
    - 完整构建 0 errors
    - 测试可编译（通过 dotnet test --no-build 2>&1 验证）
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                         |
| ------------- | ----------------------------------- |
| Client -> API | 未认证请求跨过 API 路由进入 Handler |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | --------------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-5-01-01 | Tampering              | CycleIssue 唯一约束         | mitigate    | EF Core HasFilter 条件唯一索引 `(TenantId, IssueId, CycleId)` 阻止重复关联，数据库级原子性保证 |
| T-5-01-02 | Information Disclosure | Cycle/CycleIssue 跨租户泄露 | mitigate    | IHasTenant + BaseDbContext 全局查询过滤器自动隔离                                              |
| T-5-01-03 | Tampering              | Domain 实体构建校验绕过     | mitigate    | 私有构造函数 + 静态工厂方法保证实体始终通过验证创建（与 Issue 相同模式）                       |

**Package legitimacy:** 无新增 NuGet 包 — 所有依赖已在 Phase 1-4 验证。
</threat_model>

<verification>
1. `dotnet build src/YH.Flow.slnx --nologo` — 0 errors
2. 验证 Cycles 和 CycleIssues DbSet 在 WorkItemsDbContext 中存在
3. 验证 CycleConfiguration 和 CycleIssueConfiguration 文件存在且包含正确索引
4. 验证 AddCycles 迁移文件在 `src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/` 目录下
5. `dotnet test src/YH.Flow.slnx --no-build --no-restore` — 所有现有测试通过
</verification>

<success_criteria>

- Cycle 和 CycleIssue 领域实体创建完成，实现 IHasTenant/ISoftDeletable/IAuditableEntity
- Cycle 实体包含 Create/AssignSortOrder/Update/UpdateRestricted/Archive/Unarchive/FreezeSnapshot/SoftDelete 工厂方法和实例方法
- CycleIssue 包含 (TenantId, IssueId, CycleId) 条件唯一索引
- WorkItemsDbContext 包含 Cycles + CycleIssues DbSet
- AddCycles 迁移生成成功
- BurndownCalculator 服务实现完成（CalculateCompletionChartAsync + BuildSnapshotAsync）
- CycleDto 和 CycleProgressDto 定义正确的响应格式
- TestCycleFactory 和 CycleDomainTests/CycleIssueDomainTests 创建完成
- 构建 0 errors，现有测试全部通过
  </success_criteria>

<output>
创建 `.planning/phases/05-cycle/05-01-SUMMARY.md` 记录本计划执行结果（3 tasks, 3 commits, 验证结果）
</output>
