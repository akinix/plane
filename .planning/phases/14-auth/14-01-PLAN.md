---
phase: 14-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/web/package.json
  - clients/web/tsconfig.json
  - clients/web/vite.config.ts
  - clients/web/index.html
  - clients/web/.env
  - clients/web/app/root.tsx
  - clients/web/app/entry.client.tsx
  - clients/web/app/routes.ts
  - clients/web/styles/globals.css
  - clients/web/public/
  - clients/web/core/
  - clients/web/src/lib/types/**
  - clients/web/src/lib/utils/**
autonomous: true
requirements:
  - SCAFF-01
  - SCAFF-04
  - UI-03
user_setup: []

must_haves:
  truths:
    - "`npm run dev` 启动 Vite 开发服务器，浏览器可访问并显示页面"
    - "@plane/types 和 @plane/utils 可通过 `import { cn } from '@plane/utils'` 在代码中引用"
    - "Vite alias `@plane/*` 正确解析到 `src/lib/*`"
    - "页面主题可在暗色和亮色之间切换，刷新后保持偏好"
  artifacts:
    - path: "clients/web/package.json"
      provides: "Dependencies manifest with React 19, Vite 7, React Router 7, MobX, Tailwind CSS 4"
      min_lines: 40
    - path: "clients/web/vite.config.ts"
      provides: "Vite config with React Router plugin, @plane/* alias, Tailwind CSS"
      contains: "alias.*@plane"
    - path: "clients/web/app/root.tsx"
      provides: "Root layout with ThemeProvider and Outlet"
      contains: "ThemeProvider"
    - path: "clients/web/src/lib/types/index.ts"
      provides: "Forked @plane/types barrel export"
      contains: "export"
    - path: "clients/web/src/lib/utils/index.ts"
      provides: "Forked @plane/utils barrel export"
      contains: "cn"
  key_links:
    - from: "clients/web/vite.config.ts"
      to: "src/lib/*"
      via: "resolve.alias"
      pattern: "@plane"
    - from: "clients/web/app/root.tsx"
      to: "next-themes ThemeProvider"
      via: "import"
      pattern: "next-themes"
---

<objective>
创建 Flow Web 前端开发环境（React 19 + Vite 7 + TypeScript 5 + Tailwind CSS 4 + React Router 7 per D-02），
Fork @plane/types 和 @plane/utils 包到 src/lib/ per D-03，实现暗色/亮色主题切换 per UI-03。

Purpose: 搭建基础的 Vite 项目骨架，使 Plane 的 Fork 包可在 Flow Web 项目中编译和引用
Output: 可直接 `npm run dev` 启动的开发环境
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-auth/14-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
<name>Task 1: 创建 Vite + React Router 7 项目骨架（per D-01、D-02、D-04）</name>
<files>
  clients/web/package.json
  clients/web/tsconfig.json
  clients/web/vite.config.ts
  clients/web/index.html
  clients/web/app/root.tsx
  clients/web/app/entry.client.tsx
  clients/web/app/routes.ts
  clients/web/styles/globals.css
  clients/web/public/.gitkeep
  clients/web/core/.gitkeep
  clients/web/.env
</files>
<read_first>
- D:\github\akinix-plane\apps\web\app\root.tsx (root layout 模式)
- D:\github\akinix-plane\apps\web\app\entry.client.tsx (入口模式)
- D:\github\akinix-plane\apps\web\vite.config.ts (Vite 配置模式)
- D:\github\akinix-plane\apps\web\react-router.config.ts (React Router 配置)
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md (D-01~D-04 决策)
</read_first>
<action>
在 clients/web/ 目录下创建完整的 Vite + React Router 7 SPA 项目。

**镜像 Plane apps/web/ 布局结构（per D-01）**：使用 app/（路由/页面）、core/（组件/Store/Hooks/服务）、styles/、public/ 顶层目录。

**使用 React Router v7 as route solution（per D-02）**，不使用 TanStack Router。

**1. `package.json`:**

- name: "@yh-flow/web", private: true, type: "module"
- scripts: dev="vite", build="react-router build", preview="vite preview"
- dependencies: react@^19.0.0, react-dom@^19.0.0, react-router@^7.0.0, mobx@^6.12.0, mobx-react@^9.1.0, mobx-utils@^6.0.8, next-themes@^0.4.0, clsx@^2.1.0, tailwind-merge@^3.4.0, lucide-react@^0.469.0, humps@^2.0.0, axios@^1.16.0, zod@^3.25.0
- devDependencies: vite@^7.0.0, @vitejs/plugin-react@^4.0.0, typescript@^5.8.0, @types/react@^19.0.0, @types/react-dom@^19.0.0, tailwindcss@^4.0.0, @tailwindcss/vite@^4.0.0, vite-tsconfig-paths@^5.0.0

**2. `tsconfig.json`:**

- compilerOptions: strict: true, target: esnext, module: esnext, moduleResolution: bundler, jsx: react-jsx, paths: { "@/_": ["./app/_"], "@plane/types": ["./src/lib/types"], "@plane/utils": ["./src/lib/utils"], "@plane/ui": ["./src/lib/ui"], "@plane/editor": ["./src/lib/editor"] }, verbatimModuleSyntax: true, isolatedModules: true, noEmit: true
- include: ["app", "src"]

**3. `vite.config.ts` (per D-04 - Vite alias @plane/_ → src/lib/_):**

- 使用 @vitejs/plugin-react 和 vite-tsconfig-paths 插件
- resolve.alias: @plane/types -> src/lib/types, @plane/utils -> src/lib/utils, @plane/ui -> src/lib/ui, @plane/editor -> src/lib/editor
- server: { port: 5173, host: true }

**4. `index.html`:**

- 标准 Vite HTML 入口，包含 `<div id="root">` 和 `<script type="module" src="/app/entry.client.tsx">`

**5. `app/entry.client.tsx`:**

- `import { HydratedRouter } from "react-router/dom"`, `import { StrictMode } from "react"`
- `hydrateRoot(document.getElementById("root"), <StrictMode><HydratedRouter /></StrictMode>)`

**6. `app/root.tsx`:**

- 参照 Plane 的 root.tsx 布局（已读文件），但去掉 next/link、next/navigation 等 Next.js 特定的 import
- 使用 react-router 的 `<Links>`, `<Meta>`, `<Outlet>`, `<Scripts>` 等组件
- 包裹 `<ThemeProvider themes={["light", "dark"]} defaultTheme="system" attribute="data-theme">`（来自 next-themes）
- Layout 函数（HTML shell）+ default export（AppProvider + Outlet）+ HydrateFallback
- 引入 globalStyles（`@/styles/globals.css?url`）

**7. `app/routes.ts`:**

- `import { type RouteConfig } from "@react-router/dev/routes"`
- `export default [] as RouteConfig`（暂时空路由，后续 phase 添加）

**8. `styles/globals.css`:**

- Tailwind CSS 4 标准 import: `@import "tailwindcss"`
- 添加 Plane 风格的 CSS 变量基础层（参考 packages/tailwind-config/variables.css 中的 OKLCH 色值模式）
- dark 模式变量使用 `@media (prefers-color-scheme: dark)` 和 `[data-theme="dark"]` 选择器

**9. `.env`:**

- `VITE_API_BASE_URL=https://localhost:7030/api/v1`

**10. 创建空目录结构：** clients/web/public/（放 .gitkeep）, clients/web/core/（放 .gitkeep）

将所有文件中涉及 Plane 原始版权的内容用 `// FLOW:` 标记（per D-06）。
</action>
<verify>
<automated>cd clients/web && npm install 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>npm install 成功，tsc 无类型错误，npm run dev 可启动开发服务器</done>
<acceptance_criteria>

- `clients/web/package.json` 存在，dependencies 包含 react@^19.0.0、react-router@^7.0.0、mobx、next-themes
- `clients/web/vite.config.ts` 存在，resolve.alias 包含 @plane/types、@plane/utils、@plane/ui、@plane/editor 映射
- `clients/web/app/root.tsx` 存在，包含 ThemeProvider 和 Outlet
- `clients/web/app/entry.client.tsx` 存在，使用 React Router v7 的 HydratedRouter
- `clients/web/app/routes.ts` 存在，导出 RouteConfig
- `clients/web/styles/globals.css` 存在，包含 @import "tailwindcss"
- `clients/web/tsconfig.json` 存在，paths 包含 @plane/\* 映射
- `clients/web/.env` 存在，包含 VITE_API_BASE_URL
  </acceptance_criteria>
  </task>

<task type="auto">
<name>Task 2: 配置暗色/亮色主题系统（per UI-03）</name>
<files>
  clients/web/app/root.tsx
  clients/web/styles/globals.css
</files>
<read_first>
- 当前目录 clients/web/app/root.tsx（Task 1 创建）
- D:\github\akinix-plane\packages\tailwind-config\variables.css（Plane Tailwind 变量模式）
- D:\github\akinix-plane\packages\utils\src\theme\index.ts（主题工具函数导出）
</read_first>
<action>
在 Task 1 创建的 root.tsx 和 globals.css 基础上补充完整主题系统：

**1. `styles/globals.css` 添加：**

- CSS 自定义属性根层（:root）定义 light 主题变量（参考 Plane 的 variables.css 中的 OKLCH 颜色空间）
- `[data-theme="dark"]` 选择器定义 dark 主题变量
- 基础 body 样式：`font-family: 'Inter Variable', sans-serif`
- `@layer base` 中的重置样式

**2. `app/root.tsx` 完善：**

- ThemeProvider 配置：`themes={["light", "dark"]} defaultTheme="system" attribute="data-theme" enableSystem`
- 在 Layout 函数中添加字体 import：`@fontsource-variable/inter`（直接 import）
- 添加 Plane 风格的根布局类名：`bg-canvas text-primary` 等
- 确保 `<html>` 标签有 `suppressHydrationWarning` 属性

**3. 创建 `clients/web/app/provider.tsx`（参照 Plane 的 provider.tsx 模式）：**

- AppProvider 组件包裹 ThemeProvider + children
- 后续 phase 添加 AuthProvider、StoreProvider 等

**4. 在所有修改位置添加 `// FLOW:` 标记（per D-06）。**
</action>
<verify>
<automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>TypeScript 编译通过，浏览器中打开页面可在 light/dark 主题间切换，刷新后偏好保持</done>
<acceptance_criteria>

- `root.tsx` 的 Layout 函数中 ThemeProvider 使用 attribute="data-theme"
- `styles/globals.css` 包含 `[data-theme="dark"]` CSS 选择器定义的主题变量
- `clients/web/app/provider.tsx` 存在，导出自定义 AppProvider 组件并包裹 ThemeProvider
  </acceptance_criteria>
  </task>

<task type="auto">
<name>Task 3: Fork @plane/types 和 @plane/utils 包到 src/lib/（per D-03、D-05、D-06）</name>
<files>
  clients/web/src/lib/types/**
  clients/web/src/lib/utils/**
</files>
<read_first>
- D:\github\akinix-plane\packages\types\src\（完整类型定义目录）
- D:\github\akinix-plane\packages\types\src\index.ts（barrel export）
- D:\github\akinix-plane\packages\utils\src\（完整工具函数目录）
- D:\github\akinix-plane\packages\utils\src\index.ts（barrel export）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（D-03: src/lib/ 目录、D-04: Vite alias、D-05: 复制方式、D-06: // FLOW: 标记）
</read_first>
<action>
将 @plane/types 和 @plane/utils 两个包通过代码复制方式（per D-05）Fork 到 `clients/web/src/lib/`：

**@plane/types fork（src/lib/types/）：**

- 复制 `packages/types/src/` 下所有文件到 `clients/web/src/lib/types/`
- 删除平面不需要的类型文件（仅在编辑器/看板等后续 phase 中用到的类型文件保留，但确保编译无断引用）
- 调整 import 路径：将所有 `@plane/types` 自引用改为相对路径
- 移除 `@plane/constants`、`@plane/utils` 等外部包的 import 依赖（改为相对路径引用 src/lib/utils 或直接内联）
- 保留 barrel export `index.ts` 结构（per STRUCTURE.md 的 barrel 模式）

**@plane/utils fork（src/lib/utils/）：**

- 复制 `packages/utils/src/` 下所有文件到 `clients/web/src/lib/utils/`
- 调整 import 路径：将 `@plane/types` 引用改为 `../types` 相对路径
- 移除对 `@plane/constants` 的依赖（将 E_PASSWORD_STRENGTH 等枚举直接内联到 auth.ts 中）
- 确保 `cn()` 函数（clients/web 使用的核心工具）正常工作——cn 依赖 clsx + tailwind-merge
- `theme/` 目录保留，`editor/` 目录保留
- 保留 barrel export `index.ts` 结构

**对所有修改的文件添加 `// FLOW:` 标记注释（per D-06），具体模式：**

- Fork 的副本文件头部添加：`// FLOW: Forked from @plane/types/src/xxx.ts`
- 任何修改行旁边添加：`// FLOW: adjusted import path`
- 内联的枚举/常量旁边添加：`// FLOW: inlined from @plane/constants`
  </action>
  <verify>
  <automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript 编译通过，src/lib/types/index.ts 和 src/lib/utils/index.ts 均可正常导入。验证命令：`node -e "const m = require('./clients/web/tsconfig.json'); console.log(JSON.stringify(m.compilerOptions.paths['@plane/types']))"`</done>
  <acceptance_criteria>
- `clients/web/src/lib/types/index.ts` 存在，导出 @plane/types 的全部类型
- `clients/web/src/lib/utils/index.ts` 存在，导出 cn、checkEmailValidity、getPasswordStrength 等核心工具函数
- `import { cn } from "@plane/utils"` 在 tsc 编译中无错误
- `import type { IUser } from "@plane/types"` 在 tsc 编译中无错误
- 所有修改的 Fork 文件包含 `// FLOW:` 标记
  </acceptance_criteria>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                            |
| ------------- | -------------------------------------- |
| client→server | Vite dev server clients from localhost |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                           |
| --------- | --------- | ---------------------- | ----------- | --------------------------------------------------------- |
| T-14-01   | Tampering | npm install            | mitigate    | 锁定 package.json 依赖版本；首次 install 后验证 lockfile  |
| T-14-SC   | Tampering | npm/pip/cargo installs | mitigate    | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |

</threat_model>

<verification>
1. `cd clients/web && npm install` 成功
2. `cd clients/web && npx tsc --noEmit` 无错误
3. `cd clients/web && npx vite build` 成功（或 react-router build 成功）
4. 验证 vite.config.ts 中 @plane/* 别名映射正确
</verification>

<success_criteria>

- 开发环境 `npm run dev` 可启动并在浏览器显示页面
- TypeScript 编译无错误
- @plane/types 和 @plane/utils 可在代码中正常 import
- 暗色/亮色主题可切换且刷新后保持
  </success_criteria>

<output>
创建 `.planning/phases/14-auth/14-01-SUMMARY.md` 完成任务后生成。
</output>
