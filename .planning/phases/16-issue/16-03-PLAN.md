---
phase: 16-issue
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - yh-flow/clients/web/app/issues/[issueId]/page.tsx
  - yh-flow/clients/web/app/components/issues/issue-detail-main.tsx
  - yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx
  - yh-flow/clients/web/app/components/issues/property-editor.tsx
  - yh-flow/clients/web/app/components/issues/comment-list.tsx
  - yh-flow/clients/web/app/components/issues/comment-input.tsx
  - yh-flow/clients/web/app/components/issues/delete-issue-modal.tsx
autonomous: true
requirements:
  - ISSU-03
  - ISSU-04
  - ISSU-05
  - ISSU-06
  - ISSU-08
user_setup: []
must_haves:
  truths:
    - "用户打开 Issue 详情页可看到双栏布局：左侧 TipTap 富文本编辑器显示 Issue 描述，右侧属性面板"
    - "用户可在属性面板中内联编辑状态/优先级/负责人/标签/估算/截止日期"
    - "用户可在描述区域使用 TipTap 富文本编辑器（@plane/editor）编辑 Issue 描述"
    - "用户可看到 Issue 评论列表并创建/编辑/删除评论"
    - "用户可删除 Issue（软删除，带确认对话框）"
  artifacts:
    - path: "yh-flow/clients/web/app/issues/[issueId]/page.tsx"
      provides: "Issue 详情路由页面"
    - path: "yh-flow/clients/web/app/components/issues/issue-detail-main.tsx"
      provides: "左栏 Issue 描述 + TipTap 编辑器"
    - path: "yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx"
      provides: "右栏属性面板"
    - path: "yh-flow/clients/web/app/components/issues/property-editor.tsx"
      provides: "内联属性编辑器组件（通用——支持状态/优先级/负责人/标签/估算/截止日期）"
    - path: "yh-flow/clients/web/app/components/issues/comment-list.tsx"
      provides: "评论列表（显示头像、内容、时间）"
    - path: "yh-flow/clients/web/app/components/issues/comment-input.tsx"
      provides: "评论输入框（使用 LiteTextEditor）"
    - path: "yh-flow/clients/web/app/components/issues/delete-issue-modal.tsx"
      provides: "确认删除弹窗"
  key_links:
    - from: "app/issues/[issueId]/page.tsx"
      to: "src/lib/hooks/use-issues.ts"
      via: "useIssue 获取单个 Issue 数据"
    - from: "app/components/issues/issue-detail-main.tsx"
      to: "@plane/editor"
      via: "RichTextEditorWithRef（TipTap 富文本编辑器）"
    - from: "app/components/issues/comment-list.tsx"
      to: "src/lib/hooks/use-comments.ts"
      via: "useComments 获取评论列表"
---

<objective>
实现 Issue 详情页面。创建双栏布局（per D-P16-09）——左侧 IssueDetailMain（@plane/editor 描述编辑器）和右侧 IssueDetailSidebar（属性面板），实现 PropertyEditor 内联编辑（per D-P16-10），创建评论系统（CommentList + CommentInput per D-P16-11），创建 DeleteIssueModal（软删除 per ISSUE-05）。

Purpose: 用户可查看和编辑 Issue 详细信息，管理评论，删除 Issue。
Output: 完整工作的 Issue 详情页。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-issue/16-CONTEXT.md
@.planning/phases/16-issue/16-RESEARCH.md
@.planning/phases/16-issue/16-UI-SPEC.md
@yh-flow/clients/web/src/lib/mock-data.ts
@yh-flow/clients/web/src/lib/hooks/use-issues.ts
@yh-flow/clients/web/src/lib/hooks/use-comments.ts
@yh-flow/clients/web/app/store/issue.store.ts
@yh-flow/clients/web/app/store/types.ts
@yh-flow/clients/web/src/lib/types/issues/issue.ts
@yh-flow/clients/web/src/lib/types/issues.ts
@yh-flow/clients/web/src/lib/types/state.ts
@yh-flow/clients/web/src/lib/types/issues/activity/issue_comment.ts
@yh-flow/clients/web/src/lib/types/issues/activity/issue_activity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Issue 详情页面和双栏布局组件</name>
  <files>
    yh-flow/clients/web/app/issues/[issueId]/page.tsx
    yh-flow/clients/web/app/components/issues/issue-detail-main.tsx
    yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts（useIssue、useIssueMutations——获取和更新数据）
    yh-flow/clients/web/src/lib/mock-data.ts（MOCK_ISSUES、MOCK_STATES、MOCK_LABELS——获取状态和标签数据）
    yh-flow/clients/web/src/lib/types/state.ts（IState 类型和 TStateGroups）
    yh-flow/clients/web/src/lib/editor/（@plane/editor 的 RichTextEditorWithRef——阅读导出的组件和 API）
    yh-flow/clients/web/src/lib/types/issues/issue.ts（TIssue 类型）
    yh-flow/clients/web/app/components/project/project-dashboard.tsx（布局模式参考——双栏结构 Tailwind 类名）
  </read_first>
  <action>
    所有组件使用 observer() 包裹、useStore() 访问 IssueStore。
    使用 @plane/utils 的 cn() 拼接类名。语义 token（bg-custom-background-* 等）来自 @plane/tailwind-config。
    import { TIssue, TIssuePriorities } from "@plane/types"。

    Step 1: 创建 app/issues/[issueId]/page.tsx（详情路由页面 per D-P16-04 路由格式）:
    - 使用 useParams 获取 workspaceId, projectId, issueId
    - const { data: issue, isLoading } = useIssue(projectId, issueId)
    - 加载状态：显示 Loader 骨架屏（左+右双栏骨架）
    - Issue 不存在：显示 "Issue 不存在或已被删除" 文本
    - 正常渲染：
      a) 顶部返回按钮（← Back to Issues，navigate 回列表页）
      b) 双栏布局容器（flex flex-row gap-4）per D-P16-09：
         - 左栏（flex-[2] 约 65%）：IssueDetailMain
         - 右栏（flex-[1] 约 35%）：IssueDetailSidebar
      c) 下方（左栏底部，IssueDetailMain 之后）：CommentList + CommentInput
    - 使用 useIssueMutations().updateIssue 的 mutate 函数传递给子组件

    Step 2: 创建 app/components/issues/issue-detail-main.tsx（左栏）:
    - Props: { issue: TIssue; workspaceId: string; onUpdate: (data: Partial&lt;TIssue&gt;) => void }
    - 显示 Issue 标题：大型 h2 可编辑文本（点击变为 input 编辑模式，blur 保存）
    - 状态进度指示器（小标签显示所属状态组颜色 + 名称）
    - 分隔线
    - Issue 描述区域：使用 @plane/editor 的 RichTextEditorWithRef（per D-P16-12）
      - ref: EditorRefApi（用于获取 HTML 内容）
      - editable: true
      - initialValue: issue.description_html（如果为空则为默认空内容）
      - containerClassName: "p-4 min-h-[200px]"
      - workspaceSlug: 透传 workspaceId
      - fileHandler / mentionHandler: 设为空对象（mock 阶段无需文件上传）
    - 输入框下方保存按钮（"保存"），调用 onUpdate({ description_html: editorRef.getHTML() })
    - 加载状态：显示 Loader 骨架屏

    Step 3: 创建 app/components/issues/issue-detail-sidebar.tsx（右栏 per D-P16-09）:
    - Props: { issue: TIssue; workspaceId: string; projectId: string; onUpdate: (data: Partial&lt;TIssue&gt;) => void }
    - 渲染属性面板列表（每个属性一行，显示标签 + 值）：
      a) 状态（State）：点击显示下拉选择器——从 MOCK_STATES 过滤 project_id 匹配的选项，分组显示（按 TStateGroups 分组：Backlog/Unstarted/Started/Completed/Cancelled），每个选项显示颜色 dot + 名称
      b) 优先级（Priority）：点击显示下拉选择器——urgent/high/medium/low/none，显示对应颜色和中文标签
      c) 负责人（Assignees）：点击显示下拉选择器——当前项目成员列表（临时从 MOCK_MEMBERS 获取），显示头像+名称
      d) 标签（Labels）：点击显示多选下拉面板——从 MOCK_LABELS 过滤，显示颜色 dot + 名称
      e) 估算（Estimate）：点击显示输入框——输入数字，显示 "X 点"
      f) 截止日期（Due Date）：点击显示日期选择器——使用原生 input type="date" 或简单 date picker
    - 每个属性采用内联编辑方式（per D-P16-10）：点击展开下拉/弹窗，选择后即时调用 onUpdate
    - 每个属性显示当前值，无值显示"未设置"（text-custom-text-400 灰色）
    - 使用 @plane/ui Dropdown 组件（单选/多选模式）
    - 底部：删除按钮（红色，点击打开 DeleteIssueModal）
    - 使用 date-fns 的 format 格式化日期

  </action>
  <acceptance_criteria>
    - app/issues/[issueId]/page.tsx 渲染双栏布局（左 flex-[2] 右 flex-[1]）
    - IssueDetailMain 使用 @plane/editor 的 RichTextEditorWithRef 渲染 Issue 描述
    - IssueDetailSidebar 渲染 6 个属性字段：状态、优先级、负责人、标签、估算、截止日期
    - 每个属性采用内联编辑模式（点击展开，选择后即时保存）
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    Issue 详情页双栏布局完成，左栏编辑器 + 右栏属性面板均通过类型检查
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建内联属性编辑器（PropertyEditor）</name>
  <files>
    yh-flow/clients/web/app/components/issues/property-editor.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/types/state.ts（IState 类型参考）
    yh-flow/clients/web/src/lib/types/issues.ts（TIssuePriorities 和 IIssueLabel 类型）
    yh-flow/clients/web/src/lib/ui/dropdown/（现有 Dropdown 组件的 API——单/多选的使用方式）
    yh-flow/clients/web/src/lib/ui/（Avatar, Badge 等组件的用法）
    yh-flow/clients/web/src/lib/mock-data.ts（MOCK_STATES, MOCK_LABELS, MOCK_MEMBERS——选项数据源）
  </read_first>
  <action>
    创建 app/components/issues/property-editor.tsx，作为可复用的内联属性编辑器组件。
    该组件封装了所有 Issue 属性的内联编辑逻辑，被 IssueDetailSidebar 调用。

    组件结构：
    - 通用 Props: { label: string; currentValue: ReactNode; onEdit: () => void; children: ReactNode（可选——渲染编辑面板）}
    - 或每个属性类型一个专用子组件：

    导出子组件：
    - PropertyEditorState: Props { value: string | null; states: IState[]; onSelect: (stateId: string) => void }
      - 显示当前状态颜色 dot + 名称或"未设置"
      - 点击展开下拉面板，显示所有状态选项，按 TStateGroups 分组（Backlog/Unstarted/Started/Completed/Cancelled），每组用分隔线
      - 使用 @plane/ui Dropdown 组件（如果可用）或自定义弹出面板
      - 每个选项显示颜色 Dot（12x12px 圆点） + 名称
      - 选择后调用 onSelect(stateId)

    - PropertyEditorPriority: Props { value: TIssuePriorities | null; onSelect: (priority: TIssuePriorities) => void }
      - 显示当前优先级的中文标签（紧急/高/中/低/无）和对应颜色
      - 点击展开 5 个选项：紧急(crimson)、高(orange)、中(amber)、低(blue)、无(grey)
      - 每个选项显示颜色点 + 中文标签

    - PropertyEditorAssignee: Props { value: string[]; members: IWorkspaceMember[]; onSelect: (assigneeIds: string[]) => void }
      - 显示当前负责人头像（Avatar 组件）
      - 显示 "未指派"（如无 assignee）
      - 点击展开成员列表，每个显示 Avatar + 名称
      - 单选模式（选择后替换）

    - PropertyEditorLabels: Props { value: string[]; labels: IIssueLabel[]; onSelect: (labelIds: string[]) => void }
      - 显示已有标签（Tag 组件，颜色 dot + 名称）
      - 点击展开多选面板，每个显示颜色 dot + 名称
      - 支持切换选中/取消

    - PropertyEditorEstimate: Props { value: string | null; onSelect: (estimate: string | null) => void }
      - 显示当前估算值（"X 点"）或"未估算"
      - 点击展开数字输入框 + 常用值快速选择（0, 1, 2, 3, 5, 8, 13）

    - PropertyEditorDate: Props { value: string | null; onSelect: (date: string | null) => void }
      - 显示格式化的日期或"未设置"
      - 点击展开日历面板（使用原生 input type="date" 的最小方案）
      - 提供"清除日期"按钮

    所有子组件使用 observer() 或纯函数组件。
    内联编辑模式 per D-P16-10：点击字段值直接展开下拉/弹窗编辑器。
    使用 use-outside-click-detector 钩子在点击外部时关闭编辑面板。

  </action>
  <acceptance_criteria>
    - property-editor.tsx 存在并导出 6 个子组件（State/Priority/Assignee/Labels/Estimate/Date）
    - 每个编辑器支持点击展开和点击外部关闭
    - 选择后立即触发 onSelect 回调
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    PropertyEditor 组件创建完毕，6 个属性编辑器子组件均通过类型检查
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建评论系统（CommentList + CommentInput）和删除功能</name>
  <files>
    yh-flow/clients/web/app/components/issues/comment-list.tsx
    yh-flow/clients/web/app/components/issues/comment-input.tsx
    yh-flow/clients/web/app/components/issues/delete-issue-modal.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-comments.ts（useComments、useCreateComment、useUpdateComment、useDeleteComment hook 签名）
    yh-flow/clients/web/src/lib/types/issues/activity/issue_comment.ts（TIssueComment 类型）
    yh-flow/clients/web/src/lib/types/issues/activity/issue_activity.ts（TIssueActivity 类型——用于活动日志）
    yh-flow/clients/web/src/lib/hooks/use-issues.ts（useIssueMutations——用于 Issue 删除）
    yh-flow/clients/web/src/lib/mock-data.ts（MOCK_ISSUE_ACTIVITIES——活动数据）
    yh-flow/clients/web/src/lib/ui/（Avatar, Button, Modal 等现有组件）
    yh-flow/clients/web/src/lib/editor/（@plane/editor 的 LiteTextEditorWithRef——轻量编辑器用于评论）
  </read_first>
  <action>
    Step 1: 创建 app/components/issues/comment-list.tsx:
    - Props: { issueId: string; workspaceId: string; projectId: string }
    - 使用 useComments(issueId) 获取评论列表
    - 区块标题："评论"（h4，text-h4-semibold 样式）
    - 加载状态：显示 Loader 骨架屏
    - 空评论：显示"暂无评论"
    - 评论列表渲染（按 created_at 时间顺序）：
      a) 每个评论卡片：
         - 用户头像（Avatar，来自 comment.actor_detail）
         - 用户名 + 相对时间（date-fns formatDistanceToNow）
         - 评论内容（使用 dangerouslySetInnerHTML 渲染 comment_html——@plane/editor 已做 XSS 过滤）
         - 如果是当前用户评论：编辑和删除按钮（hover 显示）
      b) 编辑模式：点击编辑 → 评论内容变为 LiteTextEditor（内联编辑 per Claude's discretion）
         - 保存按钮调用 useUpdateComment().mutate
         - 取消按钮恢复原始内容
      c) 删除模式：点击删除 → 弹出 confirm 对话框（"确定删除此评论吗？"）→ 确认后调用 useDeleteComment().mutate
    - 使用 observer() 包裹

    Step 2: 创建 app/components/issues/comment-input.tsx:
    - Props: { issueId: string }
    - 显示在 CommentList 下方，顺序：评论列表 → 评论输入框（per D-P16-11）
    - 使用 @plane/editor 的 LiteTextEditorWithRef（轻量级，评论场景 per RESEARCH.md open question 1）
      - ref: 获取 HTML 内容
      - placeholder: "输入评论内容..."
      - editable: true
      - 容器 min-h-[80px]
    - 提交按钮（"发送"），禁用状态（无内容时灰色）
    - 提交时调用 useCreateComment().mutate({ issueId, data: { comment_html: htmlContent } })，提交后清空编辑器
    - 使用 observer() 包裹

    Step 3: 创建 app/components/issues/delete-issue-modal.tsx:
    - Props: { isOpen: boolean; onClose: () => void; issueId: string; issueName: string; projectId: string; onDelete: () => void }
    - 使用 @plane/ui 的 AlertModal 组件（已 fork 的确认对话框 per RESEARCH.md "Don't Hand-Roll"）
    - 标题："删除 Issue"
    - 内容："确定删除此 Issue 吗？此操作不可撤销。"（per UI-SPEC.md Copywriting Contract）
    - 显示 Issue 名称（issueName）
    - 确认按钮：红色（bg-danger-primary），"删除"
    - 取消按钮：白色，"取消"
    - 确认后调用 useIssueMutations().deleteIssue（软删除设置 archived_at），然后 onDelete 回调（navigate 回列表页）
    - 加载状态：提交中禁用删除按钮并显示 spinner

  </action>
  <acceptance_criteria>
    - CommentList 使用 useComments 获取数据并渲染评论卡片（头像 + 名称 + 时间 + HTML 内容）
    - CommentList 支持评论编辑（内联 LiteTextEditor）和删除（confirm 对话框）
    - CommentInput 使用 LiteTextEditorWithRef 创建新评论
    - DeleteIssueModal 使用 AlertModal，确认后调用软删除并返回列表页
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    评论系统（列表 + 输入 + 编辑/删除）和 Issue 删除功能完成，全部通过类型检查
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                 |
| --------------- | --------------------------------------------------------------------------- |
| Comment input   | User text entered via LiteTextEditor — rendered via dangerouslySetInnerHTML |
| Property editor | User selection of state/priority/assignee/labels — triggers mutation        |

## STRIDE Threat Register

| Threat ID  | Category               | Component                              | Disposition | Mitigation Plan                                                                      |
| ---------- | ---------------------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------ |
| T-16-03-01 | Spoofing               | Comment actor identity                 | accept      | Mock layer assigns local user ID; real API will use server-authenticated identity    |
| T-16-03-02 | Tampering              | dangerouslySetInnerHTML in CommentList | accept      | @plane/editor sanitizes output HTML; no user-generated script injection              |
| T-16-03-03 | Information Disclosure | Issue detail accessible via URL        | accept      | No real auth in mock layer; Phase 14 AuthenticationWrapper protects workspace-layout |

</threat_model>

<verification>
1. `npx tsc --noEmit` — 类型检查通过
2. `grep -c "RichTextEditorWithRef" app/components/issues/issue-detail-main.tsx` — 使用了 @plane/editor
3. `grep -c "LiteTextEditorWithRef" app/components/issues/comment-input.tsx` — 评论使用了 LiteTextEditor
4. `grep -c "AlertModal" app/components/issues/delete-issue-modal.tsx` — 删除使用了 AlertModal
5. 每个 PropertyEditor 子组件检查导出：`grep -c "export" app/components/issues/property-editor.tsx`
</verification>

<success_criteria>

- Issue 详情页渲染双栏布局（65/35 split）
- @plane/editor 的 RichTextEditorWithRef 用于 Issue 描述编辑
- 6 个属性字段（状态/优先级/负责人/标签/估算/截止日期）全部支持内联编辑
- 评论系统支持创建/编辑/删除
- Issue 软删除带确认对话框
- `npx tsc --noEmit` 零错误
  </success_criteria>

<output>
Create `.planning/phases/16-issue/16-03-SUMMARY.md` when done
</output>
