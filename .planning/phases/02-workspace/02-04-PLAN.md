---
phase: 02-workspace
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/CreateWorkspace/CreateWorkspaceResponse.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/GetWorkspace/GetWorkspaceQuery.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/GetWorkspace/GetWorkspaceResponse.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceResponse.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQuery.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/CheckWorkspaceSlug/CheckWorkspaceSlugQuery.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/ISlugGenerator.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/SlugGenerator.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandValidator.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/GetWorkspace/GetWorkspaceQueryHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/GetWorkspace/GetWorkspaceEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceCommandValidator.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CheckWorkspaceSlug/CheckWorkspaceSlugQueryHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CheckWorkspaceSlug/CheckWorkspaceSlugEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
  - yh-flow/src/Tests/Workspace.Tests/Services/SlugGeneratorTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/CreateWorkspaceTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/SoftDeleteSlugReleaseTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/SlugUniqueTests.cs
autonomous: true
requirements: [REQ-2.1, REQ-2.3]
user_setup: []

must_haves:
  truths:
    - "用户可调 POST /api/v1/workspaces/ 创建工作区（含 slug 自动生成或显式提供）"
    - "创建工作区自动建一条 Admin 角色 WorkspaceMember + 设 OwnerId（D-06）"
    - "用户可调 GET /api/v1/workspaces/{slug}/ 获取工作区详情（含 Plane 兼容字段）"
    - "Admin 可调 PATCH /api/v1/workspaces/{slug}/ 更新工作区（名称/Logo/描述/时区/设置占位）；Member/Guest 返回 403"
    - "Owner 可调 DELETE /api/v1/workspaces/{slug}/ 软删除工作区（追加 __{epoch} 到 slug 释放原 slug）"
    - "用户可调 GET /api/v1/users/me/workspaces/ 列出所属工作区（Plane 分页格式）"
    - "用户可调 POST /api/v1/workspaces/slug-check/ 检查 slug 可用性（贴 Plane workspace-slug-check）"
    - "SlugGenerator 严格 [a-z0-9-] 格式 + 受限词拒绝（66 项）+ 冲突重试 5 次加随机后缀"
  artifacts:
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/SlugGenerator.cs"
      provides: "D-07/D-09 slug 生成与校验服务"
      contains: "RestrictedSlugs"
      min_lines: 50
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandHandler.cs"
      provides: "创建工作区 + 自动建 Admin member（D-06）"
      contains: "AddOwnerAsync"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceCommandHandler.cs"
      provides: "软删除 + slug epoch 释放（D-08）"
      contains: "SoftDelete"
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/SoftDeleteSlugReleaseTests.cs"
      provides: "D-08 软删除后 slug 可被新 workspace 复用的集成测试"
      contains: "SoftDelete"
  key_links:
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandHandler.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/SlugGenerator.cs"
      via: "GenerateUniqueSlugAsync(name) 解析 slug"
      pattern: "slugGenerator"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandHandler.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs"
      via: "AddOwnerAsync 创建 Admin member"
      pattern: "WorkspaceRole\\.Admin"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceCommandHandler.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/Workspace.cs"
      via: "SoftDelete(now) 方法实现 slug epoch 释放"
      pattern: "SoftDelete"
---

<objective>
Wave 3：实现 Workspace 的 CRUD 业务端点 + slug 生成服务。这是 REQ-2.1（CRUD + slug）+ REQ-2.3（设置）的端点层。

Purpose: Plane 路由契约（CONTEXT §canonical_refs 引用 `apps/api/plane/app/urls/workspace.py`）规定了 workspace 路由全集；本 plan 实现核心子集：create/get/update/delete/list-mine/slug-check。SlugGenerator 实现 D-07（混合来源）/D-08（epoch 释放，通过 SoftDelete 方法已 02-02 建好）/D-09（受限词+格式校验）。CreateWorkspace handler 实现 D-06（自动 Admin member）。所有端点用 Vertical Slice 4 文件模式（贴 Identity RegisterUser 模板）。

Output: (1) SlugGenerator + ISlugGenerator（服务层）；(2) 6 个 Feature slice（每个含 Command/Query + Handler + Endpoint + Validator）；(3) WorkspaceModule.MapEndpoints 完整化（两层路由组：topLevel 无 slug + scoped {slug}）；(4) 4 个新测试（SlugGenerator unit + CreateWorkspace/SoftDelete/SlugUnique integration）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-PATTERNS.md
@.planning/phases/02-workspace/02-VALIDATION.md
@.planning/phases/02-workspace/02-03-SUMMARY.md

@yh-flow/src/Modules/Identity/Modules.Identity.Contracts/v1/Users/RegisterUser/RegisterUserCommand.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/RegisterUser/RegisterUserCommandHandler.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/RegisterUser/RegisterUserCommandValidator.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/RegisterUser/RegisterUserEndpoint.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/GetUserById/
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/GetUsers/
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/UpdateUser/
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/DeleteUser/
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/SearchUsers/
@yh-flow/src/BuildingBlocks/Shared/Persistence/PlanePagedResult.cs
@apps/api/plane/app/urls/workspace.py
@apps/api/plane/api/serializers/workspace.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SlugGenerator service（D-07/D-09）+ SlugGenerator unit tests</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Services/ISlugGenerator.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Services/SlugGenerator.cs
    yh-flow/src/Tests/Workspace.Tests/Services/SlugGeneratorTests.cs
  </files>
  <behavior>
    - ISlugGenerator 接口：`string Slugify(string input); bool IsValidSlug(string slug); Task<string> GenerateUniqueSlugAsync(string? name, CancellationToken ct);`
    - Slugify: 小写 + 数字保留 + 其他字符替换为 '-' + 合并连续 '-' + trim '-' + max 48（RESEARCH §Example 4）
    - IsValidSlug: length 3-48 + 正则 `^[a-z0-9]+(?:-[a-z0-9]+)*$` + 不在 RestrictedSlugs.List（D-09）
    - GenerateUniqueSlugAsync: name 为空抛 ArgumentException；attempt 0 用 base=Slugify(name)；attempt 1-4 加 4 字符随机后缀（小写字母+数字）形如 `{base}-{suffix}`；每次 `_db.Workspaces.AnyAsync(w => w.Slug == candidate)` 检查（**不含 IsDeleted 过滤** —— 软删除 workspace 的 slug 形如 `{orig}__{epoch}` 不会冲突）；5 次都冲突抛 ConflictException
    - SlugGeneratorTests 覆盖：(a) Slugify "ACME Corp!" → "acme-corp"；(b) Slugify 中文/emoji → safe fallback（可能空或部分）；(c) IsValidSlug "api"/"admin"/"settings" → false（受限词）；(d) IsValidSlug "Acme" → false（大写）；(e) IsValidSlug "acme" → true；(f) GenerateUniqueSlugAsync 冲突重试 mock test
  </behavior>
  <action>
    **ISlugGenerator.cs**：定义上述 3 方法接口。命名空间 `YH.Modules.Workspace.Services`。
    **SlugGenerator.cs**（PATTERNS §SlugGenerator + RESEARCH §Example 4 权威蓝图）：class 实现 ISlugGenerator，ctor 注入 `WorkspaceDbContext`（用于 AnyAsync 查询）。
    - 私有 static 方法 `Slugify` + `IsValidSlug`（**纯函数，不依赖 DbContext，便于 unit test**）。Regex 用 `[RegexGenerator]` source-gen 或 `Compiled` 选项（性能 NFR-1）。
    - 私有 static 方法 `GenerateShortSuffix()`：`RandomNumberGenerator.GetBytes(4)` 转 hex lowercase 取前 4 字符（BCL-only，贴 ApiTokenService crypto 模式）。
    - 公开方法 `GenerateUniqueSlugAsync` 实现 5 次重试循环（PATTERNS §SlugGenerator 给了完整模板）。**重要**：查询用 `_db.Workspaces.AsNoTracking().AnyAsync(w => w.Slug == candidate)` —— **不要**加 `.Where(w => !w.IsDeleted)` 因为：(a) 软删除 workspace slug 已被 SoftDelete 改成 `{orig}__{epoch}` 不会冲突；(b) 若加 IsDeleted 过滤会漏掉逻辑上仍占用 slug 的边界情况。**但** IGlobalEntity Workspace 不受 tenant filter，AsNoTracking 避免跟踪。
    - 抛 ConflictException 用 Phase 1 的异常类型（贴 MultitenancyModule.cs:175-176 `ForbiddenException` 风格查 `ConflictException` 定义位置 —— 可能在 BuildingBlocks/Web）。
    **SlugGeneratorTests.cs**（PATTERNS §SlugGeneratorTests 指明 copy ApiTokenServiceTests.cs:1-60 风格）：6 个 [Fact]/[Theory] case 覆盖上述行为。GenerateUniqueSlugAsync 用 InMemory WorkspaceDbContext + 预先 seed 一个 workspace "acme"，验证 GenerateUniqueSlugAsync("Acme") 返回 "acme-{suffix}" 形式。**这是 VALIDATION §Per-Task Verification Map 行 2-01/2-01c 对应的测试**。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~SlugGenerator" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    SlugGenerator 编译通过；6+ unit test 全过；受限词 "api"/"admin"/"settings" 拒绝；冲突重试 5 次机制验证。
  </done>
</task>

<task type="auto">
  <name>Task 2: 6 个 Workspace Feature slice（Create/Get/Update/Delete/ListUserWorkspaces/CheckSlug）+ WorkspaceModule.MapEndpoints wiring + 3 个 integration 测试</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/CreateWorkspace/CreateWorkspaceResponse.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/GetWorkspace/GetWorkspaceQuery.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/GetWorkspace/GetWorkspaceResponse.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceResponse.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQuery.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Workspaces/CheckWorkspaceSlug/CheckWorkspaceSlugQuery.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceCommandValidator.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CreateWorkspace/CreateWorkspaceEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/GetWorkspace/GetWorkspaceQueryHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/GetWorkspace/GetWorkspaceEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceCommandValidator.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/UpdateWorkspace/UpdateWorkspaceEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/DeleteWorkspace/DeleteWorkspaceEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CheckWorkspaceSlug/CheckWorkspaceSlugQueryHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/CheckWorkspaceSlug/CheckWorkspaceSlugEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/CreateWorkspaceTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/SoftDeleteSlugReleaseTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/SlugUniqueTests.cs
  </files>
  <behavior>
    - CreateWorkspaceCommand（Contracts/v1/Workspaces/CreateWorkspace/）：Name/slug?/Logo?/TimeZone? + [JsonIgnore] OwnerUserId；Response record(Guid Id, string Slug)
    - CreateWorkspaceCommandHandler：解析 slug（显式→IsValidSlug 校验，否则 GenerateUniqueSlugAsync）→ Workspace.Create → AddOwnerAsync(role=Admin) → SaveChanges → 失效 slug cache key（如已存在） → 返回 Response
    - CreateWorkspaceCommandValidator：Name NotEmpty + MaxLength(80)；slug（若提供）MaxLength(48) + 正则匹配（受限词校验放 handler，因 validator 无 DI 到 ISlugGenerator）
    - CreateWorkspaceEndpoint：POST topLevel "/" → mediator.Send → Created($"/api/v1/workspaces/{slug}", response)；RequireAuthorization()（任何认证用户）
    - GetWorkspaceQuery（Contracts）：[JsonIgnore] Slug（从 route 注入）；Response = WorkspaceDto 全字段
    - GetWorkspaceQueryHandler：用 WorkspaceDbContext.Workspaces 查 slug（**不受 tenant filter** 因为 Workspace 是 IGlobalEntity，**所以必须显式 .Where(w => w.Slug == slug && !w.IsDeleted)** —— 关键）→ 转 WorkspaceDto
    - GetWorkspaceEndpoint：GET scoped "/{slug}" → 任何登录用户可读（无 RequireWorkspaceRole 限制，但中间件已填充 context）—— **贴 Plane 行为**：非成员也能 GET（Plane 公开 workspace metadata），但 02-03 中间件允许；可选加 `.RequireAuthorization()` 仅认证
    - UpdateWorkspaceCommand：Name?/Logo?/Description?/TimeZone?/OrganizationSize?/BackgroundColor? + [JsonIgnore] Slug；Validator 全 optional 但若 Name 提供则 NotEmpty+MaxLength80
    - UpdateWorkspaceCommandHandler：查 workspace by slug → apply changes → 若 Name 改了重新生成 slug？**否** —— slug 不可改（贴 Plane behavior：update 不动 slug）；SaveChanges；若 Slug 没变跳过 cache 失效
    - UpdateWorkspaceEndpoint：PATCH scoped "/{slug}" → `.RequireWorkspaceRole(WorkspaceRole.Admin)`（D-11）
    - DeleteWorkspaceCommand：[JsonIgnore] Slug + [JsonIgnore] CurrentUserId（验证是 owner）
    - DeleteWorkspaceCommandHandler：查 workspace → 验证 OwnerId == CurrentUserId（D-06 仅 owner 可删，否则 ForbiddenException）→ workspace.SoftDelete(now) → SaveChanges → 失效 slug cache key
    - DeleteWorkspaceEndpoint：DELETE scoped "/{slug}" → `.RequireWorkspaceRole(WorkspaceRole.Admin)` + handler 内额外 owner 检查
    - ListUserWorkspacesQuery：含分页参数（贴 IPageable）；Response = PlanePagedResult&lt;WorkspaceDto&gt;
    - ListUserWorkspacesQueryHandler：当前 user 关联的 workspaces（**通过 WorkspaceMember.UserId 反查 workspaceIds，再查 Workspaces**）—— AsNoTracking，project to WorkspaceDto
    - ListUserWorkspacesEndpoint：GET topLevel "/users/me/workspaces/" 或顶层 "/workspaces/" 带 user filter → 任何认证用户
    - CheckWorkspaceSlugQuery（Contracts）：Slug；Response record(bool Exists)
    - CheckWorkspaceSlugQueryHandler：`db.Workspaces.AnyAsync(w => w.Slug == slug)` —— **注意**：是否过滤 IsDeleted？Plane 行为是软删除的 slug "已释放"，所以 `AnyAsync(w => w.Slug == slug && !w.IsDeleted)` —— 软删除后原 slug 应返回 false（可用）
    - CheckWorkspaceSlugEndpoint：POST topLevel "/slug-check/" 或 GET "/{slug}/exists/" —— 贴 Plane `workspace-slug-check/`
    - WorkspaceModule.MapEndpoints 完整化（替换 02-02 占位 `// TODO 02-04`）：定义两个路由组 topLevel + scoped；调用各 `Map{Endpoint}Endpoint()` 扩展方法
    - CreateWorkspaceTests：integration —— seed root user + POST /workspaces/ → 验证 workspace 创建 + 自动 Admin member 存在（D-06）
    - SoftDeleteSlugReleaseTests：integration —— 创建 workspace "acme" → 软删除 → 创建新 workspace "acme" → 应成功（D-08 slug 释放验证）
    - SlugUniqueTests：integration —— 并发创建同名 workspace（或顺序创建）→ 验证第二个返回带 suffix 的 slug（D-07 冲突重试）
  </behavior>
  <action>
    按 PATTERNS §Mediator Vertical Slice (4-file Feature) + §CreateWorkspace 给的完整 4 文件模板，为每个 Feature 创建：Contracts 端 Command/Query + Response（在 `Modules.Workspace.Contracts/v1/Workspaces/{Feature}/`），Modules 端 Handler + Validator + Endpoint（在 `Modules.Workspace/Features/v1/Workspaces/{Feature}/`）。
    **关键决策点：**
    (a) **GetWorkspace 跨租户查询**：WorkspaceDbContext 的 `db.Workspaces` 因为 Workspace 是 IGlobalEntity，**不会**被 tenant filter 自动过滤。这意味着 GetWorkspace 不依赖中间件填的 TenantId —— handler 直接 `db.Workspaces.AsNoTracking().FirstOrDefaultAsync(w => w.Slug == slug && !w.IsDeleted)`。**显式加 `!w.IsDeleted`** 是必须的（避免软删除 workspace 仍能 GET）。这是 D-01 的副效应：slug strategy 已经解析了 slug→workspace，但 handler 仍要按 slug 查一次拿完整 entity。
    (b) **CreateWorkspace 自动 Admin member（D-06）**：handler 内 `await memberships.AddOwnerAsync(ws.Id, cmd.OwnerUserId, ct)` —— AddOwnerAsync 是 WorkspaceMembershipService 方法，但该 service 在 02-05 才完整实现。**本 plan 内联创建 member**：`db.Members.Add(WorkspaceMember.Create(ws.Id, ownerUserId.ToString(), WorkspaceRole.Admin, isActive: true));` 一起 SaveChanges。注释引用 D-06。02-05 的 WorkspaceMembershipService.AddOwnerAsync 可重构本 handler 调用，但 02-04 独立可工作。
    (c) **DeleteWorkspace owner-only（D-06）**：handler 内 `if (ws.OwnerId != cmd.CurrentUserId) throw new ForbiddenException("Only owner can delete workspace")`。CurrentUserId 从 `ICurrentUser.GetUserId()` 注入。
    (d) **slug cache 失效**：CreateWorkspace 成功后**不需要** invalidate（slug 是新的，cache miss 自动回填）；UpdateWorkspace 若 Name 变化但不改 Slug 也不需要；DeleteWorkspace 必须 invalidate `ws:slug:{slug}`（软删除后 cache 仍指向旧 workspaceGuid 会出问题）。**注入 IDistributedCache 到 DeleteWorkspace handler**。
    (e) **Plane 分页**：ListUserWorkspaces 用 `PlanePagedResult<T>.FromPagedResponse(...)`（贴 PATTERNS §Plane-Compatible Pagination）。
    (f) **WorkspaceModule.MapEndpoints**：贴 PATTERNS §WorkspaceModule.cs 给的完整 MapEndpoints 模板 —— 两个 IEndpointRouteBuilder 组 topLevel + scoped，调用各 Map{Endpoint}Endpoint()。**这一步替换 02-02 的 `// TODO 02-04` 占位**。
    **测试**：CreateWorkspace/SoftDeleteSlugRelease/SlugUnique 三个 integration test 都依赖 WorkspaceTestFixture（02-01 建的 InMemory 版）—— 用 `WebApplicationFactory<Program>` 或直接构造 mediator + handler 调用（贴 Identity.Tests integration 风格）。若 InMemory 无法验证 unique index 约束（InMemory 不强制），可降级为 unit test 用 NSubstitute mock WorkspaceDbContext.AnyAsync 返回 true 模拟冲突。**重点是覆盖 D-06/D-07/D-08 的业务语义**，不一定要 100% 贴 PostgreSQL 行为。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/YH.Flow.slnx --nologo 2>&1 | tail -3 && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~CreateWorkspace|FullyQualifiedName~SoftDelete|FullyQualifiedName~SlugUnique" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    6 个 Feature slice 全部编译通过；WorkspaceModule.MapEndpoints 注册所有 6 个端点；3 个 integration 测试通过（CreateWorkspace 自动 Admin、SoftDelete slug 释放、SlugUnique 冲突重试）；D-06/D-07/D-08 在业务层验证。
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                            |
| -------------------- | -------------------------------------- |
| HTTP → Command/Query | 端点入口，FluentValidation 守护输入    |
| Role → Mutation      | Admin/Owner gate 在 update/delete 端点 |

## STRIDE Threat Register

| Threat ID                 | Category               | Component                                               | Disposition         | Mitigation Plan                                                                                                                             |
| ------------------------- | ---------------------- | ------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| T-2-slug                  | Tampering              | Slugify 严格性（XSS via slug）                          | mitigate            | Slugify 仅保留 [a-z0-9-]，正则拒绝其他；DB unique 约束兜底；响应 JSON 序列化自动 HTML 编码                                                  |
| T-2-slugenum              | Information Disclosure | slug-check 端点泄露 slug 可用性                         | accept              | Plane 行为接受此泄露（CONTEXT §Claude's Discretion）；非成员也能 check（公开信息）                                                          |
| T-2-slugrestricted        | Tampering              | 受限词检查漏 → 注册 "api" workspace                     | mitigate            | RestrictedSlugs.List 66 项；IsValidSlug 检查；SlugGeneratorTests 守护（Task 1）                                                             |
| T-2-eop-update [HIGH]     | Elevation of Privilege | Member/Guest 改 workspace 设置                          | mitigate [BLOCKING] | UpdateWorkspace endpoint `.RequireWorkspaceRole(WorkspaceRole.Admin)`；02-03 TenantIsolation + authz test 守护                              |
| T-2-eop-delete [HIGH]     | Elevation of Privilege | 非 owner 删除 workspace                                 | mitigate [BLOCKING] | DeleteWorkspace handler 显式 `ws.OwnerId != CurrentUserId → ForbiddenException`；RequireWorkspaceRole(Admin) + owner 双重保护               |
| T-2-n1-list               | Information Disclosure | ListUserWorkspaces N+1 查询                             | mitigate            | AsNoTracking + projection to DTO；不在循环里查；本 plan 用批量 join 查询                                                                    |
| T-2-cacheinvalid          | Information Disclosure | DeleteWorkspace 后 cache stale → 解析到软删除 workspace | mitigate            | DeleteWorkspace handler 显式 `_cache.RemoveAsync($"ws:slug:{slug}")`                                                                        |
| T-2-softdelete-idempotent | Tampering              | 重复 DELETE workspace → 多次 epoch 追加                 | accept              | 软删除后再 GET 返回 404（IsDeleted filter）；DELETE 已删除的返回 404 not 多次 epoch；handler 内 `if (ws.IsDeleted) throw NotFoundException` |
| T-2-SC                    | Tampering              | NuGet                                                   | accept              | 0 新包                                                                                                                                      |

## Threat Disposition Summary

- **mitigate [BLOCKING]**: T-2-eop-update（Admin gate）、T-2-eop-delete（owner gate）—— 端点装饰 + handler 显式检查
- **mitigate**: T-2-slug、T-2-slugrestricted、T-2-n1-list、T-2-cacheinvalid
- **accept**: T-2-slugenum（Plane 行为）、T-2-softdelete-idempotent（404 兜底）、T-2-SC
- **HIGH severity**: T-2-eop-update、T-2-eop-delete —— 本 plan Task 2 端点装饰 + 02-06 smoke 验证
  </threat_model>

<verification>
- [ ] `dotnet build src/YH.Flow.slnx` 0 warning / 0 error
- [ ] 6 个 Feature slice 编译通过；WorkspaceModule.MapEndpoints 注册所有端点
- [ ] `dotnet test src/Tests/Workspace.Tests --filter "SlugGenerator|CreateWorkspace|SoftDelete|SlugUnique"` 全过
- [ ] SlugGenerator 拒绝受限词（"api"/"admin"/"settings"）
- [ ] CreateWorkspace 自动建 Admin member（D-06）
- [ ] DeleteWorkspace 软删除后 slug 可被新 workspace 复用（D-08）
- [ ] Phase 1 测试无回归（`dotnet test src/YH.Flow.slnx --filter Identity`）
</verification>

<success_criteria>

- REQ-2.1（CRUD + slug）端点全部可调用
- REQ-2.3（settings）通过 UpdateWorkspace 实现（字段含 Logo/Description/TimeZone/OrganizationSize/BackgroundColor）
- D-06/D-07/D-08/D-09 在业务层有测试守护
- 所有非顶层端点都装饰 `.RequireWorkspaceRole(WorkspaceRole.Admin)` 或更强
  </success_criteria>

<output>
Create `.planning/phases/02-workspace/02-04-SUMMARY.md` when done. 记录：(a) 6 个端点的最终路由（贴 Plane 契约对照）；(b) CreateWorkspace 自动建 Admin member 的实现路径（内联 vs 调 WorkspaceMembershipService）；(c) SlugGenerator 5 次重试的随机后缀算法；(d) cache invalidation 策略（Create 不需要，Update/Delete 需要）。
</output>
