---
phase: 02-workspace
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Modules.Workspace.csproj
  - yh-flow/src/Modules/Workspace/Modules.Workspace/AssemblyInfo.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModuleConstants.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/Workspace.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceInvitation.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceConfiguration.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceMemberConfiguration.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceInvitationConfiguration.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceSlugStrategy.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceTenantStore.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/CurrentWorkspaceContext.cs
  - yh-flow/src/Modules/Multitenancy/Modules.Multitenancy/MultitenancyModule.cs
  - yh-flow/src/YH.Flow.slnx
autonomous: true
requirements: [REQ-2.1]
user_setup: []

must_haves:
  truths:
    - "Workspace 实体是 IGlobalEntity（不 IsMultiTenant），其下 WorkspaceMember/WorkspaceInvitation 实现隐式 IHasTenant（由 ApplyTenantIsolationByDefault 自动加）"
    - "WorkspaceDbContext 派生自 BaseDbContext，OnModelCreating 顺序为 ApplyConfigurationsFromAssembly 先 → base.OnModelCreating 后"
    - 'Finbuckle slug strategy 从 HttpContext.GetRouteValue("slug") 解析；顶层无 slug 端点返回 null（让 Phase 1 strategy 接管）'
    - "WorkspaceTenantStore 把 slug 解析成 AppTenantInfo(Id=workspaceGuid, Identifier=slug)，未找到返回 null"
    - "Workspace 软删除时调用 SoftDelete(now) 方法把 slug 改成 {slug}__{epoch_seconds}"
    - "CreateAdminMember 工厂可在创建 workspace 时自动建一条 Admin 角色 WorkspaceMember"
  artifacts:
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/Workspace.cs"
      provides: "Workspace 根实体（IGlobalEntity，ISoftDeletable，含 SoftDelete epoch 方法）"
      contains: "IGlobalEntity"
      min_lines: 40
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs"
      provides: "Workspace 模块 DbContext（贴 AuditDbContext 派生模式）"
      contains: "ApplyConfigurationsFromAssembly"
      min_lines: 30
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceSlugStrategy.cs"
      provides: "D-01 Finbuckle slug 解析 strategy"
      contains: "GetRouteValue"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceTenantStore.cs"
      provides: "D-01 slug→AppTenantInfo 解析 store（含 Redis 缓存）"
      contains: "IMultiTenantStore"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceConfiguration.cs"
      provides: "Workspace 表配置（slug unique 索引，无 TenantId 列）"
      contains: "IsUnique"
  key_links:
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs"
      to: "yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs"
      via: "继承 + OnModelCreating 链"
      pattern: "BaseDbContext"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceSlugStrategy.cs"
      to: "yh-flow/src/Modules/Multitenancy/Modules.Multitenancy/MultitenancyModule.cs"
      via: "strategy chain（external TryAddEnumerable OR AddWorkspaceTenantResolution 扩展方法，由 02-01 spike 结论决定）"
      pattern: "IMultiTenantStrategy<AppTenantInfo>"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceTenantStore.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs"
      via: "查询 Workspaces 表解析 slug→AppTenantInfo（绕过 tenant filter，因 Workspace 是 IGlobalEntity）"
      pattern: "db\\.Workspaces"
---

<objective>
Wave 1：搭建 Workspace 模块的领域层、数据层、租户解析层。这是 Phase 2 的核心架构层 —— 实体定义、DbContext 派生、Finbuckle slug 解析 wiring 全部在这里定下来，后续 02-03~02-05 在此之上添加业务 feature。

Purpose: RESEARCH §Architectural Responsibility Map 明确：(1) `Workspace = IGlobalEntity` 是租户过滤不泄露的保障；(2) `WorkspaceDbContext.OnModelCreating` 顺序错就会让 `ApplyTenantIsolationByDefault` 失败（Pitfall 6）；(3) Finbuckle slug strategy/store wiring 是 D-01 的实现，依赖 02-01 spike Q1 的结论决定具体路径（外部 TryAddEnumerable vs AddWorkspaceTenantResolution 扩展方法）。实体软删除 SoftDelete 方法实现 D-08 的 slug epoch 释放。

Output: (1) Workspace 模块 csproj + AssemblyInfo + Module 类（占位 ConfigureServices，完整 MapEndpoints 留到 02-04/05）；(2) 3 个 Domain 实体；(3) WorkspaceDbContext + 3 个 IEntityTypeConfiguration；(4) WorkspaceSlugStrategy + WorkspaceTenantStore（D-01 wiring）；(5) CurrentWorkspaceContext 实现（D-03）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-PATTERNS.md
@.planning/phases/02-workspace/02-VALIDATION.md
@.planning/phases/02-workspace/02-01-SUMMARY.md

@yh-flow/src/Modules/Auditing/Modules.Auditing/Persistence/AuditDbContext.cs
@yh-flow/src/Modules/Auditing/Modules.Auditing/AuditingModule.cs
@yh-flow/src/Modules/Auditing/Modules.Auditing/AssemblyInfo.cs
@yh-flow/src/Modules/Auditing/Modules.Auditing/Persistence/AuditRecordConfiguration.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Domain/APIToken.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Domain/UserSession.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Data/Configurations/UserSessionConfiguration.cs
@yh-flow/src/Modules/Multitenancy/Modules.Multitenancy/MultitenancyModule.cs
@yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs
@yh-flow/src/BuildingBlocks/Persistence/TenantIsolationExtensions.cs
@yh-flow/src/BuildingBlocks/Core/Domain/IHasTenant.cs
@yh-flow/src/BuildingBlocks/Core/Domain/IGlobalEntity.cs
@yh-flow/src/BuildingBlocks/Core/Domain/ISoftDeletable.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 个 Domain 实体（Workspace/WorkspaceMember/WorkspaceInvitation）+ WorkspaceModule 骨架 + Constants</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Modules.Workspace.csproj
    yh-flow/src/Modules/Workspace/Modules.Workspace/AssemblyInfo.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModuleConstants.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/Workspace.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceInvitation.cs
    yh-flow/src/YH.Flow.slnx
  </files>
  <behavior>
    - Workspace : IHasDomainEvents, IGlobalEntity, ISoftDeletable（**不** IHasTenant，per RESEARCH Pitfall 6）；含字段 Id/Name/Slug/OwnerId(Guid 标量 D-06)/Logo/OrganizationSize/TimeZone(默认"UTC")/BackgroundColor(默认"#000000")/审计字段(CreatedOnUtc/CreatedBy/LastModifiedOnUtc/LastModifiedBy/IsDeleted/DeletedOnUtc/DeletedBy)
    - Workspace.SoftDelete(DateTimeOffset now) 方法：设 IsDeleted=true/DeletedOnUtc=now/Slug = $"{Slug}__{(int)now.ToUnixTimeSeconds()}"（D-08）
    - Workspace.Create(name, slug, ownerUserId, logo?, timeZone?) 工厂（贴 APIToken.Create）
    - WorkspaceMember : IHasDomainEvents, IHasTenant, ISoftDeletable；字段 Id/TenantId(隐式)/WorkspaceId(冗余 Guid，便于跨租户查询)/UserId(标量 string，D-04)/Role(int)/IsActive(bool)/审计+软删除
    - WorkspaceMember.Create(workspaceId, userId, role, isActive=true) 工厂
    - WorkspaceInvitation : IHasDomainEvents, IHasTenant, ISoftDeletable；字段 Id/TenantId(隐式)/WorkspaceId/Email/TokenHash(string)/Role(int)/Accepted(bool)/RespondedAt(DateTimeOffset?)/Message(string?)/ExpiresAt(DateTimeOffset)/审计+软删除
    - WorkspaceInvitation.Create(workspaceId, email, tokenHash, role, ttlDays, message?) 工厂
    - WorkspaceInvitation 三状态转换：Accept()/Reject()/Revoke() 都置 RespondedAt=now；Accept() 额外置 Accepted=true；IsExpired 属性 = DateTimeOffset.UtcNow > ExpiresAt；IsValid 属性 = !Accepted && RespondedAt == null && !IsExpired（用于 accept 端点校验）
    - WorkspaceModuleConstants：SchemaName="yhschema.Workspace"（Phase 1 D-12 继承），ApiPrefix="workspaces"，SlugMaxLength=48
    - WorkspaceModule : IModule（占位）—— ConfigureServices 注册 DbContext + CurrentWorkspaceContext（scoped）；ConfigureMiddleware 留空（02-03 加中间件）；MapEndpoints 留空（02-04/05 加端点）
    - AssemblyInfo.cs：`[assembly: FshModule(typeof(WorkspaceModule), 200)]`
  </behavior>
  <action>
    创建 `Modules.Workspace` 类库项目 —— csproj 贴 `Modules.Auditing/Modules.Auditing.csproj` 结构（TargetFramework=net10.0、Nullable=enable、ImplicitUsings=enable、TreatWarningsAsErrors=true），ProjectReference 含：`BuildingBlocks/Core`、`BuildingBlocks/Persistence`、`BuildingBlocks/Web`（IModule 用）、`Modules.Workspace.Contracts`、`Modules.Multitenancy`（用 AppTenantInfo）、`Modules.Identity.Contracts`（用 UserSummary/IUserIdentityService，Task 3 已建）。
    **实体实现（PATTERNS §Domain Entity Shape + RESEARCH §Example 5 字段对照 Plane workspace.py:88-110）：**
    - `Workspace.cs`：贴 `APIToken.cs:10-58` 结构 —— 私有 setter、private ctor（EF Core）、static Create 工厂、SoftDelete 方法（**唯一区别：Workspace 是 IGlobalEntity 不是 IHasTenant**，per Pitfall 6 注释里明文写出）。SoftDelete 方法注释引用 D-08 + Plane 行为。
    - `WorkspaceMember.cs`：贴 `APIToken.cs` + `UserSession.cs` 结构。`Role` 属性类型用 `int`（DB 存 20/15/5，应用层在 02-04/05 转换 WorkspaceRole enum）。`UserId` 类型 = `string`（贴 Identity User Id 字段类型，per PATTERNS §WorkspaceMemberConfiguration 注释 "HasMaxLength(450)"）。
    - `WorkspaceInvitation.cs`：贴 `APIToken.cs` 结构（APIToken 的 Revoke() 是状态转换参考）—— 把 `Revoke()` 拆成 `Accept()`/`Reject()`/`Revoke()` 三方法，全置 `RespondedAt`；Accept 额外置 `Accepted=true`；replaces APIToken.IsActive 字段。`IsValid` 计算属性 = `!Accepted && RespondedAt == null && !IsExpired`（注释引用 D-12）。
    - **WorkspaceModule.cs**（贴 AuditingModule.cs 骨架，PATTERNS §WorkspaceModule.cs 给了完整模板）：ConfigureServices 注册 `builder.Services.AddHeroDbContext<WorkspaceDbContext>()`、`builder.Services.AddScoped<ICurrentWorkspaceContext, CurrentWorkspaceContext>()`、Health check。**本 task 不在 ConfigureServices 注册 slug strategy/store**（Task 3 才处理 Finbuckle wiring，避免本 task 编译依赖）。ConfigureMiddleware / MapEndpoints 留空方法（带 `// TODO 02-03` / `// TODO 02-04` 占位）。
    - **WorkspaceModuleConstants.cs**：贴 `IdentityModuleConstants.cs` + PATTERNS §WorkspaceModuleConstants 模板。
    - **AssemblyInfo.cs**：贴 `Modules.Auditing/AssemblyInfo.cs:1-3`，`Order=200`（介于 Multitenancy ~100 与 Auditing 300 之间）。
    把 Modules.Workspace 项目加入 slnx。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/Modules/Workspace/Modules.Workspace/Modules.Workspace.csproj --nologo 2>&1 | tail -5</automated>
  </verify>
  <done>
    3 个 Domain 实体编译通过；WorkspaceModule 注册到 FshModule 系统；Constants 就绪。Workspace 是 IGlobalEntity；WorkspaceMember/Invitation 实现 IHasTenant/ISoftDeletable。SoftDelete 方法实现 D-08 epoch。
  </done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceDbContext + 3 个 IEntityTypeConfiguration</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceConfiguration.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceMemberConfiguration.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceInvitationConfiguration.cs
  </files>
  <behavior>
    - WorkspaceDbContext 派生 BaseDbContext，ctor 签名与 AuditDbContext 完全一致
    - OnModelCreating 顺序：先 ApplyConfigurationsFromAssembly，后 base.OnModelCreating（Pitfall 6 反向会破坏 tenant filter）
    - WorkspaceConfiguration：ToTable("Workspaces", "yhschema.Workspace")；Slug unique 索引；OwnerId 标量无 FK；**不** 调用 IsMultiTenant()；不显式加 TenantId 列（IGlobalEntity）
    - WorkspaceMemberConfiguration：ToTable("WorkspaceMembers", ...)；UserId HasMaxLength(450) 标量无 FK（D-04）；复合 unique 索引 (TenantId, UserId) —— TenantId 列由 ApplyTenantIsolationByDefault 自动加
    - WorkspaceInvitationConfiguration：ToTable("WorkspaceInvitations", ...)；TokenHash HasMaxLength(64)（SHA-256 hex）unique 索引；复合索引 (TenantId, Accepted) 用于 list pending；ExpiresAt 必填
  </behavior>
  <action>
    **WorkspaceDbContext.cs**（贴 `AuditDbContext.cs:1-52` verbatim —— PATTERNS §WorkspaceDbContext.cs 给了完整模板）：ctor 签名 4 参数（`IMultiTenantContextAccessor<AppTenantInfo>`、`DbContextOptions<WorkspaceDbContext>`、`IOptions<DatabaseOptions>`、`IHostEnvironment`），3 个 DbSet（Workspaces/Members/Invitations），OnModelCreating 注释引用 `AuditDbContext.cs:48-51` warning。
    **WorkspaceConfiguration.cs（CRITICAL — Pitfall 6）**：贴 `UserSessionConfiguration.cs:7-79` 结构但 **不** 调 `IsMultiTenant()`（Workspace 是 IGlobalEntity）。关键代码点：(a) `builder.Ignore(x => x.TenantId);` 显式排除（如果 Workspace 类意外继承 IHasTenant，这里会编译错 —— 这是有意的 fail-fast）；(b) `builder.HasIndex(x => x.Slug).IsUnique();` 单列 unique（per Pitfall 4：软删除后 slug 改成 `{slug}__{epoch}` 释放原 slug）；(c) OwnerId 标量 `builder.Property(x => x.OwnerId).IsRequired();` **不**调 HasOne/HasMany（D-04 跨模块无 FK）。
    **WorkspaceMemberConfiguration.cs**：贴 `UserSessionConfiguration.cs` + PATTERNS §WorkspaceMemberConfiguration 模板。`builder.HasIndex(x => new { x.TenantId, x.UserId }).IsUnique();` —— AdjustUniqueIndexes() 会自动 widen。**注释明确：不显式调 IsMultiTenant()，由 ApplyTenantIsolationByDefault 自动加 TenantId 列**（参 `AuditRecordConfiguration.cs:13` 调 IsMultiTenant 是 Auditing 历史代码，新模块不需要）。
    **WorkspaceInvitationConfiguration.cs**：贴 UserSessionConfiguration 结构。索引：`TokenHash` unique（D-12 哈希查询）+ `(TenantId, Accepted)` 复合（list pending 邀请）。ExpiresAt `.IsRequired()`。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/Modules/Workspace/Modules.Workspace/Modules.Workspace.csproj --nologo 2>&1 | tail -5 && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~Spike" --nologo 2>&1 | tail -5</automated>
  </verify>
  <done>
    WorkspaceDbContext 编译通过；3 个配置就位；Workspace 实体确认 IGlobalEntity（不 IsMultiTenant）；WorkspaceMember/Invitation 确认会自动加 TenantId 列；Slug/TokenHash/(TenantId,UserId) 三个 unique 索引就绪。
  </done>
</task>

<task type="auto">
  <name>Task 3: Finbuckle slug strategy + WorkspaceTenantStore wiring（D-01，按 02-01 spike 结论实现）</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceSlugStrategy.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceTenantStore.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/CurrentWorkspaceContext.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Modules/Multitenancy/Modules.Multitenancy/MultitenancyModule.cs
  </files>
  <behavior>
    - WorkspaceSlugStrategy：从 HttpContext.GetRouteValue("slug") 取 slug；非空且匹配 /api/v1/workspaces/... 路径返回 slug；否则 null（顶层端点让 Phase 1 claim/header strategy 接管，Pitfall 2）
    - WorkspaceTenantStore.TryGetAsync(identifier=slug)：先查 Redis cache → miss 查 WorkspaceDbContext.Workspaces（IGlobalEntity 不受 tenant filter）→ 命中返回 AppTenantInfo(Id=workspaceGuid, Identifier=slug, Name=name) 并回填 cache；未命中返回 null 让下个 store 尝试
    - CurrentWorkspaceContext：scoped 实现 ICurrentWorkspaceContext，由 middleware 在请求 scope 填充
    - **绝对禁止**重复调 services.AddMultiTenant<AppTenantInfo>（Pitfall 1）
    - **wiring 方案由 02-01 SUMMARY 决定**：若 spike 结论是 "DI 外部 TryAddEnumerable 可行"，则在 WorkspaceModule.ConfigureServices 注册；若是 "需用 AddWorkspaceTenantResolution 扩展方法"，则在 Workspace.Contracts 暴露扩展方法 + MultitenancyModule 调用
  </behavior>
  <action>
    读取 `.planning/phases/02-workspace/02-01-SUMMARY.md` 的 Q1 spike 结论。**根据结论选择 wiring 路径：**
    **路径 A（spike 成功 — TryAddEnumerable 可行）：** 在 `WorkspaceModule.ConfigureServices` 添加：
    ```
    builder.Services.TryAddEnumerable(ServiceDescriptor.Singleton<IMultiTenantStrategy<AppTenantInfo>, WorkspaceSlugStrategy>());
    builder.Services.TryAddEnumerable(ServiceDescriptor.Scoped<IMultiTenantStore<AppTenantInfo>, WorkspaceTenantStore>());
    ```
    **路径 B（spike 失败 — 回退扩展方法）：** 在 `Workspace.Contracts` 新增 `MultiTenancy/WorkspaceTenantResolutionExtensions.cs`，暴露 `public static MultiTenantBuilder<AppTenantInfo> AddWorkspaceTenantResolution(this MultiTenantBuilder<AppTenantInfo> builder)`，方法内 `.WithDelegateStrategy<HttpContext, AppTenantInfo>(...)` + `.WithStore<WorkspaceTenantStore>(...)`（store 用 `TryAdd` 注册）；然后在 `MultitenancyModule.cs` 现有 `services.AddMultiTenant<AppTenantInfo>()` 链的末尾追加 `.AddWorkspaceTenantResolution()`（**这一行是 MultitenancyModule.cs 唯一改动**，加在 WithClaimStrategy/WithHeaderStrategy 之前以保证 slug strategy 优先）。
    **WorkspaceSlugStrategy.cs**（PATTERNS §WorkspaceSlugStrategy.cs 给了完整模板 + RESEARCH §Example 1）：实现 `IMultiTenantStrategy<AppTenantInfo>` 接口（v10.1.1 API，参考 `MultitenancyModule.cs:100-109` 现有 WithDelegateStrategy lambda）。**严格按 Pitfall 2**：检查 `httpContext.GetRouteValue("slug") is string slug && !string.IsNullOrEmpty(slug)`；否则 return null。**注释引用 D-01 + Pitfall 1/2**。
    **WorkspaceTenantStore.cs**（RESEARCH §Example 2 是权威蓝图 + PATTERNS §WorkspaceTenantStore.cs 模板）：实现 `IMultiTenantStore<AppTenantInfo>` 全部方法 —— TryGetAsync 是核心（缓存+DB lookup），AddAsync/UpdateAsync/RemoveAsync/GetAllAsync 转发到缓存失效或 no-op（workspace 是 source of truth，store 不写入）。缓存键 `ws:slug:{identifier}`，TTL 30 分钟滑动。**注意**：用 `IDistributedCache`（Phase 1 AddHeroCaching 已注册），不要直接用 Redis client。WorkspaceDbContext 注入为 Scoped（与 BaseDbContext scope 一致）。**注释引用 D-01 + Pattern 1（双租户并存）**。
    **CurrentWorkspaceContext.cs**：scoped class 实现 `ICurrentWorkspaceContext`，private 字段 + Set 方法填充。注释说明"由 WorkspaceMembershipMiddleware 在请求 scope 内填充，handler/authz 只读"（D-02）。
    **不**修改 `Program.cs` 或 `DbMigrator` —— 那是 02-03 的任务。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/YH.Flow.slnx --nologo 2>&1 | tail -5 && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~Spike" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    WorkspaceSlugStrategy + WorkspaceTenantStore 编译通过并按 spike 结论正确 wiring；CurrentWorkspaceContext 实现就绪；MultitenancyModule.cs 改动（若路径 B）最小（仅追加一行扩展方法调用）；不破坏 Phase 1 MultitenancyModule 的现有 strategy/store 链。
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                           |
| --------------------------- | ----------------------------------------------------- |
| URL → TenantId（D-01 解析） | HTTP 请求路径段的 slug 跨越信任边界进入 TenantId 设置 |

## STRIDE Threat Register

| Threat ID            | Category               | Component                                     | Disposition         | Mitigation Plan                                                                                                                                            |
| -------------------- | ---------------------- | --------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-2-slugstrategy     | Spoofing               | WorkspaceSlugStrategy                         | mitigate            | 严格检查 route value "slug" 非空（Pitfall 2）；不依赖 User（UseMultiTenant 在认证前跑）；未命中返回 null                                                   |
| T-2-tenantleak       | Information Disclosure | WorkspaceTenantStore                          | mitigate            | 查询用 `!w.IsDeleted` 过滤软删除 workspace（避免软删除的 slug 仍解析成功）；slug 不存在返回 null（让 EFCoreStore 继续），不抛异常（Pitfall 6）             |
| T-2-isolation [HIGH] | Elevation of Privilege | Workspace 实体被误加 IHasTenant               | mitigate [BLOCKING] | Workspace 显式 IGlobalEntity；WorkspaceConfiguration.Ignore(TenantId) fail-fast；测试 `SlugTenantResolve` 验证（02-03 加 integration test）                |
| T-2-eop              | Elevation of Privilege | WorkspaceMember 复合 unique 索引漏掉 TenantId | mitigate            | 复合索引 `(TenantId, UserId)` AdjustUniqueIndexes 自动 widen；同一 user 在不同 workspace 是不同行（D-04 标量 userId 支持此语义）                           |
| T-2-softdelete       | Tampering              | SoftDelete 后 slug 未释放导致 DoS             | mitigate            | Workspace.SoftDelete 方法实现 D-08 `__{epoch}` 后缀；02-04 DeleteWorkspace handler 调用它；02-06 smoke 验证                                                |
| T-2-cache            | Information Disclosure | Redis 缓存 slug→tenant 跨请求泄露             | accept              | workspace tenant 不是敏感数据（仅 workspaceGuid + slug + name）；缓存键 namespace `ws:slug:` 隔离；workspace 更新/删除需显式失效缓存（02-04 handler 处理） |
| T-2-fintenant        | Tampering              | Finbuckle AddMultiTenant 重复调用             | mitigate [BLOCKING] | 严格按 02-01 spike 结论选路径 A 或 B；路径 A 用 TryAddEnumerable 不覆盖；路径 B 仅追加扩展方法链；代码 review 验证 MultitenancyModule 现有链不变           |
| T-2-SC               | Tampering              | NuGet 包                                      | accept              | 0 新增包（02-RESEARCH §Package Legitimacy Audit）                                                                                                          |

## Threat Disposition Summary

- **mitigate [BLOCKING]**: T-2-isolation（Workspace=IGlobalEntity 守护）、T-2-fintenant（不重复 AddMultiTenant）—— 02-03/06 验证前必须确认
- **mitigate**: T-2-slugstrategy、T-2-tenantleak、T-2-eop、T-2-softdelete
- **accept**: T-2-cache（tenant metadata 非敏感）、T-2-SC（无新包）
- **HIGH severity**: T-2-isolation、T-2-fintenant —— 必须在 02-03 加 integration test + 02-06 smoke 守护
  </threat_model>

<verification>
- [ ] `dotnet build src/YH.Flow.slnx` 0 warning / 0 error（含 Workspace 模块全部 6+ 新文件 + MultitenancyModule 改动）
- [ ] Workspace 实体编译时是 IGlobalEntity（grep `: IGlobalEntity` 在 Domain/Workspace.cs 命中）
- [ ] WorkspaceDbContext.cs 文件含 `ApplyConfigurationsFromAssembly` 在 `base.OnModelCreating` 之前
- [ ] WorkspaceSlugStrategy 严格检查 slug 非空（grep `GetRouteValue("slug")` 命中）
- [ ] MultitenancyModule.cs 现有 strategy 链（WithClaimStrategy/WithHeaderStrategy）不变（diff 只增不减）
- [ ] `dotnet test src/Tests/Workspace.Tests` spike 仍跑通
</verification>

<success_criteria>

- Domain 实体 + DbContext + Finbuckle wiring 全部编译通过
- D-01/D-04/D-06/D-08 决策在代码层面有具体落点（slug strategy、OwnerId 标量、SoftDelete 方法、IGlobalEntity 守护）
- spike Q1 结论在 02-01-SUMMARY 指导的路径 A/B 选定并落地
- Phase 1 MultitenancyModule 零回归（claim/header strategy 仍工作）
  </success_criteria>

<output>
Create `.planning/phases/02-workspace/02-02-SUMMARY.md` when done. 记录：(a) spike Q1 落地的具体路径（A 或 B）；(b) Workspace 实体关键不变式（IGlobalEntity 守护）；(c) MultitenancyModule 改动点（若有，仅 1 行）；(d) 02-03 wiring 时的注意事项（如 TenantId 列自动加、unique 索引 widen 等）。
</output>
