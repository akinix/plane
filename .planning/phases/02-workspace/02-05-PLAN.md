---
phase: 02-workspace
plan: 05
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - yh-flow/src/Modules/Identity/Modules.Identity/Services/UserIdentityService.cs
  - yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Members/ListMembers/ListMembersQuery.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Members/UpdateMemberRole/UpdateMemberRoleCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Members/RemoveMember/RemoveMemberCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/CreateInvitation/CreateInvitationCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/CreateInvitation/CreateInvitationResponse.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/ListInvitations/ListInvitationsQuery.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/RevokeInvitation/RevokeInvitationCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/AcceptInvitation/AcceptInvitationCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/RejectInvitation/RejectInvitationCommand.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/IInvitationTokenService.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/WorkspaceMembershipService.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/ListMembers/ListMembersQueryHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/ListMembers/ListMembersEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/UpdateMemberRole/UpdateMemberRoleCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/UpdateMemberRole/UpdateMemberRoleCommandValidator.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/UpdateMemberRole/UpdateMemberRoleEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/RemoveMember/RemoveMemberCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/RemoveMember/RemoveMemberEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationCommandValidator.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/ListInvitations/ListInvitationsQueryHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/ListInvitations/ListInvitationsEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RevokeInvitation/RevokeInvitationCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RevokeInvitation/RevokeInvitationEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RejectInvitation/RejectInvitationCommandHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RejectInvitation/RejectInvitationEndpoint.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
  - yh-flow/src/Tests/Workspace.Tests/Services/InvitationTokenServiceTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/ListMembersBatchTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/InvitationHashTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/InvitationInvalidateTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/InvitationTtlTests.cs
autonomous: true
requirements: [REQ-2.2, REQ-2.4]
user_setup: []

must_haves:
  truths:
    - "Admin 可调 GET /api/v1/workspaces/{slug}/members/ 列出成员（含 role+UserSummary，无 N+1，D-05 batch 解析）"
    - "Admin 可调 PATCH /api/v1/workspaces/{slug}/members/{id}/ 改成员 role（不能改自己提权，Member→Admin 需要 Admin 权限）"
    - "Admin 可调 DELETE /api/v1/workspaces/{slug}/members/{id}/ 软删除成员"
    - "Admin 可调 POST /api/v1/workspaces/{slug}/invitations/ 创建邀请（生成 CSPRNG token + SHA-256 哈希存库 + 返回 raw token 一次）"
    - "Admin 可调 GET /api/v1/workspaces/{slug}/invitations/ 列出 pending 邀请"
    - "Admin 可调 DELETE /api/v1/workspaces/{slug}/invitations/{id}/ 撤销邀请（token 失效）"
    - "受邀用户可调 POST /api/v1/workspaces/invitations/{token}/accept/ 接受邀请（验证 token hash + TTL + 未响应 + 自动建 WorkspaceMember，accept 后 token 失效）"
    - "受邀用户可调 POST /api/v1/workspaces/invitations/{token}/reject/ 拒绝邀请（token 失效）"
    - "DB 中无明文 token（只存 SHA-256 哈希，D-12）"
    - "Identity 模块实现 UserIdentityService.GetUsersByIdsAsync 批量查用户（单次 SQL）"
  artifacts:
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs"
      provides: "D-12 邀请 token CSPRNG + SHA-256 哈希"
      contains: "RandomNumberGenerator"
    - path: "yh-flow/src/Modules/Identity/Modules.Identity/Services/UserIdentityService.cs"
      provides: "D-04/D-05 跨模块批量用户解析实现"
      contains: "GetUsersByIdsAsync"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/WorkspaceMembershipService.cs"
      provides: "成员 CRUD 服务"
      contains: "AddOwnerAsync"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/ListMembers/ListMembersQueryHandler.cs"
      provides: "无 N+1 的成员列表（batch IUserIdentityService）"
      contains: "GetUsersByIdsAsync"
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/InvitationHashTests.cs"
      provides: "D-12 token 哈希存储测试（DB 无明文）"
      contains: "TokenHash"
  key_links:
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/ListMembers/ListMembersQueryHandler.cs"
      to: "yh-flow/src/Modules/Identity/Modules.Identity/Services/UserIdentityService.cs"
      via: "IUserIdentityService.GetUsersByIdsAsync 批量查"
      pattern: "GetUsersByIdsAsync"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationCommandHandler.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs"
      via: "GenerateToken() → raw + hash"
      pattern: "InvitationTokenService"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs"
      via: "ValidateAsync(rawToken) → 查 hash + 校验 IsValid"
      pattern: "ValidateAsync"
---

<objective>
Wave 4：实现 Member 端点（REQ-2.2）+ Invitation 端点（REQ-2.4）+ 跨模块 IUserIdentityService 实现 + 邀请 token crypto 服务。这是 Phase 2 业务层最重的一个 plan —— 涉及 11 个 Feature slice + 2 个核心服务。

Purpose: D-05（batch IUserIdentityService 避免 N+1）是 NFR-1 性能保障；D-12（CSPRNG + SHA-256 + TTL + accept/revoke 失效）是 REQ-2.4 安全保障（防 token 伪造/重放）。INVITATION 机制贴 Plane `app/urls/invite.py` 契约。WorkspaceMembershipService 把 02-04 内联的 AddOwnerAsync 重构到服务（02-04 内联逻辑可保留或重构，二选一）。

Output: (1) UserIdentityService 实现（在 Identity 模块）；(2) InvitationTokenService（贴 ApiTokenService crypto 模式）；(3) WorkspaceMembershipService；(4) 5 个 Member Feature + 5 个 Invitation Feature + 1 个 Leave（可选合并到 RemoveMember）；(5) 5 个新测试覆盖 D-05/D-12/N+1/hash/invalidate/TTL。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-PATTERNS.md
@.planning/phases/02-workspace/02-VALIDATION.md
@.planning/phases/02-workspace/02-04-SUMMARY.md

@yh-flow/src/Modules/Identity/Modules.Identity/Services/ApiTokenService.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Services/SessionService.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Services/UserService.cs
@yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/GetUsers/GetUsersQueryHandler.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/AssignUserRoles/
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/DeleteUser/
@yh-flow/src/Modules/Identity/Modules.Identity/Features/v1/Users/RegisterUser/
@apps/api/plane/api/views/invite.py
@apps/api/plane/api/views/member.py
@apps/api/plane/api/serializers/invite.py
@apps/api/plane/api/serializers/member.py
@apps/api/plane/api/urls/invite.py
@apps/api/plane/api/urls/member.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserIdentityService 实现（Identity 模块）+ WorkspaceMembershipService + InvitationTokenService（含 unit tests）</name>
  <files>
    yh-flow/src/Modules/Identity/Modules.Identity/Services/UserIdentityService.cs
    yh-flow/src/Modules/Identity/Modules.Identity/IdentityModule.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Services/IInvitationTokenService.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Services/WorkspaceMembershipService.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Tests/Workspace.Tests/Services/InvitationTokenServiceTests.cs
  </files>
  <behavior>
    - UserIdentityService.GetUsersByIdsAsync：用 IdentityDbContext 单次 SQL `Where(u => userIds.Contains(u.Id)).AsNoTracking()` 投影到 UserSummary record，返回 Dictionary&lt;Guid, UserSummary&gt;（D-05 N+1 避免）
    - UserIdentityService 在 IdentityModule.ConfigureServices 注册为 IUserIdentityService 的 Scoped/Transient
    - InvitationTokenService：静态方法 GenerateToken() 返回 (rawToken, hash)（贴 ApiTokenService.cs:175-187 crypto 模式）；HashToken(raw) 静态方法；ValidateAsync(rawToken) 查 WorkspaceInvitation by TokenHash=HashToken(raw) → 返回 invitation 或 null；CreateInvitationAsync(workspaceId, email, role, ttlDays, message) 返回 raw token + persist hash only
    - TTL 默认 7 天（RESEARCH A3），配置键 `Workspace:InvitationTokenTtlDays`，从 IOptions 读
    - WorkspaceMembershipService：ctor 注入 WorkspaceDbContext；方法 AddOwnerAsync(workspaceId, userId, ct) → 插入 Admin member；ListAsync(workspaceId, ct)；UpdateRoleAsync(memberId, role, ct)；RemoveAsync(memberId, ct)；LeaveAsync(workspaceId, userId, ct)（self-remove）
    - InvitationTokenServiceTests：unit test 5 case：(a) GenerateToken 返回不同 raw（每次随机）；(b) HashToken(raw) 一致 deterministic；(c) HashToken(raw) != raw（无明文）；(d) TTL 默认 7 天（创建时 ExpiresAt = now + 7d）；(e) 32 字节 token hex lowercase（贴 ApiTokenService 长度）
  </behavior>
  <action>
    **UserIdentityService.cs（在 Identity 模块）**：贴 `UserService.cs` 结构 —— class 实现 `IUserIdentityService`（02-01 Task 3 已定义契约），ctor 注入 IdentityDbContext 或对应 user store。`GetUsersByIdsAsync(IEnumerable<Guid> userIds, CancellationToken ct)` 实现：`var users = await _db.Users.AsNoTracking().Where(u => userIds.Contains(u.Id)).ToDictionaryAsync(u => u.Id, u => new UserSummary(u.Id, u.DisplayName, u.Email, u.AvatarUrl), ct);`。**关键**：单次 SQL（D-05），不用循环；用 `Contains` 让 EF 翻译成 `IN (...)`。
    **IdentityModule.cs 注册**：在 ConfigureServices 追加 `builder.Services.TryAdd<IUserIdentityService, UserIdentityService>();`（ServiceLifetime.Scoped，与 UserService 一致）。
    **IInvitationTokenService.cs**：定义接口 `(string RawToken, string Hash) GenerateToken(); string HashToken(string rawToken); Task<WorkspaceInvitation?> ValidateAsync(string rawToken, CancellationToken ct); Task<(WorkspaceInvitation Invitation, string RawToken)> CreateAsync(Guid workspaceId, string email, WorkspaceRole role, int ttlDays, string? message, CancellationToken ct);`。
    **InvitationTokenService.cs**（贴 `ApiTokenService.cs:1-9` imports + PATTERNS §IInvitationTokenService 给了完整模板）：ctor 注入 WorkspaceDbContext + IOptions&lt;WorkspaceTokenOptions&gt;（含 TtlDays 默认 7）。
    - 静态 `GenerateToken()`：`var bytes = RandomNumberGenerator.GetBytes(TokenByteLength=32); var raw = Convert.ToHexString(bytes).ToLowerInvariant(); var hash = HashToken(raw); return (raw, hash);`
    - 静态 `HashToken(string raw)`：`var bytes = Encoding.UTF8.GetBytes(raw); var hashBytes = SHA256.HashData(bytes); return Convert.ToHexString(hashBytes).ToLowerInvariant();`
    - `CreateAsync(...)`：调 GenerateToken → WorkspaceInvitation.Create(workspaceId, email, hash, (int)role, ttlDays, message) → db.Invitations.Add → SaveChanges → 返回 (invitation, raw)（**raw 只返回一次，不落库**）。
    - `ValidateAsync(raw)`：`var hash = HashToken(raw); var inv = await db.Invitations.AsNoTracking().FirstOrDefaultAsync(i => i.TokenHash == hash, ct); return inv?.IsValid == true ? inv : null;`（**注意 IsValid 是 WorkspaceInvitation 实体的计算属性：!Accepted && RespondedAt == null && !IsExpired**）。
    **WorkspaceMembershipService.cs**（贴 `SessionService.cs` CRUD-over-DbContext 模式 + PATTERNS §WorkspaceMembershipService）：5 个方法实现。AddOwnerAsync 创建 `WorkspaceMember.Create(workspaceId, userId, WorkspaceRole.Admin, isActive: true)`。其他方法用 tenant-filtered DbContext 操作（自动按当前 workspaceId 过滤）。
    **WorkspaceModule.ConfigureServices 注册**：追加 `builder.Services.AddScoped<ISlugGenerator, SlugGenerator>();`（02-04 已建但可能没注册 —— 验证）、`builder.Services.AddScoped<IInvitationTokenService, InvitationTokenService>();`、`builder.Services.AddScoped<WorkspaceMembershipService>();`。
    **WorkspaceTokenOptions.cs**：在 Workspace.Contracts 或 Modules.Workspace/Configuration 加 `public sealed class WorkspaceTokenOptions { public int InvitationTokenTtlDays { get; init; } = 7; }`，绑定 `builder.Services.Configure<WorkspaceTokenOptions>(configuration.GetSection("Workspace"));`。
    **InvitationTokenServiceTests.cs**：5 个 [Fact] case，**纯静态方法测试**（不依赖 DbContext），贴 `ApiTokenServiceTests.cs:1-60` 风格。这是 VALIDATION §Per-Task 行 2-07/2-09 对应测试。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/YH.Flow.slnx --nologo 2>&1 | tail -3 && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~InvitationTokenService" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    UserIdentityService 在 Identity 模块注册并可批量查；InvitationTokenService 实现贴 ApiTokenService crypto 模式；WorkspaceMembershipService 5 方法就位；5 个 token unit test 全过；TTL 默认 7 天从配置可读。
  </done>
</task>

<task type="auto">
  <name>Task 2: 5 个 Member Feature slice（ListMembers/UpdateMemberRole/RemoveMember/Leave）+ ListMembersBatch integration test</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Members/ListMembers/ListMembersQuery.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Members/UpdateMemberRole/UpdateMemberRoleCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Members/RemoveMember/RemoveMemberCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/ListMembers/ListMembersQueryHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/ListMembers/ListMembersEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/UpdateMemberRole/UpdateMemberRoleCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/UpdateMemberRole/UpdateMemberRoleCommandValidator.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/UpdateMemberRole/UpdateMemberRoleEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/RemoveMember/RemoveMemberCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Members/RemoveMember/RemoveMemberEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/ListMembersBatchTests.cs
  </files>
  <behavior>
    - ListMembersQuery：[JsonIgnore] WorkspaceId（从 ICurrentWorkspaceContext）+ 分页；Response = PlanePagedResult&lt;WorkspaceMemberDto&gt;
    - ListMembersQueryHandler（**N+1 避免 D-05 关键**）：(1) db.Members.AsNoTracking().Where(m => m.WorkspaceId == ctx.CurrentWorkspaceId).ProjectTo MemberDto(without User) → paged；(2) 收集 member.UserIds；(3) `var users = await userIdentity.GetUsersByIdsAsync(userIds, ct);`；(4) zip member + user → WorkspaceMemberDto.User 填充 —— **整个 handler 仅 2 次 SQL（members + users），无 N+1**
    - ListMembersEndpoint：GET scoped "/{slug}/members/" → `.RequireWorkspaceRole(WorkspaceRole.Member, WorkspaceRole.Admin)`（Member/Admin 可看，Guest 不可）
    - UpdateMemberRoleCommand：[JsonIgnore] WorkspaceId + MemberId + Role (WorkspaceRole) + [JsonIgnore] CurrentUserId
    - UpdateMemberRoleCommandValidator：Role 必须 In (Admin=20, Member=15, Guest=5)
    - UpdateMemberRoleCommandHandler：查 member by id（自动 tenant filter）；**显式检查 target member != self**（防 Member 自我提权为 Admin）—— `if (member.UserId == currentUserId && newRole == Admin) throw ForbiddenException`；调 WorkspaceMembershipService.UpdateRoleAsync
    - UpdateMemberRoleEndpoint：PATCH scoped "/{slug}/members/{memberId}/" → `.RequireWorkspaceRole(WorkspaceRole.Admin)`
    - RemoveMemberCommand：WorkspaceId + MemberId + CurrentUserId
    - RemoveMemberCommandHandler：查 member；**检查 target != self**（self-removal 用 LeaveAsync，普通 RemoveMember 不允许 self）—— 若 self throw；Admin 可删其他 Admin（仅 Owner 可删 Admin，贴 Plane）；调 RemoveAsync
    - RemoveMemberEndpoint：DELETE scoped "/{slug}/members/{memberId}/" → `.RequireWorkspaceRole(WorkspaceRole.Admin)`
    - WorkspaceModule.MapEndpoints 注册 Member 4 端点（List/UpdateRole/Remove/Leave —— Leave 单独端点 POST "/{slug}/members/leave/"，任何 Member 可调）
    - ListMembersBatchTests：integration —— seed workspace + 5 members + 调 ListMembers → 验证返回 5 个 DTO 全部含 UserSummary；**关键**：EF Core 日志或 NSubstitute 验证 IUserIdentityService.GetUsersByIdsAsync 被调用**恰好一次**（而非 5 次，D-05 N+1 验证）
  </behavior>
  <action>
    按 PATTERNS §Mediator Vertical Slice + §ListMembers 批量解析关键模式 + RESEARCH Pitfall 3（N+1）实现。**ListMembers 是 NFR-1 性能关键端点**。
    **ListMembersQueryHandler 的两阶段模式（D-05 核心）：**
    ```
    // 阶段 1：分页查 members（不 join Identity）
    var paged = await db.Members.AsNoTracking()
        .Where(m => m.WorkspaceId == workspaceId)
        .OrderBy(m => m.Role).ThenBy(m => m.CreatedOnUtc)
        .ProjectTo<MemberStub>(cfg)  // 仅 Id/Role/IsActive/UserId
        .ToPagedAsync(query.Page, query.PageSize, ct);
    // 阶段 2：batch 查 users
    var userIds = paged.Items.Select(m => Guid.Parse(m.UserId)).Distinct();
    var users = await userIdentity.GetUsersByIdsAsync(userIds, ct);
    // 阶段 3：zip
    var dtos = paged.Items.Select(m => new WorkspaceMemberDto {
        Id = m.Id, Role = m.Role, IsActive = m.IsActive,
        User = users.GetValueOrDefault(Guid.Parse(m.UserId))
    });
    return PlanePagedResult<WorkspaceMemberDto>.FromPagedResponse(paged with { Items = dtos });
    ```
    **UpdateMemberRole 防 EOP（T-2-eop-self）**：handler 内 `if (targetMember.UserId == currentUserId.ToString() && newRole == WorkspaceRole.Admin) throw new ForbiddenException("Cannot promote self");`。其他 role 变更允许（Admin 可以降自己为 Member 但需要至少留一个 Admin，可加业务规则）。
    **RemoveMember 防 EOP**：handler `if (targetMember.UserId == currentUserId.ToString()) throw new ForbiddenException("Use leave endpoint instead");`。
    **Leave 端点（可选合并）**：单独 POST "/{slug}/members/leave/" —— 任何 Member 可调，调 WorkspaceMembershipService.LeaveAsync(workspaceId, currentUserId)。**简化**：本 plan 实现 RemoveMember + Leave 两个端点共用 handler 内部判断（route 不同）。
    **测试**：ListMembersBatchTests 验证 batch 解析 —— 用 NSubstitute stub `IUserIdentityService`，验证 `GetUsersByIdsAsync` 接收的参数含全部 5 个 userId，**调用次数 == 1**。这是 D-05 N+1 避免的直接证据（VALIDATION §Per-Task 行 2-06 对应）。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~ListMembers" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    4 个 Member 端点全部编译 + 注册；ListMembersBatch 测试通过证明 N+1 避免成功（GetUsersByIdsAsync 恰好调用 1 次）；防 EOP 自我提权检查就位。
  </done>
</task>

<task type="auto">
  <name>Task 3: 5 个 Invitation Feature slice（Create/List/Revoke/Accept/Reject）+ 4 个 invitation integration 测试（hash/invalidate/TTL）</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/CreateInvitation/CreateInvitationCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/CreateInvitation/CreateInvitationResponse.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/ListInvitations/ListInvitationsQuery.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/RevokeInvitation/RevokeInvitationCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/AcceptInvitation/AcceptInvitationCommand.cs
    - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/v1/Invitations/RejectInvitation/RejectInvitationCommand.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationCommandValidator.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/CreateInvitation/CreateInvitationEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/ListInvitations/ListInvitationsQueryHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/ListInvitations/ListInvitationsEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RevokeInvitation/RevokeInvitationCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RevokeInvitation/RevokeInvitationEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RejectInvitation/RejectInvitationCommandHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/RejectInvitation/RejectInvitationEndpoint.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/InvitationHashTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/InvitationInvalidateTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/InvitationTtlTests.cs
  </files>
  <behavior>
    - CreateInvitationCommand：[JsonIgnore] WorkspaceId + Email + Role (WorkspaceRole) + Message?；Response record(Guid InvitationId, string Token, string Slug) —— **raw token 仅返回一次**
    - CreateInvitationCommandValidator：Email NotEmpty + EmailAddress；Role In (Admin/Member/Guest)
    - CreateInvitationCommandHandler：调 invitationTokenService.CreateAsync(workspaceId, email, role, ttlDays, message) → 返回 raw token + invitation；**可选**调 INotificationService.SendInvitationNotificationAsync（Phase 11 实现，本阶段 stub 注入 null 或不调用）；返回 Response 含 raw token + slug（让前端拼邀请 URL）
    - CreateInvitationEndpoint：POST scoped "/{slug}/invitations/" → `.RequireWorkspaceRole(WorkspaceRole.Admin)`；返回 201 + Response（**raw token 只在响应体出现一次**）
    - ListInvitationsQuery：[JsonIgnore] WorkspaceId + 状态过滤（pending/accepted/all）；Response = PlanePagedResult&lt;WorkspaceInvitationDto&gt;
    - ListInvitationsQueryHandler：db.Invitations.AsNoTracking().Where(i => i.WorkspaceId == workspaceId && (filter.AcceptedOnly ? i.Accepted : true)) 分页
    - ListInvitationsEndpoint：GET scoped "/{slug}/invitations/" → `.RequireWorkspaceRole(WorkspaceRole.Admin)`
    - RevokeInvitationCommand：[JsonIgnore] WorkspaceId + InvitationId
    - RevokeInvitationCommandHandler：查 invitation by id；调 invitation.Revoke()（置 RespondedAt）；SaveChanges
    - RevokeInvitationEndpoint：DELETE scoped "/{slug}/invitations/{invitationId}/" → `.RequireWorkspaceRole(WorkspaceRole.Admin)`
    - AcceptInvitationCommand：Token（**来自 URL 路径段**）+ [JsonIgnore] CurrentUserId；Response record(Guid MemberId, Guid WorkspaceId)
    - AcceptInvitationCommandHandler：调 invitationTokenService.ValidateAsync(token) → null throw NotFoundException；再调 invitation.Accept() → 创建 WorkspaceMember(role 取 invitation.Role, isActive=true) → SaveChanges
    - AcceptInvitationEndpoint：POST **顶层** "/invitations/{token}/accept/" → `.RequireAuthorization()`（任何认证用户）；**不在 workspace scope 内**（受邀者可能尚非成员）
    - RejectInvitationCommand：Token；Response record(bool Success)
    - RejectInvitationCommandHandler：调 invitationTokenService.ValidateAsync → invitation.Reject() → SaveChanges
    - RejectInvitationEndpoint：POST 顶层 "/invitations/{token}/reject/" → `.RequireAuthorization()`
    - WorkspaceModule.MapEndpoints 注册 5 个 Invitation 端点（Create/List/Revoke 在 scoped 组；Accept/Reject 在 topLevel 组）
    - InvitationHashTests：integration —— Create 邀请 → 查 db.Invitations → 验证 TokenHash 存在但**无 RawToken 字段**（明文不入库）；raw 仅出现在 CreateInvitationResponse
    - InvitationInvalidateTests：integration —— Create + Accept → 验证 Accept 后 ValidateAsync 返回 null（token 失效）；Create + Revoke → 验证同样；Create + Reject → 同样
    - InvitationTtlTests：unit/integration —— Create + 模拟时间前进 8 天（构造 ExpiresAt = now - 1day 或用 ITimestampProvider stub）→ ValidateAsync 返回 null（TTL 失效）
  </behavior>
  <action>
    按 PATTERNS §Invitation Feature slices（贴 Identity RegisterUser 模式 + ApiTokenService token 模式）。**关键路径差异：**
    (a) **Accept/Reject 是顶层路由（不在 {slug}/ 内）**：受邀者可能不是任何 workspace 的成员（甚至无 user 账户 —— Plane 支持 email 邀请未注册用户），所以 accept/reject 不能走 workspace-scoped route。**路由**：`POST /api/v1/workspaces/invitations/{token}/accept/` —— 顶层 + token 在路径段 + 任何认证用户可调（`RequireAuthorization()`）。
    (b) **Create 返回 raw token 仅一次**：CreateInvitationResponse.Token 字段只在 POST 响应体出现；ListInvitations/GetInvitation 响应**不**含 token（仅 TokenHash 内部）。这是 D-12 的实现 —— token 类似 password 一次性可见。
    (c) **INotificationService 预留（D-10）**：CreateInvitationCommandHandler 内 `var notifier = ctx.RequestServices?.GetService<INotificationService>(); if (notifier is not null) await notifier.SendInvitationNotificationAsync(invitation.Email, ws.Id, ws.Slug, rawToken, ct);`。**Phase 11 实现**，本阶段 notifier == null，跳过 —— 创建邀请返回链接给调用方，调用方负责投递（贴 D-10 决策）。
    (d) **AcceptInvitation 创建 member（D-12 状态机）**：handler 流程：`var inv = await tokenService.ValidateAsync(token, ct); if (inv is null) throw NotFoundException("Invalid or expired token"); var ws = await db.Workspaces.AsNoTracking().FirstAsync(w => w.Id == inv.WorkspaceId, ct); var member = WorkspaceMember.Create(ws.Id, currentUser.Id.ToString(), (WorkspaceRole)inv.Role, isActive: true); db.Members.Add(member); inv.Accept(now); await db.SaveChangesAsync(ct); return new AcceptResponse(member.Id, ws.Id);`。**两次 SaveChanges 合并**：在同一 transaction 内（EF Core 默认）。
    (e) **InvitationTokenService.CreateAsync 已在 Task 1 实现**，本 task 直接调；Accept/Reject/Revoke 调用 WorkspaceInvitation 实体的方法（02-02 Task 1 已建 Accept/Reject/Revoke 三方法）。
    (f) **TTL 测试时间 mock**：直接构造 WorkspaceInvitation 实体设 ExpiresAt = DateTimeOffset.UtcNow.AddHours(-1)（已过期）注入 db，调 ValidateAsync → 应返回 null。**避免依赖系统时间**，或在 WorkspaceInvitation 实体的 IsExpired 计算属性里注入 IClock（可选简化）。
    (g) **WorkspaceModule.MapEndpoints**：把 5 个 Invitation 端点追加到 02-04 的 MapEndpoints。Accept/Reject 在 topLevel 组（贴 Identity 顶层路由模式）。
    **测试覆盖（VALIDATION §Per-Task 行 2-07/2-08/2-09）：**
    - InvitationHashTests：assert `db.Invitations.First().TokenHash` 长度 == 64（SHA-256 hex）；assert `db.Invitations.First()` **不**含 RawToken 字段（实体无该属性 —— 验证 entity 设计）
    - InvitationInvalidateTests：3 个 case（accept/revoke/reject 各一）—— assert 二次 ValidateAsync(token) 返回 null
    - InvitationTtlTests：1 case —— 构造过期 invitation → ValidateAsync 返回 null
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~InvitationHash|FullyQualifiedName~InvitationInvalidate|FullyQualifiedName~InvitationTtl" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    5 个 Invitation 端点全部编译 + 注册；3 个 integration 测试 + 1 个 TTL 测试通过；token DB 无明文（只存 hash）；accept/revoke/reject 后 token 失效；TTL 过期拒绝。D-10/D-12 完整实现。
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                                 |
| ---------------- | ----------------------------------------------------------- |
| Admin → Mutation | Admin gate 在 invitation/member mutation 端点               |
| Token → Accept   | raw token 来自 URL 路径段，跨信任边界进入 validate pipeline |
| Cross-module     | Workspace → Identity.Contracts 跨模块调用                   |

## STRIDE Threat Register

| Threat ID           | Category                     | Component                            | Disposition         | Mitigation Plan                                                                                                    |
| ------------------- | ---------------------------- | ------------------------------------ | ------------------- | ------------------------------------------------------------------------------------------------------------------ |
| T-2-n1 [HIGH]       | Information Disclosure / DoS | ListMembers N+1 → 性能瓶颈           | mitigate [BLOCKING] | D-05 batch IUserIdentityService；ListMembersBatchTests 验证 GetUsersByIdsAsync 恰好 1 次；AsNoTracking             |
| T-2-token [HIGH]    | Spoofing                     | 邀请 token 伪造                      | mitigate [BLOCKING] | CSPRNG RandomNumberGenerator.GetBytes(32) = 256 bit；SHA-256 哈希；DB 唯一索引 TokenHash；InvitationHashTests 守护 |
| T-2-replay [HIGH]   | Repudiation                  | accept/revoke/reject 后 token 仍可用 | mitigate [BLOCKING] | 状态机 RespondedAt 置位 → IsValid=false；InvitationInvalidateTests 3 case 守护                                     |
| T-2-ttl             | Tampering                    | token 无 TTL → 永久有效              | mitigate            | ExpiresAt + IsExpired 计算；TTL 默认 7 天配置；InvitationTtlTests 守护                                             |
| T-2-tokenleak       | Information Disclosure       | DB dump 暴露可邀用的 token           | mitigate            | 只存 TokenHash 不存 RawToken；InvitationHashTests 验证 entity 无 RawToken 字段                                     |
| T-2-eop-self [HIGH] | Elevation of Privilege       | Member 改自己 role → Admin           | mitigate [BLOCKING] | UpdateMemberRole handler 显式 self+Admin 检查 throw ForbiddenException；单元/integration test 守护                 |
| T-2-eop-target      | Elevation of Privilege       | Guest 调 Admin-only 端点             | mitigate            | `.RequireWorkspaceRole(WorkspaceRole.Admin)` 装饰 Create/Update/Revoke/Remove 端点；02-03 authz test 守护          |
| T-2-crossmodule     | Tampering                    | 跨模块 FK 泄露                       | mitigate            | UserIdentityService 仅返回 UserSummary 4 字段；不暴露 PasswordHash；Select projection（02-01 Task 3 守护）         |
| T-2-acceptpublic    | Elevation of Privilege       | accept 端点公众可调                  | mitigate            | accept 端点 `.RequireAuthorization()`（任何认证用户）；ValidateAsync 内部校验 token hash 存在且 IsValid            |
| T-2-acceptdouble    | Repudiation                  | 重复 accept 同一 token → 多个 member | mitigate            | invitation.Accept() 置 RespondedAt；二次 ValidateAsync 返回 null；InvitationInvalidateTests 覆盖                   |
| T-2-notifier        | Information Disclosure       | INotificationService 误发邮件        | accept              | Phase 2 不实现（D-10）；notifier==null 时跳过；Phase 11 实现时再审计                                               |
| T-2-SC              | Tampering                    | NuGet                                | accept              | 0 新包                                                                                                             |

## Threat Disposition Summary

- **mitigate [BLOCKING]**: T-2-n1（N+1）、T-2-token（CSPRNG）、T-2-replay（状态机）、T-2-eop-self（self-Admin 检查）—— 本 plan 三个 task 的测试直接守护
- **mitigate**: T-2-ttl、T-2-tokenleak、T-2-eop-target、T-2-crossmodule、T-2-acceptpublic、T-2-acceptdouble
- **accept**: T-2-notifier（Phase 11 placeholder）、T-2-SC
- **HIGH severity**: T-2-n1、T-2-token、T-2-replay、T-2-eop-self、T-2-acceptdouble —— 全部在本 plan 测试覆盖
  </threat_model>

<verification>
- [ ] `dotnet build src/YH.Flow.slnx` 0 warning / 0 error（含 11 个 Feature slice + 3 个 service + Identity 模块扩展）
- [ ] `dotnet test src/Tests/Workspace.Tests --filter "InvitationTokenService|ListMembers|InvitationHash|InvitationInvalidate|InvitationTtl"` 全过
- [ ] ListMembers batch 解析（GetUsersByIdsAsync 调用 1 次）验证
- [ ] Invitation token DB 无明文（TokenHash 唯一，无 RawToken 字段）
- [ ] accept/revoke/reject 后 token 失效（3 个 invalidate case）
- [ ] TTL 过期 token 拒绝
- [ ] Phase 1 Identity.Tests 412/412 仍全绿（UserIdentityService 扩展不破坏现有 IUserService）
</verification>

<success_criteria>

- REQ-2.2（成员管理：list/update-role/remove/leave）全部端点可调用
- REQ-2.4（邀请：create/list/revoke/accept/reject）全部端点可调用
- D-04/D-05/D-10/D-12 实现并有测试守护
- NFR-1（N+1 避免）验证；NFR-2（token 哈希）验证
- 11 个 Feature slice 全部 Vertical Slice 模式贴 Identity 模块结构
  </success_criteria>

<output>
Create `.planning/phases/02-workspace/02-05-SUMMARY.md` when done. 记录：(a) UserIdentityService 在 Identity 模块的具体注册路径；(b) InvitationTokenService TTL 默认值与配置键；(c) 11 个端点的最终路由（贴 Plane `app/urls/invite.py` + `api/urls/member.py` 对照）；(d) ListMembers 两阶段查询的 EF SQL（验证 Contains→IN 翻译）；(e) INotificationService 预留接入点（Phase 11 实现）。
</output>
