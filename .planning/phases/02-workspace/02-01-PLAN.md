---
phase: 02-workspace
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj
  - yh-flow/src/Tests/Workspace.Tests/GlobalUsings.cs
  - yh-flow/src/Tests/Workspace.Tests/Usings.cs
  - yh-flow/src/Tests/Workspace.Tests/TestData/WorkspaceTestFixture.cs
  - yh-flow/src/Tests/Workspace.Tests/Spike/FinbuckleExternalStrategyRegistrationTests.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/Modules.Workspace.Contracts.csproj
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/AssemblyInfo.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/ICurrentWorkspaceContext.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/IWorkspaceTenantResolver.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/INotificationService.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/WorkspaceRole.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/DTOs/WorkspaceDto.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/DTOs/WorkspaceMemberDto.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/DTOs/WorkspaceInvitationDto.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/Constants/RestrictedSlugs.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/UserSummary.cs
  - yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/IUserIdentityService.cs
  - yh-flow/src/YH.Flow.slnx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Workspace.Tests 项目编译通过，可运行 dotnet test（哪怕空集合）"
    - "Workspace.Contracts 类库暴露 ICurrentWorkspaceContext / WorkspaceRole / RestrictedSlugs / DTOs / INotificationService"
    - "Identity.Contracts 暴露 UserSummary + IUserIdentityService（供 02-05 批量查使用）"
    - "Wave 0 Q1 spike 验证：Finbuckle 10.1.1 是否允许 builder 外追加 IMultiTenantStrategy/IMultiTenantStore（决定 02-02 wiring 方案）"
  artifacts:
    - path: "yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj"
      provides: "Workspace.Tests 测试项目骨架"
      contains: "Microsoft.NET.Test.Sdk"
    - path: "yh-flow/src/Tests/Workspace.Tests/Spike/FinbuckleExternalStrategyRegistrationTests.cs"
      provides: "Q1 spike — 验证 Finbuckle DI 外部追加"
      contains: "IMultiTenantStrategy"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/ICurrentWorkspaceContext.cs"
      provides: "D-03 跨模块租户上下文契约"
      contains: "CurrentWorkspaceId"
    - path: "yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/IUserIdentityService.cs"
      provides: "D-04/D-05 批量用户解析契约"
      contains: "GetUsersByIdsAsync"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/Constants/RestrictedSlugs.cs"
      provides: "D-09 受限 slug 黑名单（66 项，移植 Plane）"
      contains: "RESTRICTED_WORKSPACE_SLUGS"
  key_links:
    - from: "yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj"
      to: "yh-flow/src/YH.Flow.slnx"
      via: "slnx 项目引用"
      pattern: "Workspace\\.Tests"
---

<objective>
Wave 0：搭建 Phase 2 的全部前置基础设施 —— 解锁后续所有 plan 的执行。

Purpose: 后续 5 个 plan（02-02~02-06）的 `<automated>` 验证命令（`dotnet test src/Tests/Workspace.Tests --filter ...`）全部依赖 `Workspace.Tests` 项目存在；`Modules.Workspace` 的所有 feature slice 依赖 `Modules.Workspace.Contracts` 暴露的 DTO/接口；`ListMembers` 批量查依赖 `Identity.Contracts` 的 `IUserIdentityService`。Wave 0 还要解开 RESEARCH §Open Question Q1 —— Finbuckle 10.1.1 builder 外追加 strategy 的可行性，否则 02-02 的 wiring 无从下手。

Output: (1) Workspace.Tests 项目脚手架 + 共享 fixture + Q1 spike 测试；(2) Workspace.Contracts 类库（DTOs/接口/常量）；(3) Identity.Contracts 扩展（UserSummary + IUserIdentityService）；(4) Q1 spike 的结论落入 02-01-SUMMARY.md 指导 02-02。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-PATTERNS.md
@.planning/phases/02-workspace/02-VALIDATION.md

@yh-flow/src/Tests/Identity.Tests/Identity.Tests.csproj
@yh-flow/src/Tests/Identity.Tests/GlobalUsings.cs
@yh-flow/src/BuildingBlocks/Core/Context/ICurrentUser.cs
@yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/UserDto.cs
@yh-flow/src/Modules/Multitenancy/Modules.Multitenancy/MultitenancyModule.cs
@yh-flow/src/YH.Flow.slnx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace.Tests 项目脚手架 + 共享 fixture + Q1 spike 测试</name>
  <files>
    yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj
    yh-flow/src/Tests/Workspace.Tests/GlobalUsings.cs
    yh-flow/src/Tests/Workspace.Tests/Usings.cs
    yh-flow/src/Tests/Workspace.Tests/TestData/WorkspaceTestFixture.cs
    yh-flow/src/Tests/Workspace.Tests/Spike/FinbuckleExternalStrategyRegistrationTests.cs
    yh-flow/src/YH.Flow.slnx
  </files>
  <behavior>
    - spike 测试 1：在 ServiceCollection 上 `services.AddMultiTenant<AppTenantInfo>().WithClaimStrategy("...").WithStore<...>()` 之后（或之前），通过 `services.TryAddEnumerable(ServiceDescriptor.Singleton<IMultiTenantStrategy<AppTenantInfo>, WorkspaceSlugStrategyStub>())` 追加一个 strategy，验证 `IMultiTenantStrategy<AppTenantInfo>` 集合解析时包含 stub（数量 >= 2）。
    - spike 测试 2：同样的方式追加 `IMultiTenantStore<AppTenantInfo>` stub，验证解析时含 stub。
    - spike 测试 3（如果前两个失败）：验证回退方案 —— 在 `Workspace.Contracts` 暴露 `AddWorkspaceTenantResolution(this MultiTenantBuilder<AppTenantInfo> builder)` 扩展方法，从 MultitenancyModule 内链式调用，能正确注册 strategy + store。
    - 共享 fixture：能拿到 `WorkspaceDbContext`（InMemory 起步，可后续切 Testcontainers）+ seed 一个 root user（贴 Identity.Tests fixture 模式）。
  </behavior>
  <action>
    复制 `yh-flow/src/Tests/Identity.Tests/Identity.Tests.csproj` 结构创建 `Workspace.Tests.csproj`：
    - PackageReference 全部一致（xunit + Shouldly + AutoFixture + NSubstitute + Microsoft.EntityFrameworkCore.InMemory + Microsoft.NET.Test.Sdk），ProjectReference 指向 `Modules.Workspace` 和 `Modules.Workspace.Contracts`（注意：这俩项目在本 plan Task 2 才创建，所以 csproj 的 ProjectReference 要写上但 spike 测试可以只依赖 `Modules.Multitenancy` + Finbuckle packages 来验证 DI 行为，避免循环依赖；如有编译顺序问题，spike 测试可临时只引用 `Modules.Multitenancy`）。
    - GlobalUsings.cs / Usings.cs 复制 Identity.Tests 同名文件，namespace 改 `YH.Tests.Workspace`。
    - **WorkspaceTestFixture.cs**：贴 Identity.Tests 的 `IdentityTestFixture` 模式（若存在）或参考 `Tests/Architecture.Tests` —— 用 `DbContextOptionsBuilder<WorkspaceDbContext>().UseInMemoryDatabase("workspace-tests")` 起一个 InMemory 实例 + seed root user（Guid + email）；本阶段 InMemory 即可，integration test 可在 02-03 切 Testcontainers（不阻塞 Wave 0）。
    - **FinbuckleExternalStrategyRegistrationTests.cs（核心 spike）**：写两个最小 stub 类（在 spike 测试文件内 private nested 即可）：`WorkspaceSlugStrategyStub : IMultiTenantStrategy<AppTenantInfo>` 和 `WorkspaceTenantStoreStub : IMultiTenantStore<AppTenantInfo>`（API 严格按 Finbuckle v10.1.1 `IMultiTenantStrategy`/`IMultiTenantStore` 接口，参考 `MultitenancyModule.cs:98-111` 的现有用法）。测试用例：构造 `ServiceCollection`，调用 `services.AddMultiTenant<AppTenantInfo>().WithClaimStrategy(...)` 模拟 Phase 1 注册，然后 `services.TryAddEnumerable(ServiceDescriptor.Singleton<IMultiTenantStrategy<AppTenantInfo>, WorkspaceSlugStrategyStub>())`；`var sp = services.BuildServiceProvider(); var strategies = sp.GetServices<IMultiTenantStrategy<AppTenantInfo>>(); strategies.Count().ShouldBeGreaterThan(1);`。store 同理。**测试必须真实运行（不可 skip）**。
    - 把 Workspace.Tests 项目加入 `yh-flow/src/YH.Flow.slnx`（参考 Identity.Tests 的 `<Project Path="..." />` 条目）。
    - 实施注意事项：(a) spike 测试若使用 `WorkspaceDbContext` 但 02-01 Task 2 才建该类型，spike 测试暂时只验证 DI 注册（不涉及 DbContext），等 02-02 完成后再补 integration 测试；(b) 不要 skip 失败的测试 —— 如果 builder 外追加失败，spike 测试应该断言失败，并在 SUMMARY 写明"回退到 AddWorkspaceTenantResolution 扩展方法路径"作为 02-02 的指导。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/YH.Flow.slnx --nologo 2>&1 | tail -5 && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~Spike" --nologo 2>&1 | tail -15</automated>
  </verify>
  <done>
    Workspace.Tests 项目编译通过、加入 slnx；Q1 spike 测试运行（无论 pass 还是 fail 都有明确结果）；fixture 可被后续测试继承；SUMMARY 记录 spike 结论（"DI 外部追加可行" 或 "需用 AddWorkspaceTenantResolution 扩展方法"），作为 02-02 wiring 的权威指导。
  </done>
</task>

<task type="auto">
  <name>Task 2: Workspace.Contracts 类库（ICurrentWorkspaceContext / WorkspaceRole / DTOs / 常量 / INotificationService 抽象）</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/Modules.Workspace.Contracts.csproj
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/AssemblyInfo.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/ICurrentWorkspaceContext.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/IWorkspaceTenantResolver.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/INotificationService.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/WorkspaceRole.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/DTOs/WorkspaceDto.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/DTOs/WorkspaceMemberDto.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/DTOs/WorkspaceInvitationDto.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace.Contracts/Constants/RestrictedSlugs.cs
    yh-flow/src/YH.Flow.slnx
  </files>
  <behavior>
    - ICurrentWorkspaceContext 暴露 CurrentWorkspaceId/Slug/CurrentUserRole（D-03）+ Set 方法（middleware 填充）
    - WorkspaceRole 枚举：Admin=20, Member=15, Guest=5（贴 Plane ROLE_CHOICES，per D-11）
    - WorkspaceDto 含 Plane workspace.py 对应字段（id/slug/name/logo/owner_id/organization_size/timezone/background_color/created_at/updated_at/deleted_at）
    - WorkspaceMemberDto 含 id/role/is_active/user(UserSummary?)
    - WorkspaceInvitationDto 含 id/email/role/accepted/responded_at/created_at/message/expires_at
    - RestrictedSlugs 常量含 Plane `RESTRICTED_WORKSPACE_SLUGS` 全部 66 项（移植 `apps/api/plane/utils/constants.py`）
    - INotificationService 占位接口（D-10，Phase 11 实现）
  </behavior>
  <action>
    创建 `Modules.Workspace.Contracts` 类库项目（csproj 贴 `Modules.Auditing.Contracts` csproj 结构 —— TargetFramework=net10.0、Nullable=enable、ImplicitUsings=enable、TreatWarningsAsErrors=true）。无外部 PackageReference 起步（DTO 是纯 POCO）；如 DTO 命名空间需要 Mediator 的 ICommand/IQuery，再加 Mediator package —— 但本 plan 只建 DTOs/接口/常量，**不在 Contracts 里放 Command/Query 类型**（那些放 02-04/02-05 的 Contracts/v1/ 子目录，本 plan 跳过）。AssemblyInfo.cs 留空（不挂 FshModule —— Contracts 不是 module；参 `Modules.Auditing.Contracts/AssemblyInfo.cs`）。
    **核心类型定义（D-03 / D-09 / D-11 / D-12 全部体现）：**
    - `ICurrentWorkspaceContext`（贴 `ICurrentUser.cs` shape，per PATTERNS §ICurrentWorkspaceContext）：`Guid? CurrentWorkspaceId { get; }`、`string? Slug { get; }`、`WorkspaceRole? CurrentUserRole { get; }`、`void Set(Guid workspaceId, string slug, WorkspaceRole? role);`。注释说明：实现位于 `Modules.Workspace`（不在 Contracts），由 `WorkspaceMembershipMiddleware` 在请求 scope 内填充。
    - `WorkspaceRole` enum：`Admin = 20, Member = 15, Guest = 5`（注释引用 Plane `app/permissions/workspace.py` ROLE_CHOICES）。
    - DTOs 三件套（贴 `Modules.Identity.Contracts/DTOs/UserDto.cs` plain POCO 风格）：字段对齐 Plane `db/models/workspace.py` —— WorkspaceDto 含 OrganizationSize/TimeZone(默认 "UTC")/BackgroundColor(默认 "#000000")/Logo。WorkspaceMemberDto.User 类型 = `YH.Modules.Identity.Contracts.DTOs.UserSummary?`（依赖 Task 3 创建）。WorkspaceInvitationDto 字段对齐 Plane `WorkspaceMemberInvite`：email/token(响应里不放明文，只在创建响应里返回一次)/role/accepted/responded_at/message/created_at。
    - `RestrictedSlugs` 静态类：`public static readonly HashSet<string> List = new(StringComparer.OrdinalIgnoreCase) { ... };` 完整 66 项从 `apps/api/plane/utils/constants.py` 的 `RESTRICTED_WORKSPACE_SLUGS` 字面移植（"api"、"admin"、"settings"、"auth"、"web"、"billing"、"sign-in"、"sign-up"、"signin"、"signup"、"profile"、"invitations"、... 全部 66）。**注释标明数据来源**。
    - `INotificationService`：占位接口（per D-10），方法签名预留 —— `Task SendInvitationNotificationAsync(string email, Guid workspaceId, string slug, string token, CancellationToken ct);`（Phase 11 实现，Phase 2 不调用，仅在 CreateInvitation handler 里 resolve 但 short-circuit if null）。**注释明确"Phase 11 placeholder"**。
    - `IWorkspaceTenantResolver`：可选 facade（per PATTERNS §No Analog，下游模块才用）—— 定义为 `Guid? ResolveWorkspaceId();`，**不在此 plan 实现具体类**（仅接口，待 Phase 3 真正消费时再加），避免 YAGNI。
    把项目加入 `YH.Flow.slnx`。
    **编译注意**：WorkspaceMemberDto 引用 UserSummary → Workspace.Contracts.csproj 必须 `<ProjectReference Include="..\..\Identity\Modules.Identity.Contracts\Modules.Identity.Contracts.csproj" />`（Task 3 会先创建 UserSummary，所以本 task 编译时 UserSummary 已存在）。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/Modules/Workspace/Modules.Workspace.Contracts/Modules.Workspace.Contracts.csproj --nologo 2>&1 | tail -5</automated>
  </verify>
  <done>
    Workspace.Contracts 项目编译通过、加入 slnx；6 个公开类型（接口 + enum + 3 DTO + 常量）就绪；RestrictedSlugs 含 66 项；UserSummary 跨项目引用打通。
  </done>
</task>

<task type="auto">
  <name>Task 3: Identity.Contracts 扩展（UserSummary DTO + IUserIdentityService 批量解析契约）</name>
  <files>
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/DTOs/UserSummary.cs
    yh-flow/src/Modules/Identity/Modules.Identity.Contracts/Services/IUserIdentityService.cs
  </files>
  <behavior>
    - UserSummary record 含 Id/DisplayName/Email/AvatarUrl（4 字段，per CONTEXT §Claude's Discretion）
    - IUserIdentityService.GetUsersByIdsAsync(IEnumerable&lt;Guid&gt;) 返回 IReadOnlyDictionary&lt;Guid, UserSummary&gt;，单次 SQL 批量解析（D-05 N+1 避免）
    - 不修改现有 IUserService 接口表面（仅新增并行接口）
  </behavior>
  <action>
    在 `Modules.Identity.Contracts` 项目内新增两个文件（不修改任何现有文件 —— 这俩项目已加入 slnx，编译自动包含新文件）：
    - `DTOs/UserSummary.cs`：`public record UserSummary(Guid Id, string? DisplayName, string? Email, string? AvatarUrl);` —— 贴 UserDto 的 4 个核心字段瘦身版（CONTEXT §Claude's Discretion 指定）。命名空间 `YH.Modules.Identity.Contracts.DTOs`。
    - `Services/IUserIdentityService.cs`：接口 `public interface IUserIdentityService { Task<IReadOnlyDictionary<Guid, UserSummary>> GetUsersByIdsAsync(IEnumerable<Guid> userIds, CancellationToken ct); }`。注释明确：(a) Phase 2 的实现位于 `Modules.Identity/Services/UserIdentityService.cs`（02-05 plan 实现，**本 task 仅定义契约**）；(b) 实现用 `Where(u => userIds.Contains(u.Id)).AsNoTracking()` 单次 SQL（贴 `GetUsersQueryHandler.cs:8-21` batch shape）。
    编译时不应破坏 Phase 1（不修改任何已存在的文件，纯新增）。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/Modules/Identity/Modules.Identity.Contracts/Modules.Identity.Contracts.csproj --nologo 2>&1 | tail -5 && dotnet test src/Tests/Identity.Tests --nologo 2>&1 | tail -5</automated>
  </verify>
  <done>
    UserSummary + IUserIdentityService 编译通过；Phase 1 Identity.Tests 412/412 仍全绿（不回归）。
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                |
| -------------------- | ------------------------------------------ |
| (本 plan 无网络入口) | Wave 0 是基础设施 + 契约，无 HTTP 端点暴露 |

## STRIDE Threat Register

| Threat ID | Category               | Component                                            | Disposition | Mitigation Plan                                                                                                                                           |
| --------- | ---------------------- | ---------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Information Disclosure | `RestrictedSlugs.cs` 黑名单不完整 → 受限 slug 被占用 | mitigate    | 字面移植 Plane `RESTRICTED_WORKSPACE_SLUGS` 全部 66 项；在 02-04 SlugGenerator 测试里 assert 黑名单覆盖（`api`/`admin`/`settings` 等关键项）              |
| T-02-02   | Tampering              | `UserSummary` 暴露敏感字段（如 password hash）       | mitigate    | DTO 仅含 4 字段（Id/DisplayName/Email/AvatarUrl），显式排除 PasswordHash/SecurityStamp；UserIdentityService 实现时用 Select projection（02-05 plan 守护） |
| T-02-03   | Information Disclosure | Q1 spike 在 SUMMARY 文档泄露 Finbuckle 内部实现细节  | accept      | spike 结论属于架构设计文档，需明文记录；不含敏感数据                                                                                                      |
| T-02-SC   | Tampering              | NuGet restore 风险                                   | accept      | 0 新增包（RESEARCH §Package Legitimacy Audit 确认）；现有包已在 Phase 1 审计                                                                              |

## Threat Disposition Summary

- **mitigate**: T-02-01（黑名单完整性 + 测试）、T-02-02（DTO 字段裁剪）
- **accept**: T-02-03（架构文档）、T-02-SC（无新包）
- **transfer**: 无（Phase 2 无第三方服务）
- **HIGH severity**: 无 —— Wave 0 是契约层，无运行时入口
  </threat_model>

<verification>
- [ ] `dotnet build src/YH.Flow.slnx` 0 warning / 0 error（含 Workspace.Tests + Workspace.Contracts + Identity.Contracts 扩展）
- [ ] `dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~Spike"` 运行并产出 Q1 结论
- [ ] `dotnet test src/Tests/Identity.Tests` 仍 412/412 全绿（无回归）
- [ ] Q1 spike 结论在 02-01-SUMMARY.md 明确记录（决定 02-02 wiring 路径）
</verification>

<success_criteria>

- Workspace.Tests 项目可独立运行（哪怕无测试 case）
- Workspace.Contracts 暴露 6+ 个公开类型供后续 plan 引用
- Identity.Contracts 新增 UserSummary + IUserIdentityService 不破坏 Phase 1
- Q1 spike 解锁 D-01 的 Finbuckle wiring 方案选择
  </success_criteria>

<output>
Create `.planning/phases/02-workspace/02-01-SUMMARY.md` when done. **关键**：SUMMARY 必须明确写出 Q1 spike 的结论（"DI 外部 TryAddEnumerable 可行" 或 "需用 AddWorkspaceTenantResolution 扩展方法回退方案"），作为 02-02 wiring 的权威指导。
</output>
