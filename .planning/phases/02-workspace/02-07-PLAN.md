---
phase: 02-workspace
plan: 07
plan_id: 02-07
type: execute
wave: 6
gap_closure: true
depends_on: [02-05, 02-06]
files_modified:
  - yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj
  - yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/FinbuckleTestTenantScope.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/CrossTenantListUserWorkspacesTests.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs
autonomous: false # 关系测试需要 Docker（Testcontainers.PostgreSql 启 postgres:17-alpine 容器）
requirements: [REQ-2.1, NFR-2]
requirements_addressed: [REQ-2.1, NFR-2]
user_setup:
  - service: docker
    why: "Testcontainers.PostgreSql 启动 postgres:17-alpine 容器以驱动真实 Finbuckle AdjustUniqueIndexes 管线；无 Docker 关系测试无法运行"
    dashboard_config:
      - task: "启动 Docker Desktop（或 Linux dockerd）"
        location: "Docker Desktop（Windows/macOS）/ systemctl start docker（Linux）"
      - task: "确认 docker 守护进程可拉取 postgres:17-alpine 镜像（首次启动会自动 pull）"
        location: "docker info && docker pull postgres:17-alpine"

must_haves:
  truths:
    - "在真实 PostgreSQL（postgres:17-alpine via Testcontainers）上，用户在两个不同 workspace 租户各有一条 active WorkspaceMember 行时，调用 ListUserWorkspacesQueryHandler 一次返回两个 WorkspaceId（CR-02 修复后行为）"
    - "WorkspacePostgresFixture 启动 postgres:17-alpine 容器 + 应用 Workspace 模块迁移 + 配置 Finbuckle 多租户管线（AdjustUniqueIndexes 在真实 PG schema 上生效），可作为 02-08 关系测试的复用基建"
    - "Workspace.Tests + Identity.Tests 门禁保持绿（新增关系测试仅通过 Testcontainers 过滤器选择执行，不破坏 InMemory 现有 96/96 + 412/412）"
  artifacts:
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs"
      provides: "Testcontainers Postgres + Workspace schema 迁移 + Finbuckle 多租户 DbContext 装配（IAsyncLifetime + IDisposable）"
      min_lines: 80
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/FinbuckleTestTenantScope.cs"
      provides: "IDisposable 助手：临时切换 IMultiTenantContextSetter.MultiTenantContext 到指定 workspace 租户，模拟 AcceptInvitation/ListUserWorkspaces 顶层端点的多租户作用域"
      min_lines: 35
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/CrossTenantListUserWorkspacesTests.cs"
      provides: "两个不同 workspace 租户各 seed 一条 active member 行 → ListUserWorkspaces 返回两个 workspaceId 的断言测试（xUnit [Fact]、IClassFixture<WorkspacePostgresFixture>）"
      min_lines: 60
  key_links:
    - from: "yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs"
      via: "DbContextOptionsBuilder.UseNpgsql(connectionString) + ApplyConfigurationsFromAssembly + BaseDbContext OnModelCreating 调用链"
      pattern: "UseNpgsql|MigrateAsync|WorkspaceDbContext"
    - from: "yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs"
      to: "Finbuckle.MultiTenant.Abstractions.IMultiTenantContextSetter"
      via: "构造 WorkspaceDbContext 时注入固定 AppTenantInfo（root 或可被测试临时切换），让 AdjustUniqueIndexes 在真实 PG 上运行"
      pattern: "IMultiTenantContextSetter|MultiTenantContext<AppTenantInfo>"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs"
      to: "Microsoft.EntityFrameworkCore.IgnoreQueryFilters()"
      via: "_db.Members.IgnoreQueryFilters().Where(...).Select(WorkspaceId).Distinct() 跨租户聚合查询"
      pattern: "IgnoreQueryFilters"
---

<objective>
本计划（gap-closure Wave 6）交付两项耦合工作：

1. **关系型测试基建** —— 在 Workspace.Tests 中新增基于 `Testcontainers.PostgreSql`
   （`postgres:17-alpine`）的 `WorkspacePostgresFixture`，配置真实 Finbuckle 多租户管线
   （`AdjustUniqueIndexes` 在真实 PG schema 上生效，而不是 InMemory provider 无法执行的影子属性行为）。
   该 fixture 是 CR-01/02/03 三个 CRITICAL 缺口修复的**结构性前置条件**：
   `02-REVIEW.md §Notes on the 02-06 closeout tests` 明确指出 InMemory 套件
   "structurally uncatchable"——`MembershipMiddlewareTests.cs:43-49` 与
   `TenantIsolationTests.cs:34-52` 自身注释承认 InMemory 不应用租户过滤器。

2. **CR-02 修复** —— `ListUserWorkspacesQueryHandler` 当前被 Finbuckle 租户过滤器限制，
   只返回用户当前租户的成员行（REQ-2.1 未满足）。修复 = 查询加 `.IgnoreQueryFilters()`
   （跨工作区聚合是端点目的），配一个关系测试断言两个不同租户的成员行都被返回。

本计划是 02-08（CR-01 + CR-03 修复）的依赖：02-08 复用 `WorkspacePostgresFixture` 驱动
accept-invitation 跨租户路径。

Purpose: 修复 `02-REVIEW.md` CR-02（CRITICAL 租户作用域缺陷），同时建立 02-08 必需的
关系型测试基建，使三个 CRITICAL 缺口首次成为可被自动化捕获的缺陷。
Output: WorkspacePostgresFixture + FinbuckleTestTenantScope + CrossTenantListUserWorkspacesTests + ListUserWorkspacesQueryHandler 的 IgnoreQueryFilters 修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-REVIEW.md
@.planning/phases/02-workspace/02-HUMAN-UAT.md
@.planning/phases/02-workspace/02-05-SUMMARY.md
@.planning/phases/02-workspace/02-06-SUMMARY.md

# Source-of-truth 文件（动代码前必读）

@yh-flow/AGENTS.md
@yh-flow/CLAUDE.md
@yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs

# 关系测试 fixture 的可复用模拟（非依赖项，只取模式参考）

@yh-flow/src/Tests/Integration.Tests/Infrastructure/FshWebApplicationFactory.cs
@yh-flow/src/Tests/Integration.Tests/Infrastructure/TestConstants.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 WorkspacePostgresFixture + FinbuckleTestTenantScope（关系型测试基建）</name>
  <files>
    yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj,
    yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs,
    yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/FinbuckleTestTenantScope.cs
  </files>
  <read_first>
    - yh-flow/AGENTS.md（10 条金律，尤其 Rule 4 禁改 BuildingBlocks、Rule 7 CancellationToken、Rule 8 验证器配对——本任务只加测试 fixture 不触发 Rule 8）
    - yh-flow/src/Tests/Integration.Tests/Infrastructure/FshWebApplicationFactory.cs:29-77（**可复用模式**：PostgreSqlBuilder 链式配置、IAsyncLifetime.InitializeAsync 启容器、DisposeAsync 清理、WithAutoRemove(true).WithCleanUp(true)）
    - yh-flow/src/Tests/Integration.Tests/Infrastructure/TestConstants.cs:9-11（Jwt 签名键、Issuer、Audience——**不直接复用**，但可作为 fixture 内部常量风格参考）
    - yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs:32-40（OnModelCreating 调用顺序：AppendGlobalQueryFilter → base.OnModelCreating → ApplyTenantIsolationByDefault；fixture 构造的 DbContext 必须复用此语义）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs:31-66（**fixture 要装配的目标类型**；构造参数：IMultiTenantContextAccessor<AppTenantInfo> + DbContextOptions<WorkspaceDbContext> + IOptions<DatabaseOptions> + IHostEnvironment）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/MultiTenancy/WorkspaceSlugStrategy.cs:41-61（理解 IMultiTenantStrategy 非泛型契约 + Priority 语义——fixture **不需要**装 strategy，只装 MultiTenantContextAccessor）
    - .planning/phases/02-workspace/02-RESEARCH.md（Pitfall 6 + Architectural Responsibility Map + Finbuckle 10.1.0 版本约束）
    - .planning/phases/02-workspace/02-REVIEW.md:93-130（CR-02 根因段落 + "relational integration test (PostgreSQL via Testcontainers — the project already has the harness per AGENTS.md) is required; the InMemory suite cannot exercise this path"）
    - yh-flow/src/Directory.Packages.props:108-110（Testcontainers.PostgreSql 4.11.0 已在中央版本管理；csproj 加 PackageReference 无 Version 即可）
    - 现有 yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj（当前只有 Microsoft.EntityFrameworkCore.InMemory，缺 Testcontainers.PostgreSql + Npgsql.EntityFrameworkCore.PostgreSQL + Finbuckle）
  </read_first>
  <behavior>
    - WorkspacePostgresFixture 实现 IAsyncLifetime：InitializeAsync 启 PostgreSqlContainer（postgres:17-alpine、随机数据库名、postgres/integration_test_pwd 凭据、WithAutoRemove+WithCleanUp），DisposeAsync 调 _postgres.DisposeAsync
    - fixture 暴露 `WorkspaceDbContext CreateContextForTenant(AppTenantInfo tenant)` 工厂方法：每次返回新 scoped DbContext，其 MultiTenantContextSetter 已设为传入 tenant，让 Finbuckle AdjustUniqueIndexes 在真实 PG schema 上生效
    - fixture.InitializeAsync 在容器启动后调 `await using (var ctx = CreateContextForTenant(rootTenant)) { await ctx.Database.MigrateAsync(); }`（ApplyConfigurationsFromAssembly + BaseDbContext.OnModelCreating 链跑在真实 PG 上）
    - FinbuckleTestTenantScope 实现 IDisposable：构造时保存旧 MultiTenantContext、调 setter 设新 tenant；Dispose 还原旧值（允许测试方法内临时切租户作用域模拟顶层端点）
    - CrossTenantListUserWorkspacesTests 在 Task 2 完成后能用此 fixture 通过（Task 1 不要求测试通过，只要求 fixture 编译并能 migrate 真实 PG）
  </behavior>
  <action>
    1. **csproj 包引用**（yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj 第一个 ItemGroup 追加）：
       - `<PackageReference Include="Testcontainers.PostgreSql" />`（中央版本 4.11.0，不带 Version 属性）
       - `<PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />`（中央版本 10.0.1，让 DbContext options builder 在测试中用 UseNpgsql）
       - `<PackageReference Include="Finbuckle.MultiTenant" />`、`<PackageReference Include="Finbuckle.MultiTenant.EntityFrameworkCore" />`（10.1.0，让测试能直接构造 MultiTenantContext<AppTenantInfo> + 调用 TenantNotSetMode）
       - `<PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />`（构造 IHostEnvironment 用，参考 FshWebApplicationFactory.cs:17 已用；如已传递可省）
       - **不要**加 Testcontainers.Minio / Redis / Aspire.Hosting——本 fixture 只跑 Workspace schema + Postgres
       - 在文件顶部 XML 注释说明：Wave 6 (02-07) adds relational PG fixture — required for CR-01/02/03 coverage that InMemory cannot provide

    2. **WorkspacePostgresFixture.cs**（路径 yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs）：
       - namespace `YH.Tests.Workspace.Integration.PostgresFixtures`
       - `public sealed class WorkspacePostgresFixture : IAsyncLifetime, IDisposable`
       - 字段：`private readonly PostgreSqlContainer _postgres = new PostgreSqlBuilder("postgres:17-alpine").WithDatabase("workspace_tests").WithUsername("postgres").WithPassword("integration_test_pwd").WithAutoRemove(true).WithCleanUp(true).Build();`
       - 字段：`private readonly ServiceProvider _rootServiceProvider;`（装 IMultiTenantContextAccessor<AppTenantInfo> + IOptions<DatabaseOptions> + IHostEnvironment 的最小 DI 容器）
       - `public string ConnectionString => _postgres.GetConnectionString();`
       - `public async Task InitializeAsync()`：先 `await _postgres.StartAsync();`，然后建 root ServiceProvider（services.AddLogging; services.Configure<DatabaseOptions>(o => { o.Provider = "POSTGRESQL"; o.ConnectionString = ConnectionString; o.MigrationsAssembly = "YH.Flow.Migrations.PostgreSQL"; }); services.AddSingleton<IHostEnvironment, TestHostEnvironment>(); services.AddFinbuckleMultiTenant<AppTenantInfo>()——**注意**：不一定要完整 Finbuckle DI；最简方案是测试自己 new 一个 IMultiTenantContextAccessor<AppTenantInfo> 实例。**planner 建议**：仿 FshWebApplicationFactory.cs:51-58 的最小手装路径，不依赖完整 fsh ModuleLoader）。
       - 迁移：`await using var ctx = CreateContextForTenant(RootTenant); await ctx.Database.MigrateAsync();`（用真实迁移程序集 YH.Flow.Migrations.PostgreSQL 的 InitialWorkspace 迁移——参考 02-RESEARCH §Pitfall 6 注释，BaseDbContext.OnModelCreating 调 ApplyTenantIsolationByDefault 会在真实 PG 上创建影子 TenantId 列 + 复合唯一索引）
       - `public WorkspaceDbContext CreateContextForTenant(AppTenantInfo tenant)`：构造 `MultiTenantContextAccessor<AppTenantInfo>`（或 mock IMultiTenantContextAccessor<AppTenantInfo>，其 MultiTenantContext.TenantInfo = tenant），DbContextOptionsBuilder<WorkspaceDbContext>().UseNpgsql(ConnectionString, sql => sql.MigrationsAssembly("YH.Flow.Migrations.PostgreSQL")).UseApplicationServiceProvider(...) 然后 `new WorkspaceDbContext(accessor, options, Options.Create(databaseOptions), hostEnv)`。**关键**：options builder 必须启用 Finbuckle EFCore 扩展（参考 FshWebApplicationFactory 在 WebHost 内已自动注册；测试直接 new DbContext 需手装，或在 ServiceProvider 上 Build 后 GetRequiredService）。如手装困难，可改用 ServiceProvider + AddDbContext<WorkspaceDbContext>(o => o.UseNpgsql(...)) 路径
       - `public static readonly AppTenantInfo RootTenant = new("root", "Root", "", "root@local", issuer: "YH.local");`（参考 FshWebApplicationFactory.cs:262-268 的 AppTenantInfo 构造签名）
       - `public new async Task DisposeAsync()`：先 `_rootServiceProvider?.Dispose();` 再 `await _postgres.DisposeAsync();`
       - `public void Dispose()`：同步入口转发到 DisposeAsync().GetAwaiter().GetResult()（仅在 fixture 终结时调）

    3. **FinbuckleTestTenantScope.cs**（路径同 PostgresFixtures 子目录）：
       - `internal sealed class FinbuckleTestTenantScope : IDisposable`
       - 构造参数 `(IMultiTenantContextSetter setter, AppTenantInfo newTenant)`
       - 字段：`private readonly IMultiTenantContextSetter _setter;`、`private readonly IMultiTenantContext? _previous;`
       - 构造体：`_setter = setter; _previous = setter.MultiTenantContext; setter.MultiTenantContext = new MultiTenantContext<AppTenantInfo>(newTenant);`
       - `public void Dispose()`：`_setter.MultiTenantContext = _previous;`
       - 此类让 Task 2 测试 + 02-08 accept-invitation 测试能在 SaveChanges 前切租户作用域（模拟顶层端点上 Finbuckle MultiTenantContext 的状态切换）

    4. **辅助：TestHostEnvironment**（如 IHostEnvironment 需要实体）：
       - 在 fixture 文件内私有 nested class `private sealed class TestHostEnvironment : IHostEnvironment`，EnvironmentName="Development"、ApplicationName="Workspace.Tests"、ContentRootPath=Directory.GetCurrentDirectory()——参考 fsh 模板已存在此类或最简 stub
       - 或在 csproj 引用 Microsoft.AspNetCore.Hosting 已有的引用（Npgsql EF Core 传递依赖通常含）——若 IHostEnvironment 在 BaseDbContext 只用于 IsDevelopment() 判定（看 BaseDbContext.cs:57 `environment.IsDevelopment()`），可简化为传 `null` 并接受 OnConfiguring 不进 ConfigureHeroDatabase 分支（因为 connectionString 由 options builder 提供）

    5. **不要**在本任务创建 CrossTenantListUserWorkspacesTests.cs（那是 Task 2 的工作）；本任务只交付 fixture + scope 助手 + csproj 包引用

  </action>
  <verify>
    <automated>cd D:\github\akinix-plane && dotnet build yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj --nologo</automated>
    构建必须 0 warnings / 0 errors（TreatWarningsAsErrors=true）。验证 fixture 编译通过 + Testcontainers 包引用解析到中央版本 4.11.0。
    额外门禁（人工抽样，Docker 已启动时）：
    `dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal`（应保持 96/96，新 fixture 还未消费不影响现有测试）
  </verify>
  <done>
    - yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj 含 PackageReference Testcontainers.PostgreSql + Npgsql.EntityFrameworkCore.PostgreSQL + Finbuckle.MultiTenant[.EntityFrameworkCore]
    - WorkspacePostgresFixture.cs 存在、实现 IAsyncLifetime、暴露 CreateContextForTenant(AppTenantInfo) 和 RootTenant 静态字段、最小行数 ≥ 80
    - FinbuckleTestTenantScope.cs 存在、实现 IDisposable、构造时切 MultiTenantContext / Dispose 还原
    - `dotnet build Workspace.Tests.csproj` = 0 warnings / 0 errors
    - `dotnet test Workspace.Tests`（不跑 Postgres 过滤器）保持 96/96 绿
  </done>
  <acceptance_criteria>
    - `grep -n "Testcontainers.PostgreSql" yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj` 命中（无 Version 属性，走中央版本管理）
    - `grep -n "class WorkspacePostgresFixture" yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs` 命中
    - `grep -n "IAsyncLifetime\|PostgreSqlContainer\|CreateContextForTenant\|MigrateAsync" yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/WorkspacePostgresFixture.cs` 四个标识符全部命中
    - `grep -n "class FinbuckleTestTenantScope\|IMultiTenantContextSetter\|IDisposable" yh-flow/src/Tests/Workspace.Tests/Integration/PostgresFixtures/FinbuckleTestTenantScope.cs` 全部命中
    - `dotnet build yh-flow/src/Tests/Workspace.Tests/Workspace.Tests.csproj --nologo` 退出码 0
    - 不引用 Testcontainers.Minio / Redis / Aspire.Hosting（`grep -c "Minio\|Redis\|Aspire" Workspace.Tests.csproj` = 0 或仅含原有传递）
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-02 修复（ListUserWorkspacesQueryHandler 加 IgnoreQueryFilters）+ 跨租户关系测试</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs,
    yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/CrossTenantListUserWorkspacesTests.cs
  </files>
  <read_first>
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs:54-60（**被改点**：当前 `_db.Members.AsNoTracking().Where(...).Select(m => m.WorkspaceId).Distinct()` 没有 IgnoreQueryFilters）
    - .planning/phases/02-workspace/02-REVIEW.md:96-130（**CR-02 单一真相源**：根因 + 修复代码片段 + "Add a relational test that seeds memberships in two distinct tenants and asserts both workspace ids surface"）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs:28-46（**IHasTenant 标量 + WorkspaceId**：理解 TenantId 影子属性 vs WorkspaceId 显式列的差异，REVIEW §CR-02 阐明 Finbuckle 过滤器作用在 TenantId 而非 WorkspaceId）
    - yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs:35-40（ApplyTenantIsolationByDefault 调用点 → 验证 WorkspaceMember 在真实 PG 上确有 TenantId 影子列被过滤）
    - yh-flow/AGENTS.md（Rule 7 CancellationToken、Rule 1 模块边界——本任务改的是同模块 handler，无跨模块边界风险）
    - Task 1 交付的 WorkspacePostgresFixture.cs（**fixture 用法**：测试通过 IClassFixture<WorkspacePostgresFixture> 注入、调 fixture.CreateContextForTenant(seedTenant) seed 数据、再切到第三个 tenant 调 handler 验证跨租户返回）
    - .planning/phases/02-workspace/02-HUMAN-UAT.md:25-28（🟥 步骤 1 Create workspace + 步骤 11 Cross-workspace isolation 是 CR-02 修复后的人工仲裁门禁——本任务的自动化关系测试是它们的前置）
    - 现有 yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceLifecycleSmokeTests.cs:286-303（**反模式参考**：02-06 现有测试用单租户 fixture 无法捕获 CR-02；本任务测试必须 seed 至少 2 个不同 workspace 租户）
  </read_first>
  <behavior>
    - ListUserWorkspacesQueryHandler 第 54-60 行的 `_db.Members.AsNoTracking().Where(...)` 链加入 `.IgnoreQueryFilters()`（必须在 AsNoTracking 之前或之后均可，但 EF Core 语义要求 IgnoreQueryFilters 是可链式方法）
    - 新增 CrossTenantListUserWorkspacesTests: seed 两个不同 workspace 租户（workspaceA.Id ≠ workspaceB.Id，二者均为 IGlobalEntity 不被过滤），每个 workspace 下 seed 一条 active WorkspaceMember 行（相同 userId，TenantId = 该 workspace.Id），调 ListUserWorkspacesQueryHandler → 断言返回的两个 workspaceId 集合 = {workspaceA.Id, workspaceB.Id}
    - 同一测试在 InMemory 上"假绿"地通过（因为 InMemory 不应用过滤器），所以**必须**用 Task 1 的 WorkspacePostgresFixture 驱动真实 PG 才能真正验证修复
    - 边界用例：seed 一条 IsActive=false 的成员行（同一 userId）→ 断言该 workspaceId **不**返回（验证 Where 子句 m.IsActive 过滤保留）
  </behavior>
  <action>
    1. **修复 ListUserWorkspacesQueryHandler.cs**（第 54-60 行段）：
       - 当前：
         ```
         var workspaceIds = await _db.Members
             .AsNoTracking()
             .Where(m => m.UserId == query.UserId && m.IsActive && !m.IsDeleted)
             .Select(m => m.WorkspaceId)
             .Distinct()
             .ToListAsync(cancellationToken)
             .ConfigureAwait(false);
         ```
       - 修复后：在 `_db.Members` 之后立刻链 `.IgnoreQueryFilters()`（CR-02: cross-workspace aggregation is the endpoint purpose；顶层端点 DbContext 作用域是用户当前/最后租户，会过滤掉所有其他租户的成员行，违反 REQ-2.1）
       - 在 `.IgnoreQueryFilters()` 调用上方加单行 XML 注释：`// CR-02 (02-REVIEW.md): 跨工作区聚合是端点目的；顶层端点 DbContext 作用域是当前/最后租户，会过滤掉其他工作区的成员行 → 必须 IgnoreQueryFilters 才能实现 REQ-2.1`
       - 保留 `m.IsActive && !m.IsDeleted` Where 子句（active 过滤是业务规则，与租户过滤独立）
       - 同步更新 handler 顶部 `<remarks>` 第 16-19 行的 "resolution path" 段：把"memberships live in the WorkspaceMembers table with the cross-workspace scalar WorkspaceId + UserId. We project WorkspaceId values for the user from db.Members"后面追加一句明确"db.Members 查询必须 IgnoreQueryFilters——顶层端点 DbContext 默认作用域是当前租户，但 ListUserWorkspaces 的目的就是跨租户聚合（CR-02 修复）"
       - **不要**改 handler 的 Workspaces 查询（第 68-94 行）：Workspaces 是 IGlobalEntity，本就不被过滤

    2. **新增测试 CrossTenantListUserWorkspacesTests.cs**（路径 yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/CrossTenantListUserWorkspacesTests.cs）：
       - namespace `YH.Tests.Workspace.Integration.CrossTenant`
       - `public sealed class CrossTenantListUserWorkspacesTests : IClassFixture<WorkspacePostgresFixture>`
       - 构造注入 `WorkspacePostgresFixture _fixture`，字段保存
       - 测试方法 `[Fact] public async Task Returns_Workspaces_Across_Distinct_Tenants_On_Real_Postgres()`：
         a) 在 fixture.RootTenant 作用域下 seed 两个 Workspace 行（IGlobalEntity 不过滤）：workspaceA = Workspace.Create(name: "Acme", slug: "acme", owner: ownerId)、workspaceB = Workspace.Create(name: "Globex", slug: "globex", owner: ownerId)。两个 SaveChangesAsync 在 RootTenant scope 下
         b) 切到 workspaceA.Id 租户作用域（用 FinbuckleTestTenantScope），seed `WorkspaceMember.Create(workspaceId: workspaceA.Id, userId: sameUserId, role: 20)`，调 SaveChangesAsync（此时 Finbuckle 把行的 TenantId 影子属性盖成 workspaceA.Id 字符串）
         c) 切到 workspaceB.Id 租户作用域，seed 另一条 WorkspaceMember(workspaceId: workspaceB.Id, userId: sameUserId, role: 15)
         d) **关键断言**：切回 RootTenant 作用域（模拟顶层端点的"用户当前租户=平台"场景，既不是 A 也不是 B），构造 ListUserWorkspacesQuery(UserId = sameUserId)，new ListUserWorkspacesQueryHandler(db) 调 Handle → 断言 `result.Items.Select(i => i.Id).ShouldBe(new[] { workspaceA.Id, workspaceB.Id }, ignoreOrder: true)`
         e) 边界用例（同测试或独立 [Fact]）：在 workspaceA 作用域再 seed 一条 IsActive=false 的成员行 → 重新查 → 断言 inactive 行对应的 workspaceId 不在结果中（如该 workspaceId 已在 A 中则不影响；最干净是给同一 userId 在 workspaceA 下加一条 IsActive=false 但 WorkspaceId=workspaceA.Id 的行，断言结果仍是两 workspace，且不重复）
       - **测试隔离**：每个测试方法用独立 fixture.Database.EnsureDeleted() / EnsureCreated() 或在 fixture 的 InitializeAsync 已 migrate 后用 `await _fixture.ResetWorkspaceTablesAsync()`（如 fixture 不提供则测试内手动 `await db.Database.ExecuteSqlRawAsync("TRUNCATE workspace.\"WorkspaceMembers\", workspace.\"Workspaces\" RESTART IDENTITY CASCADE;")` 在测试开始时）
       - using directives：`using YH.Tests.Workspace.Integration.PostgresFixtures; using YH.Modules.Workspace.Data; using YH.Modules.Workspace.Domain; using YH.Modules.Workspace.Features.v1.Workspaces.ListUserWorkspaces; using YH.Modules.Workspace.Contracts.v1.Workspaces.ListUserWorkspaces; using YH.Framework.Shared.Multitenancy; using Shouldly; using Xunit; using Mediator;`
       - **trait 标记**：`[Trait("Category", "Postgres")]` `[Trait("Category", "RequiresDocker")]`（让 CI/本地能过滤；与 02-06 现有 InMemory 测试隔离）

    3. **不要**改 Workspace.Tests 现有 96 个测试（InMemory 套件继续覆盖非租户作用域路径）
    4. **不要**改 WorkspaceMember 实体、Configuration、迁移（CR-02 是查询层修复，不涉及 schema）

  </action>
  <verify>
    <automated>cd D:\github\akinix-plane && dotnet build yh-flow/src/YH.Flow.slnx --nologo && dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal --filter "FullyQualifiedName~Postgres|FullyQualifiedName~CrossTenant|Category=Postgres" && dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal && dotnet test yh-flow/src/Tests/Identity.Tests --nologo --verbosity minimal</automated>
    构建必须 0/0。新关系测试在 Docker 启动时通过。现有 Workspace.Tests 96/96 + Identity.Tests 412/412 保持绿（InMemory 测试不受影响）。
    若 CI 无 Docker，关系测试会被 filter 跳过或报 "Docker not reachable"——本任务接受此状态但 autonomous=false 必须在执行前由用户确认 Docker 可用。
  </verify>
  <done>
    - ListUserWorkspacesQueryHandler.cs 第 54 行附近 `grep -n IgnoreQueryFilters` 命中
    - CrossTenantListUserWorkspacesTests.cs 存在、含 `[Fact]` + `[Trait("Category", "Postgres")]` 标记、IClassFixture<WorkspacePostgresFixture>
    - 测试方法体含 "seed 两个不同租户的成员行 → 切回 RootTenant 作用域 → 断言两个 workspaceId 都返回" 的逻辑（最小行数 ≥ 60）
    - `dotnet build YH.Flow.slnx` = 0 warnings / 0 errors
    - `dotnet test Workspace.Tests --filter Postgres` 在 Docker 启动时 = 0 failed（含新 CrossTenant 测试）
    - `dotnet test Workspace.Tests`（全量）= 96/96 + 新增关系测试 全绿
    - `dotnet test Identity.Tests` = 412/412（零回归）
    - handler `<remarks>` 段落已更新说明 CR-02 修复
  </done>
  <acceptance_criteria>
    - `grep -n "IgnoreQueryFilters" yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs` 命中
    - `grep -n "CR-02\|02-REVIEW" yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Workspaces/ListUserWorkspaces/ListUserWorkspacesQueryHandler.cs` 命中（注释引用根因文档）
    - `grep -n "IClassFixture<WorkspacePostgresFixture>\|Returns_Workspaces_Across_Distinct_Tenants_On_Real_Postgres\|Category=.Postgres." yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/CrossTenantListUserWorkspacesTests.cs` 全部命中
    - 测试方法体含至少两个不同 workspace 租户的 seed + 切回 RootTenant 作用域 + 双 workspaceId 断言（不是单租户 fixture）
    - `dotnet build yh-flow/src/YH.Flow.slnx --nologo` 退出码 0
    - `dotnet test yh-flow/src/Tests/Workspace.Tests --filter "Category=Postgres"` 在 Docker 启动时退出码 0（新测试通过）
    - `dotnet test yh-flow/src/Tests/Workspace.Tests` 退出码 0（96/96 InMemory 保持 + 新关系测试）
    - `dotnet test yh-flow/src/Tests/Identity.Tests` 退出码 0（412/412 零回归）
    - 未触碰 WorkspaceMember.cs / WorkspaceMemberConfiguration.cs / 任何 .Migration 文件（`git diff --name-only yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/ yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/ yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/` 应为空或仅注释更新）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                     |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 顶层端点 → DbContext          | `GET /api/v1/users/me/workspaces/` 无 `{slug}`，Finbuckle slug strategy 返回 null，DbContext 作用域 = 用户当前/最后租户（CR-02 根因）           |
| 真实 PG schema → 测试 fixture | Testcontainers Postgres 启独立容器，迁移 YH.Flow.Migrations.PostgreSQL 在真实 schema 上跑 AdjustUniqueIndexes（InMemory provider 不执行此管线） |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                      | Disposition | Mitigation Plan                                                                                                                                                                                         |
| ---------- | ---------------------- | -------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-07-01 | Information Disclosure | ListUserWorkspaces 跨租户聚合                                  | mitigate    | CR-02 修复（Task 2）让跨租户聚合查询通过 IgnoreQueryFilters 正确返回用户所有 workspace，避免信息丢失（REQ-2.1 未满足）而非信息泄露；Where(m => m.UserId == query.UserId) 是用户自身数据，不涉跨用户读取 |
| T-02-07-02 | Tampering              | Testcontainers Postgres 容器配置                               | accept      | 测试 fixture 用固定凭据 postgres/integration_test_pwd、WithAutoRemove+WithCleanUp；容器隔离，不接入生产网络；测试结束自动清理                                                                           |
| T-02-07-03 | Denial of Service      | Docker 不可用时关系测试阻塞 CI                                 | mitigate    | 测试加 `[Trait("Category", "Postgres")]` + `[Trait("Category", "RequiresDocker")]`，可被 filter 跳过；autonomous=false 在执行前确认 Docker 可用                                                         |
| T-02-07-SC | Tampering              | NuGet 包安装（Testcontainers.PostgreSql / Npgsql / Finbuckle） | mitigate    | 全部为已审计包：Testcontainers.PostgreSql 4.11.0、Npgsql.EntityFrameworkCore.PostgreSQL 10.0.1、Finbuckle 10.1.0 均在 Directory.Packages.props 中央版本管理已验证；不引入新包源                         |

</threat_model>

<verification>
## 计划级验证

### 自动化（必须通过）

- `dotnet build yh-flow/src/YH.Flow.slnx --nologo` → 0 warnings / 0 errors
- `dotnet test yh-flow/src/Tests/Workspace.Tests --filter "Category=Postgres"` → 新 CrossTenant 测试通过（Docker 启动时）
- `dotnet test yh-flow/src/Tests/Workspace.Tests` → 96/96 + 新关系测试 全绿
- `dotnet test yh-flow/src/Tests/Identity.Tests` → 412/412（零回归）

### 人工门禁（修复后必跑，由 /gsd-execute-phase 02 完成后用户执行）

- **02-HUMAN-UAT.md 🟥 步骤 1（Create workspace → 自动 Admin member）** —— CR-02 修复后 ListUserWorkspaces 应返回该 workspace
- **02-HUMAN-UAT.md 🟥 步骤 11（Cross-workspace isolation）** —— 跨租户隔离正确性

### Goal-Backward Truths 校验

1. **Truth "用户在两个不同 workspace 租户各有一条 active 成员行时，ListUserWorkspaces 返回两个 workspaceId"** → 由 CrossTenantListUserWorkspacesTests.Returns_Workspaces_Across_Distinct_Tenants_On_Real_Postgres 断言
2. **Truth "WorkspacePostgresFixture 可作为 02-08 关系测试的复用基建"** → 由 fixture 暴露的 CreateContextForTenant + FinbuckleTestTenantScope 在 02-08 计划中被复用验证
3. **Truth "Workspace.Tests + Identity.Tests 门禁保持绿"** → 由 dotnet test 双门禁通过验证
   </verification>

<success_criteria>

- [x] Workspace.Tests.csproj 引用 Testcontainers.PostgreSql + Npgsql.EntityFrameworkCore.PostgreSQL + Finbuckle.MultiTenant[.EntityFrameworkCore]
- [x] WorkspacePostgresFixture 启动 postgres:17-alpine 容器、应用 Workspace 迁移、暴露 CreateContextForTenant + RootTenant
- [x] FinbuckleTestTenantScope 可临时切换 IMultiTenantContextSetter.MultiTenantContext
- [x] ListUserWorkspacesQueryHandler 第 54 行附近含 `.IgnoreQueryFilters()`
- [x] CrossTenantListUserWorkspacesTests 在真实 PG 上验证两个不同租户的成员行都被返回
- [x] 构建门禁 0 warnings / 0 errors
- [x] Workspace.Tests + Identity.Tests 全量测试保持绿
- [x] autonomous=false（Docker 依赖），CI 在无 Docker 环境可通过 Trait filter 跳过
      </success_criteria>

<output>
完成后创建 `.planning/phases/02-workspace/02-07-SUMMARY.md`（参考 02-05/02-06 SUMMARY 结构：
tech-stack.added、key-files.created、modified、decisions、requirements-completed: [REQ-2.1, NFR-2]）。
SUMMARY 必须记录：
1. WorkspacePostgresFixture 的精确装配路径（让 02-08 能直接复用）
2. CreateContextForTenant 签名（02-08 测试调此方法 seed invitation 数据）
3. FinbuckleTestTenantScope 用法（02-08 accept-invitation 测试需要切租户作用域）
4. CR-02 修复的精确行号 + IgnoreQueryFilters 调用
5. 测试隔离策略（TRUNCATE / 独立数据库 / 顺序执行）
</output>
