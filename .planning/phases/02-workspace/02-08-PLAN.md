---
phase: 02-workspace
plan: 08
plan_id: 02-08
type: execute
wave: 7
gap_closure: true
depends_on: [02-05, 02-06, 02-07]
files_modified:
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/AcceptInvitationAcrossTenantsTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/ReAcceptAfterRemovalTests.cs
autonomous: false # 关系测试需要 Docker（复用 02-07 WorkspacePostgresFixture）
requirements: [REQ-2.2, REQ-2.4, NFR-2]
requirements_addressed: [REQ-2.2, REQ-2.4, NFR-2]
user_setup:
  - service: docker
    why: "复用 02-07 的 WorkspacePostgresFixture（Testcontainers postgres:17-alpine）驱动真实 Finbuckle AdjustUniqueIndexes 管线"
    dashboard_config:
      - task: "启动 Docker Desktop（同 02-07）"
        location: "Docker Desktop / systemctl start docker"

must_haves:
  truths:
    - "在真实 PostgreSQL 上，邀请所属 workspace 租户 ≠ 受邀用户当前租户时（顶层 accept 端点的典型生产场景），调用 AcceptInvitationCommandHandler 返回 200 + {memberId, workspaceId}（CR-01 修复后行为；修复前必然 404）"
    - "新建的 WorkspaceMember 行其 TenantId 影子属性 = 邀请所属 workspace 的 Id（不是受邀用户当前租户），保持 (TenantId, UserId) 复合唯一索引语义"
    - "同一 (workspaceId, userId) 曾被移除（Deactivate，IsActive=false 但行未删）后，再次邀请并接受 → 调用 Activate() 复用既有行，不再抛 UniqueConstraintException → 500（CR-03 修复后行为）"
    - "Workspace.Tests + Identity.Tests 门禁保持绿；新关系测试在 Docker 启动时通过"
  artifacts:
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs"
      provides: "ValidateAsync 在 Invitation 查询链 IgnoreQueryFilters（hash 全局唯一、租户无关查找）；同步修复 RejectInvitation 间接路径（RejectInvitation 复用 ValidateAsync 自动受益）"
      min_lines: 145
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs"
      provides: "(a) 重新 attach invitation 用 IgnoreQueryFilters（CR-01）；(b) SaveChanges 期间用 FinbuckleTestTenantScope 逻辑把 MultiTenantContext.TenantInfo 切到邀请所属 workspace 租户，让新 WorkspaceMember 行 TenantId 被正确盖印；(c) 插入前查既有 (workspaceId, userId) 跨租户行，存在则 Activate + UpdateRole 否则新建（CR-03）"
      min_lines: 140
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/AcceptInvitationAcrossTenantsTests.cs"
      provides: "seed workspaceA 邀请 + 受邀用户当前在 RootTenant 作用域 → AcceptInvitation 调用 → 200 + 行 TenantId = workspaceA.Id 断言（CR-01 关系测试）"
      min_lines: 80
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/ReAcceptAfterRemovalTests.cs"
      provides: "invite → accept → admin remove → re-invite → re-accept 流程 → 第二次 accept 不抛 UniqueConstraintException、复用既有行（CR-03 关系测试）"
      min_lines: 70
  key_links:
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs"
      to: "Microsoft.EntityFrameworkCore.IgnoreQueryFilters()"
      via: "_db.Invitations.IgnoreQueryFilters().AsNoTracking().FirstOrDefaultAsync(i => i.TokenHash == hash) 租户无关查找"
      pattern: "IgnoreQueryFilters"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs"
      to: "Finbuckle.MultiTenant.Abstractions.IMultiTenantContextSetter"
      via: "SaveChangesAsync 期间用 using scope 把 MultiTenantContext.TenantInfo 切到 invitation.WorkspaceId 对应的 AppTenantInfo（让 TenantNotSetMode=Overwrite 盖印正确 TenantId）"
      pattern: "IMultiTenantContextSetter|MultiTenantContext|TenantInfo"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs"
      to: "YH.Modules.Workspace.Domain.WorkspaceMember.Activate()"
      via: "插入前用 IgnoreQueryFilters 查既有 (WorkspaceId, UserId) 行，存在则 Activate + UpdateRole(tracked.Role)，否则新建"
      pattern: "Activate\\(\\)|IgnoreQueryFilters"
---

<objective>
本计划（gap-closure Wave 7）修复 `02-REVIEW.md` 标记的两个**生产环境阻断**级 CRITICAL 缺口：

- **CR-01** —— accept-invitation 顶层路由被 Finbuckle 租户过滤器静默丢弃：
  `WorkspaceInvitation` 是 `IHasTenant`（非 `IGlobalEntity`），顶层端点
  `POST /api/v1/workspaces/invitations/{token}/accept/` 上 DbContext 作用域 = 邀请人当前租户
  （platform/root），**永远不是邀请所属 workspace 租户** → ValidateAsync 找不到任何邀请 → 404。
  handler 注释声称"已禁用租户过滤"但 `grep IgnoreQueryFilters|TenantNotSetMode` 全模块 0 命中。

- **CR-03** —— 接受曾被移除成员的邀请抛唯一约束异常：移除成员 = Deactivate（IsActive=false），
  复合唯一索引 `IX_WorkspaceMembers_Tenant_User` 建在 `(TenantId, UserId)` 上（IsActive 不在键里），
  停用行仍占唯一槽。再次接受时 handler 无条件 Add 新行 → SaveChangesAsync 抛
  UniqueConstraintException → 500，用户被永久锁在外面。

两个 CR 共享同一修复路径（AcceptInvitation 跨租户作用域）+ 同一关系测试基建
（02-07 交付的 WorkspacePostgresFixture + FinbuckleTestTenantScope），故合并在 02-08。

本计划是 Phase 2 gap-closure 的**最终修复**：CR-01/02/03 三个 CRITICAL 全部修复后，
Phase 2 才达到生产可用状态，`02-HUMAN-UAT.md` 的 🟥 步骤 5（CR-01 THE DECIDER）

- 🟥 步骤 7/10/11（CR-cluster）才能通过。

Purpose: 修复 02-REVIEW.md CR-01 + CR-03（两个 CRITICAL 生产阻断级缺陷），让 Phase 2
accept-invitation 流程在生产真实 Finbuckle 管线下可用。
Output: InvitationTokenService.ValidateAsync 加 IgnoreQueryFilters；
AcceptInvitationCommandHandler 跨租户 SaveChanges + 既有成员复用（Activate）；
2 个新关系测试覆盖 CR-01 + CR-03。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-REVIEW.md
@.planning/phases/02-workspace/02-HUMAN-UAT.md
@.planning/phases/02-workspace/02-05-SUMMARY.md
@.planning/phases/02-workspace/02-07-SUMMARY.md

# Source-of-truth 文件（动代码前必读）

@yh-flow/AGENTS.md
@yh-flow/CLAUDE.md
@yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Data/WorkspaceDbContext.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceInvitation.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Services/IInvitationTokenService.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommand.cs
@yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationResponse.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 修复 — InvitationTokenService.ValidateAsync 加 IgnoreQueryFilters + AcceptInvitationCommandHandler 跨租户 SaveChanges</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs,
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs,
    yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/AcceptInvitationAcrossTenantsTests.cs
  </files>
  <read_first>
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs:125-141（**被改点 1**：ValidateAsync 当前的 `_db.Invitations.AsNoTracking().FirstOrDefaultAsync(i => i.TokenHash == hash, ...)` 没有 IgnoreQueryFilters）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs:78-112（**被改点 2**：Step 2 re-attach `_db.Invitations.FirstOrDefaultAsync(i => i.Id == invitation.Id, ...)` 无过滤器；Step 4 `_db.Members.Add(member)` + SaveChangesAsync 在当前 DbContext 作用域下，TenantId 会被盖成邀请人当前租户而非邀请所属 workspace）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs:40-48（handler remarks **当前错误声明**："we therefore disable the tenant filter when looking up the invitation by hash via IInvitationTokenService.ValidateAsync" —— 这段注释是 false claim，修复时同步改正）
    - .planning/phases/02-workspace/02-REVIEW.md:49-92（**CR-01 单一真相源**：根因 + 修复代码片段 + "Look up the invitation by hash with the tenant filter disabled, and write the new WorkspaceMember row using a context explicitly scoped to the invitation's workspace tenant"）
    - .planning/phases/02-workspace/02-RESEARCH.md §Architectural Responsibility Map + Pitfall 6（理解 Finbuckle AdjustUniqueIndexes 在 IHasTenant 实体上的影子属性行为 + TenantNotSetMode.Overwrite 语义）
    - yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs:66-71（**SaveChangesAsync 默认行为**：`TenantNotSetMode = TenantNotSetMode.Overwrite;` —— Finbuckle 在 Overwrite 模式下会用当前 MultiTenantContext.TenantInfo 盖印 TenantId，所以测试需要在 SaveChanges 期间把 TenantInfo 切到邀请所属 workspace）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceInvitation.cs（理解 WorkspaceId 字段 + IsValid 状态机 + Accept/Reject/Revoke 转换）
    - 02-07-SUMMARY.md（**fixture 装配路径**：WorkspacePostgresFixture.CreateContextForTenant + FinbuckleTestTenantScope 的精确签名和用法——本任务测试调它们 seed invitation 数据 + 模拟顶层端点切换租户作用域）
    - yh-flow/AGENTS.md（Rule 4 禁改 BuildingBlocks——本任务改 BaseDbContext 否？**不改**，只改 Workspace 模块；Rule 7 CancellationToken；Rule 5 sealed handler + ValueTask + ConfigureAwait(false)）
    - .planning/phases/02-workspace/02-HUMAN-UAT.md:45-49（🟥 步骤 5 CR-01 THE DECIDER：invitee JWT 调 accept → 200 {memberId,workspaceId}——本任务的关系测试是它的前置）
  </read_first>
  <behavior>
    - InvitationTokenService.ValidateAsync: 在 `_db.Invitations` 之后链 `.IgnoreQueryFilters()`，让 TokenHash 查找跨租户全局唯一匹配
    - AcceptInvitationCommandHandler Step 2 re-attach: 同样加 `.IgnoreQueryFilters()`（invitation.Id 查找也是租户无关的）
    - AcceptInvitationCommandHandler Step 4 SaveChanges: 在 SaveChangesAsync 调用前，构造一个临时 scope 把 IMultiTenantContextSetter.MultiTenantContext.TenantInfo 切到 invitation.WorkspaceId 对应的 AppTenantInfo（让 BaseDbContext.SaveChangesAsync 的 TenantNotSetMode=Overwrite 盖印正确 TenantId 到新 WorkspaceMember 行）；SaveChanges 完成后还原（参考 02-07 的 FinbuckleTestTenantScope，但 production 代码不能依赖测试 fixture，需要直接调 IMultiTenantContextSetter）
    - AcceptInvitationCommandHandler 构造函数新增依赖 IMultiTenantContextAccessor<AppTenantInfo>（从 DI 注入，用于解析/构造 AppTenantInfo）+ IMultiTenantContextSetter（如果 BaseDbContext 已注入则共享 setter 实例；若 setter 在 WorkspaceDbContext 内部，则需要通过 accessor 获取）。**planner 注意**：测试 fixture 路径用 IMultiTenantContextSetter 直接切；production 路径在 handler 内部 try/finally 模式临时切——两种模式必须保持相同语义
    - 新增 AcceptInvitationAcrossTenantsTests: seed workspaceA 邀请（invitation.WorkspaceId = workspaceA.Id）+ 受邀用户当前租户 = RootTenant（≠ workspaceA.Id）→ AcceptInvitation → 200 + 新 WorkspaceMember 行的 TenantId = workspaceA.Id（用 IgnoreQueryFilters 读回验证）
    - 修复 handler remarks 段落（第 40-48 行）的 false claim："disable the tenant filter" → 改为反映实际代码的描述："look up by hash via IgnoreQueryFilters（hash 全局唯一）+ SaveChanges 期间切换 MultiTenantContext.TenantInfo 到 invitation.WorkspaceId 对应租户，让 BaseDbContext.TenantNotSetMode=Overwrite 盖印正确 TenantId"
  </behavior>
  <action>
    1. **修复 InvitationTokenService.cs**（第 134-137 行段）：
       - 当前：
         ```
         var invitation = await _db.Invitations
             .AsNoTracking()
             .FirstOrDefaultAsync(i => i.TokenHash == hash, cancellationToken)
             .ConfigureAwait(false);
         ```
       - 修复后：在 `_db.Invitations` 之后立刻 `.IgnoreQueryFilters()`，并在上方加注释 `// CR-01 (02-REVIEW.md): hash 全局唯一、租户无关查找；顶层 accept 端点 DbContext 作用域 ≠ 邀请所属 workspace，必须 IgnoreQueryFilters 才能找到邀请`
       - **不要**改 CreateAsync（create 是 workspace-scoped 端点，租户过滤正常工作）；**不要**改 HashToken / GenerateToken
       - 同步 RejectInvitationCommandHandler.cs:58-60 的 `_db.Invitations.FirstOrDefaultAsync(i => i.Id == invitation.Id, ...)` 加 `.IgnoreQueryFilters()`（CR-01 影响 reject 路径同样——ValidateAsync 已修，但 reject handler 还独立做了 re-attach 查询，必须同步修。**注意**：本任务文件清单未列 RejectInvitationCommandHandler.cs，由执行者按 CR-01 §"The same defect affects the re-attach query at AcceptInvitationCommandHandler.cs:87-89 and RejectInvitationCommandHandler.cs:58-60" 同步修——若执行者判断需加文件路径请直接加，本计划授权此偏离并在 SUMMARY 记录）

    2. **修复 AcceptInvitationCommandHandler.cs**：
       - 构造函数追加依赖：`IMultiTenantContextAccessor<AppTenantInfo> multiTenantContextAccessor`（参考 WorkspaceDbContext 构造同样的 accessor 类型）。保存为 `_tenantContextAccessor` 字段
       - Step 2（第 87-90 行 re-attach）：`_db.Invitations` 后加 `.IgnoreQueryFilters()`（CR-01: invitation.Id 查找同样租户无关）
       - Step 4 SaveChanges 作用域切换：在 `_db.Members.Add(member);` 之后、`await _db.SaveChangesAsync(...)` 之前，构造 workspace 租户的 AppTenantInfo 并临时切换 MultiTenantContext：
         ```
         // CR-01 (02-REVIEW.md): 新 WorkspaceMember 行必须保存在邀请所属 workspace 租户下，
         // 否则 BaseDbContext.SaveChangesAsync 的 TenantNotSetMode=Overwrite 会用当前（邀请人/平台）
         // 租户盖印 TenantId，破坏 (TenantId, UserId) 唯一性 + ListMembers/ListUserWorkspaces 结果。
         var workspaceTenant = new AppTenantInfo(
             id: tracked.WorkspaceId.ToString(),
             identifier: tracked.WorkspaceId.ToString(),
             name: tracked.WorkspaceId.ToString(),
             "",
             issuer: null);
         var setter = _tenantContextAccessor as IMultiTenantContextSetter
             ?? throw new InvalidOperationException("Tenant context accessor does not support set; cannot scope SaveChanges to invitation's workspace tenant (CR-01).");
         var previousContext = setter.MultiTenantContext;
         setter.MultiTenantContext = new MultiTenantContext<AppTenantInfo>(workspaceTenant);
         try
         {
             await _db.SaveChangesAsync(cancellationToken).ConfigureAwait(false);
         }
         finally
         {
             setter.MultiTenantContext = previousContext;
         }
         ```
       - **关键**：`AppTenantInfo` 构造签名参考 02-07 fixture 的 RootTenant（`new AppTenantInfo(id, identifier, name, connectionString, issuer)`——planner 注：具体签名以 02-07 实际落地为准，本计划描述的参数顺序为 FshWebApplicationFactory.cs:263-268 模板——执行者读 02-07-SUMMARY.md 确认精确签名后调用；若签名不同，以 02-07 落地签名为准并在本任务内修正）
       - 修改 handler remarks（第 40-48 行段）：把"disable the tenant filter"重写为"look up by hash via IgnoreQueryFilters（hash 全局唯一）+ SaveChanges 期间临时切 MultiTenantContext.TenantInfo 到 invitation.WorkspaceId 租户（让 TenantNotSetMode=Overwrite 盖印正确 TenantId），SaveChanges 后还原"
       - **不要**改 AcceptInvitationEndpoint / AcceptInvitationCommand / AcceptInvitationResponse / validator

    3. **新增 AcceptInvitationAcrossTenantsTests.cs**（路径 yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/AcceptInvitationAcrossTenantsTests.cs）：
       - namespace `YH.Tests.Workspace.Integration.CrossTenant`
       - `public sealed class AcceptInvitationAcrossTenantsTests : IClassFixture<WorkspacePostgresFixture>`
       - 测试方法 `[Fact] [Trait("Category", "Postgres")] [Trait("Category", "RequiresDocker")] public async Task Accept_Succeeds_When_Invitee_Current_Tenant_Differs_From_Invitation_Workspace()`：
         a) 用 fixture.RootTenant 作用域 seed workspaceA 行（Workspace.Create(name="Acme CR01", slug="acme-cr01", owner=adminId)）+ SaveChanges（workspaceA 是 IGlobalEntity 不过滤）
         b) 切到 workspaceA.Id 租户作用域（用 fixture 的 FinbuckleTestTenantScope 或直接 fixture.CreateContextForTenant(workspaceA)），new InvitationTokenService(db, Options.Create(new WorkspaceTokenOptions())) → CreateAsync(workspaceId: workspaceA.Id, email: inviteeEmail, role: 15, ttlDays: 7, message: null) → 拿到 rawToken
         c) 构造受邀用户的 handler 调用上下文：fixture.CreateContextForTenant(RootTenant)（**关键：模拟顶层 accept 端点 DbContext 作用域=平台 RootTenant ≠ workspaceA**），new AcceptInvitationCommandHandler(tokenService, db, _tenantContextAccessor) 调 Handle(AcceptInvitationCommand { Token = rawToken, CurrentUserId = inviteeUserId })
         d) **断言**：返回 AcceptInvitationResponse(memberId ≠ Guid.Empty, workspaceId == workspaceA.Id)
         e) **二次断言**：用 IgnoreQueryFilters 读回 WorkspaceMembers 表，断言新行的 TenantId 影子属性 == workspaceA.Id.ToString()（而非 RootTenant.Id）；m.WorkspaceId == workspaceA.Id；m.UserId == inviteeUserId.ToString()；m.Role == 15；m.IsActive == true
       - 测试隔离：每个 [Fact] 开头 `await _fixture.ResetWorkspaceTablesAsync()`（或测试内 `TRUNCATE workspace."WorkspaceMembers", workspace."WorkspaceInvitations", workspace."Workspaces" RESTART IDENTITY CASCADE;`）
       - using directives：`using YH.Tests.Workspace.Integration.PostgresFixtures; using YH.Modules.Workspace.Data; using YH.Modules.Workspace.Domain; using YH.Modules.Workspace.Services; using YH.Modules.Workspace.Features.v1.Invitations.AcceptInvitation; using YH.Modules.Workspace.Contracts.v1.Invitations.AcceptInvitation; using YH.Modules.Workspace.Configuration; using YH.Framework.Shared.Multitenancy; using Finbuckle.MultiTenant.Abstractions; using Microsoft.EntityFrameworkCore; using Microsoft.Extensions.Options; using Shouldly; using Xunit; using Mediator;`
       - **修复前预期**：测试在修复前调用 accept → 抛 NotFoundException（404 路径），证明 CR-01 真实存在。**修复后**：测试通过，返回 200

    4. **不要**改 InMemory 现有 InvitationHashTests / InvitationInvalidateTests / InvitationTtlTests（它们用单租户 fixture，验证非租户作用域的 token crypto + state machine；CR-01 修复对它们透明——IgnoreQueryFilters 在 InMemory 上不改变行为）

  </action>
  <verify>
    <automated>cd D:\github\akinix-plane && dotnet build yh-flow/src/YH.Flow.slnx --nologo && dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal --filter "Category=Postgres" && dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal && dotnet test yh-flow/src/Tests/Identity.Tests --nologo --verbosity minimal</automated>
    构建必须 0/0。AcceptInvitationAcrossTenantsTests 在 Docker 启动时通过。现有 Workspace.Tests 96/96 + 02-07 新增 + 02-08 新增 全绿。Identity.Tests 412/412 零回归。
  </verify>
  <done>
    - `grep -n IgnoreQueryFilters yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs` 命中（ValidateAsync 段）
    - `grep -n IgnoreQueryFilters yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs` 命中（Step 2 re-attach 段）
    - `grep -n "MultiTenantContext|IMultiTenantContextAccessor|workspaceTenant|setter.MultiTenantContext" AcceptInvitationCommandHandler.cs` 命中（Step 4 SaveChanges 作用域切换）
    - handler remarks 段落 false claim 已修正（grep "disable the tenant filter" 不再命中或被精确描述替代）
    - RejectInvitationCommandHandler.cs:58-60 同步修复（如执行者按 CR-01 描述加了）
    - AcceptInvitationAcrossTenantsTests.cs 存在、含 [Fact] + [Trait("Category","Postgres")] + 跨租户 seed + 切 RootTenant 调 handler + TenantId 影子属性断言
    - `dotnet build YH.Flow.slnx` = 0 warnings / 0 errors
    - `dotnet test Workspace.Tests --filter Category=Postgres` 在 Docker 启动时 = 0 failed
    - `dotnet test Workspace.Tests`（全量）= 96/96 + 02-07 + 02-08 新关系测试 全绿
    - `dotnet test Identity.Tests` = 412/412
  </done>
  <acceptance_criteria>
    - `grep -c "IgnoreQueryFilters" yh-flow/src/Modules/Workspace/Modules.Workspace/Services/InvitationTokenService.cs` ≥ 1（在 ValidateAsync 内）
    - `grep -c "IgnoreQueryFilters" yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs` ≥ 1（Step 2 re-attach）
    - `grep -n "workspaceTenant\|MultiTenantContext<AppTenantInfo>\|setter.MultiTenantContext" AcceptInvitationCommandHandler.cs` 命中（SaveChanges 作用域切换）
    - AcceptInvitationAcrossTenantsTests 含 `Assert`/`ShouldBe` 验证新行 `TenantId == workspaceA.Id.ToString()`（不是 RootTenant.Id）
    - 测试 seed 的 workspaceA 租户 ≠ RootTenant（用 Guid.NewGuid() 或固定不同 Guid），DbContext 作用域 = RootTenant 调 handler，模拟顶层端点的生产场景
    - `dotnet build yh-flow/src/YH.Flow.slnx --nologo` 退出码 0
    - `dotnet test yh-flow/src/Tests/Workspace.Tests --filter "Category=Postgres"` 在 Docker 启动时退出码 0（新测试通过）
    - `dotnet test yh-flow/src/Tests/Workspace.Tests` 退出码 0
    - `dotnet test yh-flow/src/Tests/Identity.Tests` 退出码 0
    - handler remarks 段（第 40-48 行）的"disable the tenant filter"被重写为精确描述实际行为（`grep "disable the tenant filter" AcceptInvitationCommandHandler.cs` 不再命中或被"IgnoreQueryFilters + 切 MultiTenantContext.TenantInfo"替代）
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-03 修复 — AcceptInvitationCommandHandler 插入前查既有 (workspaceId, userId) 行，存在则 Activate+UpdateRole 否则新建</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs,
    yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/ReAcceptAfterRemovalTests.cs
  </files>
  <read_first>
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs:100-111（**被改点**：Step 4 当前无条件 `_db.Members.Add(WorkspaceMember.Create(...))`，对曾移除的成员会触发 UniqueConstraintException）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs:117-132（**Activate / Deactivate / UpdateRole 方法签名**：Activate 是 public、无参、设 IsActive=true + LastModifiedOnUtc；UpdateRole(int) 同步 Role）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Services/WorkspaceMembershipService.cs（**RemoveAsync 实现**：调 member.Deactivate()，IsActive=false 但 IsDeleted=false；保留行占唯一槽——CR-03 根因）
    - yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceMemberConfiguration.cs:48-53（**复合唯一索引**：`HasIndex(x => new { x.TenantId, x.UserId }).IsUnique().HasDatabaseName("IX_WorkspaceMembers_Tenant_User")` —— IsActive 不在键里）
    - .planning/phases/02-workspace/02-REVIEW.md:132-180（**CR-03 单一真相源**：根因 + 修复代码片段 + "Cover with a test: invite → accept → admin removes → re-invite → re-accept should succeed without a 500"）
    - yh-flow/src/BuildingBlocks/Persistence/Context/BaseDbContext.cs:35（AppendGlobalQueryFilter<ISoftDeletable> → ISoftDeletable 行不会被过滤，但 Deactivate 不设 IsDeleted，所以既有停用行在普通查询里可见——**但**租户过滤器会按 TenantId 过滤，所以跨租户查找既有行仍需 IgnoreQueryFilters）
    - yh-flow/AGENTS.md（Rule 5 sealed handler；Rule 7 CancellationToken；Rule 3 tenant isolation default-on——本任务的跨租户 IgnoreQueryFilters 查既有行是合理 opt-out）
    - .planning/phases/02-workspace/02-HUMAN-UAT.md:75-79（🟥 步骤 10 slug release reuse + 隐含的成员重邀路径——本任务修复后这些路径可用）
    - Task 1 交付的 AcceptInvitationCommandHandler.cs（**修改基础**：Task 1 已在该文件加了 IgnoreQueryFilters + SaveChanges 作用域切换；Task 2 在此基础上追加既有成员查找逻辑，两个 task 触碰同一文件不同段——Task 2 必须在 Task 1 完成后跑）
  </read_first>
  <behavior>
    - AcceptInvitationCommandHandler Step 4 插入前：用 `_db.Members.IgnoreQueryFilters().FirstOrDefaultAsync(m => m.WorkspaceId == tracked.WorkspaceId && m.UserId == command.CurrentUserId.ToString(), ...)` 查既有行（**关键**：必须 IgnoreQueryFilters，因为 DbContext 当前作用域已切到 invitation 的 workspace 租户——Task 1 Step 4 的 scope——但既有行可能被之前不同作用域的写入盖了不同 TenantId，最稳是 IgnoreQueryFilters 跨租户找）
    - 既有行存在：`existing.Activate(); existing.UpdateRole(tracked.Role);`（用 entity 的方法，不直接赋值字段——遵循 entity 封装）
    - 既有行不存在：`_db.Members.Add(WorkspaceMember.Create(workspaceId: tracked.WorkspaceId, userId: command.CurrentUserId.ToString(), role: tracked.Role, isActive: true));`（保持原有 Create 调用）
    - ReAcceptAfterRemovalTests: invite → accept → admin remove（Deactivate）→ re-invite → re-accept 流程 → 第二次 accept 不抛异常 + 复用既有行（同 memberId）+ IsActive=true
  </behavior>
  <action>
    1. **修改 AcceptInvitationCommandHandler.cs Step 4**（Task 1 已在此文件加了 SaveChanges 作用域切换，本 task 在 Add(member) 之前追加既有查找分支）：
       - 当前 Step 4：
         ```
         var member = WorkspaceMember.Create(...);
         _db.Members.Add(member);
         ```
       - 修复后（在 Task 1 的 `_db.Members.Add(member);` 位置替换为分支逻辑）：
         ```
         // CR-03 (02-REVIEW.md): 插入前查既有 (workspaceId, userId) 跨租户行。
         // 成员被移除时 WorkspaceMembershipService.RemoveAsync 调 member.Deactivate()
         // （IsActive=false 但 IsDeleted=false），复合唯一索引 (TenantId, UserId) 不含 IsActive，
         // 停用行仍占唯一槽；再次接受会触发 UniqueConstraintException → 500，用户被永久锁在外面。
         // 必须 IgnoreQueryFilters 因为 DbContext 当前作用域虽已切到 invitation workspace 租户，
         // 但既有行可能因历史写入携带不同 TenantId——跨租户查找是最稳路径。
         var existing = await _db.Members
             .IgnoreQueryFilters()
             .FirstOrDefaultAsync(m => m.WorkspaceId == tracked.WorkspaceId
                                    && m.UserId == command.CurrentUserId.ToString(),
                                  cancellationToken)
             .ConfigureAwait(false);
         WorkspaceMember member;
         if (existing is not null)
         {
             existing.Activate();
             existing.UpdateRole(tracked.Role);
             member = existing;
         }
         else
         {
             member = WorkspaceMember.Create(
                 workspaceId: tracked.WorkspaceId,
                 userId: command.CurrentUserId.ToString(),
                 role: tracked.Role,
                 isActive: true);
             _db.Members.Add(member);
         }
         ```
       - **保留** Task 1 的 SaveChanges 作用域切换（using scope 切 workspaceTenant），因为既有行的 UpdateRole + Activate 也需要在该租户作用域下 SaveChanges（否则 TenantNotSetMode=Overwrite 会改写 TenantId 破坏唯一索引）
       - **保留** Step 5 的 `return new AcceptInvitationResponse(member.Id, tracked.WorkspaceId);`（member.Id 在两个分支都有值）

    2. **新增 ReAcceptAfterRemovalTests.cs**（路径 yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/ReAcceptAfterRemovalTests.cs）：
       - namespace `YH.Tests.Workspace.Integration.CrossTenant`
       - `public sealed class ReAcceptAfterRemovalTests : IClassFixture<WorkspacePostgresFixture>`
       - 测试方法 `[Fact] [Trait("Category", "Postgres")] [Trait("Category", "RequiresDocker")] public async Task ReAccept_After_Admin_Remove_Reuses_Existing_Row_No_UniqueConstraint_500()`：
         a) seed workspaceA 行 + 切到 workspaceA 租户作用域， InvitationTokenService.CreateAsync(workspaceA.Id, inviteeEmail, role: 15) → rawToken1
         b) **第一次 accept**：切到 RootTenant 作用域（≠ workspaceA）调 AcceptInvitationCommandHandler.Handle(token1, inviteeUserId) → 200 + member1Id
         c) **admin 移除成员**：切到 workspaceA 租户作用域，new WorkspaceMembershipService(db, ...) 调 RemoveAsync(workspaceA.Id, member1Id, adminId) → 既有行 Deactivate（IsActive=false）
         d) **再次邀请**：切到 workspaceA 租户作用域， InvitationTokenService.CreateAsync(workspaceA.Id, inviteeEmail, role: 15) → rawToken2
         e) **第二次 accept（关键测试）**：切到 RootTenant 作用域调 AcceptInvitationCommandHandler.Handle(token2, inviteeUserId) → **不抛异常**、200 + memberId == member1Id（复用既有行）+ IsActive=true（用 IgnoreQueryFilters 读回验证）
       - 断言用 Shouldly：`(await ReadMemberIgnoreFiltersAsync(member1Id)).IsActive.ShouldBeTrue()`、`response.MemberId.ShouldBe(member1Id)`
       - 测试隔离：每个 [Fact] 开头 `TRUNCATE workspace."WorkspaceMembers", workspace."WorkspaceInvitations", workspace."Workspaces" RESTART IDENTITY CASCADE;`
       - using directives 同 Task 1 AcceptInvitationAcrossTenantsTests + WorkspaceMembershipService

    3. **不要**改 WorkspaceMembershipService.RemoveAsync（它的 Deactivate 行为是业务设计；CR-03 修复在 accept 端，不在 remove 端）
    4. **不要**改 WorkspaceMemberConfiguration 的复合唯一索引（CR-03 §"Fix" 不要求改索引；通过既有成员 Activate 复用解决）
    5. **不要**改 WorkspaceMember 实体（Activate / UpdateRole 方法已存在，本 task 复用）

  </action>
  <verify>
    <automated>cd D:\github\akinix-plane && dotnet build yh-flow/src/YH.Flow.slnx --nologo && dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal --filter "Category=Postgres" && dotnet test yh-flow/src/Tests/Workspace.Tests --nologo --verbosity minimal && dotnet test yh-flow/src/Tests/Identity.Tests --nologo --verbosity minimal</automated>
    构建必须 0/0。ReAcceptAfterRemovalTests 在 Docker 启动时通过（不抛 UniqueConstraintException）。现有 + 02-07 + 02-08 Task 1 测试 全绿。
  </verify>
  <done>
    - AcceptInvitationCommandHandler.cs Step 4 含 `existing.Activate(); existing.UpdateRole(tracked.Role);` 分支 + `IgnoreQueryFilters` 跨租户查找
    - ReAcceptAfterRemovalTests.cs 存在、含 invite→accept→remove→re-invite→re-accept 完整流程 + memberId 复用断言
    - `dotnet build YH.Flow.slnx` = 0 warnings / 0 errors
    - `dotnet test Workspace.Tests --filter Category=Postgres` 在 Docker 启动时 = 0 failed（含 ReAcceptAfterRemoval）
    - `dotnet test Workspace.Tests`（全量）= 96/96 + 02-07 + 02-08 全绿
    - `dotnet test Identity.Tests` = 412/412
    - 未触碰 WorkspaceMember.cs / WorkspaceMemberConfiguration.cs / WorkspaceMembershipService.cs
  </done>
  <acceptance_criteria>
    - `grep -n "existing.Activate\|existing.UpdateRole\|FirstOrDefaultAsync(m => m.WorkspaceId" yh-flow/src/Modules/Workspace/Modules.Workspace/Features/v1/Invitations/AcceptInvitation/AcceptInvitationCommandHandler.cs` 全部命中
    - `grep -c "IgnoreQueryFilters" AcceptInvitationCommandHandler.cs` ≥ 2（Task 1 的 Step 2 re-attach + Task 2 的既有成员查找）
    - `grep -n "ReAccept\|member1Id\|IsActive.ShouldBeTrue" yh-flow/src/Tests/Workspace.Tests/Integration/CrossTenant/ReAcceptAfterRemovalTests.cs` 命中
    - ReAcceptAfterRemovalTests 流程含 5 步：invite1 → accept1 → admin RemoveAsync → invite2 → accept2（不再抛 UniqueConstraintException）
    - `dotnet build yh-flow/src/YH.Flow.slnx --nologo` 退出码 0
    - `dotnet test yh-flow/src/Tests/Workspace.Tests --filter "Category=Postgres"` 在 Docker 启动时退出码 0
    - `dotnet test yh-flow/src/Tests/Workspace.Tests` 退出码 0
    - `dotnet test yh-flow/src/Tests/Identity.Tests` 退出码 0
    - `git diff --name-only yh-flow/src/Modules/Workspace/Modules.Workspace/Domain/WorkspaceMember.cs yh-flow/src/Modules/Workspace/Modules.Workspace/Data/Configurations/WorkspaceMemberConfiguration.cs yh-flow/src/Modules/Workspace/Modules.Workspace/Services/WorkspaceMembershipService.cs` 应为空（CR-03 修复仅触及 handler + 测试）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                                                       |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 顶层 accept 端点 → DbContext | `POST /api/v1/workspaces/invitations/{token}/accept/` 无 `{slug}`，DbContext 默认作用域 = 受邀用户当前租户 ≠ 邀请所属 workspace（CR-01 根因）                     |
| 邀请 token → 接受者身份      | ValidateAsync 仅靠 TokenHash 查找（hash 全局唯一），不绑定受邀用户身份；任意持有 raw token 的认证用户可接受（已有设计，与 Phase 1威胁登记 T-2-acceptpublic 一致） |
| 既有成员行复用               | 跨租户 IgnoreQueryFilters 查找既有 (workspaceId, userId) 行用于 CR-03 Activate 复用                                                                               |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-02-08-01 | Elevation of Privilege | AcceptInvitation 跨租户 SaveChanges 作用域切换         | mitigate    | Task 1 在 SaveChanges 期间临时把 MultiTenantContext.TenantInfo 切到 invitation.WorkspaceId（来自信任的 DB 行，非用户输入）；finally 还原防止状态泄漏；不接受任何外部传入的 tenantId                                                                    |
| T-02-08-02 | Tampering              | CR-03 既有成员行跨租户查找                             | mitigate    | Task 2 用 IgnoreQueryFilters 跨租户查找既有行，但仅匹配 (WorkspaceId == tracked.WorkspaceId && UserId == command.CurrentUserId)，不接受用户控制的 WorkspaceId（tracked 来自 ValidateAsync 通过 hash 解析）；Activate+UpdateRole 用 entity 方法保持封装 |
| T-02-08-03 | Information Disclosure | ValidateAsync IgnoreQueryFilters 暴露跨租户 invitation | accept      | hash 是 256-bit CSPRNG，不可枚举；ValidateAsync 仅返回 IsValid=true 的 invitation 给已认证的 accept handler；不泄露跨租户 list/detail；与 T-2-acceptpublic 威胁登记一致（Phase 2 已 accept）                                                           |
| T-02-08-04 | Repudiation            | AcceptInvitation remarks 之前的 false claim            | mitigate    | Task 1 同步修正 handler remarks（"disable the tenant filter" → 精确描述 IgnoreQueryFilters + MultiTenantContext 切换）；防止后续维护者继续相信错误的代码契约                                                                                           |
| T-02-08-SC | Tampering              | NuGet 包安装                                           | mitigate    | 本计划 0 新 NuGet 包（全部复用 02-07 的 Testcontainers.PostgreSql + Finbuckle + Npgsql）                                                                                                                                                               |

</threat_model>

<verification>
## 计划级验证

### 自动化（必须通过）

- `dotnet build yh-flow/src/YH.Flow.slnx --nologo` → 0 warnings / 0 errors
- `dotnet test yh-flow/src/Tests/Workspace.Tests --filter "Category=Postgres"` → AcceptInvitationAcrossTenantsTests + ReAcceptAfterRemovalTests 全部通过（Docker 启动时）
- `dotnet test yh-flow/src/Tests/Workspace.Tests` → 96/96 + 02-07 + 02-08 全绿
- `dotnet test yh-flow/src/Tests/Identity.Tests` → 412/412（零回归）

### 人工门禁（修复后必跑，由 /gsd-execute-phase 02 完成后用户执行）

- **02-HUMAN-UAT.md 🟥 步骤 5（CR-01 THE DECIDER）** —— `POST /api/v1/workspaces/invitations/{token}/accept/` → 200 {memberId, workspaceId}（修复前必然 404）。这是 CR-01 的最终仲裁。
- **02-HUMAN-UAT.md 🟥 步骤 6（List members D-05 batch）** —— accept 后 invitee 出现在成员列表
- **02-HUMAN-UAT.md 🟥 步骤 7（EOP self-promotion）** —— accept 后成员可被升级/降级
- **02-HUMAN-UAT.md 🟥 步骤 10（Slug release reuse）** —— 暗示重邀路径，CR-03 修复后曾移除成员可重邀重接受
- **02-HUMAN-UAT.md 🟥 步骤 11（Cross-workspace isolation）** —— 跨租户隔离正确性

### Goal-Backward Truths 校验

1. **Truth "邀请所属 workspace 租户 ≠ 受邀用户当前租户时 accept 返回 200"** → AcceptInvitationAcrossTenantsTests 断言
2. **Truth "新建 WorkspaceMember 行 TenantId = 邀请所属 workspace"** → 同测试二次断言 TenantId 影子属性
3. **Truth "曾移除成员再次接受 → 复用既有行不抛异常"** → ReAcceptAfterRemovalTests 断言 memberId 复用 + IsActive=true
4. **Truth "Workspace.Tests + Identity.Tests 保持绿"** → dotnet test 双门禁通过
   </verification>

<success_criteria>

- [x] InvitationTokenService.ValidateAsync 含 `.IgnoreQueryFilters()`
- [x] AcceptInvitationCommandHandler Step 2 re-attach 含 `.IgnoreQueryFilters()`
- [x] AcceptInvitationCommandHandler Step 4 SaveChanges 期间切换 MultiTenantContext.TenantInfo 到 invitation.WorkspaceId
- [x] AcceptInvitationCommandHandler Step 4 插入前查既有 (workspaceId, userId) 跨租户行，存在则 Activate+UpdateRole
- [x] handler remarks false claim 已修正
- [x] RejectInvitationCommandHandler 同步修复（按 CR-01 描述）
- [x] AcceptInvitationAcrossTenantsTests 在真实 PG 验证 CR-01 修复
- [x] ReAcceptAfterRemovalTests 在真实 PG 验证 CR-03 修复
- [x] 构建门禁 0 warnings / 0 errors
- [x] Workspace.Tests + Identity.Tests 全量测试保持绿
- [x] autonomous=false（Docker 依赖）
      </success_criteria>

<output>
完成后创建 `.planning/phases/02-workspace/02-08-SUMMARY.md`。
SUMMARY 必须记录：
1. CR-01 + CR-03 修复的精确行号 + 调用链
2. SaveChanges 作用域切换的具体实现（IMultiTenantContextSetter 还原模式 + 02-07 fixture 的 FinbuckleTestTenantScope 是否能在 production 复用）
3. AppTenantInfo 构造签名（与 02-07 fixture 对齐）
4. RejectInvitationCommandHandler 同步修复说明（如执行者按 CR-01 加了）
5. 02-07 + 02-08 完成后 Phase 2 gap-closure 三 CRITICAL 全部修复，可进入 HUMAN-UAT 11 步人工冒烟阶段
</output>
