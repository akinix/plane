---
phase: 17-calendar-gantt-sheet
plan: 04
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-view.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/issue-row.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-header.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-header-column.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-table.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/base-spreadsheet-root.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/issue-column.tsx
  - yh-flow/clients/web/app/components/issues/spreadsheet-view/columns/
  - yh-flow/clients/web/src/lib/mock-data.ts
autonomous: true
requirements: [SHEE-01, SHEE-02]

must_haves:
  truths:
    - "用户可以从 issues 页切换到电子表格视图"
    - "电子表格以表格形式展示 issue 字段列"
    - "用户单击单元格可直接内联编辑属性（SHEE-02）"
    - "优先级/负责人等属性渲染对应的编辑控件"
    - "用户可拖拽调整列宽"
    - "用户可多选行进行批量编辑"
    - "空状态显示'暂无 Issue'"
  artifacts:
    - path: "yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-view.tsx"
      provides: "Spreadsheet main container"
    - path: "yh-flow/clients/web/app/components/issues/spreadsheet-view/columns/"
      provides: "Column type components (16 types)"
  key_links:
    - from: "spreadsheet-view.tsx"
      to: "@plane/types, @/../src/lib/hooks/use-issues, @/../src/lib/mock-data"
      via: "imports"
    - from: "spreadsheet-view.tsx"
      to: "app/components/issues/filters/types"
      via: "column visibility TColumnVisibility"
---

<objective>
Fork Plane 电子表格视图组件到 yh-flow（per D-P17-03）。复制 apps/web/core/components/issues/issue-layouts/spreadsheet/ 到 app/components/issues/spreadsheet-view/，适配 yh-flow 导入路径。

目的: 实现 SHEE-01（电子表格视图）和 SHEE-02（内联编辑），提供 16 种列类型组件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-calendar-gantt-sheet/17-CONTEXT.md — D-P17-03, D-P17-08
@.planning/phases/17-calendar-gantt-sheet/17-UI-SPEC.md — Spreadsheet interaction contract

# Plane source to fork

@apps/web/core/components/issues/issue-layouts/spreadsheet/ — full directory

# yh-flow patterns

@yh-flow/clients/web/app/components/issues/list-view.tsx — existing table/row pattern
@yh-flow/clients/web/app/components/issues/kanban-view.tsx — view container pattern
@yh-flow/clients/web/src/lib/hooks/use-issues.ts — data hooks
@yh-flow/clients/web/src/lib/mock-data.ts — mock data
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fork Plane 电子表格核心组件</name>
  <files>
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/README.md (CREATE)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/base-spreadsheet-root.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/issue-column.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/issue-row.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-header.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-header-column.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-table.tsx (CREATE)
  </files>
  <read_first>
    apps/web/core/components/issues/issue-layouts/spreadsheet/ — Plane spreadsheet directory
    apps/web/core/components/issues/issue-layouts/spreadsheet/base-spreadsheet-root.tsx
    apps/web/core/components/issues/issue-layouts/spreadsheet/issue-row.tsx — key component
    apps/web/core/components/issues/issue-layouts/spreadsheet/spreadsheet-table.tsx
    yh-flow/clients/web/app/components/issues/list-view.tsx — yh-flow table pattern reference
  </read_first>
  <action>
    遵照 D-P17-03：从 Plane apps/web/core/components/issues/issue-layouts/spreadsheet/ 复制电子表格组件到 app/components/issues/spreadsheet-view/。

    每个文件添加 `// FLOW: Forked from Plane spreadsheet/` 标记。

    创建 base-spreadsheet-root.tsx — 复制 Plane 版：
    - Spreadsheet root 容器
    - 使用 useIssues 获取 issue 数据
    - 加载中显示 skeleton 行（per UI-SPEC）
    - 空状态显示 "暂无 Issue" 居中

    创建 issue-row.tsx — 复制 Plane 版：
    - 单行 issue 渲染（水平滚动容器）
    - 每行包含所有可见列
    - Checkbox 选择（多选支持 per SHEE-02 batch edit）
    - @hello-pangea/dnd DragHandle 支持行拖拽排序（per UI-SPEC）

    创建 issue-column.tsx — 复制 Plane 版：
    - 列容器组件，包装每个 column cell

    创建 spreadsheet-header.tsx — 复制 Plane 版：
    - 表头行（水平滚动 + 粘性）
    - 每列的表头标签名称
    - 列宽拖拽调整（spreadsheet-header-column pattern）

    创建 spreadsheet-header-column.tsx — 复制 Plane 版：
    - 单列表头组件
    - 列名显示 + 拖拽调整宽度句柄

    创建 spreadsheet-table.tsx — 复制 Plane 版：
    - 表格主体区域（虚拟滚动 if Plane implements it）
    - 列/行网格布局
    - 内联编辑状态管理（D-P17-08：单击切入编辑模式）

    所有导入路径适配规则（同 Plan 17-02/03）：
    - `@/components/issues/issue-layouts/spreadsheet/` → `./`
    - `@plane/types` / `@plane/ui` / `@plane/utils` → 保持
    - Plane 内部 hooks → yh-flow 等价替换
    - 所有修改用 `// FLOW:` 标记

    内联编辑实现注意（per D-P17-08）：
    - 单击单元格 → 显示文本
    - 单击文本 → 切换为当前列类型的编辑控件
    - 点击外部或 Enter → 保存；Escape → 取消
    - 自动保存 debounce 800ms（per UI-SPEC）

  </action>
  <acceptance_criteria>
    1. spreadsheet-view/ 目录创建，包含 6 个核心组件文件 + README
    2. 所有导入路径适配到 yh-flow
    3. `npx tsc --noEmit` 不新增 spreadsheet 相关错误
  </acceptance_criteria>
  <verify>
    <automated>ls -la app/components/issues/spreadsheet-view/ && wc -l app/components/issues/spreadsheet-view/*.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep "spreadsheet" | grep -c "error" || echo "no new spreadsheet errors"</automated>
  </verify>
  <done>
    * 所有电子表格核心组件 fork 完成
    * 导入路径适配完成
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fork 16 种列类型组件 + 创建 SpreadsheetView 主容器</name>
  <files>
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/columns/ (CREATE directory + 16 column type files)
    - yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-view.tsx (CREATE)
    - yh-flow/clients/web/src/lib/mock-data.ts (MODIFY if needed)
  </files>
  <read_first>
    apps/web/core/components/issues/issue-layouts/spreadsheet/columns/ — Plane 16 column components
    apps/web/core/components/issues/issue-layouts/spreadsheet/spreadsheet-view.tsx — Plane main view
    yh-flow/clients/web/app/components/issues/property-editor.tsx — existing yh-flow property editor for reference
  </read_first>
  <action>
    创建 columns/ 目录，从 Plane 复制 16 种列类型组件：

    1. state-column.tsx — 状态列（显示状态名称+颜色圆点，单击显示状态选择器）
    2. priority-column.tsx — 优先级列（显示优先级标签，单击显示优先级选择器：紧急/高/中/低/无）
    3. assignee-column.tsx — 负责人列（头像显示，单击显示用户选择器）
    4. due-date-column.tsx — 截止日期列（日期显示，单击显示 date picker）
    5. start-date-column.tsx — 开始日期列（同 due-date 模式）
    6. label-column.tsx — 标签列（标签徽章显示，单击显示标签选择器）
    7. cycle-column.tsx — 周期列
    8. module-column.tsx — 模块列
    9. estimate-column.tsx — 估算列（数字输入，快速选项）
    10. created-on-column.tsx — 创建时间列（只读）
    11. updated-on-column.tsx — 更新时间列（只读）
    12. attachment-column.tsx — 附件计数列（只读）
    13. link-column.tsx — 链接计数列（只读）
    14. sub-issue-column.tsx — 子 Issue 计数列（只读）
    15. header-column.tsx — 行首列（ID + 拖拽句柄 + checkbox）

    每个列组件模式：
    ```tsx
    // FLOW: Forked from Plane spreadsheet/columns/{name}
    type TProps = { issue: TIssue; onChange: (issueId: string, field: string, value: any) => void };
    export const {Name}Column = ({ issue, onChange }: TProps) => { ... };
    ```

    列组件契约：
    - 只读列（created-on, updated-on, attachment, link, sub-issue）显示文本值，不响应单击编辑
    - 可编辑列（state, priority, assignee, due-date, start-date, label, cycle, module, estimate）支持单击切入编辑模式
    - 编辑模式使用原生控件（select/date input/number input）—— 不依赖 @plane/ui Dropdown（保持与 Phase 16 一致的简化风格）
    - 每个可编辑列在编辑结束时调用 onChange(issueId, fieldName, newValue)

    创建 spreadsheet-view.tsx — SpreadsheetView 主容器：
    1. Props: `{ workspaceId: string; projectId: string }`
    2. 使用 useIssues(projectId) 获取 issue 数据
    3. 使用 `useStore().issue.visibleColumnIds` 决定显示哪些列（如果 visibleColumnIds 为空则显示所有列）
    4. 渲染 SpreadsheetTable + SpreadsheetHeader + IssueRows
    5. 管理内联编辑状态（当前正在编辑的 cell: { issueId, columnKey } | null）
    6. 自动保存：用户完成编辑后触发 updateIssue.mutate（800ms debounce per UI-SPEC）
    7. 加载/空/错误状态处理（per UI-SPEC）
    8. 支持多选行（使用 store.issue.selectedIssueIds）
    9. 批量编辑 action bar（复用 Phase 16 的 BulkActionBar）

  </action>
  <acceptance_criteria>
    1. columns/ 目录包含 16 个列组件文件
    2. SpreadsheetView 组件导出 named export
    3. 单击单元格可切入编辑模式
    4. 编辑完成后触发 updateIssue
    5. `npx tsc --noEmit` 不新增 spreadsheet 错误
  </acceptance_criteria>
  <verify>
    <automated>ls app/components/issues/spreadsheet-view/columns/*.tsx | wc -l</automated>
    <automated>grep -q "SpreadsheetView" app/components/issues/spreadsheet-view/spreadsheet-view.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep "spreadsheet" | grep -c "error" || echo "no spreadsheet errors"</automated>
  </verify>
  <done>
    * 16 种列类型组件完成
    * SpreadsheetView 主容器完成
    * 内联编辑（SHEE-02）实现
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID   | Category  | Component        | Disposition | Mitigation Plan                                                                            |
| ----------- | --------- | ---------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-17-SHE-01 | Tampering | Cell inline edit | mitigate    | 800ms debounce 防止频繁 API 调用；乐观更新 + 错误回滚模式（同 Phase 16 useIssueMutations） |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
ls app/components/issues/spreadsheet-view/columns/ | wc -l
grep -q "SpreadsheetView" app/components/issues/spreadsheet-view/spreadsheet-view.tsx
```
</verification>

<success_criteria>

- [ ] 16 种列类型组件 fork 完成
- [ ] SpreadsheetView 主容器完成
- [ ] 单击内联编辑（SHEE-02）
- [ ] 列宽拖拽调整
- [ ] 多选批量编辑
- [ ] `npx tsc --noEmit` 不新增错误

输出: app/components/issues/spreadsheet-view/ 完整目录
</success_criteria>

<output>
创建 `.planning/phases/17-calendar-gantt-sheet/17-04-SUMMARY.md`
</output>
