---
phase: 17-calendar-gantt-sheet
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - yh-flow/clients/web/app/components/issues/gantt-view/gantt-view.tsx
  - yh-flow/clients/web/app/components/issues/gantt-view/blocks/
  - yh-flow/clients/web/app/components/issues/gantt-view/chart/
  - yh-flow/clients/web/app/components/issues/gantt-view/sidebar/
  - yh-flow/clients/web/app/components/issues/gantt-view/views/week-view.tsx
  - yh-flow/clients/web/app/components/issues/gantt-view/views/month-view.tsx
  - yh-flow/clients/web/app/components/issues/gantt-view/views/quarter-view.tsx
  - yh-flow/clients/web/app/components/issues/gantt-view/contexts/
  - yh-flow/clients/web/app/components/issues/gantt-view/data/
  - yh-flow/clients/web/app/components/issues/gantt-view/helpers/
autonomous: true
requirements: [GANT-01, GANT-02]

must_haves:
  truths:
    - "用户可以从 issues 页切换到甘特图视图"
    - "甘特图横向显示 issue 时间跨度（起止日期）"
    - "用户可通过天/周/月/季度缩放切换（D-P17-16）"
    - "Issue 之间的依赖关系用连线表示（GANT-02）"
    - "左侧边栏显示 issue 列表（360px宽，含标识符、名称、负责人）"
    - "Issue 进度条基于子 issue 比例计算"
    - "用户可拖拽移动/扩展/缩短 issue bar 来调整日期"
    - "空状态显示'暂无 Issue'"
  artifacts:
    - path: "yh-flow/clients/web/app/components/issues/gantt-view/gantt-view.tsx"
      provides: "Gantt chart main container with zoom controls"
    - path: "yh-flow/clients/web/app/components/issues/gantt-view/chart/"
      provides: "Gantt chart time axis rendering"
    - path: "yh-flow/clients/web/app/components/issues/gantt-view/views/"
      provides: "4 zoom level views (day/week/month/quarter)"
  key_links:
    - from: "gantt-view.tsx"
      to: "@plane/types, @hello-pangea/dnd, @/../src/lib/hooks/use-issues"
      via: "imports"
    - from: "gantt-view.tsx"
      to: "app/components/issues/filters/use-group-by"
      via: "optional group-by support"
---

<objective>
Fork Plane 甘特图组件到 yh-flow（per D-P17-02）。复制 apps/web/core/components/gantt-chart/ 到 app/components/issues/gantt-view/，适配 yh-flow 导入路径。

目的: 实现 GANT-01（甘特图视图）和 GANT-02（依赖关系连线），支持 4 级缩放（D-P17-16）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-calendar-gantt-sheet/17-CONTEXT.md — D-P17-02, D-P17-07, D-P17-16
@.planning/phases/17-calendar-gantt-sheet/17-UI-SPEC.md — Gantt interaction contract

# Plane source to fork

@apps/web/core/components/gantt-chart/ — full directory (blocks, chart, sidebar, views, contexts, data, helpers, constants, index, root)

# yh-flow patterns

@yh-flow/clients/web/app/components/issues/kanban-view.tsx — view container pattern
@yh-flow/clients/web/app/store/issue.store.ts — IssueStore activation
@yh-flow/clients/web/src/lib/hooks/use-issues.ts — data hooks
@yh-flow/clients/web/src/lib/mock-data.ts — mock issues (check start_date/target_date)
@yh-flow/clients/web/src/lib/types/base-layouts/gantt/core.ts — Plane gantt types (already forked)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fork Plane 甘特图组件（blocks/chart/sidebar/views/contexts/data/helpers）</name>
  <files>
    - yh-flow/clients/web/app/components/issues/gantt-view/README.md (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/constants.ts (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/index.ts (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/root.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/blocks/ (CREATE directory + copy Plane blocks/*)
    - yh-flow/clients/web/app/components/issues/gantt-view/chart/ (CREATE directory + copy Plane chart/*)
    - yh-flow/clients/web/app/components/issues/gantt-view/sidebar/ (CREATE directory + copy Plane sidebar/*)
    - yh-flow/clients/web/app/components/issues/gantt-view/views/week-view.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/views/month-view.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/views/quarter-view.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/gantt-view/contexts/ (CREATE directory + copy Plane contexts/*)
    - yh-flow/clients/web/app/components/issues/gantt-view/data/ (CREATE directory + copy Plane data/*)
    - yh-flow/clients/web/app/components/issues/gantt-view/helpers/ (CREATE directory + copy Plane helpers/*)
  </files>
  <read_first>
    apps/web/core/components/gantt-chart/ — entire Plane gantt-chart directory (root.tsx is entry point)
    apps/web/core/components/gantt-chart/constants.ts — BLOCK_HEIGHT, HEADER_HEIGHT, SIDEBAR_WIDTH (360px) etc.
    apps/web/core/components/gantt-chart/views/ — week-view, month-view, quarter-view (day-view also if exists)
    yh-flow/clients/web/src/lib/types/base-layouts/gantt/core.ts — already-forked Plane gantt types
    yh-flow/clients/web/src/lib/types/base-layouts/gantt/extended.ts — check if it exists
  </read_first>
  <action>
    遵照 D-P17-02：从 Plane apps/web/core/components/gantt-chart/ 复制整个甘特图目录到 app/components/issues/gantt-view/。

    文件结构（每个文件添加 `// FLOW: Forked from Plane gantt-chart/` 标记）：

    README.md — 标识 fork 来源

    constants.ts — 复制 Plane gantt-chart/constants.ts：
    - BLOCK_HEIGHT = 44px (per UI-SPEC)
    - HEADER_HEIGHT = 48px (per UI-SPEC)
    - SIDEBAR_WIDTH = 360px (per UI-SPEC)
    - 其他常量保持 Plane 原始值

    index.ts — 复制 Plane gantt-chart/index.ts

    root.tsx — 复制 Plane gantt-chart/root.tsx（甘特图根组件入口）:
    - 使用 useIssues hook 获取 issue 数据
    - 按 start_date 排序
    - 计算默认时间范围（完成前 7 天 ~ 开始后 7 天 + 至今，保证上下文可见 per specific ideas）
    - 管理 zoom level（day/week/month/quarter）, 初始 default: "month"
    - 管理拖拽状态（移动 bar / resize bar ends）
    - 计算子 issue 进度百分比

    blocks/ — 复制 Plane gantt-chart/blocks/*：
    - issue-block.tsx — 甘特图 issue 方块组件（显示名称/进度条/拖拽句柄）
    - block-drag-handle.tsx — 拖拽调整起止时间的句柄组件

    chart/ — 复制 Plane gantt-chart/chart/*：
    - chart-main.tsx — 甘特图时间轴主要渲染区域
    - chart-today.tsx — 今天标记线（accent color per UI-SPEC）
    - chart-header.tsx — 时间轴顶部日期头

    sidebar/ — 复制 Plane gantt-chart/sidebar/*：
    - sidebar.tsx — 左侧 issue 列表（SIDEBAR_WIDTH=360px）
    - sidebar-item.tsx — 单行 issue 信息（标识符、名称、负责人头像）

    views/ — 复制 Plane gantt-chart/views/*：
    - week-view.tsx — 周视图
    - month-view.tsx — 月视图（默认）
    - quarter-view.tsx — 季度视图
    - (如果有) day-view.tsx — 天视图

    contexts/ — 复制 Plane gantt-chart/contexts/*：
    - gantt-context.tsx — 甘特图 React Context（管理 zoom/scroll/selected issue 等状态）

    data/ — 复制 Plane gantt-chart/data/*：
    - gantt-data.ts — 甘特图数据转换（issue → gantt bar 数据）

    helpers/ — 复制 Plane gantt-chart/helpers/*：
    - date-helper.ts — 日期计算工具（根据 zoom level 计算 ticks/columns）

    导入路径适配规则（同 Plan 17-02 日历的规则）：
    - `@/components/gantt-chart/` → `./`
    - `@/components/issues/issue-layouts/helpers/` → 内联替换
    - `@plane/types` / `@plane/ui` / `@plane/utils` → 保持
    - Plane `useIssues` → `@/../src/lib/hooks/use-issues` 的 `useIssues`
    - Plane store hooks → yh-flow `useStore` from `@/lib/store-context`

    甘特图特异适配：
    - 删除对 Plane 内部 store 的直接引用（@/store/issue/、@/store/project/ 等），替换为 yh-flow 等价
    - 依赖关系连线（GANT-02）保持 Plane 的 SVG 连线逻辑，Phase 17 为只读显示 read-only
    - zoom 切换 UI 放在 gantt-view.tsx 的 header 区域（天/周/月/季度按钮组 per UI-SPEC）
    - 所有修改处使用 `// FLOW:` 标记

  </action>
  <acceptance_criteria>
    1. gantt-view/ 目录创建，包含 blocks/chart/sidebar/views/contexts/data/helpers 子目录
    2. 导入路径适配完成
    3. `npx tsc --noEmit` 不新增 gantt 相关错误
    4. 4 种 zoom level view 文件存在
  </acceptance_criteria>
  <verify>
    <automated>ls -la app/components/issues/gantt-view/ && ls -la app/components/issues/gantt-view/blocks/ && ls -la app/components/issues/gantt-view/views/</automated>
    <automated>npx tsc --noEmit 2>&1 | grep "gantt" | grep -c "error" || echo "no new gantt errors"</automated>
    <automated>grep -c "FLOW:" app/components/issues/gantt-view/root.tsx</automated>
  </verify>
  <done>
    * 所有甘特图组件 fork 完成
    * 导入路径适配完成
    * 4 级缩放视图存在
    * 依赖关系连线 (GANT-02) 保持 Plane 实现
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 GanttView 主容器 + zoom 控制</name>
  <files>
    - yh-flow/clients/web/app/components/issues/gantt-view/gantt-view.tsx (CREATE)
    - yh-flow/clients/web/src/lib/mock-data.ts (MODIFY, ensure start_date/target_date on enough mock issues for gantt testing)
  </files>
  <read_first>
    apps/web/core/components/gantt-chart/root.tsx — Plane gantt root component (entry point)
    yh-flow/clients/web/app/components/issues/kanban-view.tsx — yh-flow view container pattern
    yh-flow/clients/web/app/components/issues/filters/use-group-by.ts — for future group-by integration
    yh-flow/clients/web/src/lib/mock-data.ts — current mock issues check
  </read_first>
  <action>
    创建 gantt-view.tsx — GanttView 主容器组件：

    1. Props: `{ workspaceId: string; projectId: string }`
    2. 使用 `useIssues(projectId)` 获取 issue 列表
    3. 筛选有 start_date 或 target_date 的 issue（甘特图需要日期范围）
    4. zoom level 管理:
       - 4 个等级：day / week / month / quarter (per D-P17-16)
       - 默认: "month"
       - 顶部 header 显示 zoom 按钮组（天/周/月/季度）
       - 点击切换 zoom level → 重新计算时间轴刻度
    5. 使用 Plane gantt root.tsx 的渲染逻辑：
       - 左侧 sidebar（SIDEBAR_WIDTH=360px）
       - 右侧 chart 区域（时间轴 + issue bars）
    6. 依赖关系连线（GANT-02）:
       - 如果 mock issue 包含 `blocked_by` / `blocks` 字段则显示 SVG 箭头连线
       - Phase 17 为只读显示 read-only（per CONTEXT deferred ideas）
    7. 加载/空/状态处理（per UI-SPEC）:
       - 加载中: skeleton bar placeholders
       - 空状态: "暂无 Issue" + 说明"此项目还没有需要展示甘特图的 Issue，请创建带起止日期的 Issue。"
       - 有 issue 但无日期: 也显示空状态
    8. 进度条计算:
       - 有子 issue 的 issue: 已关闭子 issue / 总子 issue 比例
       - 无子 issue: 0%（未完成）或 100%（已完成，completed_at 不为空）

    mock-data.ts 更新：
    - 确保 MOCK_ISSUES 有足够 issue 带 start_date + target_date（当前已有 issue-2, issue-5, issue-8, issue-9, issue-12, issue-16 等）
    - 为部分 issue 添加 parent_id 关系以测试进度条（如 issue-2 设置 parent_id 关联其子 issue）
    - 添加 `blocked_by` 字段到部分 issue 以测试依赖连线（GANT-02）
    - 检查 TIssue 类型是否包含 blocked_by/blocked_by_ids —— 如果不包含则在类型中扩展并添加 mock 数据

  </action>
  <acceptance_criteria>
    1. GanttView 组件导出 named export
    2. 4 级 zoom 切换工作
    3. 有 date 的 issue 显示为横向 bar
    4. 左侧 sidebar 显示 issue 列表
    5. 进度条按子 issue 比例显示
    6. `npx tsc --noEmit` 不新增 gantt 错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "GanttView" app/components/issues/gantt-view/gantt-view.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep "gantt" | grep -c "error" || echo "no gantt errors"</automated>
  </verify>
  <done>
    * GanttView 主容器组件完成
    * 4 级 zoom 切换（天/周/月/季度）
    * 依赖连线显示 (GANT-02)
    * 进度条计算
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID    | Category          | Component               | Disposition | Mitigation Plan                                                                          |
| ------------ | ----------------- | ----------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-17-GANT-01 | Denial of Service | Gantt chart rendering   | accept      | 大量 issue 导致甘特图性能下降是可接受的权衡（Plane 原始实现无虚拟滚动，Phase 17 不引入） |
| T-17-GANT-02 | Tampering         | Drag to resize/move bar | mitigate    | StrictMode ref guard 防止同一拖拽事件重复触发 updateIssue                                |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
ls app/components/issues/gantt-view/blocks/ | wc -l
ls app/components/issues/gantt-view/views/ | wc -l
grep -q "day\|week\|month\|quarter" app/components/issues/gantt-view/gantt-view.tsx
```
</verification>

<success_criteria>

- [ ] 完整 fork Plane gantt-chart 目录结构到 app/components/issues/gantt-view/
- [ ] 所有导入路径适配到 yh-flow
- [ ] GanttView 主容器完成，含 zoom 控制
- [ ] 依赖关系连线 (GANT-02)
- [ ] 进度条计算基于子 issue
- [ ] 左侧 sidebar 360px
- [ ] `npx tsc --noEmit` 不新增 gantt 错误

输出: app/components/issues/gantt-view/ 完整目录
</success_criteria>

<output>
创建 `.planning/phases/17-calendar-gantt-sheet/17-03-SUMMARY.md`
</output>
