---
phase: 17-calendar-gantt-sheet
plan: 06
type: execute
wave: 4
depends_on: [17-02, 17-03, 17-04, 17-05]
files_modified:
  - yh-flow/clients/web/app/issues/page.tsx
  - yh-flow/clients/web/app/components/issues/list-view.tsx
  - yh-flow/clients/web/app/components/issues/kanban-view.tsx
  - yh-flow/clients/web/app/hooks/use-view-switcher.ts
autonomous: true
requirements: [CALN-01, CALN-02, GANT-01, GANT-02, SHEE-01, SHEE-02, FILT-01, FILT-02, FILT-03, FILT-04, KANB-04]

must_haves:
  truths:
    - "Issues 页面视图切换按钮显示全部 5 种视图（列表/看板/日历/甘特/电子表格）"
    - "单击日历视图按钮 → 加载 CalendarView（lazy）"
    - "单击甘特视图按钮 → 加载 GanttView（lazy）"
    - "单击电子表格视图按钮 → 加载 SpreadsheetView（lazy）"
    - "列表视图使用新的 FilterBar 替换原有 QuickFilterBar"
    - "看板视图接入 useSubGroupBy 支持子分组（KANB-04）"
    - "所有视图共享统一的筛选/排序/分组状态"
    - "所有视图都可通过 FilterSaveModal 保存配置"
  artifacts:
    - path: "yh-flow/clients/web/app/issues/page.tsx"
      provides: "Updated issue page with all 5 views"
      modified: true
    - path: "yh-flow/clients/web/app/components/issues/list-view.tsx"
      provides: "Updated list-view using FilterBar"
      modified: true
    - path: "yh-flow/clients/web/app/components/issues/kanban-view.tsx"
      provides: "Updated kanban with subgroup support"
      modified: true
  key_links:
    - from: "issues/page.tsx"
      to: "calendar-view/calendar-view.tsx"
      via: "React.lazy import"
    - from: "issues/page.tsx"
      to: "gantt-view/gantt-view.tsx"
      via: "React.lazy import"
    - from: "issues/page.tsx"
      to: "spreadsheet-view/spreadsheet-view.tsx"
      via: "React.lazy import"
---

<objective>
将所有新视图（日历/甘特/电子表格）集成到 Issue 页面视图切换系统中，为需要筛选的视图接入 FilterBar，为看板添加子分组（KANB-04），完成 Phase 17 的整体集成。

目的: 将所有 Phase 17 功能集成到用户可访问的页面中，完成 5 种视图的切换、筛选引擎的接入、子分组支持。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-calendar-gantt-sheet/17-CONTEXT.md — D-P17-05, D-P17-06, D-P17-08~12
@.planning/phases/17-calendar-gantt-sheet/17-UI-SPEC.md — Interaction contract for all views

# Existing files to modify

@yh-flow/clients/web/app/issues/page.tsx — current 2-view toggle
@yh-flow/clients/web/app/components/issues/list-view.tsx — current filter+sort usage
@yh-flow/clients/web/app/components/issues/kanban-view.tsx — current kanban (needs subgroup)
@yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx — new filter bar

# New views to integrate (from Plans 02-04)

@yh-flow/clients/web/app/components/issues/calendar-view/calendar-view.tsx
@yh-flow/clients/web/app/components/issues/gantt-view/gantt-view.tsx
@yh-flow/clients/web/app/components/issues/spreadsheet-view/spreadsheet-view.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 更新 Issue 页面支持 5 种视图切换</name>
  <files>
    - yh-flow/clients/web/app/issues/page.tsx (MODIFY)
    - yh-flow/clients/web/app/hooks/use-view-switcher.ts (CREATE)
    - yh-flow/clients/web/app/store/issue.store.ts (MODIFY if needed for activeView sync)
  </files>
  <read_first>
    yh-flow/clients/web/app/issues/page.tsx — current view toggle (lines 1-95)
    yh-flow/clients/web/app/components/issues/filters/types.ts — TViewLayout type
    yh-flow/clients/web/app/store/issue.store.ts — activeView and setActiveView
  </read_first>
  <action>
    更新 `app/issues/page.tsx`（per D-P17-05：延用 Phase 16 Issue Layout HOC 模式）：

    导入变更：
    - 添加 CalendarDays、GitBranch（或 GitFork）、Table 图标 from lucide-react（用于日历/甘特/电子表格）
    - 添加 3 个 React.lazy views：
      `const IssuesCalendarView = lazy(() => import("@/components/issues/calendar-view/calendar-view"));`
      `const IssuesGanttView = lazy(() => import("@/components/issues/gantt-view/gantt-view"));`
      `const IssuesSpreadsheetView = lazy(() => import("@/components/issues/spreadsheet-view/spreadsheet-view"));`

    视图切换按钮扩展（当前已有 列表 + 看板）：
    完整 5 按钮 view toggle：
    | Icon | Label | activeView value |
    |------|-------|-----------------|
    | List | 列表 | "list" |
    | Kanban | 看板 | "kanban" |
    | CalendarDays | 日历 | "calendar" |
    | GitBranch | 甘特 | "gantt" |
    | Table | 表格 | "spreadsheet" |

    视图渲染区域扩展（当前只有 列表 + 看板 的 conditional render）：
    ```tsx
    {store.issue.activeView === "list" && <IssueListView ... />}
    {store.issue.activeView === "kanban" && <Suspense ...><IssuesKanbanView ... /></Suspense>}
    {store.issue.activeView === "calendar" && <Suspense ...><IssuesCalendarView ... /></Suspense>}
    {store.issue.activeView === "gantt" && <Suspense ...><IssuesGanttView ... /></Suspense>}
    {store.issue.activeView === "spreadsheet" && <Suspense ...><IssuesSpreadsheetView ... /></Suspense>}
    ```

    所有 Suspense fallback 统一为 "加载中..."（per UI-SPEC）。

    每个视图按钮使用 cn() 做条件 className：
    - 激活状态: bg-custom-background-100, text-custom-text-100, shadow-sm
    - 非激活: text-custom-text-400, hover:text-custom-text-200

    创建 `app/hooks/use-view-switcher.ts`（可选抽象，提高可维护性）：
    如果 page.tsx 变得过大，将视图切换逻辑抽取为 useViewSwitcher hook：
    - `activeView: TViewLayout` — 当前视图
    - `viewOptions: Array<{ value: TViewLayout; label: string; icon: React.ComponentType }>` — 全部 5 个视图配置
    - `setActiveView(view: TViewLayout): void` — 切换视图

    注意：保持与 IssueStore.activeView 的同步——切换视图时调用 store.issue.setActiveView(newView)。
    **不**删除现有功能。确保所有 3 个新视图使用 `React.lazy` + `Suspense` 懒加载。

  </action>
  <acceptance_criteria>
    1. 页面顶部 view toggle 显示 5 个按钮
    2. 单击日历/甘特/电子表格按钮显示对应新视图
    3. 列表/看板视图保持原有功能
    4. `npx tsc --noEmit` 零新增错误
    5. 所有视图通过 React.lazy 懒加载
  </acceptance_criteria>
  <verify>
    <automated>grep -c "calendar\|gantt\|spreadsheet" app/issues/page.tsx && echo "views present"</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
    <automated>grep -c "lazy" app/issues/page.tsx</automated>
  </verify>
  <done>
    * Issue 页面支持 5 种视图切换
    * 3 个新视图通过 lazy 加载
    * 列表/看板视图不受影响
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 更新列表视图接入 FilterBar + 看板接入子分组</name>
  <files>
    - yh-flow/clients/web/app/components/issues/list-view.tsx (MODIFY)
    - yh-flow/clients/web/app/components/issues/kanban-view.tsx (MODIFY)
  </files>
  <read_first>
    yh-flow/clients/web/app/components/issues/list-view.tsx — current list view with QuickFilterBar and sort
    yh-flow/clients/web/app/components/issues/kanban-view.tsx — current kanban view
    yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx — new FilterBar
    yh-flow/clients/web/app/components/issues/filters/use-group-by.ts — groupBy hooks
    yh-flow/clients/web/app/components/issues/filters/use-sub-group-by.ts — subgroup hooks
    yh-flow/clients/web/app/components/issues/filters/use-filters.ts — useFilters hook
    yh-flow/clients/web/app/components/issues/filters/use-sorting.ts — useSorting hook
    yh-flow/clients/web/app/components/issues/filters/column-selector.tsx — column selector
    yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx — save view modal
  </read_first>
  <action>
    更新 `app/components/issues/list-view.tsx`：

    导入变更：
    - 替换 QuickFilterBar import → FilterBar import
    - 添加 useFilters, useSorting imports
    - 添加 useStore 导入（已有）

    组件变更：
    1. 使用 `useFilters()` 和 `useSorting()` 替换从 store.issue.filters 直接读取的模式
    2. 使用 FilterBar 替换 QuickFilterBar：
       ```tsx
       <FilterBar
         workspaceId={workspaceId}
         projectId={projectId}
         viewType="list"
         showSorting={true}
         showGroupBy={false}
       />
       ```
    3. 移除原有的 QuickFilterBar 渲染代码和排序按钮组
    4. 在页面右侧添加 "自定义列" 按钮（打开 ColumnSelector）和 "保存视图" 按钮（打开 FilterSaveModal）
    5. 将 store.issue.filters 读取改为使用 useFilters 返回值（向后兼容：读 useFilters 同时更新 store）
    6. 保持 BulkActionBar、Pagination、IssueRow 等不变

    更新 `app/components/issues/kanban-view.tsx`（KANB-04 子分组/Swimlane）：

    导入变更：
    - 添加 `import { useSubGroupBy } from "@/components/issues/filters/use-sub-group-by";`
    - 添加 FilterBar import

    组件变更：
    1. 在 kanban header 区域添加 FilterBar：
       ```tsx
       <FilterBar workspaceId={workspaceId} projectId={projectId} viewType="kanban" showSorting={false} showGroupBy={true} />
       ```
    2. 添加子分组（Swimlane）支持（KANB-04）：
       - 在 GroupBy selector 旁边添加 "子分组" dropdown
       - 使用 useSubGroupBy hook 管理子分组状态
       - 选择子分组类型后，每个列内再按子分组划分 swimlane
       - 子分组选项: None / State / Priority（per TSubGroupByOptions）
       - 子分组后每列的 issue 按 swimlane 分区显示
    3. 保持现有拖拽和筛选逻辑不变
    4. 在每个列的 header 添加子分组标签行

    对两个组件共享的注意事项：
    - useFilters hook 的 URL 同步应自动处理筛选状态的 URL 持久化
    - ColumnSelector 的打开/关闭状态用本地 useState
    - FilterSaveModal 在当前 view 打开时使用当前 filter/sort/column 配置

  </action>
  <acceptance_criteria>
    1. list-view 使用 FilterBar 替代 QuickFilterBar
    2. list-view 显示"自定义列"和"保存视图"按钮
    3. kanban-view 显示 FilterBar + GroupBy 选择器
    4. kanban-view 子分组 dropdown 工作（KANB-04）
    5. `npx tsc --noEmit` 零新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "FilterBar" app/components/issues/list-view.tsx</automated>
    <automated>grep -q "FilterSaveModal\|ColumnSelector" app/components/issues/list-view.tsx || echo "may be in page.tsx"</automated>
    <automated>grep -q "useSubGroupBy" app/components/issues/kanban-view.tsx || echo "subgroup not integrated"</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    * 列表视图使用新的 FilterBar
    * 看板视图接入子分组 (KANB-04)
    * 所有视图可用"保存视图"按钮
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID   | Category          | Component            | Disposition | Mitigation Plan                                         |
| ----------- | ----------------- | -------------------- | ----------- | ------------------------------------------------------- |
| T-17-INT-01 | Denial of Service | React.lazy loading   | accept      | 3 个新视图懒加载，不会影响首屏加载时间                  |
| T-17-INT-02 | Tampering         | View switching state | mitigate    | ImageStore.activeView 使用 MobX action 管理，确保一致性 |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit

# Verify 5 views in page.tsx

grep -c "calendar\|gantt\|spreadsheet\|list\|kanban" app/issues/page.tsx
grep -c "lazy" app/issues/page.tsx

# Verify list-view uses FilterBar

grep -c "FilterBar" app/components/issues/list-view.tsx
grep -c "FilterSaveModal" app/components/issues/list-view.tsx

# Verify kanban subgroup

grep -c "subGroup" app/components/issues/kanban-view.tsx

```
</verification>

<success_criteria>
- [ ] Issue 页面 view toggle 包含全部 5 种视图
- [ ] 3 个新视图通过 React.lazy + Suspense 加载
- [ ] 列表视图使用 FilterBar（替换 QuickFilterBar）
- [ ] 看板视图接入子分组（KANB-04）
- [ ] 所有视图可通过 ColumnSelector 自定义列
- [ ] 所有视图可通过 FilterSaveModal 保存配置
- [ ] `npx tsc --noEmit` 零新增错误

输出: 更新后的 issues/page.tsx, list-view.tsx, kanban-view.tsx
</success_criteria>

<output>
创建 `.planning/phases/17-calendar-gantt-sheet/17-06-SUMMARY.md`
</output>
```
