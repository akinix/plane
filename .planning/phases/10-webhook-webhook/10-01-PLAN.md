# Phase 10: Webhook — Plan 10-01: 集成测试

**Wave:** 1 (single wave)
**Dependencies:** None — Webhook module already complete
**Requirements:** REQ-10.1 ~ REQ-10.2

---

## Objective

为现有 Webhook 模块添加集成测试，覆盖完整的事件驱动投递链路。

## Tasks

### T10.01: WebhookFixture + InMemory 测试基础设施

- 创建 `WebhookTestFixture`（沿袭 PageTestFixture / ViewTestFixture 模式）
- 配置 InMemory EF Core DbContext（独立数据库名 per test method → Guid 隔离）
- 注册服务：WebhookDbContext, IWebhookSecretProtector, IWebhookDispatcher（NSubstitute mock）
- 参考文件：
  - `yh-flow/src/Tests/Webhooks.Tests/`（现有单元测试模式）
  - `yh-flow/src/Tests/View.Tests/Fixtures/ViewTestFixture.cs`（InMemory fixture 模式）

### T10.02: Subscription CRUD 集成测试

- **CreateSubscription** — 创建订阅 → 验证 DbContext 中存在且字段正确
- **CreateSubscription** — 不合法 URL 拒绝（相对路径、无 scheme）
- **CreateSubscription** — 空事件列表拒绝
- **DeleteSubscription** — 删除后订阅不可用（不返回已删除订阅）
- **DeleteSubscription** — 重复删除不报错（幂等）
- **GetSubscriptions** — 返回活跃订阅列表
- **GetSubscriptions** — 排除已删除订阅

### T10.03: 事件 Fanout + 投递集成测试

- **FanoutHandler 匹配** — 发布 IIntegrationEvent → 验证匹配订阅的 DispatchJob 入队
- **FanoutHandler 不匹配** — 无匹配订阅时不入队
- **FanoutHandler 通配符** — 事件类型 `*` 匹配所有事件
- **FanoutHandler 不活跃订阅** — IsActive=false 的不触发投递
- **FanoutHandler 跳过 null TenantId** — 全局事件不触发

### T10.04: DispatchJob 集成测试

- **DispatchJob 成功投递** — HttpClient mock → 200 响应 → WebhookDelivery.Success=true
- **DispatchJob 临时错误重试** — 5xx 响应 → 抛出异常 → Hangfire 重试
- **DispatchJob 永久错误** — 4xx(非408/429) 响应 → 不抛出异常
- **DispatchJob 签名** — 验证 X-Webhook-Signature header 存在且为 HMAC-SHA256 格式

### T10.05: 验证

- 所有测试全部通过
- 构建 0 错误
- 回归验证 Webhooks.Tests 原 54 个单元测试不受影响

---

## Files to Create

```
yh-flow/src/Tests/Webhooks.Tests/Fixtures/
  WebhookTestFixture.cs
yh-flow/src/Tests/Webhooks.Tests/Integration/
  SubscriptionTests.cs
  FanoutTests.cs
  DispatchJobTests.cs
```

## Files to Modify

- `yh-flow/src/Tests/Webhooks.Tests/GlobalUsings.cs` — 添加新 using

## Verification Criteria

- [ ] 所有新集成测试通过（≥15 个新测试）
- [ ] 原 54 个单元测试仍全部通过
- [ ] 构建 0 错误 0 警告

## Test Gate

```
dotnet test src/Tests/Webhooks.Tests/Webhooks.Tests.csproj --nologo --verbosity normal
```
