---
phase: 06-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/ModuleConstants.cs
  - src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/ModuleDto.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/Module.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleIssue.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleMember.cs
  - src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleLink.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleIssueConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleMemberConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleLinkConfiguration.cs
  - src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
  - src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
  - src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/*_AddModules.cs
  - src/Tests/WorkItems.Tests/Domain/ModuleDomainTests.cs
  - src/Tests/WorkItems.Tests/Domain/ModuleIssueDomainTests.cs
  - src/Tests/WorkItems.Tests/Domain/ModuleMemberDomainTests.cs
  - src/Tests/WorkItems.Tests/Domain/ModuleLinkDomainTests.cs
  - src/Tests/WorkItems.Tests/TestData/TestModuleFactory.cs
autonomous: true
requirements: [REQ-6.1, REQ-6.2]
user_setup: []
must_haves:
  truths:
    - "Module entity exists with all fields（Id, Name, Description, Status, StartDate, TargetDate, SortOrder, LeadId, ProjectId, ArchivedAt, Version）and behavior methods（Create, Update, Archive, Unarchive, SoftDelete）"
    - "ModuleIssue bridge entity exists with soft-delete support（IHasTenant + ISoftDeletable）"
    - "ModuleMember M2M through entity exists（IHasTenant + ISoftDeletable）"
    - "ModuleLink entity exists（IHasTenant + ISoftDeletable）with Title/Url/Metadata fields"
    - "Module 实体纳入 WorkItemsDbContext（yhschema.WorkItems schema），与 Issue/Cycle 共享同一个 DbContext"
    - "WorkItemsDbContext 暴露 Modules/ModuleIssues/ModuleMembers/ModuleLinks DbSet 属性"
    - "EF 迁移 AddModules 在 yhschema.WorkItems 创建 Modules/ModuleIssues/ModuleMembers/ModuleLinks 表（含正确的唯一索引）"
    - "Module 常量类定义验证限制（NameMaxLength=255, DescriptionMaxLength=10000, DefaultSortOrder=65535.0）"
    - "ModuleDto 定义所有注释字段（total_issues, completed_issues, status 等）"
    - "dotnet build 0 errors"
  artifacts:
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/Module.cs"
      provides: "Module domain entity with IHasTenant, ISoftDeletable, IAuditableEntity"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleIssue.cs"
      provides: "ModuleIssue bridge entity with IHasTenant, ISoftDeletable"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleMember.cs"
      provides: "ModuleMember M2M through entity with IHasTenant, ISoftDeletable"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleLink.cs"
      provides: "ModuleLink entity with IHasTenant, ISoftDeletable"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleConfiguration.cs"
      provides: "EF Core configuration for Module entity with unique indexes"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleIssueConfiguration.cs"
      provides: "EF Core configuration for ModuleIssue with conditional unique index"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleMemberConfiguration.cs"
      provides: "EF Core configuration for ModuleMember"
    - path: "src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleLinkConfiguration.cs"
      provides: "EF Core configuration for ModuleLink"
    - path: "src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/ModuleConstants.cs"
      provides: "Module validation constants"
    - path: "src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/ModuleDto.cs"
      provides: "Module response DTO with annotation fields"
  key_links:
    - from: "WorkItemsDbContext"
      to: "ModuleConfiguration + ModuleIssueConfiguration + ModuleMemberConfiguration + ModuleLinkConfiguration"
      via: "ApplyConfigurationsFromAssembly in OnModelCreating"
    - from: "ModuleConfiguration"
      to: "WorkItemsModuleConstants.SchemaName"
      via: 'ToTable("Modules", WorkItemsModuleConstants.SchemaName)'
    - from: "ModuleIssueConfiguration"
      to: "WorkItemsModuleConstants.SchemaName"
      via: 'ToTable("ModuleIssues", WorkItemsModuleConstants.SchemaName)'
    - from: "ModuleConfiguration"
      to: "唯一索引"
      via: "(TenantId, ProjectId, Name) HasFilter + (TenantId, ProjectId, ArchivedAt) query index"
---

<objective>
创建 Module、ModuleIssue、ModuleMember、ModuleLink 领域实体，EF 配置，迁移，Contracts DTOs，以及测试脚手架。

Purpose: 为 Phase 6 所有 Module CRUD 和 Module-Issue 关联端点提供实体基础和持久化能力。Module 纳入 WorkItemsDbContext（yhschema.WorkItems schema），与 Issue/Cycle 共享同一个 DbContext。
Output: Module.cs, ModuleIssue.cs, ModuleMember.cs, ModuleLink.cs, 4 个 EF 配置, AddModules 迁移, Contracts DTOs, 测试脚手架
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cycle/05-01-SUMMARY.md
@.planning/phases/06-module/06-CONTEXT.md

# Existing reference files（绝对路径）：

@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/Cycle.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/CycleIssue.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleConfiguration.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleIssueConfiguration.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/CycleConstants.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleDto.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
@D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/WorkItemsModuleConstants.cs
@D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/TestFactories.cs
@D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/WorkItemsTestFixture.cs
@D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/Domain/CycleDomainTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModuleContracts（Constants + ModuleDto）+ WorkItemsModule Route stub</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/CycleConstants.cs
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/CycleDto.cs
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
  </read_first>
  <files>
    NEW: src/Modules/WorkItems/Modules.WorkItems.Contracts/Constants/ModuleConstants.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems.Contracts/DTOs/ModuleDto.cs
    MODIFIED: src/Modules/WorkItems/Modules.WorkItems/WorkItemsModule.cs
  </files>
  <action>
    创建 Module 常量类和响应 DTO，添加 Module 路由组桩代码到 WorkItemsModule.cs。

    **ModuleConstants.cs（namespace: YH.Modules.WorkItems.Contracts.Constants）:**
    - `NameMaxLength = 255`
    - `DescriptionMaxLength = 10000`
    - `DefaultSortOrder = 65535.0`
    - `DefaultStatus = "planned"`
    - `StatusMaxLength = 20`（最长 "in-progress" 11 字符，预留空间）

    **ModuleDto.cs（namespace: YH.Modules.WorkItems.Contracts.DTOs）:**
    - 字段集严格对应 Module 实体属性:
      - `Id(Guid)`, `Name(string)`, `Description(string?)`
      - `Status(string)` — 默认 "planned"
      - `StartDate(DateTimeOffset?)`, `TargetDate(DateTimeOffset?)`
      - `SortOrder(double)` — 默认 65535.0
      - `LeadId(Guid?)` — 负责人
      - `ProgressSnapshot(string?)`, `ArchivedAt(DateTimeOffset?)`
      - `LogoProps(string?)`, `Version(int)` — 默认 1
      - `ProjectId(Guid)`
    - 注释字段（查询时计算）:
      - `TotalIssues(int)`, `CompletedIssues(int)`, `CancelledIssues(int)`
      - `StartedIssues(int)`, `UnstartedIssues(int)`, `BacklogIssues(int)`
    - JSON 序列化: `[JsonPropertyName("created_at")]` on `CreatedAt(DateTimeOffset)`, `[JsonPropertyName("updated_at")]` on `UpdatedAt(DateTimeOffset?)`
    - 命名风格: snake_case via JsonPropertyName，Plane 兼容
    - 与 CycleDto 模式一致，使用 List 类型

    **WorkItemsModule.cs 修改:**
    - 在 `MapEndpoints` 中添加 Module 路由组桩代码（实际端点在 06-02/06-03 接入）:
      ```csharp
      // Module route group (Phase 6) — endpoints registered in plans 06-02 and 06-03
      var modules = endpoints
          .MapGroup("api/v{version:apiVersion}/workspaces/{slug}/projects/{projectId}/modules")
          .WithTags("Modules")
          .WithApiVersionSet(apiVersionSet);
      ```
    - 在 `ConfigureServices` 中不需要额外注册（Module 使用 WorkItemsDbContext，已有）

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error" -NotMatch</automated>
  </verify>
  <done>
    - ModuleConstants.cs, ModuleDto.cs 文件存在
    - WorkItemsModule.cs 包含 modules 路由组桩代码
    - 构建 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Module/ModuleIssue/ModuleMember/ModuleLink domain entities + EF configurations + WorkItemsDbContext update</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/Cycle.cs
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Domain/CycleIssue.cs
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleConfiguration.cs
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/CycleIssueConfiguration.cs
    @D:/github/akinix-plane/yh-flow/src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
  </read_first>
  <files>
    NEW: src/Modules/WorkItems/Modules.WorkItems/Domain/Module.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleIssue.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleMember.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Domain/ModuleLink.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleConfiguration.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleIssueConfiguration.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleMemberConfiguration.cs
    NEW: src/Modules/WorkItems/Modules.WorkItems/Data/Configurations/ModuleLinkConfiguration.cs
    MODIFIED: src/Modules/WorkItems/Modules.WorkItems/Data/WorkItemsDbContext.cs
  </files>
  <action>
    创建 4 个领域实体和 4 个 EF 配置，更新 WorkItemsDbContext。严格遵循 Cycle 领域实体模式。

    **Module.cs（namespace: YH.Modules.WorkItems.Domain）:**
    - 实现 `IHasTenant`, `ISoftDeletable`, `IAuditableEntity`
    - 字段（私有 setter，符合 Domain 模式）:
      - `Id(Guid)`, `Name(string)`, `Description(string?)`
      - `Status(string)` — 默认 "planned"
      - `StartDate(DateTimeOffset?)`, `TargetDate(DateTimeOffset?)`
      - `SortOrder(double)` — 默认 65535.0
      - `LeadId(Guid?)` — 负责人（标量，无 FK 导航）
      - `ProgressSnapshot(string?)`, `ArchivedAt(DateTimeOffset?)`, `LogoProps(string?)`
      - `Version(int)` — 默认 1，乐观并发令牌
      - `ProjectId(Guid)` — 标量 FK，无跨模块导航
    - IHasTenant: `public string TenantId { get; private set; } = default!;`
    - IAuditableEntity: CreatedOnUtc, CreatedBy, LastModifiedOnUtc, LastModifiedBy
    - ISoftDeletable: IsDeleted, DeletedOnUtc, DeletedBy
    - 私有构造函数 `private Module() { }` — EF Core
    - **工厂方法 `Create`:**
      ```csharp
      public static Module Create(
          string name,
          Guid projectId,
          string? status = null,
          DateTimeOffset? startDate = null,
          DateTimeOffset? targetDate = null,
          string? description = null,
          Guid? leadId = null)
      ```
      - 验证: name 非空, projectId 非空
      - Status 默认 "planned"
      - Module **无** date 配对规则（与 Cycle 不同 — Module 的 StartDate/TargetDate 独立可为 null）
    - **方法:**
      - `Update(string? name, string? description, string? status, DateTimeOffset? startDate, DateTimeOffset? targetDate, Guid? leadId, double? sortOrder)`: PATCH 语义（null 跳过）。注意: Module 无 COMPLETED 编辑限制（与 Cycle 不同）
      - `UpdateStatus(string? status)`: 单独的 status 更新方法（PATCH 语义，null 跳过）
      - `Archive()`: `ArchivedAt = DateTimeOffset.UtcNow`
      - `Unarchive()`: `ArchivedAt = null`
      - `FreezeSnapshot(string snapshotJson)`: `ProgressSnapshot = snapshotJson`
      - `SoftDelete(DateTimeOffset now)`: 标准软删除，幂等

    **ModuleIssue.cs（namespace: YH.Modules.WorkItems.Domain）:**
    - 实现 `IHasTenant`, `ISoftDeletable`
    - 字段: `Id(Guid)`, `IssueId(Guid)`, `ModuleId(Guid)`, `CreatedOnUtc(DateTimeOffset)`
    - IHasTenant: `public string TenantId { get; private set; } = default!;`
    - ISoftDeletable: IsDeleted, DeletedOnUtc, DeletedBy
    - 私有构造函数
    - **工厂方法**: `public static ModuleIssue Create(Guid issueId, Guid moduleId)` — 验证非空 Guid
    - **方法**: `SoftDelete(DateTimeOffset now)` — 幂等

    **ModuleMember.cs（namespace: YH.Modules.WorkItems.Domain）:**
    - 实现 `IHasTenant`, `ISoftDeletable`
    - 字段: `Id(Guid)`, `MemberId(Guid)`, `ModuleId(Guid)`, `CreatedOnUtc(DateTimeOffset)`
    - IHasTenant: `public string TenantId { get; private set; } = default!;`
    - ISoftDeletable: IsDeleted, DeletedOnUtc, DeletedBy
    - 私有构造函数
    - **工厂方法**: `public static ModuleMember Create(Guid memberId, Guid moduleId)` — 验证非空 Guid
    - **方法**: `SoftDelete(DateTimeOffset now)` — 幂等

    **ModuleLink.cs（namespace: YH.Modules.WorkItems.Domain）:**
    - 实现 `IHasTenant`, `ISoftDeletable`
    - 字段: `Id(Guid)`, `Title(string, max 255)`, `Url(string, max 2048)`, `Metadata(string?)`（JSON 字符串）
    - `ModuleId(Guid)`, `CreatedOnUtc(DateTimeOffset)`
    - IHasTenant: `public string TenantId { get; private set; } = default!;`
    - ISoftDeletable: IsDeleted, DeletedOnUtc, DeletedBy
    - 私有构造函数
    - **工厂方法**: `public static ModuleLink Create(string title, string url, string? metadata, Guid moduleId)` — 验证 title 非空, url 非空
    - **方法**: `Update(string? title, string? url, string? metadata)` — PATCH 语义

    **ModuleConfiguration.cs（namespace: YH.Modules.WorkItems.Data.Configurations）:**
    - `ToTable("Modules", WorkItemsModuleConstants.SchemaName)`
    - `HasKey(x => x.Id)`
    - 属性:
      - `Name`: IsRequired, HasMaxLength(ModuleConstants.NameMaxLength) — 255
      - `SortOrder`: IsRequired, HasDefaultValue(ModuleConstants.DefaultSortOrder) — 65535.0
      - `Status`: IsRequired, HasMaxLength(ModuleConstants.StatusMaxLength) — 20, HasDefaultValue("planned")
      - `Version`: IsRequired, HasDefaultValue(1), `IsConcurrencyToken()`
      - `ArchivedAt`: IsRequired(false)
      - 审计列: CreatedOnUtc(IsRequired), CreatedBy(HasMaxLength(450)), LastModifiedBy(HasMaxLength(450)), DeletedBy(HasMaxLength(450))
    - 索引:
      - **唯一索引 (TenantId, ProjectId, Name)** 仅对未删除记录:
        ```csharp
        builder.HasIndex(x => new { x.TenantId, x.ProjectId, x.Name })
            .IsUnique()
            .HasDatabaseName("IX_Modules_Tenant_Project_Name")
            .HasFilter("[DeletedOnUtc] IS NULL");
        ```
      - **查询索引 (TenantId, IsDeleted, ProjectId):**
        ```csharp
        builder.HasIndex(x => new { x.TenantId, x.IsDeleted, x.ProjectId })
            .HasDatabaseName("IX_Modules_Tenant_Deleted_Project");
        ```
      - **查询索引 (TenantId, ProjectId, SortOrder):**
        ```csharp
        builder.HasIndex(x => new { x.TenantId, x.ProjectId, x.SortOrder })
            .HasDatabaseName("IX_Modules_Tenant_Project_SortOrder");
        ```
      - **归档查询索引 (TenantId, ProjectId, ArchivedAt):**
        ```csharp
        builder.HasIndex(x => new { x.TenantId, x.ProjectId, x.ArchivedAt })
            .HasDatabaseName("IX_Modules_Tenant_Project_Archived");
        ```

    **ModuleIssueConfiguration.cs（namespace: YH.Modules.WorkItems.Data.Configurations）:**
    - `ToTable("ModuleIssues", WorkItemsModuleConstants.SchemaName)`
    - `HasKey(x => x.Id)`
    - 属性: IssueId(IsRequired), ModuleId(IsRequired), CreatedOnUtc(IsRequired)
    - **条件唯一索引**（per D-06: (TenantId, ModuleId, IssueId) WHERE deleted_at IS NULL）:
      ```csharp
      builder.HasIndex(x => new { x.TenantId, x.ModuleId, x.IssueId })
          .IsUnique()
          .HasDatabaseName("IX_ModuleIssues_Tenant_Module_Issue")
          .HasFilter("[DeletedOnUtc] IS NULL");
      ```

    **ModuleMemberConfiguration.cs（namespace: YH.Modules.WorkItems.Data.Configurations）:**
    - `ToTable("ModuleMembers", WorkItemsModuleConstants.SchemaName)`
    - `HasKey(x => x.Id)`
    - 属性: MemberId(IsRequired), ModuleId(IsRequired), CreatedOnUtc(IsRequired)
    - 条件唯一索引 (TenantId, ModuleId, MemberId):
      ```csharp
      builder.HasIndex(x => new { x.TenantId, x.ModuleId, x.MemberId })
          .IsUnique()
          .HasDatabaseName("IX_ModuleMembers_Tenant_Module_Member")
          .HasFilter("[DeletedOnUtc] IS NULL");
      ```

    **ModuleLinkConfiguration.cs（namespace: YH.Modules.WorkItems.Data.Configurations）:**
    - `ToTable("ModuleLinks", WorkItemsModuleConstants.SchemaName)`
    - `HasKey(x => x.Id)`
    - 属性: Title(IsRequired, HasMaxLength(255)), Url(IsRequired, HasMaxLength(2048)), Metadata(nvarchar(max)), ModuleId(IsRequired), CreatedOnUtc(IsRequired)
    - 查询索引 (TenantId, IsDeleted, ModuleId):
      ```csharp
      builder.HasIndex(x => new { x.TenantId, x.IsDeleted, x.ModuleId })
          .HasDatabaseName("IX_ModuleLinks_Tenant_Deleted_Module");
      ```

    **WorkItemsDbContext.cs 修改:**
    - 在 CycleIssues DbSet 之后添加 4 个 DbSet 属性:
      ```csharp
      /// <summary>Modules table (Phase 6).</summary>
      public DbSet<Module> Modules => Set<Module>();

      /// <summary>Module-Issue M2M through table (Phase 6).</summary>
      public DbSet<ModuleIssue> ModuleIssues => Set<ModuleIssue>();

      /// <summary>Module-Member M2M through table (Phase 6).</summary>
      public DbSet<ModuleMember> ModuleMembers => Set<ModuleMember>();

      /// <summary>Module-Link table (Phase 6).</summary>
      public DbSet<ModuleLink> ModuleLinks => Set<ModuleLink>();
      ```
    - 不需要修改 OnModelCreating — ApplyConfigurationsFromAssembly 自动发现新配置
    - 验证 `using YH.Modules.WorkItems.Domain;` 已存在

    **注意**: 不修改任何现有实体或配置，只新增 Module 相关文件。

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error" -NotMatch</automated>
  </verify>
  <done>
    - Module.cs, ModuleIssue.cs, ModuleMember.cs, ModuleLink.cs 4 个领域实体文件存在
    - 4 个 EF 配置文件的索引和唯一约束正确
    - WorkItemsDbContext 包含 Modules/ModuleIssues/ModuleMembers/ModuleLinks DbSet
    - 构建 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate AddModules EF migration + Create test scaffolds（TestModuleFactory + Domain tests for all 4 entities）</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/TestFactories.cs
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/TestData/WorkItemsTestFixture.cs
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/Domain/CycleDomainTests.cs
    @D:/github/akinix-plane/yh-flow/src/Tests/WorkItems.Tests/Domain/CycleIssueDomainTests.cs
    @D:/github/akinix-plane/yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/ — 查看最近迁移文件以了解命名模式
  </read_first>
  <files>
    NEW: src/Tests/WorkItems.Tests/Domain/ModuleDomainTests.cs
    NEW: src/Tests/WorkItems.Tests/Domain/ModuleIssueDomainTests.cs
    NEW: src/Tests/WorkItems.Tests/Domain/ModuleMemberDomainTests.cs
    NEW: src/Tests/WorkItems.Tests/Domain/ModuleLinkDomainTests.cs
    NEW: src/Tests/WorkItems.Tests/TestData/TestModuleFactory.cs
    NEW: src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/*_AddModules.cs
  </files>
  <action>
    生成 EF 迁移并创建测试工厂 + 4 组领域实体单元测试。

    **EF 迁移生成:**
    - 在 yh-flow 目录执行（使用 PowerShell）:
      ```powershell
      dotnet ef migrations add AddModules --project src/Host/YH.Flow.Migrations.PostgreSQL --startup-project src/Host/YH.Flow.Api --output-dir WorkItems
      ```
    - 迁移在 `yhschema.WorkItems` 创建 "Modules", "ModuleIssues", "ModuleMembers", "ModuleLinks" 表
    - 验证迁移生成的 Up() 包含:
      - Modules 表: (Id, Name, Description, Status, StartDate, TargetDate, SortOrder, LeadId, ProgressSnapshot, ArchivedAt, LogoProps, Version, ProjectId, TenantId, 审计列)
      - ModuleIssues 表: (Id, IssueId, ModuleId, CreatedOnUtc, TenantId, 软删除列)
      - ModuleMembers 表: (Id, MemberId, ModuleId, CreatedOnUtc, TenantId, 软删除列)
      - ModuleLinks 表: (Id, Title, Url, Metadata, ModuleId, CreatedOnUtc, TenantId, 软删除列)
      - 所有唯一索引和查询索引

    **TestModuleFactory.cs（namespace: YH.Tests.WorkItems.TestData）:**
    ```csharp
    internal static class TestModuleFactory
    {
        private static readonly Guid DefaultProjectId = Guid.Parse("00000000-0000-0000-0000-000000000001");

        public static Module CreateValid(
            string name = "Sprint Planning",
            Guid? projectId = null,
            string? status = null,
            DateTimeOffset? startDate = null,
            DateTimeOffset? targetDate = null)
        {
            return Module.Create(name, projectId ?? DefaultProjectId, status,
                startDate ?? DateTimeOffset.UtcNow,
                targetDate ?? DateTimeOffset.UtcNow.AddDays(14));
        }

        public static Module CreatePlanned(
            string name = "Q2 Features",
            Guid? projectId = null)
        {
            return Module.Create(name, projectId ?? DefaultProjectId,
                status: "planned", startDate: DateTimeOffset.UtcNow.AddDays(7),
                targetDate: DateTimeOffset.UtcNow.AddDays(28));
        }

        public static Module CreateCompleted(
            string name = "Completed Module",
            Guid? projectId = null)
        {
            return Module.Create(name, projectId ?? DefaultProjectId,
                status: "completed",
                startDate: DateTimeOffset.UtcNow.AddDays(-30),
                targetDate: DateTimeOffset.UtcNow.AddDays(-1));
        }
    }

    internal static class TestModuleIssueFactory
    {
        public static ModuleIssue CreateValid(Guid issueId, Guid moduleId)
            => ModuleIssue.Create(issueId, moduleId);
    }

    internal static class TestModuleMemberFactory
    {
        public static ModuleMember CreateValid(Guid memberId, Guid moduleId)
            => ModuleMember.Create(memberId, moduleId);
    }

    internal static class TestModuleLinkFactory
    {
        public static ModuleLink CreateValid(
            string title = "Figma Design",
            string url = "https://figma.com/file/abc",
            string? metadata = null,
            Guid? moduleId = null)
            => ModuleLink.Create(title, url, metadata,
                moduleId ?? Guid.NewGuid());
    }
    ```

    **ModuleDomainTests.cs（namespace: YH.Tests.WorkItems.Domain）:**
    - `Create_WithValidArgs_SetsProperties`: 验证 Name/ProjectId/Status/SortOrder/Version
    - `Create_WithEmptyName_Throws`: name="" → ArgumentException
    - `Create_WithEmptyProjectId_Throws`: projectId=Guid.Empty → ArgumentException
    - `Create_DefaultStatusIsPlanned`: status=null → Status == "planned"
    - `Create_WithCustomStatus_SetsStatus`: status="in-progress" → Status == "in-progress"
    - `Update_ChangesName`: Update name → Name 改变
    - `Update_WithNullParams_DoesNotChange`: Update() 无参 → 字段不变
    - `UpdateStatus_ChangesStatus`: UpdateStatus("completed") → Status == "completed"
    - `UpdateStatus_WithNull_DoesNotChange`: UpdateStatus(null) → Status 不变
    - `Archive_SetsArchivedAt`: Archive() → ArchivedAt 非 null
    - `Unarchive_ClearsArchivedAt`: Archive() → Unarchive() → ArchivedAt 为 null
    - `SoftDelete_SetsDeletedFlag`: SoftDelete → IsDeleted=true
    - `SoftDelete_IsIdempotent`: 首次删除后再次 SoftDelete 不改变 DeletedOnUtc

    **ModuleIssueDomainTests.cs（namespace: YH.Tests.WorkItems.Domain）:**
    - `Create_WithValidArgs_SetsProperties`: IssueId/ModuleId 匹配
    - `Create_WithEmptyIssueId_Throws`: Guid.Empty → ArgumentException
    - `Create_WithEmptyModuleId_Throws`: Guid.Empty → ArgumentException
    - `SoftDelete_SetsDeletedFlag`: SoftDelete → IsDeleted=true
    - `IsDeleted_DefaultIsFalse`: 新创建 → IsDeleted == false

    **ModuleMemberDomainTests.cs（namespace: YH.Tests.WorkItems.Domain）:**
    - `Create_WithValidArgs_SetsProperties`: MemberId/ModuleId 匹配
    - `Create_WithEmptyMemberId_Throws`: Guid.Empty → ArgumentException
    - `Create_WithEmptyModuleId_Throws`: Guid.Empty → ArgumentException
    - `SoftDelete_SetsDeletedFlag`
    - `IsDeleted_DefaultIsFalse`

    **ModuleLinkDomainTests.cs（namespace: YH.Tests.WorkItems.Domain）:**
    - `Create_WithValidArgs_SetsProperties`: Title/Url/ModuleId 匹配
    - `Create_WithEmptyTitle_Throws`: title="" → ArgumentException
    - `Create_WithEmptyUrl_Throws`: url="" → ArgumentException
    - `Update_ChangesTitleAndUrl`: Update → Title/Url 更新
    - `Update_WithNullParams_DoesNotChange`: Update(null, null, null) → 字段不变
    - `SoftDelete_SetsDeletedFlag`

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String "error" -NotMatch</automated>
  </verify>
  <done>
    - 迁移 AddModules 生成，在 yhschema.WorkItems 创建 Modules/ModuleIssues/ModuleMembers/ModuleLinks 表
    - TestModuleFactory.cs 和 4 个 Domain 测试文件存在
    - 构建 0 errors
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                         |
| ------------- | ----------------------------------- |
| Client -> API | 未认证请求跨过 API 路由进入 Handler |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                     |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | --------------------------------------------------- |
| T-6-01-01 | Tampering              | Module 唯一约束 (TenantId, ProjectId, Name)        | mitigate    | EF Core HasFilter 条件唯一索引 + 数据库级原子性保证 |
| T-6-01-02 | Tampering              | ModuleIssue 唯一约束 (TenantId, ModuleId, IssueId) | mitigate    | EF Core HasFilter 条件唯一索引，阻止重复关联        |
| T-6-01-03 | Information Disclosure | Module/ModuleIssue 跨租户泄露                      | mitigate    | IHasTenant + BaseDbContext 全局查询过滤器自动隔离   |
| T-6-01-04 | Tampering              | Domain 实体构建校验绕过                            | mitigate    | 私有构造函数 + 静态工厂方法保证实体始终通过验证创建 |

**Package legitimacy:** 无新增 NuGet 包 — 所有依赖已在 Phase 1-5 验证。
</threat_model>

<verification>
1. `dotnet build src/YH.Flow.slnx --nologo` — 0 errors
2. 验证 Modules/ModuleIssues/ModuleMembers/ModuleLinks DbSet 在 WorkItemsDbContext 中存在
3. 验证 4 个 EF 配置文件的索引正确
4. 验证 AddModules 迁移文件在 `src/Host/YH.Flow.Migrations.PostgreSQL/WorkItems/` 目录下
5. 验证 ModuleDto 包含所有注释字段（total_issues, completed_issues, status 等）
</verification>

<success_criteria>

- Module、ModuleIssue、ModuleMember、ModuleLink 4 个领域实体创建完成
- Module 实体实现 IHasTenant/ISoftDeletable/IAuditableEntity，包含 Create/Update/UpdateStatus/Archive/Unarchive/FreezeSnapshot/SoftDelete 方法
- Module 特有: Status 字段默认 "planned"，唯一索引 (TenantId, ProjectId, Name) WHERE deleted IS NULL
- ModuleIssue 实现了 (TenantId, ModuleId, IssueId) 条件唯一索引
- WorkItemsDbContext 包含 4 个新 DbSet
- AddModules 迁移生成成功
- ModuleDto 定义正确的响应格式（含注释字段）
- TestModuleFactory 和 4 组 Domain 测试创建完成
- 构建 0 errors

</success_criteria>

<output>
创建 `.planning/phases/06-module/06-01-SUMMARY.md` 记录本计划执行结果（3 tasks, 验证结果）
</output>
