---
phase: 07-page
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/Modules/Page/Modules.Page/PageModule.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/PageDtoMapper.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageCommandValidator.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/GetPage/GetPageEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/GetPage/GetPageQueryHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageCommandValidator.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageCommandValidator.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/ListPages/ListPagesEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/ListPages/ListPagesQueryHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageSummary/GetPageSummaryEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageSummary/GetPageSummaryQueryHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/ArchivePageEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/ArchivePageCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/UnarchivePageEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/UnarchivePageCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageDescription/GetPageDescriptionEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageDescription/GetPageDescriptionQueryHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionCommandValidator.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/AddFavorite/AddFavoriteEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/AddFavorite/AddFavoriteCommandHandler.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/RemoveFavorite/RemoveFavoriteEndpoint.cs
  - src/Modules/Page/Modules.Page/Features/v1/Pages/RemoveFavorite/RemoveFavoriteCommandHandler.cs
autonomous: true
requirements: [REQ-7.1, REQ-7.2]
user_setup: []
must_haves:
  truths:
    - "POST /api/v1/workspaces/{slug}/projects/{projectId}/pages/ creates a page and returns 201 + PageDetailDto"
    - "GET /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId} returns 200 + PageDetailDto"
    - "PATCH /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId} updates a page and returns 200 + PageDetailDto"
    - "DELETE /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId} soft-deletes a page and returns 204"
    - "GET /api/v1/workspaces/{slug}/projects/{projectId}/pages/ returns 200 + list of PageDto (top-level pages only by default)"
    - "GET /api/v1/workspaces/{slug}/projects/{projectId}/pages-summary/ returns 200 + summary object"
    - "POST /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId}/archive/ archives a page (sets ArchivedAt)"
    - "DELETE /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId}/archive/ unarchives a page"
    - "GET /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId}/description/ returns 200 + PageDetailDto (description fields)"
    - "PATCH /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId}/description/ updates description_html/stripped/json"
    - "POST /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId}/favorite/ adds a page to user favorites"
    - "DELETE /api/v1/workspaces/{slug}/projects/{projectId}/pages/{pageId}/favorite/ removes a page from user favorites"
    - "All endpoints use [RequireWorkspaceRole(Admin, Member)] authorization"
    - "dotnet build src/YH.Flow.slnx produces 0 errors"
  artifacts:
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/PageDtoMapper.cs"
      provides: "DTO mapper (Page entity -> PageDto / PageDetailDto)"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageEndpoint.cs"
      provides: "POST /pages/ endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/GetPage/GetPageEndpoint.cs"
      provides: "GET /pages/{pageId} endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageEndpoint.cs"
      provides: "PATCH /pages/{pageId} endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageEndpoint.cs"
      provides: "DELETE /pages/{pageId} endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/ListPages/ListPagesEndpoint.cs"
      provides: "GET /pages/ list endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageSummary/GetPageSummaryEndpoint.cs"
      provides: "GET /pages-summary/ endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/ArchivePageEndpoint.cs"
      provides: "POST /pages/{pageId}/archive/ endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageDescription/GetPageDescriptionEndpoint.cs"
      provides: "GET /pages/{pageId}/description/ endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionEndpoint.cs"
      provides: "PATCH /pages/{pageId}/description/ endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/AddFavorite/AddFavoriteEndpoint.cs"
      provides: "POST /pages/{pageId}/favorite/ endpoint"
    - path: "src/Modules/Page/Modules.Page/Features/v1/Pages/RemoveFavorite/RemoveFavoriteEndpoint.cs"
      provides: "DELETE /pages/{pageId}/favorite/ endpoint"
    - path: "src/Modules/Page/Modules.Page/PageModule.cs"
      provides: "Full endpoint wiring in MapEndpoints"
  key_links:
    - from: "PageModule.MapEndpoints"
      to: "All endpoint extension methods"
      via: "MapCreatePageEndpoint, MapGetPageEndpoint, etc."
    - from: "Each Endpoint"
      to: "Mediator.Send(Command/Query)"
      via: "IMediator injected by minimal API"
    - from: "Each Handler"
      to: "PageDbContext"
      via: "constructor DI"
    - from: "PageDtoMapper"
      to: "PageDto / PageDetailDto"
      via: "ToDto / ToDetailDto mapping methods"
---

<objective>
实现 Page 模块的所有 API 端点：核心 CRUD + Summary + Archive/Unarchive + Description CRUD + Favorite。

Purpose: 完成 Phase 7 REQ-7.1（Page CRUD、层级结构、归档）和 REQ-7.2（Description CRUD、收藏功能）的所有功能端点。所有端点遵循 Project 模块的垂直切片架构和 [RequireWorkspaceRole] 授权模式。
Output: 17 个端点实现（含 Endpoint + Handler + Validator），DTO mapper，PageModule 端点注册
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-page/07-01-SUMMARY.md
@.planning/phases/07-page/07-CONTEXT.md

# Existing reference files (absolute paths, MUST read for pattern):

# Project CRUD endpoint pattern (exact reference for all Page endpoints)

@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/CreateProject/CreateProjectEndpoint.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/CreateProject/CreateProjectCommandHandler.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/GetProject/GetProjectEndpoint.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/GetProject/GetProjectQueryHandler.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/UpdateProject/UpdateProjectEndpoint.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/DeleteProject/DeleteProjectEndpoint.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/DeleteProject/DeleteProjectCommandHandler.cs

# DTO mapper pattern

@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/ProjectDtoMapper.cs

# ProjectModule full endpoint wiring pattern

@D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/ProjectModule.cs

# Domain entities (from Plan 01 - created by this phase)

@D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Domain/Page.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Domain/ProjectPage.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Domain/PageFavorite.cs

# Contracts (from Plan 01 - created by this phase)

@D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDto.cs
@D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDetailDto.cs

# Authorization pattern

@D:/github/akinix-plane/yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleExtensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PageDtoMapper + Create/Get/Update/Delete endpoints</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/ProjectDtoMapper.cs — 映射器模式
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/CreateProject/CreateProjectEndpoint.cs — 端点模式（mappost, RequireWorkspaceRole, ClaimsPrincipal 用户 ID）
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/CreateProject/CreateProjectCommandHandler.cs — Handler 模式（DbContext + SaveChanges）
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/GetProject/GetProjectEndpoint.cs — GET 端点（/projectId 参数）
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/GetProject/GetProjectQueryHandler.cs — GET Handler（AsNoTracking + FirstOrDefault）
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/DeleteProject/DeleteProjectEndpoint.cs — DELETE 端点（返回 204 NoContent）
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/DeleteProject/DeleteProjectCommandHandler.cs — DELETE Handler（SoftDelete + SaveChanges）
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Domain/Page.cs — 实体定义
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDto.cs — DTO 定义
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDetailDto.cs — 详情 DTO 定义
  </read_first>
  <files>
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/PageDtoMapper.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageCommandHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/CreatePage/CreatePageCommandValidator.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/GetPage/GetPageEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/GetPage/GetPageQueryHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageCommandHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePage/UpdatePageCommandValidator.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageCommandHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/DeletePage/DeletePageCommandValidator.cs
  </files>
  <action>
    实现核心 CRUD 端点的完整垂直切片。

    **PageDtoMapper.cs** (`YH.Modules.Page.Features.v1.Pages`):
    - 使用别名: `using PageEntity = YH.Modules.Page.Domain.Page;`
    - 命名空间: `YH.Modules.Page.Features.v1.Pages`
    - `internal static class PageDtoMapper`:
      - `ToDto(PageEntity p)` → `PageDto`:
        映射 Id, Name, Access = (int)p.Access, Color, IsLocked, IsGlobal, SortOrder, OwnedBy, ParentId, ArchivedAt, ViewProps, LogoProps, ExternalSource, ExternalId, CreatedAt = p.CreatedOnUtc, UpdatedAt = p.LastModifiedOnUtc
      - `ToDetailDto(PageEntity p)` → `PageDetailDto`:
        调用 ToDto 设置基础字段，然后设置 DescriptionHtml, DescriptionStripped, DescriptionJson

    **CreatePageEndpoint** (`POST /`):
    - 模式与 CreateProjectEndpoint 完全相同
    - 接收 `CreatePageCommand` + `ClaimsPrincipal user` + `IMediator mediator`
    - 从 `user.GetUserId()` 获取 OwnedBy，设置 `command.OwnedBy = Guid.Parse(userId)`
    - 返回 `TypedResults.Created($"/api/v1/workspaces/...", result)`
    - **`.RequireWorkspaceRole(WorkspaceRole.Admin, WorkspaceRole.Member)`**
    - 使用 RouteHandlerBuilder pattern: `internal static RouteHandlerBuilder MapCreatePageEndpoint(this IEndpointRouteBuilder endpoints) { return endpoints.MapPost("/", ...)... }`

    **CreatePageCommandHandler** — 实现 `ICommandHandler<CreatePageCommand, CreatePageResponse>`:
    - 构造注入 `PageDbContext _db`
    - Handle 流程:
      1. 验证 command.OwnedBy != Guid.Empty
      2. 调用 `Page.Create(name, projectId, ownedBy, ...)` — projectId 从路由参数获取（通过 command 字段传入）
      3. `_db.Pages.Add(page)`
      4. 自动创建 ProjectPage 桥接实体（将 page 关联到 project）: `_db.ProjectPages.Add(ProjectPage.Create(page.Id, command.ProjectId))`
         - 注意：ProjectId 需要从路由提取或在 Command 中携带
         - 建议在 Command 中添加 `[JsonIgnore]` 的 `ProjectId(Guid)` 字段，由 Endpoint 从路由 {projectId} 参数设置
      5. `await _db.SaveChangesAsync(cancellationToken)`
      6. 返回 `new CreatePageResponse(page.Id)`
    - **重点**: CreatePageCommand 需要添加 `[JsonIgnore] public Guid ProjectId { get; set; }` 字段（从路由提取）

    **CreatePageCommandValidator** — `FluentValidation.AbstractValidator<CreatePageCommand>`:
    - RuleFor(x => x.Name).NotEmpty().MaximumLength(255)
    - RuleFor(x => x.OwnedBy).NotEmpty()
    - RuleFor(x => x.ProjectId).NotEmpty()
    - SortOrder 可选范围验证（如 > 0 或默认）

    **GetPageEndpoint** (`GET /{pageId}`):
    - `MapGet("/{pageId}", async (Guid pageId, IMediator mediator, ...) => ...)`
    - 返回 `TypedResults.Ok(await mediator.Send(new GetPageQuery { PageId = pageId }))`
    - `.RequireAuthorization()` （获取 page 需要认证）
    - 注意：与 Project GetProject 相同模式，公开读取

    **GetPageQueryHandler** — `IQueryHandler<GetPageQuery, PageDetailDto>`:
    - 构造注入 `PageDbContext _db`
    - 查询: `_db.Pages.AsNoTracking().FirstOrDefaultAsync(p => p.Id == query.PageId && !p.IsDeleted)`
    - 如果 null → throw NotFoundException
    - 返回 `PageDtoMapper.ToDetailDto(page)`
    - **注意**: Page 是 IGlobalEntity（无 TenantId），所以不需要 tenant 过滤。访问控制通过 Private 字段处理。

    **UpdatePageEndpoint** (`PATCH /{pageId}`):
    - `MapPatch("/{pageId}", async (Guid pageId, UpdatePageCommand command, IMediator mediator, ...) => ...)`
    - 设置 `command.PageId = pageId`（[JsonIgnore]，从路由提取）
    - 返回 `TypedResults.Ok(await mediator.Send(command))`
    - `.RequireWorkspaceRole(Admin, Member)`

    **UpdatePageCommandHandler** — `ICommandHandler<UpdatePageCommand, PageDetailDto>`:
    - 构造注入 `PageDbContext _db`
    - 加载: `_db.Pages.FirstOrDefaultAsync(p => p.Id == command.PageId && !p.IsDeleted)` → throw NotFoundException if null
    - 调用 `page.Update(name, descriptionHtml, descriptionStripped, descriptionJson, access, color, parentId, sortOrder, viewProps, logoProps, isGlobal)`
    - Verify Private page access: 如果 page.Access == Private 且当前用户不是 OwnedBy，则 throw ForbiddenException
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 `PageDtoMapper.ToDetailDto(page)`

    **UpdatePageCommandValidator**:
    - RuleFor(x => x.PageId).NotEmpty()
    - Name 非空验证（如果提供）: When(x => x.Name != null, () => RuleFor(x => x.Name).NotEmpty().MaximumLength(255))

    **DeletePageEndpoint** (`DELETE /{pageId}`):
    - `MapDelete("/{pageId}", async (Guid pageId, IMediator mediator, ...) => ...)`
    - `.RequireWorkspaceRole(Admin, Member)`
    - 返回 `TypedResults.NoContent()` — 与 Project Delete 模式相同

    **DeletePageCommandHandler** — `ICommandHandler<DeletePageCommand, bool>`:
    - 加载 Page → throw NotFoundException if null
    - `page.SoftDelete(DateTimeOffset.UtcNow)`
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 true

    **DeletePageCommandValidator**:
    - RuleFor(x => x.PageId).NotEmpty()

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error|Error\(s\)|失败" -NotMatch</automated>
  </verify>
  <done>
    - PageDtoMapper.cs 创建完成（ToDto 和 ToDetailDto 方法）
    - Create/Get/Update/Delete 端点、Handler、Validator 创建完成
    - Create 端点自动创建 ProjectPage 桥接实体
    - 构建 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ListPages + GetPageSummary + Archive/Unarchive endpoints</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Domain/Page.cs — Page 实体（SortOrder, ArchivedAt, ParentId, Access 字段）
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Data/PageDbContext.cs — DbContext（Pages, ProjectPages DbSet）
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDto.cs — 列表 DTO 定义
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDetailDto.cs — 详情 DTO 定义
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/Features/v1/Projects/DeleteProject/DeleteProjectEndpoint.cs — Archive 的 DELETE 端点模式参考
  </read_first>
  <files>
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/ListPages/ListPagesEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/ListPages/ListPagesQueryHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageSummary/GetPageSummaryEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageSummary/GetPageSummaryQueryHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/ArchivePageEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/ArchivePageCommandHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/UnarchivePageEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/ArchivePage/UnarchivePageCommandHandler.cs
  </files>
  <action>
    实现列表、Summary 和归档端点。

    **ListPagesEndpoint** (`GET /`):
    - `MapGet("/", async ([AsParameters] ListPagesQuery query, IMediator mediator, ...) => ...)`
    - 使用 `[AsParameters]` 绑定查询参数到 `ListPagesQuery`
    - `query.ProjectId` 从路由 {projectId} 提取（[JsonIgnore] 字段 + Endpoint 设置）
    - `.RequireWorkspaceRole(Admin, Member)`
    - 返回 `TypedResults.Ok(await mediator.Send(query))`

    **ListPagesQueryHandler** — `IQueryHandler<ListPagesQuery, List<PageDto>>`:
    - 构造注入 `PageDbContext _db`
    - 查询基础: `_db.Pages.AsNoTracking().Where(p => !p.IsDeleted)`
    - 筛选逻辑:
      - `query.IsArchived == true` → `.Where(p => p.ArchivedAt != null)` （已归档）
      - `query.IsArchived == false` → `.Where(p => p.ArchivedAt == null)` （活跃，默认）
      - `query.Parent.HasValue` → `.Where(p => p.ParentId == query.Parent)` （指定父级）
      - `query.Parent == null` 且未指定 parent 参数 → `.Where(p => p.ParentId == null)` （顶层页面，per CONTEXT.md Claude's Discretion — 默认为顶层）
      - 如果要获取所有页面（含子页面），客户端可以传递 `parent=` 空值？实际上 CONTEXT.md 说"Page 列表默认仅返回顶层（parent == null）"
      - 实现: 默认 `p.ParentId == null`；如果 `query.IncludeChildren == true` 则不过滤
    - 排序: `.OrderBy(p => p.SortOrder).ThenByDescending(p => p.CreatedOnUtc)`（per CONTEXT.md）
    - 可加分页: `.Skip((query.PageNumber - 1) * query.PageSize).Take(query.PageSize)`
    - 映射: `.Select(p => PageDtoMapper.ToDto(p)).ToListAsync()`
    - 注意: Page 是 IGlobalEntity，无 tenant 自动过滤 — 但通过 projectId 筛选 + 路由上下文确保正确隔离

    **GetPageSummaryEndpoint** (`GET /pages-summary/`):
    - 使用独立路由组（已在 07-01 Plan 01 PageModule.cs 中创建 pages-summary 组）
    - `MapGet("/", async ([AsParameters] GetPageSummaryQuery query, IMediator mediator, ...) => ...)`
    - `.RequireWorkspaceRole(Admin, Member)`

    **GetPageSummaryQueryHandler** — `IQueryHandler<GetPageSummaryQuery, object>`:
    - 构造注入 `PageDbContext _db`
    - 返回匿名对象:
      ```csharp
      var totalPages = await _db.Pages.CountAsync(p => !p.IsDeleted, cancellationToken);
      var totalArchived = await _db.Pages.CountAsync(p => !p.IsDeleted && p.ArchivedAt != null, cancellationToken);
      var recentPages = await _db.Pages
          .AsNoTracking()
          .Where(p => !p.IsDeleted && p.ArchivedAt == null)
          .OrderByDescending(p => p.LastModifiedOnUtc ?? p.CreatedOnUtc)
          .Take(5)
          .Select(p => PageDtoMapper.ToDto(p))
          .ToListAsync(cancellationToken);
      return new { total_pages = totalPages, total_archived_pages = totalArchived, recently_updated = recentPages };
      ```

    **ArchivePageEndpoint** (`POST /{pageId}/archive/`):
    - `MapPost("/{pageId}/archive", async (Guid pageId, IMediator mediator, ...) => ...)`
    - 返回 `TypedResults.Ok(await mediator.Send(new ArchivePageCommand { PageId = pageId }))`
    - `.RequireWorkspaceRole(Admin, Member)`

    **ArchivePageCommandHandler** — `ICommandHandler<ArchivePageCommand, bool>`:
    - 加载 Page（排除已删除）→ throw NotFoundException
    - `page.Archive()` — 设置 ArchivedAt = DateTimeOffset.UtcNow
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 true

    **UnarchivePageEndpoint** (`DELETE /{pageId}/archive/`):
    - `MapDelete("/{pageId}/archive", async (Guid pageId, IMediator mediator, ...) => ...)`
    - 返回 `TypedResults.Ok(await mediator.Send(new UnarchivePageCommand { PageId = pageId }))`
    - `.RequireWorkspaceRole(Admin, Member)`

    **UnarchivePageCommandHandler** — `ICommandHandler<UnarchivePageCommand, bool>`:
    - 加载 Page → throw NotFoundException
    - `page.Unarchive()` — 设置 ArchivedAt = null
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 true

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error|Error\(s\)|失败" -NotMatch</automated>
  </verify>
  <done>
    - ListPages + GetPageSummary 端点创建完成
    - ArchivePage + UnarchivePage 端点创建完成
    - 列表默认仅返回顶层页面（ParentId == null），按 SortOrder + -CreatedAt 排序
    - 构建 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Description CRUD + Favorite endpoints + PageModule full wiring</name>
  <read_first>
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Domain/PageFavorite.cs — 收藏实体
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/Data/PageDbContext.cs — PageFavorites DbSet
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page/PageModule.cs — 当前路由注册（添加新路由）
    @D:/github/akinix-plane/yh-flow/src/Modules/Project/Modules.Project/ProjectModule.cs — 完整路由注册模式
    @D:/github/akinix-plane/yh-flow/src/Modules/Page/Modules.Page.Contracts/DTOs/PageDetailDto.cs — 详情 DTO（含 description 字段）
  </read_first>
  <files>
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageDescription/GetPageDescriptionEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/GetPageDescription/GetPageDescriptionQueryHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionCommandHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/UpdatePageDescription/UpdatePageDescriptionCommandValidator.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/AddFavorite/AddFavoriteEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/AddFavorite/AddFavoriteCommandHandler.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/RemoveFavorite/RemoveFavoriteEndpoint.cs
    NEW: src/Modules/Page/Modules.Page/Features/v1/Pages/RemoveFavorite/RemoveFavoriteCommandHandler.cs
    MODIFIED: src/Modules/Page/Modules.Page/PageModule.cs
  </files>
  <action>
    实现 Description CRUD、Favorite 端点，并在 PageModule.cs 注册所有端点。

    **GetPageDescriptionEndpoint** (`GET /{pageId}/description/`):
    - 路由: `"/{pageId}/description"`（注意：在 pages 路由组下，完整路径为 `/workspaces/{slug}/projects/{projectId}/pages/{pageId}/description/`）
    - `MapGet("/{pageId}/description", async (Guid pageId, IMediator mediator, ...) => ...)`
    - 返回 `TypedResults.Ok(await mediator.Send(new GetPageDescriptionQuery { PageId = pageId }))`
    - `.RequireAuthorization()` — 任意认证用户可读 description

    **GetPageDescriptionQueryHandler** — `IQueryHandler<GetPageDescriptionQuery, PageDetailDto>`:
    - 加载 Page（AsNoTracking）→ throw NotFoundException
    - 返回 `PageDtoMapper.ToDetailDto(page)`（detail DTO 包含 description_html/stripped/json）

    **UpdatePageDescriptionEndpoint** (`PATCH /{pageId}/description/`):
    - `MapPatch("/{pageId}/description", async (Guid pageId, UpdatePageDescriptionCommand command, IMediator mediator, ...) => ...)`
    - 设置 `command.PageId = pageId`
    - 返回 `TypedResults.Ok(await mediator.Send(command))`
    - `.RequireWorkspaceRole(Admin, Member)`

    **UpdatePageDescriptionCommandHandler** — `ICommandHandler<UpdatePageDescriptionCommand, PageDetailDto>`:
    - 加载 Page → throw NotFoundException
    - 调用 `page.UpdateDescription(command.DescriptionHtml, command.DescriptionStripped, command.DescriptionJson)`
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 `PageDtoMapper.ToDetailDto(page)`

    **UpdatePageDescriptionCommandValidator**:
    - RuleFor(x => x.PageId).NotEmpty()

    **AddFavoriteEndpoint** (`POST /{pageId}/favorite/`):
    - `MapPost("/{pageId}/favorite", async (Guid pageId, ClaimsPrincipal user, IMediator mediator, ...) => ...)`
    - 从 `user.GetUserId()` 获取 userId
    - 创建 `AddFavoriteCommand { PageId = pageId, UserId = userId }`（UserId 用 [JsonIgnore]）
    - 返回 `TypedResults.Ok(await mediator.Send(command))`
    - `.RequireWorkspaceRole(Admin, Member)`

    **AddFavoriteCommandHandler** — `ICommandHandler<AddFavoriteCommand, bool>`:
    - 构造注入 `PageDbContext _db`
    - 检查是否已收藏: `_db.PageFavorites.AnyAsync(f => f.PageId == command.PageId && f.UserId == command.UserId && !f.IsDeleted)`
    - 如果已存在 → 返回 true（幂等）
    - 创建 `PageFavorite.Create(command.PageId, command.UserId)`
    - `_db.PageFavorites.Add(favorite)`
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 true

    **RemoveFavoriteEndpoint** (`DELETE /{pageId}/favorite/`):
    - `MapDelete("/{pageId}/favorite", async (Guid pageId, ClaimsPrincipal user, IMediator mediator, ...) => ...)`
    - 从 `user.GetUserId()` 获取 userId
    - 创建 `RemoveFavoriteCommand { PageId = pageId, UserId = userId }`
    - 返回 `TypedResults.Ok(await mediator.Send(command))`
    - `.RequireWorkspaceRole(Admin, Member)`

    **RemoveFavoriteCommandHandler** — `ICommandHandler<RemoveFavoriteCommand, bool>`:
    - 查找收藏记录: `_db.PageFavorites.FirstOrDefaultAsync(f => f.PageId == command.PageId && f.UserId == command.UserId && !f.IsDeleted, cancellationToken)`
    - 如果不存在 → 返回 true（幂等）
    - 如果 PageFavorite 实现 ISoftDeletable，调用 SoftDelete；否则 Remove
    - PageFavorite 实现 ISoftDeletable（已在 07-01 中确定）→ 软删除
    - `await _db.SaveChangesAsync(cancellationToken)`
    - 返回 true

    **PageModule.cs 完整路由注册**:
    - 在 `ConfigureServices` 中保持原有（无需修改 — Task 1 已注册）
    - 在 `MapEndpoints` 中替换 07-01 的路由组桩代码为完整注册:
    ```csharp
    var apiVersionSet = endpoints.NewApiVersionSet()
        .HasApiVersion(new ApiVersion(1))
        .ReportApiVersions()
        .Build();

    // Page routes under /workspaces/{slug}/projects/{projectId}/pages/
    var pages = endpoints
        .MapGroup("api/v{version:apiVersion}/workspaces/{slug}/projects/{projectId}/pages")
        .WithTags("Pages")
        .WithApiVersionSet(apiVersionSet);

    pages.MapCreatePageEndpoint();
    pages.MapGetPageEndpoint();
    pages.MapUpdatePageEndpoint();
    pages.MapDeletePageEndpoint();
    pages.MapListPagesEndpoint();

    // Page summary route (no {pageId} parameter)
    var pageSummary = endpoints
        .MapGroup("api/v{version:apiVersion}/workspaces/{slug}/projects/{projectId}/pages-summary")
        .WithTags("Pages")
        .WithApiVersionSet(apiVersionSet);

    pageSummary.MapGetPageSummaryEndpoint();
    ```
    - 添加 using 导入: `using YH.Modules.Page.Features.v1.Pages.CreatePage;`, `using YH.Modules.Page.Features.v1.Pages.GetPage;`, `using YH.Modules.Page.Features.v1.Pages.UpdatePage;`, `using YH.Modules.Page.Features.v1.Pages.DeletePage;`, `using YH.Modules.Page.Features.v1.Pages.ListPages;`, `using YH.Modules.Page.Features.v1.Pages.GetPageSummary;`, `using YH.Modules.Page.Features.v1.Pages.ArchivePage;`, `using YH.Modules.Page.Features.v1.Pages.GetPageDescription;`, `using YH.Modules.Page.Features.v1.Pages.UpdatePageDescription;`, `using YH.Modules.Page.Features.v1.Pages.AddFavorite;`, `using YH.Modules.Page.Features.v1.Pages.RemoveFavorite;`
    - 注意: Archive/Description/Favorite 路由在 pages 组下注册（`/{pageId}/archive`, `/{pageId}/description`, `/{pageId}/favorite` 由各自的 Endpoint 使用 MapPost/MapDelete/MapGet 在 `"/{pageId}/..."` 路径注册，这些在 pages 组中自动拼接）

  </action>
  <verify>
    <automated>dotnet build src/YH.Flow.slnx --nologo 2>&1 | Select-String -Pattern "error|Error|Error\(s\)|失败" -NotMatch</automated>
  </verify>
  <done>
    - GetPageDescription/UpdatePageDescription 端点创建完成（GET/PATCH /{pageId}/description/）
    - AddFavorite/RemoveFavorite 端点创建完成（POST/DELETE /{pageId}/favorite/）
    - Favorite 操作使用 ISoftDeletable 软删除
    - PageModule.cs MapEndpoints 包含所有端点的完整注册
    - 构建 0 errors
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                         |
| ------------- | ----------------------------------- |
| Client -> API | 未认证请求跨过 API 路由进入 Handler |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                                  |
| --------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-7-02-01 | Information Disclosure | Private Page 内容泄露          | mitigate    | Handler 在返回 Page detail 前检查 Access==Private 时用户是否是 OwnedBy；端点使用 [RequireWorkspaceRole]                                                          |
| T-7-02-02 | Tampering              | 跨项目访问 Page                | mitigate    | ListPagesQuery 使用 ProjectId 筛选，确保 Page 仅在所属项目内可见                                                                                                 |
| T-7-02-03 | Spoofing               | Favorite 冒充他人收藏          | mitigate    | AddFavorite RemoveFavorite 从 ClaimsPrincipal 获取 UserId，不接受客户端输入                                                                                      |
| T-7-02-04 | Tampering              | 内容 XSS 注入                  | accept      | DescriptionHtml 按 Plane 模式存储裸 HTML；XSS 防护由前端渲染时处理（output encoding + TipTap）                                                                   |
| T-7-02-05 | Information Disclosure | Page 跨租户泄露（无 TenantId） | mitigate    | Page 是 IGlobalEntity（无 TenantId），但路由参数 {slug} + {projectId} 结合 [RequireWorkspaceRole] 保证用户在正确的工作区和项目中操作。项目级可见性确保跨租户隔离 |

**Package legitimacy:** 无新增 NuGet 包。
</threat_model>

<verification>
1. `dotnet build src/YH.Flow.slnx --nologo` — 0 errors
2. 遍历验证每个端点文件存在（17 个端点）
3. 验证 PageModule.cs MapEndpoints 注册了所有端点
4. 验证所有端点使用了正确的 [RequireWorkspaceRole] 或 [RequireAuthorization] 授权
5. 验证 Create 端点自动创建 ProjectPage 桥接实体
6. 验证 ListPages 默认仅返回顶层页面，排序为 SortOrder + -CreatedAt
</verification>

<success_criteria>

- 12 个 API 端点全部创建完成（Create/Get/Update/Delete/List/Summary/Archive/Unarchive/GetDescription/UpdateDescription/AddFavorite/RemoveFavorite）
- 所有端点遵循 Project 模块的垂直切片架构（Endpoint + Handler + Validator）
- CreatePage 自动创建 ProjectPage 桥接实体
- ListPages 默认仅返回顶层页面（父级为空）
- PageModule.cs MapEndpoints 注册所有端点
- 构建 0 errors

</success_criteria>

<output>
创建 `.planning/phases/07-page/07-02-SUMMARY.md` 记录本计划执行结果
</output>
