---
phase: 14-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - clients/web/src/lib/ui/**
  - clients/web/src/lib/editor/**
  - clients/web/package.json
autonomous: true
requirements:
  - SCAFF-02
  - SCAFF-03
user_setup: []

must_haves:
  truths:
    - "`import { Input } from '@plane/ui'` 可在代码中正常使用"
    - "`import { Button } from '@plane/ui'` 可在代码中正常使用"
    - "@plane/editor 的核心 TipTap 编辑器逻辑编译通过，不含 Yjs/Hocuspocus 引用"
    - "Vite alias `@plane/ui` 和 `@plane/editor` 正确解析到 src/lib/ 下的 fork 目录"
  artifacts:
    - path: "clients/web/src/lib/ui/index.ts"
      provides: "Forked @plane/ui barrel export"
      contains: "export"
      min_lines: 20
    - path: "clients/web/src/lib/editor/index.ts"
      provides: "Forked @plane/editor barrel export（不含 Yjs）"
      contains: "export"
      min_lines: 10
    - path: "clients/web/src/lib/ui/button/button.tsx"
      provides: "@plane/ui 的 Button 组件（含 loading、variant、size props）"
      contains: "ButtonProps"
    - path: "clients/web/src/lib/ui/form-fields/input.tsx"
      provides: "Input 组件用于认证表单"
      contains: "Input"
  key_links:
    - from: "clients/web/tsconfig.json"
      to: "src/lib/ui"
      via: "compilerOptions.paths"
      pattern: "@plane/ui"
---

<objective>
Fork @plane/ui 组件库和 @plane/editor（TipTap）编辑器到 clients/web/src/lib/ 目录。

Purpose: 使 Flow Web 前端能复用 Plane 的 UI 组件（Button、Input、Spinner 等）和 TipTap 富文本编辑器
Output: 可通过 `import { Button } from '@plane/ui'` 和 `import { Editor } from '@plane/editor'` 引用 Fork 包
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-auth/14-01-PLAN.md（依赖的 Plan 14-01 创建的项目结构）
@.planning/codebase/STACK.md（验证 Tailwind CSS 4 版本兼容性）
@.planning/codebase/CONVENTIONS.md（编码规范和模式）
@.planning/phases/14-auth/14-CONTEXT.md（D-03~D-07 决策）
</context>

<tasks>

<task type="auto">
<name>Task 1: Fork @plane/ui 组件库（核心 UI 组件子集）</name>
<files>
  clients/web/src/lib/ui/**
</files>
<read_first>
- D:\github\akinix-plane\packages\ui\src\index.ts（barrel exports 了解完整导出结构）
- D:\github\akinix-plane\packages\ui\src\button\（Button 组件结构和 props 类型）
- D:\github\akinix-plane\packages\ui\src\form-fields\（Input、TextArea 等表单字段组件）
- D:\github\akinix-plane\packages\ui\src\spinners\（Spinner 加载组件）
- D:\github\akinix-plane\packages\ui\src\form-fields\password\indicator.tsx（PasswordStrengthIndicator）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（D-03: src/lib/ 目录、D-05: 复制方式、D-06: // FLOW: 标记）
</read_first>
<action>
将 @plane/ui 组件库通过代码复制方式（per D-05）Fork 到 `clients/web/src/lib/ui/`，仅包含当前 phase（认证页面）和后续 phase 必需的 UI 组件子集：

**复制以下组件目录到 `clients/web/src/lib/ui/`：**

- `button/` — Button 组件（variant: primary/secondary/tertiary/link, size: sm/md/lg/xl, loading spinner）
- `form-fields/` — Input 组件（含 input.tsx, textarea.tsx, checkbox.tsx, root.tsx）以及 `password/indicator.tsx`（PasswordStrengthIndicator）
- `spinners/` — Spinner 加载组件（circular-spinner.tsx, circular-bar-spinner.tsx）
- `loader.tsx` — Loader 骨架屏组件
- `avatar/` — Avatar 用户头像组件
- `badge/` — Badge 标签组件
- `constants/` — UI 常量
- `tag/` — Tag 标签
- `tooltip/` — Tooltip 提示
- `utils/` — UI 工具函数
- `header/` — 通用 Header 组件
- `typography/` — 排版组件
- `content-wrapper/` — 内容包装器
- `link/` — 链接组件
- `control-link/` — 控制链接
- `card/` — 卡片组件
- `collapsible/` — 折叠面板
- `modals/` — 模态框
- `popovers/` — 弹出菜单
- `dropdown/` — 下拉组件
- `dropdowns/` — 多功能下拉选择器
- `tables/` — 表格组件
- `tabs/` — 标签页
- `progress/` — 进度条
- `row/` — 行组件
- `scroll-area.tsx` — 滚动区域
- `drag-handle.tsx` — 拖拽手柄
- `drop-indicator.tsx` — 拖拽放置指示器
- `sortable/` — 可排序列表
- `favorite-star.tsx` — 收藏星标
- `breadcrumbs/` — 面包屑导航
- `color-picker/` — 颜色选择器
- `oauth/` — OAuth 按钮

**import 路径调整：**

- 所有 `@plane/utils` 引用改为 `../../utils` 或 `../utils`（相对路径）
- 所有 `@plane/types` 引用改为 `../../types` 或 `../types`（相对路径）
- 所有 `@plane/constants` 引用——如果仅用于 auth/UI 的小枚举，内联之；如果包含大量配置常量（如 API_BASE_URL），则在 src/lib/ 下新增 `constants/` 目录复制相关文件
- `@plane/i18n` 引用——暂时注释或替换为直文字符串（Phase 14 不使用 i18n）

**创建 barrel export `index.ts`：**

- 参照 packages/ui/src/index.ts 结构，仅导出已复制的组件
- 使用 `export * from "./button"` 模式（per CONVENTIONS.md barrel 模式）

**在所有文件中添加 `// FLOW:` 标记（per D-06）：**

- 文件头部：`// FLOW: Forked from @plane/ui/src/<path>`
- 修改的 import 路径旁：`// FLOW: adjusted import path`
- 内联的常量旁：`// FLOW: inlined from @plane/constants`

**不复制（后续 phase 需要时再 Fork）：**

- color-picker（Phase 15 需要 emoji-icon-picker）
- auth-form（将根据我们的 JWT 认证流程重写）
  </action>
  <verify>
  <automated>cd clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript 编译通过，@plane/ui 核心组件可正常导入。验证命令：`cd clients/web && node -e "require('fs').readdirSync('./src/lib/ui').filter(f=>f.endsWith('.ts')||f.endsWith('.tsx')).forEach(f=>console.log(f))"`</done>
  <acceptance_criteria>
- `clients/web/src/lib/ui/index.ts` 存在且 barrel export 至少 15 个组件目录
- `clients/web/src/lib/ui/button/button.tsx` 存在，导出 Button 组件（含 loading prop）
- `clients/web/src/lib/ui/form-fields/input.tsx` 存在，导出 Input 组件
- `clients/web/src/lib/ui/spinners/circular-spinner.tsx` 存在，导出 Spinner
- `import { Input, Button, Spinner } from "@plane/ui"` tsc 编译无错误
- 所有 Fork 文件包含 `// FLOW:` 标记
  </acceptance_criteria>
  </task>

<task type="auto">
<name>Task 2: Fork @plane/editor（剥离 Yjs 协作依赖）</name>
<files>
  clients/web/src/lib/editor/**
</files>
<read_first>
- D:\github\akinix-plane\packages\editor\src\index.ts（编辑器 barrel exports）
- D:\github\akinix-plane\packages\editor\src\core\（核心编辑器逻辑）
- D:\github\akinix-plane\packages\editor\src\ce\（CE 扩展——社区版本）
- D:\github\akinix-plane\packages\editor\src\ee\（EE 扩展——企业版，协作相关，可能要跳过）
- D:\github\akinix-plane\packages\editor\package.json（依赖检查）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（D-07: 剥离 Yjs 依赖）
</read_first>
<action>
将 @plane/editor 通过代码复制方式（per D-05）Fork 到 `clients/web/src/lib/editor/`，并立即剥离 Yjs/Hocuspocus 协作依赖（per D-07）：

**复制 `packages/editor/src/` 下以下内容到 `clients/web/src/lib/editor/`：**

- `core/` 目录完整复制——这是 TipTap 编辑器核心部分（extensions、hooks、plugins、constants、contexts、helpers、types、components）
- `ce/` 目录复制——社区版扩展
- `styles/` 目录复制——编辑器样式
- `lib.ts` 文件
- `index.ts` barrel export

**Yjs 剥离操作（关键）：**

1. 搜索 `packages/editor/src/` 中所有 import Yjs/Hocuspocus 的文件：
   - `import * as Y from "yjs"` → 删除或标记 `// FLOW: Yjs removed`
   - `import { HocuspocusProvider } from "@hocuspocus/provider"` → 删除
   - `import { yCollab, yUndoManagerKeymap } from "y-prosemirror"` → 删除
   - `import { yIndexedDB } from "y-indexeddb"` → 删除
2. 移除协作相关的自定义 extensions：
   - 删除 `ee/` 目录下的企业版协作扩展（如果有）
   - 删除所有依赖 Yjs 的 hook 和组件
3. 保留：
   - TipTap 核心 extensions（bold, italic, heading, list, link, code, image, mentions, slash command 等）
   - Markdown serialization/deserialization
   - 编辑器组件 wrapper
   - 编辑器样式

**import 路径调整：**

- `@plane/types` → `../types`
- `@plane/utils` → `../utils`
- `@plane/ui` → `../ui`
- `@plane/constants` → 内联或 `../constants`（如需要）
- 删除 `@plane/logger`、`@plane/i18n` 等依赖（在 fork 中注释或移除）

**添加到 `clients/web/package.json`：**

```json
"dependencies": {
  "@tiptap/core": "^2.22.0",
  "@tiptap/pm": "^2.22.0",
  "@tiptap/react": "^2.22.0",
  "@tiptap/starter-kit": "^2.22.0",
  "@tiptap/extension-horizontal-rule": "^2.22.0",
  "@tiptap/extension-underline": "^2.22.0",
  "@tiptap/extension-text-style": "^2.22.0",
  "@tiptap/extension-color": "^2.22.0",
  "@tiptap/extension-text-align": "^2.22.0",
  "@tiptap/extension-font-family": "^2.22.0",
  "@tiptap/extension-image": "^2.22.0",
  "@tiptap/extension-link": "^2.22.0",
  "@tiptap/extension-placeholder": "^2.22.0",
  "@tiptap/extension-code-block-lowlight": "^2.22.0",
  "tiptap-markdown": "^0.8.0",
  "lowlight": "^3.0.0"
}
```

**在所有修改的文件中添加 `// FLOW:` 标记（per D-06）：**

- `// FLOW: Forked from @plane/editor`
- `// FLOW: Yjs removed`
- `// FLOW: adjusted import path`

**注意：** @plane/editor 的 fork 包在此 phase 仅需编译通过。实际使用编辑器组件在第 16 阶段（Issue 详情 TipTap 集成）中完成。所以编译器检查通过就是本 task 的完成标准。
</action>
<verify>
<automated>cd clients/web && npm install @tiptap/core @tiptap/pm @tiptap/react @tiptap/starter-kit @tiptap/extension-horizontal-rule @tiptap/extension-underline @tiptap/extension-text-style @tiptap/extension-color @tiptap/extension-text-align @tiptap/extension-font-family @tiptap/extension-image @tiptap/extension-link @tiptap/extension-placeholder @tiptap/extension-code-block-lowlight tiptap-markdown lowlight 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | head -40</automated>
</verify>
<done>TypeScript 编译通过，@plane/editor 的 fork 包可以在 flow web 项目中编译（不含 Yjs 引用）。验证命令：`cd clients/web && grep -r "yjs\|hocuspocus\|y-indexeddb\|y-prosemirror\|y-protocols" src/lib/editor/ --include="*.ts" --include="*.tsx"`（应无结果）</done>
<acceptance_criteria>

- `clients/web/src/lib/editor/core/` 存在，包含 TipTap 编辑器核心代码
- `clients/web/src/lib/editor/index.ts` 存在，导出编辑器模块
- `grep -r "yjs\|hocuspocus" src/lib/editor/` 无匹配（Yjs 剥离完成）
- `import { Editor } from "@plane/editor"` 在 tsc 编译中无错误
- 所有 Fork 文件包含 `// FLOW:` 标记
  </acceptance_criteria>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary    | Description              |
| ----------- | ------------------------ |
| npm install | 外部包管理工具引入依赖包 |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                                           |
| --------- | --------- | ------------ | ----------- | --------------------------------------------------------- |
| T-14-02   | Tampering | npm install  | mitigate    | 只安装经过验证的 @tiptap/\* 系列包，锁定版本号            |
| T-14-SC   | Tampering | npm packages | mitigate    | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |

</threat_model>

<verification>
1. `cd clients/web && npx tsc --noEmit` 无错误
2. `grep -r "yjs\|hocuspocus\|@hocuspocus\|y-indexeddb\|y-prosemirror\|y-protocols" clients/web/src/lib/editor/` 无输出
3. `cd clients/web && npx vite build` 成功
</verification>

<success_criteria>

- @plane/ui 组件 fork 包可通过 `import { Button, Input, Spinner } from "@plane/ui"` 使用
- @plane/editor fork 包编译通过且不含 Yjs/Hocuspocus 引用
- 所有 Fork 文件标注 `// FLOW:` 标记
  </success_criteria>

<output>
创建 `.planning/phases/14-auth/14-02-SUMMARY.md` 完成任务后生成。
</output>
