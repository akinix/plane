---
phase: 14-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - clients/web/app/auth/sign-in/page.tsx
  - clients/web/app/auth/sign-up/page.tsx
  - clients/web/app/auth/forgot-password/page.tsx
  - clients/web/app/auth/reset-password/page.tsx
  - clients/web/app/routes.ts
  - clients/web/core/store/auth.store.ts
  - clients/web/core/store/root.store.ts
  - clients/web/core/lib/wrappers/authentication-wrapper.tsx
  - clients/web/core/lib/store-context.tsx
  - clients/web/app/provider.tsx
  - clients/web/app/page.tsx
  - clients/web/app/root.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
user_setup: []

must_haves:
  truths:
    - "用户在 `/auth/sign-in` 页面输入邮箱密码可登录（JWT），登录后跳转到 /（含 workspace/create 占位路由）"
    - "用户在 `/auth/sign-up` 页面可创建账户，注册后自动登录并跳转到 /workspace/create（per D-09）"
    - "用户在 `/auth/forgot-password` 页面输入邮箱发送重置链接"
    - "用户通过重置链接进入 `/auth/reset-password?token=xxx` 页面可设置新密码"
    - "登录后 token 持久化到 localStorage，刷新页面不丢失会话（per AUTH-04）"
    - "用户点击退出按钮清除 token 并跳转到 /auth/sign-in（per AUTH-05）"
    - "未认证用户访问 `/` 自动重定向到 `/auth/sign-in`（per AUTH-06）"
    - "认证页面保持 Plane 居中卡片式布局风格（per D-08）"
  artifacts:
    - path: "clients/web/app/auth/sign-in/page.tsx"
      provides: "登录页面（邮箱+密码）"
      contains: "signIn"
      min_lines: 40
    - path: "clients/web/app/auth/sign-up/page.tsx"
      provides: "注册页面"
      contains: "signUp"
    - path: "clients/web/app/auth/forgot-password/page.tsx"
      provides: "忘记密码页面"
      contains: "forgotPassword"
    - path: "clients/web/app/auth/reset-password/page.tsx"
      provides: "重置密码页面（从 URL query 取 token）"
      contains: "resetPassword"
    - path: "clients/web/core/store/auth.store.ts"
      provides: "AuthStore MobX 状态管理"
      contains: "AuthStore"
    - path: "clients/web/core/lib/wrappers/authentication-wrapper.tsx"
      provides: "路由保护组件，控制登录状态重定向"
      contains: "AuthenticationWrapper"
  key_links:
    - from: "auth/sign-in/page.tsx"
      to: "AuthService.signIn"
      via: "API call"
      pattern: "signIn"
    - from: "auth.store.ts"
      to: "localStorage"
      via: "getToken/setToken"
      pattern: "localStorage"
    - from: "authentication-wrapper.tsx"
      to: "AuthStore"
      via: "observer"
      pattern: "AuthStore"
---

<objective>
实现完整的 JWT 认证流程：登录、注册、忘记密码/重置密码、Token 持久化、退出登录、路由保护。

Purpose: 用户可使用邮箱密码完成注册/登录/退出，未认证用户被自动引导到登录页
Output: 功能完整的认证页面和认证状态管理
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-auth/14-CONTEXT.md（D-08~D-13 认证相关决策）
@.planning/phases/14-auth/14-01-PLAN.md（项目骨架，root.tsx，provider.tsx）
@.planning/phases/14-auth/14-03-PLAN.md（FlowApiService，AuthService 定义）
@.planning/codebase/CONVENTIONS.md（MobX Store 模式、import 分组、组件模式）
@apps/web/app/(all)/layout.tsx（Plane 认证页面布局模式）
@apps/web/core/components/auth-screens/header.tsx（Plane 认证页头部）
</context>

<tasks>

<task type="auto">
<name>Task 1: 实现 AuthStore（MobX）和 StoreProvider</name>
<files>
  clients/web/core/store/auth.store.ts
  clients/web/core/store/root.store.ts
  clients/web/core/lib/store-context.tsx
  clients/web/app/provider.tsx
</files>
<read_first>
- D:\github\akinix-plane\packages\services\src\auth\auth.service.ts（Plane AuthService API 模式参考）
- D:\github\akinix-plane\packages\types\src\auth.ts（IEmailCheckData、IPasswordSignInData 等类型）
- D:\github\akinix-plane\apps\web\core\store\root.store.ts（Plane CoreRootStore 模式）
- D:\github\akinix-plane\.planning\codebase\CONVENTIONS.md（MobX 模式：makeObservable + runInAction + computedFn）
</read_first>
<action>
按照 Plane 的 MobX Store 模式（per CONVENTIONS.md）创建：

**1. `clients/web/core/store/auth.store.ts` — AuthStore：**

```typescript
import { action, computed, makeObservable, observable, runInAction } from "mobx";
import type { IUser } from "@plane/types";
import { AuthService, getToken, removeToken, setToken } from "@/lib/services/auth.service";

export interface IAuthStore {
  // Observables
  currentUser: IUser | null;
  isLoading: boolean;
  isAuthenticated: boolean;
  // Actions
  initAuth: () => Promise<void>;
  signIn: (email: string, password: string) => Promise<void>;
  signUp: (email: string, password: string, firstName?: string, lastName?: string) => Promise<void>;
  signOut: () => Promise<void>;
  sendResetPasswordLink: (email: string) => Promise<void>;
  resetPassword: (token: string, password: string) => Promise<void>;
}

export class AuthStore implements IAuthStore {
  currentUser: IUser | null = null;
  isLoading: boolean = false;

  private authService: AuthService;

  constructor() {
    makeObservable(this, {
      currentUser: observable,
      isLoading: observable.ref,
      isAuthenticated: computed,
      initAuth: action,
      signIn: action,
      signUp: action,
      signOut: action,
    });
    this.authService = new AuthService();
  }

  get isAuthenticated(): boolean {
    return this.currentUser !== null && getToken() !== null;
  }

  initAuth = async () => {
    runInAction(() => {
      this.isLoading = true;
    });
    try {
      const token = getToken();
      if (!token) {
        runInAction(() => {
          this.currentUser = null;
        });
        return;
      }
      // 这里有 token 时获取当前用户信息（由后端 /api/v1/users/me/ 端点提供）
      // 如果后端没有 /users/me/，暂设为已登录状态
      runInAction(() => {
        this.currentUser = { id: "authenticated" } as IUser;
      });
    } finally {
      runInAction(() => {
        this.isLoading = false;
      });
    }
  };

  signIn = async (email: string, password: string) => {
    try {
      const response = await this.authService.signIn({ email, password });
      // humps 自动将 access_token camelize 为 accessToken
      if (response?.accessToken) {
        setToken(response.accessToken);
      }
      await this.initAuth();
    } catch (error) {
      throw error;
    }
  };

  signUp = async (email: string, password: string, firstName?: string, lastName?: string) => {
    try {
      const response = await this.authService.signUp({ email, password, firstName, lastName });
      // per D-09: 注册后自动登录（AuthService.signUp 已处理 setToken）
      if (response?.accessToken) {
        setToken(response.accessToken);
      }
      await this.initAuth();
    } catch (error) {
      throw error;
    }
  };

  signOut = async () => {
    try {
      await this.authService.signOut();
    } finally {
      removeToken();
      runInAction(() => {
        this.currentUser = null;
      });
    }
  };

  sendResetPasswordLink = async (email: string) => {
    return this.authService.sendResetPasswordLink({ email });
  };

  resetPassword = async (token: string, password: string) => {
    return this.authService.resetPassword(token, { password });
  };
}
```

**2. `clients/web/core/store/root.store.ts` — CoreRootStore：**

```typescript
// FLOW: 参照 Plane CoreRootStore 模式（CONVENTIONS.md）
import { AuthStore, type IAuthStore } from "./auth.store";

export interface ICoreRootStore {
  auth: IAuthStore;
}

export class CoreRootStore implements ICoreRootStore {
  auth: IAuthStore;

  constructor() {
    this.auth = new AuthStore();
  }
}
```

**3. `clients/web/core/lib/store-context.tsx` — StoreProvider React Context：**

```typescript
import React, { createContext, useContext } from "react";
import { CoreRootStore, type ICoreRootStore } from "@/store/root.store";

const StoreContext = createContext<ICoreRootStore | null>(null);

export const useStore = (): ICoreRootStore => {
  const store = useContext(StoreContext);
  if (!store) throw new Error("StoreProvider not found");
  return store;
};

export const StoreProvider = ({ children }: { children: React.ReactNode }) => {
  const [store] = React.useState(() => new CoreRootStore());
  return <StoreContext.Provider value={store}>{children}</StoreContext.Provider>;
};
```

**4. 更新 `clients/web/app/provider.tsx`：**

```typescript
import { ThemeProvider } from "next-themes";
import { StoreProvider } from "@/lib/store-context";

export function AppProvider({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider themes={["light", "dark"]} defaultTheme="system" attribute="data-theme" enableSystem>
      <StoreProvider>
        {children}
      </StoreProvider>
    </ThemeProvider>
  );
}
```

</action>
<verify>
<automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>TypeScript 编译通过。验证命令：`cd clients/web && node -e "require('fs').readdirSync('./core/store').forEach(f=>console.log(f))"` 显示 auth.store.ts 和 root.store.ts</done>
<acceptance_criteria>
- `core/store/auth.store.ts` 存在，AuthStore 类包含 currentUser、isAuthenticated、signIn、signUp、signOut 方法
- `core/store/root.store.ts` 存在，CoreRootStore 类实例化 AuthStore
- `core/lib/store-context.tsx` 存在，导出 StoreProvider 和 useStore hook
- `app/provider.tsx` 的 AppProvider 包含 StoreProvider
- `import { useStore } from "@/lib/store-context"` 编译无错误
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: 创建认证页面（登录/注册/忘记密码/重置密码）</name>
<files>
  clients/web/app/auth/sign-in/page.tsx
  clients/web/app/auth/sign-up/page.tsx
  clients/web/app/auth/forgot-password/page.tsx
  clients/web/app/auth/reset-password/page.tsx
  clients/web/app/routes.ts
</files>
<read_first>
- D:\github\akinix-plane\apps\web\app\(all)\sign-up\page.tsx（Plane 注册页面模式）
- D:\github\akinix-plane\apps\web\app\(all)\accounts\forgot-password\page.tsx（Plane 忘记密码页面模式）
- D:\github\akinix-plane\apps\web\core\components\auth-screens\header.tsx（Plane 认证头部）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（D-08: 居中卡片式、D-09: 注册后自动登录、D-10: 完整重置流程）
- D:\github\akinix-plane\apps\web\core\components\account\auth-forms\forgot-password.tsx（Plane 忘记密码表单）
</read_first>
<action>
按照 Plane 认证页面风格（per D-08）创建四个认证页面，使用 Axios AuthService 替代 Plane 的 CSRF 表单提交：

**1. `app/auth/sign-in/page.tsx` — 登录页面：**

居中卡片式布局（per D-08），左侧 Logo 区域（可选），右侧表单：

- 邮箱输入框（使用 @plane/ui 的 Input 组件）
- 密码输入框（显示/隐藏切换按钮，使用 lucide-react 的 Eye/EyeOff 图标）
- "忘记密码？" 链接跳转到 /auth/forgot-password
- "提交" 按钮（使用 @plane/ui 的 Button，loading 状态显示 Spinner）
- "还没有账户？注册" 链接跳转到 /auth/sign-up
- 提交后调用 `useStore().auth.signIn(email, password)`，成功后 useNavigate 跳转到 /（含 workspace/create 占位路由）
- 错误时显示 banner 错误消息（参照 Plane 的 AuthBanner 模式）

**2. `app/auth/sign-up/page.tsx` — 注册页面：**

- 居中卡片式布局
- 名字输入框（可选）
- 邮箱输入框
- 密码输入框 + 确认密码输入框
- 密码强度指示器（使用 @plane/utils 的 getPasswordStrength）
- "创建账户" 按钮
- 提交后调用 `useStore().auth.signUp(email, password)`，注册后自动登录并跳转到 /（per D-09）
- "已有账户？登录" 链接跳转到 /auth/sign-in

**3. `app/auth/forgot-password/page.tsx` — 忘记密码页面（per D-10）：**

- 居中卡片式布局
- 邮箱输入框
- "发送重置链接" 按钮
- 提交后调用 `useStore().auth.sendResetPasswordLink(email)`
- 成功后显示确认消息（"重置链接已发送"）
- "返回登录" 链接跳转到 /auth/sign-in

**4. `app/auth/reset-password/page.tsx` — 重置密码页面（per D-10）：**

- 从 URL query 参数读取 token（`useSearchParams`）
- 新密码输入框 + 确认密码输入框
- 密码强度指示器
- "重置密码" 按钮
- 提交后调用 `useStore().auth.resetPassword(token, password)`
- 成功后跳转到 /auth/sign-in 并显示成功消息
- 错误处理（无效 token、过期 token）

**5. 更新 `app/routes.ts` 注册认证页面路由：**

```typescript
import { type RouteConfig } from "@react-router/dev/routes";
import { flatRoutes } from "@react-router/fs-routes";

// 方式1：手动注册路由（推荐，避免 fs-routes 需特殊配置）
export default [
  {
    path: "/auth/sign-in",
    file: "app/auth/sign-in/page.tsx",
  },
  {
    path: "/auth/sign-up",
    file: "app/auth/sign-up/page.tsx",
  },
  {
    path: "/auth/forgot-password",
    file: "app/auth/forgot-password/page.tsx",
  },
  {
    path: "/auth/reset-password",
    file: "app/auth/reset-password/page.tsx",
  },
  {
    path: "/",
    file: "app/page.tsx", // placeholder 首页
  },
] satisfies RouteConfig;
```

**6. 创建 `app/page.tsx` 首页占位：**

```tsx
// 首页占位页，被 AuthenticationWrapper 包裹
import { AuthenticationWrapper } from "@/lib/wrappers/authentication-wrapper";
import { EPageTypes } from "@/lib/wrappers/authentication-wrapper";

export default function HomePage() {
  return (
    <AuthenticationWrapper pageType={EPageTypes.AUTHENTICATED}>
      <div className="flex h-screen items-center justify-center">
        <h1 className="text-2xl font-semibold">Welcome to Flow</h1>
        <button
          onClick={async () => {
            /* sign out */
          }}
        >
          Sign out
        </button>
      </div>
    </AuthenticationWrapper>
  );
}
```

**7. 创建 `app/auth/layout.tsx` 认证页面布局（可选，共享卡片样式）：**
居中容器包裹子页面，Plane 风格的居中卡片式布局。

在所有创建的文件头部添加 `// FLOW:` 标记（per D-06）。
</action>
<verify>
<automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>TypeScript 编译通过，4 个认证页面和路由配置就绪。验证命令：`cd clients/web && node -e "const f=['app/auth/sign-in/page.tsx','app/auth/sign-up/page.tsx','app/auth/forgot-password/page.tsx','app/auth/reset-password/page.tsx','app/routes.ts'];f.forEach(p=>console.log(p,require('fs').existsSync(p)?'OK':'MISSING'))"`</done>
<acceptance_criteria>

- `app/auth/sign-in/page.tsx` 存在，导出一个 React 组件，包含 email 和 password 输入框
- `app/auth/sign-up/page.tsx` 存在，包含 email、password 和 confirm password 输入框
- `app/auth/forgot-password/page.tsx` 存在，包含 email 输入框和发送按钮
- `app/auth/reset-password/page.tsx` 存在，从 URL 读取 token query 参数
- `app/routes.ts` 包含所有 4 个认证路由和首页路由
- 所有页面使用居中卡片式布局（per D-08）
- 所有页面文件包含 `// FLOW:` 标记
  </acceptance_criteria>
  </task>

<task type="auto">
<name>Task 3: 创建 AuthenticationWrapper 路由保护和退出登录功能</name>
<files>
  clients/web/core/lib/wrappers/authentication-wrapper.tsx
  clients/web/app/page.tsx
</files>
<read_first>
- D:\github\akinix-plane\apps\web\core\lib\wrappers\authentication-wrapper.tsx（Plane 路由保护组件参考）
- D:\github\akinix-plane\apps\web\helpers\authentication.helper.tsx（EPageTypes 枚举——Plane 的页面类型定义）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（AUTH-05: 退出登录、AUTH-06: 未认证重定向）
</read_first>
<action>
参照 Plane 的 AuthenticationWrapper 模式，实现简化 JWT 版本：

**1. `core/lib/wrappers/authentication-wrapper.tsx`：**

```typescript
import type { ReactNode } from "react";
import { observer } from "mobx-react";
import { useNavigate, useLocation } from "react-router";
import { useStore } from "@/lib/store-context";

export enum EPageTypes {
  PUBLIC = "PUBLIC",
  NON_AUTHENTICATED = "NON_AUTHENTICATED",
  AUTHENTICATED = "AUTHENTICATED",
}

type TAuthenticationWrapper = {
  children: ReactNode;
  pageType?: EPageTypes;
};

export const AuthenticationWrapper = observer(function AuthenticationWrapper(props: TAuthenticationWrapper) {
  const { children, pageType = EPageTypes.AUTHENTICATED } = props;
  const navigate = useNavigate();
  const location = useLocation();
  const { auth } = useStore();

  // 加载中
  if (auth.isLoading) {
    return (
      <div className="relative flex h-screen w-full items-center justify-center bg-canvas">
        {/* 使用 @plane/ui 的 Spinner 组件 */}
        <div className="size-8 animate-spin rounded-full border-2 border-custom-border-strong border-t-custom-primary" />
      </div>
    );
  }

  // PUBLIC: 所有人可访问
  if (pageType === EPageTypes.PUBLIC) return <>{children}</>;

  // NON_AUTHENTICATED: 仅未认证用户可访问（登录/注册页面）
  if (pageType === EPageTypes.NON_AUTHENTICATED) {
    if (!auth.isAuthenticated) return <>{children}</>;
    else {
      navigate("/", { replace: true });
      return null;
    }
  }

  // AUTHENTICATED: 仅认证用户可访问（per AUTH-06）
  if (pageType === EPageTypes.AUTHENTICATED) {
    if (auth.isAuthenticated) {
      return <>{children}</>;
    } else {
      // 未认证 → 重定向到登录页，带上当前路径作为 next_path
      const nextPath = encodeURIComponent(location.pathname + location.search);
      navigate(`/auth/sign-in?next_path=${nextPath}`, { replace: true });
      return null;
    }
  }

  return <>{children}</>;
});
```

**2. 在 `app/auth/sign-in/page.tsx` 中添加退出按钮（实际退出功能在 `app/page.tsx` 首页中实现）：**

首页 `app/page.tsx` 中添加退出按钮：

```tsx
import { useNavigate } from "react-router";
import { useStore } from "@/lib/store-context";
import { AuthenticationWrapper, EPageTypes } from "@/lib/wrappers/authentication-wrapper";

export default function HomePage() {
  const navigate = useNavigate();
  const { auth } = useStore();

  const handleSignOut = async () => {
    await auth.signOut();
    navigate("/auth/sign-in", { replace: true });
  };

  return (
    <AuthenticationWrapper pageType={EPageTypes.AUTHENTICATED}>
      <div className="flex h-screen items-center justify-center">
        <div className="text-center">
          <h1 className="text-2xl font-semibold mb-4">Welcome to Flow</h1>
          <button onClick={handleSignOut} className="rounded-md bg-custom-primary px-4 py-2 text-white">
            Sign out {/* per AUTH-05: 退出登录 */}
          </button>
        </div>
      </div>
    </AuthenticationWrapper>
  );
}
```

**3. 页面布局上逻辑（在 root.tsx 的 AppProvider 附近）确保 auth 初始化：**

在 `app/provider.tsx` 中增加 auth 初始化：

```typescript
import { useEffect } from "react";
import { useStore } from "@/lib/store-context";

export function AuthInitializer({ children }: { children: React.ReactNode }) {
  const { auth } = useStore();

  useEffect(() => {
    auth.initAuth();
  }, []);

  return <>{children}</>;
}
```

然后将 AuthInitializer 添加到 AppProvider 的返回中。

**4. 在首页路由上添加 AuthenticationWrapper 的 EPageTypes 参数以验证路由保护。**

在所有新建文件头部添加 `// FLOW:` 标记（per D-06）。
</action>
<verify>
<automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>TypeScript 编译通过。验证命令：`grep -q "AuthenticationWrapper" clients/web/app/page.tsx && echo "route guard active"`</done>
<acceptance_criteria>

- `core/lib/wrappers/authentication-wrapper.tsx` 存在，导出 AuthenticationWrapper 组件和 EPageTypes 枚举
- 受保护页面（首页）使用 AuthenticationWrapper pageType={EPageTypes.AUTHENTICATED}
- 未认证用户访问 / 自动重定向到 /auth/sign-in（per AUTH-06）
- 首页包含退出按钮，调用 auth.signOut() 后跳转到 /auth/sign-in（per AUTH-05）
- 注册页和登录页使用 AuthenticationWrapper pageType={EPageTypes.NON_AUTHENTICATED}
- 所有文件包含 `// FLOW:` 标记
  </acceptance_criteria>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                          |
| ----------------------------------- | ------------------------------------ |
| Browser→(fetch)→Vite proxy→.NET API | JWT token 通过 HTTP header 传输      |
| localStorage→AuthStore              | AuthStore 从 localStorage 读写 token |

## STRIDE Threat Register

| Threat ID | Category               | Component           | Disposition | Mitigation Plan                                                                    |
| --------- | ---------------------- | ------------------- | ----------- | ---------------------------------------------------------------------------------- |
| T-14-06   | Information Disclosure | JWT in localStorage | accept      | Phase 14 使用 localStorage；后续可增加 httpOnly cookie 选项（Claude's discretion） |
| T-14-07   | Spoofing               | AuthStore.initAuth  | mitigate    | 从 localStorage 读取 token 并验证存在性                                            |
| T-14-08   | Elevation of Privilege | Route guard bypass  | mitigate    | AuthenticationWrapper 在组件层保护，后端 API 也有 JWT 验证                         |
| T-14-09   | Repudiation            | signOut             | accept      | 客户端清除 token；服务端 token 失效后续增加                                        |
| T-14-SC   | Tampering              | npm packages        | mitigate    | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS]                          |

</threat_model>

<verification>
1. `cd clients/web && npx tsc --noEmit` 无错误
2. 验证所有 4 个认证页面路由配置正确
3. 验证 AuthenticationWrapper 正确处理 3 种页面类型（PUBLIC、NON_AUTHENTICATED、AUTHENTICATED）
4. 验证 AuthStore 的 signIn/signUp/signOut 方法调用正确的 AuthService 方法
5. 验证 signOut 清除 localStorage 中的 token 并重定向
</verification>

<success_criteria>

- 用户在登录页面可用邮箱密码登录（JWT），登录后 token 持久化到 localStorage
- 用户在注册页面可创建账户，注册后自动登录
- 用户可使用忘记密码/重置密码功能
- 用户可退出登录清除 token 并跳转到登录页
- 未认证用户访问受保护路由自动重定向到登录页
- 刷新页面不丢失会话（token 存在则保持登录状态）
  </success_criteria>

<output>
创建 `.planning/phases/14-auth/14-04-SUMMARY.md` 完成任务后生成。
</output>
