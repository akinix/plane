---
phase: 14-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - clients/web/src/lib/services/flow-api.service.ts
  - clients/web/src/lib/services/auth.service.ts
  - clients/web/src/lib/services/index.ts
  - clients/web/src/lib/types/error.ts
  - clients/web/src/lib/types/pagination.ts
  - clients/web/vite.config.ts
  - clients/web/.env
  - yh-flow/src/Host/YH.Flow.Api/Program.cs
autonomous: true
requirements:
  - SCAFF-05
  - SCAFF-06
  - SCAFF-07
  - SCAFF-08
  - SCAFF-09
user_setup: []

must_haves:
  truths:
    - "Axios 实例自动在请求头附加 `Authorization: Bearer <token>`"
    - "Axios 响应拦截器自动将 .NET API 的 snake_case 转换为 camelCase（使用 humps）"
    - '.NET API 返回 `{"error":"message"}` 格式时被标准化为 ErrorResponse 类型'
    - "分页响应格式兼容 Plane 前端的 TPaginatedResponse 类型"
    - "Vite 开发代理将 /api/v1/ 请求转发到 .NET API"
    - ".NET API JSON 序列化使用 SnakeCaseLower 策略"
  artifacts:
    - path: "clients/web/src/lib/services/flow-api.service.ts"
      provides: "FlowApiService 基类，JWT Bearer 拦截器、humps 转换、错误标准化"
      contains: "Authorization"
      min_lines: 80
    - path: "clients/web/src/lib/services/auth.service.ts"
      provides: "AuthService 扩展 FlowApiService，提供登录/注册/密码重置等方法"
      contains: "AuthService"
      min_lines: 50
    - path: "clients/web/src/lib/types/error.ts"
      provides: "标准化错误响应类型 ErrorResponse"
      contains: "ErrorResponse"
    - path: "yh-flow/src/Host/YH.Flow.Api/Program.cs"
      provides: ".NET API 的 SnakeCaseLower 和日期格式配置"
      contains: "SnakeCaseLower"
  key_links:
    - from: "flow-api.service.ts"
      to: "localStorage"
      via: "getToken/setToken"
      pattern: "localStorage"
    - from: "flow-api.service.ts"
      to: "humps"
      via: "response interceptor"
      pattern: "humps"
---

<objective>
构建 Flow Web 的 API 适配层：FlowApiService 基类（JWT Bearer）、错误标准化、分页适配、Vite 代理配置、.NET API SnakeCase 配置。

Purpose: 使前端能通过 JWT Bearer 认证方式调用 .NET API，保持与 Plane 前端一致的响应格式体验
Output: 功能完整的 API 服务基类和认证服务类
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-auth/14-CONTEXT.md（D-11: FlowApiService、D-12: humps、D-13: 错误标准化）
@.planning/PROJECT.md（yh-flow 后端结构）
@yh-flow/CLAUDE.md（yh-flow 项目信息、端口号）
</context>

<tasks>

<task type="auto">
<name>Task 1: 创建 FlowApiService 基类（JWT Bearer 拦截器 + humps 转换）</name>
<files>
  clients/web/src/lib/services/flow-api.service.ts
  clients/web/src/lib/services/auth.service.ts
  clients/web/src/lib/services/index.ts
  clients/web/src/lib/types/error.ts
  clients/web/src/lib/types/pagination.ts
</files>
<read_first>
- D:\github\akinix-plane\packages\services\src\api.service.ts（Plane 现有 APIService 模式——get/post/put/patch/delete 方法签名）
- D:\github\akinix-plane\packages\types\src\auth.ts（IEmailCheckData、IPasswordSignInData、ILoginTokenResponse 类型）
- D:\github\akinix-plane\packages\types\src\common.ts（TPaginationInfo 类型）
- D:\github\akinix-plane\packages\types\src\pagination.ts（TPaginatedResponse 类型）
- D:\github\akinix-plane\packages\constants\src\auth\index.ts（EAuthErrorCodes 枚举）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（D-11: FlowApiService、D-12: humps、D-13: 错误标准化）
</read_first>
<action>
创建三个核心文件：

**1. `src/lib/services/flow-api.service.ts` — FlowApiService 基类：**

参照 Plane 的 APIService（已读文件），使用 JWT Bearer 替代 withCredentials + CSRF：

```typescript
// 保持与 Plane APIService 一致的 get/post/put/patch/delete 方法签名（per STRUCTURE.md API 服务模式）
// 但用 JWT Bearer 替代 withCredentials: true（per D-11）

import axios, { type AxiosInstance, type AxiosRequestConfig } from "axios";
import humps from "humps"; // per D-12

// Token 管理
const TOKEN_KEY = "flow_access_token";
export const getToken = (): string | null => localStorage.getItem(TOKEN_KEY);
export const setToken = (token: string): void => localStorage.setItem(TOKEN_KEY, token);
export const removeToken = (): void => localStorage.removeItem(TOKEN_KEY);

// 错误响应类型（per D-13）
export interface ErrorResponse {
  error: string;
}

export abstract class FlowApiService {
  protected baseURL: string;
  private axiosInstance: AxiosInstance;

  constructor(baseURL: string) {
    this.baseURL = baseURL;
    this.axiosInstance = axios.create({ baseURL });

    // Request 拦截器：自动附加 JWT Bearer token（per D-11）
    this.axiosInstance.interceptors.request.use((config) => {
      const token = getToken();
      if (token) {
        config.headers.Authorization = `Bearer ${token}`;
      }
      return config;
    });

    // Response 拦截器：snake_case → camelCase 转换（per D-12）
    this.axiosInstance.interceptors.response.use(
      (response) => {
        if (response.data && typeof response.data === "object") {
          response.data = humps.camelizeKeys(response.data);
        }
        return response;
      },
      (error) => {
        // 401 处理：token 过期，清除并跳转到登录页
        if (error?.response?.status === 401) {
          removeToken();
          window.location.href = "/auth/sign-in";
        }
        // 错误响应标准化（per D-13）
        if (error?.response?.data) {
          const data = error.response.data;
          // .NET API 返回 {"error": "message"} 或 {"title": "message"} 格式
          const standardized: ErrorResponse = {
            error: data.error || data.title || data.detail || "Unknown error",
          };
          error.response.data = standardized;
        }
        return Promise.reject(error);
      }
    );
  }

  // 保持与 Plane APIService 一致的方法签名（per STRUCTURE.md API service patterns）
  get(url: string, params = {}, config: AxiosRequestConfig = {}) {
    return this.axiosInstance.get(url, { ...params, ...config });
  }
  post(url: string, data = {}, config: AxiosRequestConfig = {}) {
    return this.axiosInstance.post(url, data, config);
  }
  put(url: string, data = {}, config: AxiosRequestConfig = {}) {
    return this.axiosInstance.put(url, data, config);
  }
  patch(url: string, data = {}, config: AxiosRequestConfig = {}) {
    return this.axiosInstance.patch(url, data, config);
  }
  delete(url: string, data?: any, config: AxiosRequestConfig = {}) {
    return this.axiosInstance.delete(url, { data, ...config });
  }
  request(config = {}) {
    return this.axiosInstance(config);
  }
}
```

**2. `src/lib/types/error.ts` — 错误响应类型（per D-13）：**

```typescript
export interface ErrorResponse {
  error: string;
}
```

**3. `src/lib/types/pagination.ts` — 分页适配类型（per SCAFF-09）：**

```typescript
// 兼容 Plane 前端的 TPaginatedResponse 格式
export interface PlanePagedResult<T> {
  results: T;
  count?: number;
  totalResults?: number;
  totalPages?: number;
  nextCursor?: string;
  prevCursor?: string;
  nextPageResults?: boolean;
  prevPageResults?: boolean;
  extraStats?: string | null;
}

// humps 转换后的 camelCase 版本：Plane 前端无需感知 snake_case
// .NET API 返回 snake_case，humps.camelizeKeys 自动转换为 camelCase
```

**4. `src/lib/services/auth.service.ts` — AuthService（JWT 版本，替代 Plane 的 CSRF AuthService）：**

```typescript
// 参照 Plane AuthService（已读文件）保持一致的 API 语义（per CONTEXT.md code_context）
// 但使用 JWT Bearer 替代 CSRF token（per D-11）
// 端点映射到 .NET Identity API

import type { IEmailCheckData, IEmailCheckResponse, IPasswordSignInData, ILoginTokenResponse } from "@plane/types";
import { FlowApiService, setToken } from "./flow-api.service";

export class AuthService extends FlowApiService {
  private static BASE_URL = import.meta.env.VITE_API_BASE_URL || "http://localhost:5030/api/v1";

  constructor() {
    super(AuthService.BASE_URL);
  }

  // 邮箱检查
  async emailCheck(data: IEmailCheckData): Promise<IEmailCheckResponse> {
    return this.post("/auth/email-check/", data)
      .then((res) => res?.data)
      .catch((err) => {
        throw err?.response?.data;
      });
  }

  // 登录（邮箱+密码）
  async signIn(data: IPasswordSignInData): Promise<ILoginTokenResponse> {
    return this.post("/auth/sign-in/", data)
      .then((res) => {
        const tokenData = res?.data as ILoginTokenResponse;
        if (tokenData?.accessToken) {
          // humps camelized
          setToken(tokenData.accessToken);
        }
        return tokenData;
      })
      .catch((err) => {
        throw err?.response?.data;
      });
  }

  // 注册
  async signUp(data: {
    email: string;
    password: string;
    firstName?: string;
    lastName?: string;
  }): Promise<ILoginTokenResponse> {
    return this.post("/auth/sign-up/", data)
      .then((res) => {
        const tokenData = res?.data as ILoginTokenResponse;
        if (tokenData?.accessToken) {
          setToken(tokenData.accessToken);
        }
        return tokenData;
      })
      .catch((err) => {
        throw err?.response?.data;
      });
  }

  // 发送重置密码邮件
  async sendResetPasswordLink(data: { email: string }): Promise<void> {
    return this.post("/auth/forgot-password/", data)
      .then((res) => res?.data)
      .catch((err) => {
        throw err?.response?.data;
      });
  }

  // 重置密码
  async resetPassword(token: string, data: { password: string }): Promise<void> {
    return this.post(`/auth/reset-password/${token}/`, data)
      .then((res) => res?.data)
      .catch((err) => {
        throw err?.response?.data;
      });
  }

  // 退出登录（客户端清除 token）
  async signOut(): Promise<void> {
    // 通知后端使 token 失效
    return this.post("/auth/sign-out/", {})
      .then(() => removeToken())
      .catch(() => removeToken());
  }

  // token 刷新（根据后端策略实现——per Claude's discretion）
  async refreshToken(): Promise<ILoginTokenResponse> {
    return this.post("/auth/refresh/")
      .then((res) => {
        const tokenData = res?.data as ILoginTokenResponse;
        if (tokenData?.accessToken) {
          setToken(tokenData.accessToken);
        }
        return tokenData;
      })
      .catch((err) => {
        throw err?.response?.data;
      });
  }
}
```

**5. `src/lib/services/index.ts` — barrel export：**

```typescript
export { FlowApiService, getToken, setToken, removeToken } from "./flow-api.service";
export { AuthService } from "./auth.service";
```

</action>
<verify>
<automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20</automated>
</verify>
<done>所有 Service 文件 TypeScript 编译通过。验证命令：`cd clients/web && node -e "const fs=require('fs');[`src/lib/services/flow-api.service.ts`,`src/lib/services/auth.service.ts`,`src/lib/services/index.ts`,`src/lib/types/error.ts`,`src/lib/types/pagination.ts`].forEach(f=>console.log(f,fs.existsSync(f)?'OK':'MISSING'))"`</done>
<acceptance_criteria>
- `src/lib/services/flow-api.service.ts` 存在，包含 FlowApiService 抽象类
- request 拦截器中包含 `Authorization: Bearer` 逻辑
- response 拦截器包含 `humps.camelizeKeys` 调用（D-12）
- response 错误拦截器将 `{"error": "message"}` 标准化为 ErrorResponse（D-13）
- `src/lib/services/auth.service.ts` 存在，导出 AuthService 类，包含 signIn、signUp、signOut 等方法
- `import { FlowApiService } from "@/lib/services/flow-api.service"` tsc 编译无错误
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: 配置 Vite 代理和 .NET API SnakeCase 设置</name>
<files>
  clients/web/vite.config.ts
  clients/web/.env
  yh-flow/src/Host/YH.Flow.Api/Program.cs
</files>
<read_first>
- 已读 clients/web/vite.config.ts（Task 1 中 Plan 14-01 创建）
- D:\github\akinix-plane\yh-flow\src\Host\YH.Flow.Api\Program.cs（当前 Web API 启动配置，含 ConfigureHttpJsonOptions）
- D:\github\akinix-plane\.planning\phases\14-auth\14-CONTEXT.md（SCAFF-05: Vite proxy、SCAFF-06: SnakeCase）
</read_first>
<action>
**1. 更新 `clients/web/vite.config.ts` 添加 Vite 代理转发（per SCAFF-05）：**
```typescript
// 在 defineConfig 的 server 配置中增加 proxy
server: {
  host: true,
  port: 5173,
  proxy: {
    "/api": {
      target: process.env.VITE_API_PROXY_TARGET || "https://localhost:7030",
      changeOrigin: true,
      secure: false,  // 开发环境自签名证书
    },
  },
},
```

**2. 更新 `clients/web/.env` 添加代理目标配置：**

```
VITE_API_BASE_URL=http://localhost:5173/api/v1
VITE_API_PROXY_TARGET=https://localhost:7030
```

**3. 更新 `yh-flow/src/Host/YH.Flow.Api/Program.cs` 配置 SnakeCase 和日期格式（per SCAFF-06）：**

在 `ConfigureHttpJsonOptions` 代码块中添加 `PropertyNamingPolicy = JsonNamingPolicy.SnakeCaseLower` 和日期格式：

```csharp
// 原代码
builder.Services.ConfigureHttpJsonOptions(options =>
{
    options.SerializerOptions.Converters.Add(new JsonStringEnumConverter());
});

// 改为（添加 PropertyNamingPolicy 和日期格式）
using System.Text.Json;

builder.Services.ConfigureHttpJsonOptions(options =>
{
    options.SerializerOptions.PropertyNamingPolicy = JsonNamingPolicy.SnakeCaseLower;  // FLOW: SCAFF-06
    options.SerializerOptions.Converters.Add(new JsonStringEnumConverter());
    options.SerializerOptions.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;  // FLOW: SCAFF-06
});

// 同时配置 MVC 控制器 JSON 选项（当使用 [ApiController] 时）
builder.Services.Configure<Microsoft.AspNetCore.Mvc.JsonOptions>(options =>
{
    options.JsonSerializerOptions.PropertyNamingPolicy = JsonNamingPolicy.SnakeCaseLower;  // FLOW: SCAFF-06
    options.JsonSerializerOptions.Converters.Add(new JsonStringEnumConverter());
    options.JsonSerializerOptions.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;  // FLOW: SCAFF-06
});
```

在修改处添加 `// FLOW: SCAFF-06` 标记注释（per D-06）。
</action>
<verify>
<automated>cd clients/web && npx tsc --noEmit 2>&1 | head -20 && dotnet build yh-flow/src/YH.Flow.slnx 2>&1 | tail -5</automated>
</verify>
<done>TypeScript 编译通过，.NET 构建成功。验证命令：`cd clients/web && grep -q "proxy" vite.config.ts && echo "proxy OK"` 和 `grep -q "SnakeCaseLower" yh-flow/src/Host/YH.Flow.Api/Program.cs && echo "SnakeCase OK"`</done>
<acceptance_criteria>

- `clients/web/vite.config.ts` 包含 `proxy` 配置块，目标指向 https://localhost:7030
- `clients/web/.env` 包含 VITE_API_PROXY_TARGET
- `yh-flow/src/Host/YH.Flow.Api/Program.cs` 的 ConfigureHttpJsonOptions 中包含 `JsonNamingPolicy.SnakeCaseLower`
- `dotnet build yh-flow/src/YH.Flow.slnx` 编译成功（针对后端改动）
  </acceptance_criteria>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                       |
| --------------------------- | ------------------------------------------------- |
| Browser→Vite proxy→.NET API | JWT Bearer token 从浏览器经过 Vite 代理到后端 API |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                    |
| --------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------- |
| T-14-03   | Information Disclosure | JWT token in localStorage | mitigate    | token 存入 localStorage；使用 https 防止 XSS 窃取；后续可增加 httpOnly cookie 选项 |
| T-14-04   | Spoofing               | Axios request interceptor | mitigate    | 只从 localStorage 读取 token 附加到请求头                                          |
| T-14-05   | Repudiation            | Error standardization     | accept      | 标准化错误已足以 debug；完整审计日志在服务端                                       |
| T-14-SC   | Tampering              | npm packages              | mitigate    | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS]                          |

</threat_model>

<verification>
1. `cd clients/web && npx tsc --noEmit` 无错误
2. `dotnet build yh-flow/src/YH.Flow.slnx` 成功（验证后端变动）
3. 验证 FlowApiService 包含 request/response 拦截器
4. 验证 AuthService 导出 signIn/signUp/sendResetPasswordLink/resetPassword/signOut 方法
</verification>

<success_criteria>

- FlowApiService 基类可用，请求自动附加 JWT Bearer token
- humps 自动转换 snake_case↔camelCase
- .NET API 返回 SnakeCase JSON
- 错误响应标准化为 `{error: string}`
- Vite 代理正确转发 API 请求
  </success_criteria>

<output>
创建 `.planning/phases/14-auth/14-03-SUMMARY.md` 完成任务后生成。
</output>
