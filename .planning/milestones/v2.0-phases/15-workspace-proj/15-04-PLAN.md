---
phase: 15-workspace-proj
plan: 04
type: execute
wave: 4
depends_on: [15-02, 15-03]
files_modified:
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/workspaces/[workspaceId]/projects/page.tsx
  - yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx
  - yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/settings/page.tsx
  - yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/members/page.tsx
  - yh-flow/clients/web/app/components/project/project-list.tsx
  - yh-flow/clients/web/app/components/project/project-card.tsx
  - yh-flow/clients/web/app/components/project/create-project-modal.tsx
  - yh-flow/clients/web/app/components/project/project-dashboard.tsx
  - yh-flow/clients/web/app/components/project/project-settings-form.tsx
  - yh-flow/clients/web/app/components/project/project-member-list.tsx
  - yh-flow/clients/web/app/components/project/project-selector.tsx
  - yh-flow/clients/web/app/components/project/index.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05]
user_setup: []

must_haves:
  truths:
    - "User can create a project via modal/button in workspace (PROJ-01)"
    - "Project creation form includes name, identifier, emoji logo, and description fields"
    - "Project list page shows all projects in the workspace with search and sort controls (PROJ-02)"
    - "Project list can be filtered by search text input"
    - "Project dashboard shows project name, identifier, description, and placeholder for future views (PROJ-03 per D-P15-11)"
    - "Project settings page allows editing name, identifier, and description (PROJ-03 per D-P15-11)"
    - "Project members page shows member list with role management (PROJ-04)"
    - "Project selector in sidebar tree navigates between projects (PROJ-05)"
    - "Clicking a project card navigates to the project dashboard"
  artifacts:
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/projects/page.tsx"
      provides: "Project list page with search/sort"
      min_lines: 30
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx"
      provides: "Project dashboard page (framework per D-P15-11)"
      min_lines: 20
    - path: "yh-flow/clients/web/app/components/project/create-project-modal.tsx"
      provides: "Project creation modal with emoji logo selector"
      exports: ["CreateProjectModal"]
    - path: "yh-flow/clients/web/app/components/project/project-list.tsx"
      provides: "Project list with search input and sort controls"
      exports: ["ProjectList"]
    - path: "yh-flow/clients/web/app/components/project/project-dashboard.tsx"
      provides: "Project dashboard content (heading + placeholder view tabs)"
      exports: ["ProjectDashboard"]
  key_links:
    - from: "app/workspaces/[workspaceId]/projects/page.tsx"
      to: "app/components/project/project-list.tsx"
      via: "composition"
      pattern: "ProjectList"
    - from: "app/components/project/create-project-modal.tsx"
      to: "src/lib/ui/emoji-picker/emoji-picker.tsx"
      via: "EmojiPicker import for project logo"
      pattern: "EmojiPicker"
    - from: "app/routes.ts"
      to: "app/workspaces/[workspaceId]/projects/[projectId]/page.tsx + app/workspaces/[workspaceId]/projects/page.tsx"
      via: "project routes registered by this plan"
      pattern: "projects/:projectId|projects/*"
---

<objective>
实现项目管理功能：项目创建（模态框）、项目列表（搜索/排序）、项目详情仪表板（框架）、项目设置、项目成员管理和项目选择器。同时将项目路由注册到 routes.ts 中，确保路由顺序正确。

Purpose: 满足 PROJ-01~05 需求，让用户可以在工作区内创建、浏览和管理项目。
Output: 完整的项目管理页面和组件 + routes.ts 中的项目路由注册，使用 mock 数据运行。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-workspace-proj/15-CONTEXT.md

# 基础设施（Plan 1 产出）

@yh-flow/clients/web/src/lib/mock-data.ts
@yh-flow/clients/web/src/lib/hooks/use-workspaces.ts
@yh-flow/clients/web/src/lib/hooks/use-projects.ts
@yh-flow/clients/web/src/lib/hooks/use-members.ts
@yh-flow/clients/web/app/store/workspace.store.ts
@yh-flow/clients/web/app/store/project.store.ts

# 布局（Plan 2 产出）+ 工作区路由（Plan 3 产出）

@yh-flow/clients/web/app/layouts/workspace-layout.tsx
@yh-flow/clients/web/app/routes.ts

# 类型参考

@yh-flow/clients/web/src/lib/types/project/projects.ts — IProject, IPartialProject, IProjectLite, TProjectMembership
@yh-flow/clients/web/src/lib/types/workspace.ts — IWorkspace
@yh-flow/clients/web/src/lib/types/enums.ts — EUserPermissions, EUserProjectRoles
@yh-flow/clients/web/src/lib/types/common.ts — TLogoProps

# Plane 参考

@apps/web/core/components/project/card-list.tsx
@apps/web/core/components/project/card.tsx
@apps/web/core/components/project/create-project-modal.tsx
@apps/web/core/components/project/create/common-attributes.tsx
@apps/web/core/components/navigation/project-header.tsx
@apps/web/core/components/navigation/project-header-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 注册项目路由 + 创建项目列表页面（搜索/排序）和创建项目模态框</name>
  <files>
    yh-flow/clients/web/app/routes.ts
    yh-flow/clients/web/app/workspaces/[workspaceId]/projects/page.tsx
    yh-flow/clients/web/app/components/project/project-list.tsx
    yh-flow/clients/web/app/components/project/project-card.tsx
    yh-flow/clients/web/app/components/project/create-project-modal.tsx
    yh-flow/clients/web/app/components/project/index.ts
  </files>
  <read_first>
    yh-flow/clients/web/app/routes.ts — 当前路由配置（含 auth routes + workspace layout + workspace 路由 from 15-03）
    yh-flow/clients/web/src/lib/hooks/use-projects.ts — useProjects(workspaceId)
    yh-flow/clients/web/src/lib/mock-data.ts — MOCK_PROJECTS 结构
    yh-flow/clients/web/src/lib/types/project/projects.ts — IProject / IPartialProject
    yh-flow/clients/web/src/lib/types/common.ts — TLogoProps
    yh-flow/clients/web/src/lib/ui/emoji-picker/emoji-picker.tsx — EmojiPicker API
  </read_first>
  <action>
    1. 更新 `app/routes.ts`，在 workspace-layout 的 children 数组中添加项目路由。**关键：确保 `projects/:projectId` 在 `projects/*` 之前注册，否则 splat 路由会劫持所有子路径导致项目详情页不可达。**

    ```
    {
      layout: "app/layouts/workspace-layout.tsx",
      children: [
        { index: true, file: "app/workspace-redirect/page.tsx" },
        // ↓ 来自 15-03 的工作区路由
        { path: "workspaces/:workspaceId", file: "app/workspaces/[workspaceId]/page.tsx" },
        { path: "workspaces/:workspaceId/settings/*", file: "app/workspaces/[workspaceId]/settings/page.tsx" },
        { path: "workspaces/:workspaceId/members/*", file: "app/workspaces/[workspaceId]/members/page.tsx" },
        // ↓ 自此为此 plan 添加的项目路由 —— :projectId 必须在 /* 之前
        {
          path: "workspaces/:workspaceId/projects/:projectId",
          children: [
            { index: true, file: "app/workspaces/[workspaceId]/projects/[projectId]/page.tsx" },
            {
              path: "settings",
              file: "app/workspaces/[workspaceId]/projects/[projectId]/settings/page.tsx",
            },
            {
              path: "members",
              file: "app/workspaces/[workspaceId]/projects/[projectId]/members/page.tsx",
            },
          ],
        },
        {
          path: "workspaces/:workspaceId/projects/*",
          file: "app/workspaces/[workspaceId]/projects/page.tsx",
        },
      ],
    },
    ```
    - `projects/:projectId`（项目详情/设置/成员）在 `projects/*`（项目列表）之前
    - 保持 15-03 已有的 workspace 路由、auth routes 和 `/workspaces/create` 不变
    - 不要重复添加 15-03 已注册的路由

    2. 创建 `app/components/project/project-card.tsx`（与 workspace dashboard 的 ProjectCard 不同——这是列表页卡片）：
    - 组件名：`ProjectCard`
    - Props: `{ project: IProject; onClick?: () => void }`
    - 显示：Emoji logo（fromCodePoint 渲染 TLogoProps.emoji.value）、项目名称、identifier、简短描述
    - 样式：flex items-center gap-3 p-3 rounded-lg border hover:bg-custom-background-80 cursor-pointer
    - 点击卡片 → navigate 到 `/workspaces/:workspaceId/projects/${project.id}`

    3. 创建 `app/components/project/project-list.tsx`（PROJ-02）：
    - 组件名：`ProjectList`
    - Props: `{ workspaceId: string }`
    - 使用 useProjects(workspaceId) 获取项目列表
    - 搜索输入框（Search 图标 + text input），过滤项目名称（filter by name.toLowerCase().includes(query)）
    - 排序下拉（CustomSelect 或简单 select）：按名称 A-Z、名称 Z-A、创建时间排序
    - 渲染过滤和排序后的 ProjectCard 列表
    - 空状态：搜索无结果时显示 "未找到匹配的项目"；无项目时显示 "暂无项目" + 创建按钮

    4. 创建 `app/components/project/create-project-modal.tsx`（per PROJ-01）：
    - 组件名：`CreateProjectModal`
    - Props: `{ isOpen: boolean; onClose: () => void; workspaceId: string }`
    - 模态框内容：
      - 标题 "创建项目"
      - Emoji logo selector（使用 EmojiPicker 组件）
      - 项目名称（text input，必填）
      - 项目标识符（text input，自动从名称生成大写缩写，可编辑）
      - 描述（textarea，非必填）
    - 底部按钮：取消（onClose）+ 创建（mock 提交）
    - mock 提交：console.log 新项目数据，关闭 Modal，显示 toast "项目创建成功"
    - 使用 headlessui Dialog 作为 Modal 容器

    5. 创建 `app/workspaces/[workspaceId]/projects/page.tsx`：
    - 页面标题 "项目列表"
    - 右上角 "创建项目" 按钮 → 打开 CreateProjectModal
    - 渲染 ProjectList(workspaceId)

    6. 创建 `app/components/project/index.ts` 作为 barrel export。

  </action>
  <acceptance_criteria>
    - `app/routes.ts` 中 `projects/:projectId` 路由在 `projects/*` 之前注册
    - `ProjectList` 渲染搜索输入框、排序下拉和项目卡片列表，搜索过滤实时生效
    - `CreateProjectModal` 含 EmojiPicker、名称、标识符、描述字段，提交后关闭
    - 项目列表页可通过 `/workspaces/:workspaceId/projects` 访问
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Project routes registered with correct ordering. Project list with search/sort and create project modal work. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: 创建项目仪表板（框架）和项目设置页面</name>
  <files>
    yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/page.tsx
    yh-flow/clients/web/app/components/project/project-dashboard.tsx
    yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/settings/page.tsx
    yh-flow/clients/web/app/components/project/project-settings-form.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-projects.ts — useProject(workspaceId, projectId)
    yh-flow/clients/web/src/lib/types/project/projects.ts — IProject 完整字段
    apps/web/core/components/navigation/project-header.tsx — Plane 项目头导航模式
  </read_first>
  <action>
    1. 创建 `app/components/project/project-dashboard.tsx`（per PROJ-03, D-P15-11：Phase 15 仅项目详情框架）:
    - 组件名：`ProjectDashboard`
    - Props: `{ workspaceId: string; projectId: string }`
    - 使用 useProject(workspaceId, projectId) 获取项目详情
    - 展示：
      - 项目 header：Emoji logo + 项目名称 + identifier
      - 项目描述（如果有）
      - Tab/视图切换栏（placeholder 标签页）：Issues（#16 实现）、Cycles（#18 实现）、Modules（#18 实现）、Pages（#19 实现）、Views（#19 实现）
      - 当前标签页内容区：显示 "Issue 列表视图将在 Phase 16 中实现" 等占位文本
    - 标签栏使用 flex row，当前标签高亮（border-b-2 + text-custom-primary）

    2. 创建 `app/workspaces/[workspaceId]/projects/[projectId]/page.tsx`：
    - 使用 useParams() 获取 workspaceId 和 projectId
    - 渲染 ProjectDashboard(workspaceId, projectId)

    3. 创建 `app/components/project/project-settings-form.tsx`（项目设置 per PROJ-03/D-P15-11）：
    - 组件名：`ProjectSettingsForm`
    - Props: `{ workspaceId: string; projectId: string }`
    - 使用 useProject 获取当前项目数据
    - 表单字段：
      - Emoji logo（EmojiPicker 组件）
      - 项目名称（text input）
      - 标识符（text input，小写，不可修改或需警告）
      - 描述（textarea）
    - Mock 提交：显示 toast "设置已保存"

    4. 创建 `app/workspaces/[workspaceId]/projects/[projectId]/settings/page.tsx`：
    - 标题 "项目设置"
    - 渲染 ProjectSettingsForm

  </action>
  <acceptance_criteria>
    - `ProjectDashboard` 显示项目名称、identifier、描述和占位标签页（Issues/Cycles/Modules/Pages/Views）
    - 项目设置页面含 EmojiPicker、名称、标识符、描述编辑字段
    - 项目 dashboard 和设置页面可通过路由访问
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Project dashboard with placeholder tabs and settings form render correctly. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: 创建项目成员管理 + 项目选择器</name>
  <files>
    yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/members/page.tsx
    yh-flow/clients/web/app/components/project/project-member-list.tsx
    yh-flow/clients/web/app/components/project/project-selector.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-members.ts — useMembers(workspaceId)（也可用于项目成员 mock）
    yh-flow/clients/web/src/lib/hooks/use-projects.ts — useProjects(workspaceId)
    yh-flow/clients/web/src/lib/types/enums.ts — EUserProjectRoles
    yh-flow/clients/web/src/lib/types/project/projects.ts — IProjectMemberLite, TProjectMembership
    apps/web/core/components/navigation/project-header.tsx — Plane 项目选择器模式（CustomSearchSelect + 项目切换）
  </read_first>
  <action>
    1. 创建 `app/components/project/project-member-list.tsx`（per PROJ-04）：
    - 组件名：`ProjectMemberList`
    - Props: `{ workspaceId: string; projectId: string }`
    - 使用 useMembers(workspaceId) 获取成员（mock：用 workspace 成员模拟项目成员）
    - 列表布局：每行显示用户头像、姓名、邮箱、角色（Admin/Member/Guest via EUserProjectRoles）
    - 角色下拉：使用 @headlessui/react Menu 组件，可选 ADMIN(20)/MEMBER(15)/GUEST(5)
    - Mock 角色变更：console.log 变更信息 + toast "角色已更新"

    2. 创建 `app/workspaces/[workspaceId]/projects/[projectId]/members/page.tsx`：
    - 标题 "项目成员"
    - 渲染 ProjectMemberList

    3. 创建 `app/components/project/project-selector.tsx`（per PROJ-05 / D-P15-05 树形结构项目导航补充）：
    - 组件名：`ProjectSelector`
    - 与 workspace-switcher 模式类似，但用于项目级别
    - Props: `{ workspaceId: string; currentProjectId: string }`
    - 使用 useProjects(workspaceId) 获取项目列表
    - 显示当前项目名称 + 展开箭头
    - 下拉列出所有项目（含 emoji logo + 名称 + identifier）
    - 点击项目 → navigate 到 `/workspaces/${workspaceId}/projects/${project.id}`
    - 用于项目详情页顶部的快速切换（可集成到 TopBar 或项目 dashboard header 中）

    4. 确保 `app/components/project/index.ts` 已更新包含新组件导出。

  </action>
  <acceptance_criteria>
    - `ProjectMemberList` 渲染成员列表，每行含头像、姓名、邮箱、角色下拉
    - `ProjectSelector` 显示当前项目名，下拉列出所有项目并支持切换导航
    - 成员页面和选择器可通过路由和组件访问
    - `npx tsc --noEmit` 无错误
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Project member list with role management and project selector dropdown work. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                 |
| --------------------- | ----------------------------------------------------------- |
| client-side mock data | All project data is client-side mock, no server interaction |

## STRIDE Threat Register

| Threat ID | Category  | Component                            | Disposition | Mitigation Plan                                                |
| --------- | --------- | ------------------------------------ | ----------- | -------------------------------------------------------------- |
| T-15-08   | Tampering | CreateProjectModal name input        | mitigate    | Validate name max 100 chars, identifier max 10 chars uppercase |
| T-15-09   | Tampering | ProjectSettingsForm identifier input | mitigate    | Identifier should be uppercase alphanumeric only, max 10 chars |
| T-15-10   | Spoofing  | ProjectMemberList role dropdown      | accept      | Mock data only; real role enforcement requires backend Phase 3 |

</threat_model>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] routes.ts has `projects/:projectId` BEFORE `projects/*`
- [ ] Project list page shows projects with search/sort
- [ ] Create project modal opens and submits mock data
- [ ] Project dashboard shows header + placeholder view tabs
- [ ] Project settings form loads and mock-saves
- [ ] Project member list shows members with role dropdown
- [ ] Project selector switches between projects
</verification>

<success_criteria>

1. Project routes registered in routes.ts with correct ordering (projects/:projectId before projects/\*)
2. Project creation via modal with emoji logo selector per PROJ-01
3. Project list page with search filtering and sort controls per PROJ-02
4. Project dashboard with header and placeholder view tabs per PROJ-03 / D-P15-11
5. Project settings page with name, identifier, description, logo edit per PROJ-03
6. Project member list with role management per PROJ-04
7. Project selector component for navigating between projects per PROJ-05
8. All TypeScript compiles without errors
   </success_criteria>

<output>
Create `.planning/phases/15-workspace-proj/15-04-SUMMARY.md` when done
</output>
