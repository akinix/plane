---
phase: 19-pages-views
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/workspaces/[workspaceId]/pages/page.tsx
  - yh-flow/clients/web/app/components/pages/pages-list-main-content.tsx
  - yh-flow/clients/web/app/components/pages/pages-list-view.tsx
  - yh-flow/clients/web/app/components/pages/list/root.tsx
  - yh-flow/clients/web/app/components/pages/list/block.tsx
  - yh-flow/clients/web/app/components/pages/list/block-item-action.tsx
  - yh-flow/clients/web/app/components/pages/list/tab-navigation.tsx
  - yh-flow/clients/web/app/components/pages/list/search-input.tsx
  - yh-flow/clients/web/app/components/pages/list/order-by.tsx
  - yh-flow/clients/web/app/components/pages/list/applied-filters/root.tsx
  - yh-flow/clients/web/app/components/pages/list/filters/root.tsx
  - yh-flow/clients/web/app/components/pages/header/favorite-control.tsx
  - yh-flow/clients/web/app/components/pages/header/archived-badge.tsx
  - yh-flow/clients/web/app/components/pages/modals/create-page-modal.tsx
  - yh-flow/clients/web/app/components/pages/modals/page-form.tsx
  - yh-flow/clients/web/app/components/pages/modals/delete-page-modal.tsx
  - yh-flow/clients/web/app/components/pages/loaders/page-content-loader.tsx
  - yh-flow/clients/web/app/components/pages/dropdowns/actions.tsx
  - yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-04
  - PAGE-05
user_setup: []

must_haves:
  truths:
    - "用户可以在 /workspaces/:wsId/pages 看到页面列表（公开/私人/已归档 三个 Tab）"
    - "用户可以在页面列表中搜索页面名称"
    - "用户可以在页面列表中按名称/创建时间/更新时间/最近打开排序"
    - "用户可以在页面列表中切换 Tab（公开/私人/已归档）看到对应的页面"
    - "用户可以点击页面卡片进入编辑器页面"
    - "用户可以创建新页面（弹窗输入名称、选择 emoji logo、设置访问权限）"
    - "用户可以删除页面（确认弹窗）"
    - "用户可以收藏/取消收藏页面"
    - "侧边栏显示'页面'链接，点击跳转到页面列表"
    - "不同 Tab 展示对应的 empty state（暂无公开页面/暂无私人页面/没有已归档的页面）"
  artifacts:
    - path: "yh-flow/clients/web/app/routes.ts"
      provides: "Pages 列表页路由注册"
      contains: "workspaces/:workspaceId/pages" # 仅列表页路由由本 plan 注册；编辑器页路由由 19-03 注册
    - path: "yh-flow/clients/web/app/workspaces/[workspaceId]/pages/page.tsx"
      provides: "Pages 列表页入口路由组件"
      min_lines: 15
    - path: "yh-flow/clients/web/app/components/pages/pages-list-main-content.tsx"
      provides: "页面列表主容器（Tab + 搜索 + 排序）"
      min_lines: 30
    - path: "yh-flow/clients/web/app/components/pages/pages-list-view.tsx"
      provides: "页面卡片网格渲染容器"
      min_lines: 30
    - path: "yh-flow/clients/web/app/components/pages/list/root.tsx"
      provides: "页面列表核心渲染逻辑"
      min_lines: 40
    - path: "yh-flow/clients/web/app/components/pages/list/block.tsx"
      provides: "单个页面卡片（Logo + 名称 + 权限徽章 + 收藏星标 + 日期）"
      min_lines: 50
    - path: "yh-flow/clients/web/app/components/pages/modals/create-page-modal.tsx"
      provides: "创建页面弹窗"
      min_lines: 30
    - path: "yh-flow/clients/web/app/components/pages/modals/delete-page-modal.tsx"
      provides: "删除确认弹窗"
      min_lines: 30
    - path: "yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx"
      provides: "侧边栏 Pages 导航链接（'页面'）"
      contains: "页面"
  key_links:
    - from: "pages-list-main-content.tsx"
      to: "page.store.ts"
      via: "PageStore.activeTab, PageStore.filters for tab/search/sort state"
      pattern: "useStore().page"
    - from: "list/root.tsx"
      to: "use-pages.ts"
      via: "usePages hook fetches page data"
      pattern: "usePages\\(workspaceId\\)"
    - from: "list/root.tsx"
      to: "use-page-mutations.ts"
      via: "favoritePage mutation on star click"
      pattern: "favoritePage"
    - from: "list/block.tsx"
      to: "react-router"
      via: "Click navigates to editor page"
      pattern: "useNavigate|navigate.*pages/\\$\\{page"
    - from: "create-page-modal.tsx"
      to: "use-page-mutations.ts"
      via: "createPage mutation on form submit"
      pattern: "createPage"
    - from: "delete-page-modal.tsx"
      to: "use-page-mutations.ts"
      via: "deletePage mutation on confirm"
      pattern: "deletePage"
    - from: "sidebar-tree.tsx"
      to: "routes.ts"
      via: "navigate to /workspaces/:wsId/pages"
      pattern: "navigate.*/pages"
---

<objective>
创建 Pages 列表页面 UI 和路由：Tab 导航（公开/私人/已归档）、搜索、排序、页面卡片、创建/删除弹窗、收藏功能、侧边栏导航。

Purpose: 实现 PAGE-01（页面列表展示）、PAGE-04（Public/Private 权限）、PAGE-05（收藏/星标）。Fork 并适配 Plane 的 pages 列表组件到 Flow Web。
Output: 页面列表面、Tab 切换、搜索排序、创建/删除弹窗、侧边栏导航链接
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/19-pages-views/19-CONTEXT.md
@.planning/phases/19-pages-views/19-UI-SPEC.md

# Dependencies (Plan 19-01)

@yh-flow/clients/web/src/lib/hooks/use-pages.ts
@yh-flow/clients/web/src/lib/hooks/use-page-mutations.ts
@yh-flow/clients/web/app/store/page.store.ts

# Patterns to follow

@yh-flow/clients/web/app/routes.ts — 现有路由模式
@yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx — 侧边栏现有结构（PROJECT_VIEWS, 项目树）
@yh-flow/clients/web/app/workspaces/[workspaceId]/page.tsx — 入口页面模式（useParams 获取 workspaceId）
@yh-flow/clients/web/app/components/modules/modules-list-view.tsx — Phase 18 列表页模式（参考组织结构）

# Plane source files to fork

@d:/github/akinix-plane/apps/web/core/components/pages/pages-list-main-content.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/pages-list-view.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/list/root.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/list/block.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/list/block-item-action.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/list/tab-navigation.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/list/search-input.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/list/order-by.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/modals/create-page-modal.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/modals/page-form.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/modals/delete-page-modal.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/header/favorite-control.tsx
@d:/github/akinix-plane/apps/web/core/components/pages/header/archived-badge.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 Pages 列表页路由 + 侧边栏导航 + 列表入口页面</name>
  <read_first>
    @yh-flow/clients/web/app/routes.ts — 路由注册模式
    @yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx — 侧边栏现有结构（PROJECT_VIEWS, 项目树）
    @yh-flow/clients/web/app/workspaces/[workspaceId]/page.tsx — 入口页面模式（useParams 获取 workspaceId）
    @yh-flow/clients/web/app/components/modules/modules-list-view.tsx — 列表视图组件结构参考
  </read_first>
  <files>
    yh-flow/clients/web/app/routes.ts
    yh-flow/clients/web/app/workspaces/[workspaceId]/pages/page.tsx
    yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx
  </files>
  <action>
    1. 在 `routes.ts` 中，在 workspace layout 内添加 pages 列表页路由（位置：在 MODULE ROUTES 之前或之后）：

    ```
    // ====================================================================
    // PAGE ROUTES per D-P19-01 (Workspace-level, not project-level)
    // ====================================================================
    route("workspaces/:workspaceId/pages", "app/workspaces/[workspaceId]/pages/page.tsx"),
    ```

    **注意**：
    - Pages 是 workspace-level（per D-P19-01: `/workspaces/:ws/pages/`），所以这层路由不在 project routes 内
    - 编辑器页路由（`workspaces/:workspaceId/pages/:pageId`）由 Plan 19-03 负责注册，本 plan 不注册

    2. 创建 `workspaces/[workspaceId]/pages/page.tsx`（工作区页面列表入口），per 现有页面模式（用 `"use client"` + `observer` + `useParams`）：

    ```tsx
    // FLOW: Forked from Plane pages-list-main-content.tsx usage
    "use client";
    import { observer } from "mobx-react";
    import { useParams } from "react-router";
    import { PagesListMainContent } from "@/components/pages/pages-list-main-content";

    const PagesPage = observer(function PagesPage() {
      const { workspaceId } = useParams<{ workspaceId: string }>();
      if (!workspaceId) return null;
      return (
        <div className="flex h-full flex-col">
          <PagesListMainContent workspaceId={workspaceId} />
        </div>
      );
    });
    export default PagesPage;
    ```

    3. 更新 `sidebar-tree.tsx`，per UI-SPEC sidebar 变更：
    - 在项目树 **之上**（作为 workspace 级别的导航项），添加"页面"导航链接，per D-P19-01（Pages 属于 workspace 级别）
    - 在 workspace 折叠区域内的项目列表前添加：
    ```tsx
    const isPagesActive = location.pathname.match(/\/workspaces\/([^/]+)\/pages/);
    // ... 在项目展开内容和项目列表之间
    <button
      onClick={() => navigate(`/workspaces/${wsId}/pages`)}
      className={cn("flex w-full items-center gap-2 rounded-md px-2 py-1 text-xs transition-colors", {
        "text-custom-sidebar-text-100": isPagesActive,
        "text-custom-sidebar-text-300 hover:bg-custom-sidebar-background-80": !isPagesActive,
      })}
    >
      <FileText className="size-3.5" />
      <span>页面</span>
    </button>
    ```
    - 将 PROJECT_VIEWS 中的 `{ key: "pages", label: "Pages", icon: FileText }` label 改为 "Pages"（保持原样，因为 pages 现在是 workspace 级导航，不再需要从 project 子菜单进入 — 但保留在 PROJECT_VIEWS 内也无害）

    4. 所有新文件顶部添加 `// FLOW: Forked from Plane` 注释

  </action>
  <verify>
    <automated>
      grep -q "workspaces/:workspaceId/pages" yh-flow/clients/web/app/routes.ts && echo "Routes OK" && ls yh-flow/clients/web/app/workspaces/[workspaceId]/pages/page.tsx && echo "Entry page OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `routes.ts` 包含 pages 列表页路由
    - `workspaces/[workspaceId]/pages/page.tsx` 文件存在，导出默认组件
    - `sidebar-tree.tsx` 包含"页面" workspace 级导航链接
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fork 并适配 Pages 列表主容器和 Tab/搜索/排序组件</name>
  <read_first>
    批量读取以下 Plane 源文件来理解导入路径和组件签名：
    @d:/github/akinix-plane/apps/web/core/components/pages/pages-list-main-content.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/pages-list-view.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/list/tab-navigation.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/list/search-input.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/list/order-by.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/list/filters/root.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/list/applied-filters/root.tsx
  </read_first>
  <files>
    yh-flow/clients/web/app/components/pages/pages-list-main-content.tsx
    yh-flow/clients/web/app/components/pages/pages-list-view.tsx
    yh-flow/clients/web/app/components/pages/list/tab-navigation.tsx
    yh-flow/clients/web/app/components/pages/list/search-input.tsx
    yh-flow/clients/web/app/components/pages/list/order-by.tsx
    yh-flow/clients/web/app/components/pages/list/filters/root.tsx
    yh-flow/clients/web/app/components/pages/list/applied-filters/root.tsx
    yh-flow/clients/web/app/components/pages/loaders/page-content-loader.tsx
  </files>
  <action>
    Fork Plane 组件到 Flow Web 并适配：

    **通用适配规则：**
    - 添加文件头注释 `// FLOW: Forked from Plane. Original: apps/web/core/components/pages/{path}`
    - 将 `import { observer } from "mobx-react-lite"` 替换为 `import { observer } from "mobx-react"`
    - 将 Plane 的 MobX store 导入（`useApplication` 等）替换为 Flow 的 `useStore()` 模式
    - 所有 store 访问改为 `const { page: pageStore } = useStore()`
    - 将所有导入的页面数据 hooks 替换为 Flow 的 `usePages`/`usePageMutations`
    - 替换 `@plane/types` 的 TPage 导入为 `@/../src/lib/types/page/core` 或 `@plane/types`（看哪个路径能正确解析）
    - 所有文案改为中文 per UI-SPEC Copywriting Contract

    **具体组件适配：**

    1. `pages-list-main-content.tsx` — 主容器：
    - 组合 TabNavigation + SearchInput + OrderBy + PagesListView
    - 使用 PageStore.activeTab 控制当前 Tab
    - 使用 PageStore.filters 控制搜索和排序
    - 渲染 PagesListView 传递过滤后的页面

    2. `pages-list-view.tsx` — 网格容器：
    - 接收 filteredPages 数组
    - 映射到 list/root.tsx 渲染卡片网格
    - 空状态判断

    3. `list/tab-navigation.tsx` — Tab 导航：
    - 三个 Tab："公开"（public）/ "私人"（private）/ "已归档"（archived）
    - 使用 PageStore.setActiveTab 切换
    - active tab 高亮样式（bg-custom-primary/10 + text-custom-primary）

    4. `list/search-input.tsx` — 搜索输入框：
    - 受控输入，`PageStore.setSearchQuery`
    - placeholder: "搜索页面..."

    5. `list/order-by.tsx` — 排序下拉：
    - 选项：按名称排序 / 按创建时间排序 / 按更新时间排序 / 按最近打开排序
    - 使用 PageStore.setSortKey 和 PageStore.setSortBy

    6. `list/filters/root.tsx` — 筛选器容器（预留）：
    - 简单包装容器，为未来扩展预留

    7. `list/applied-filters/root.tsx` — 已应用筛选器（预留）：
    - 简单包装容器，为未来扩展预留

    8. `loaders/page-content-loader.tsx` — 加载骨架：
    - 3-4 个矩形占位块
    - 使用 bg-custom-background-80 颜色

    **文案对照（UI-SPEC）：**
    - Tab labels: "公开"、"私人"、"已归档"
    - Search placeholder: "搜索页面..."
    - Sort options: "按名称排序"、"按创建时间排序"、"按更新时间排序"、"按最近打开排序"
    - Empty state: "暂无公开页面" / "暂无私人页面" / "没有已归档的页面"
    - Empty state body: 见 UI-SPEC Copywriting Contract

  </action>
  <verify>
    <automated>
      ls yh-flow/clients/web/app/components/pages/pages-list-main-content.tsx && ls yh-flow/clients/web/app/components/pages/list/tab-navigation.tsx && echo "Page list components created"
    </automated>
  </verify>
  <acceptance_criteria>
    - 所有 8 个组件文件已创建（pages-list-main-content, pages-list-view, tab-navigation, search-input, order-by, filters/root, applied-filters/root, page-content-loader）
    - Tab 组件使用 PageStore.activeTab 控制状态
    - 搜索组件使用 PageStore.setSearchQuery
    - 排序组件使用 PageStore 的 sortKey/sortBy
    - 所有文案为中文 per UI-SPEC
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fork 并适配 Pages 卡片、弹窗和操作组件</name>
  <read_first>
    批量读取以下 Plane 源文件：
    @d:/github/akinix-plane/apps/web/core/components/pages/list/block.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/list/block-item-action.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/header/favorite-control.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/header/archived-badge.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/modals/create-page-modal.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/modals/page-form.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/modals/delete-page-modal.tsx
    @d:/github/akinix-plane/apps/web/core/components/pages/header/archived-badge.tsx
    @yh-flow/clients/web/app/components/modules/module-list-item.tsx — Flow 现有卡片组件模式参考
    @yh-flow/clients/web/app/components/modules/module-list-item-action.tsx — Flow 现有操作动作模式参考
  </read_first>
  <files>
    yh-flow/clients/web/app/components/pages/list/root.tsx
    yh-flow/clients/web/app/components/pages/list/block.tsx
    yh-flow/clients/web/app/components/pages/list/block-item-action.tsx
    yh-flow/clients/web/app/components/pages/header/favorite-control.tsx
    yh-flow/clients/web/app/components/pages/header/archived-badge.tsx
    yh-flow/clients/web/app/components/pages/modals/create-page-modal.tsx
    yh-flow/clients/web/app/components/pages/modals/page-form.tsx
    yh-flow/clients/web/app/components/pages/modals/delete-page-modal.tsx
    yh-flow/clients/web/app/components/pages/dropdowns/actions.tsx
  </files>
  <action>
    Fork Plane 组件并适配：

    **通用适配规则**（同 Task 2）：
    - 文件头 `// FLOW: Forked from Plane...`
    - `mobx-react-lite` → `mobx-react`
    - Plane store hooks → Flow `useStore()`
    - Plane data hooks → Flow `usePages`/`usePageMutations`

    1. `list/root.tsx` — 列表核心渲染：
    - 接收 `pageList: TPage[]` props
    - 映射 pageList 到 block 组件
    - 按 PageStore.filters.sortKey/sortBy 排序
    - 空状态渲染（根据 activeTab 不同显示不同文案）
    - 每个 block 点击导航到 `/workspaces/${workspaceId}/pages/${page.id}`

    2. `list/block.tsx` — 页面卡片：
    - 显示：logo_props emoji/首字符、name、access badge、favorite star、updated_at
    - 点击跳转到编辑器页面（`/workspaces/:wsId/pages/:pageId`）
    - 使用 `usePageMutations().favoritePage` 处理收藏点击
    - 使用 `useNavigate` 导航

    3. `list/block-item-action.tsx` — 悬停操作：
    - 收藏按钮（icon 切换）
    - "更多" 下拉菜单（PageDropdownActions）

    4. `header/favorite-control.tsx` — 收藏控制：
    - 星标图标切换（filled accent color / empty grey）
    - 文案：收藏 / 取消收藏
    - 调用 `usePageMutations().favoritePage`
    - Touch target: 28px

    5. `header/archived-badge.tsx` — 已归档徽章：
    - 小型 badge 显示在归档页面上
    - 使用背景 reduce opacity（50%）样式
    - 显示"已归档"文字或 archive icon

    6. `modals/create-page-modal.tsx` — 创建弹窗：
    - 使用 ModalCore from @plane/ui
    - 标题："创建页面"
    - 内嵌 PageForm
    - 保存调用 `usePageMutations().createPage`
    - 成功后关闭弹窗并 invalidate queries

    7. `modals/page-form.tsx` — 页面创建表单：
    - Name input（placeholder: "页面名称"）
    - Emoji 选择器（复用 EmojiPicker from @plane/ui）
    - Access toggle: "公开 — 工作区内所有人可查看" / "私人 — 仅创建者可查看"

    8. `modals/delete-page-modal.tsx` — 删除确认弹窗：
    - 确认文案："确定删除此页面？此操作不可撤销。" per UI-SPEC
    - 取消 / 删除按钮（删除按钮使用 bg-danger-primary）
    - 调用 `usePageMutations().deletePage`

    9. `dropdowns/actions.tsx` — 页面操作下拉菜单：
    - 选项：复制链接、归档（public/private tab）、恢复（archived tab）、删除
    - Menu items 使用 text-custom-text-200，删除项用 text-danger-primary

  </action>
  <verify>
    <automated>
      ls yh-flow/clients/web/app/components/pages/list/block.tsx && ls yh-flow/clients/web/app/components/pages/modals/create-page-modal.tsx && echo "Page block + modals created"
    </automated>
  </verify>
  <acceptance_criteria>
    - 所有 9 个组件文件已创建
    - block.tsx 使用 `usePageMutations().favoritePage` 处理收藏
    - block.tsx 使用 `useNavigate` 导航到编辑器
    - create-page-modal 使用 PageStore 控制弹窗状态
    - delete-page-modal 使用 PageStore 控制弹窗状态
    - 所有文案符合 UI-SPEC Copywriting Contract
    - EmojiPicker 复用自 @plane/ui
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                    |
| ----------------------------------- | ------------------------------ |
| Client components → Mock data layer | 只读查询和乐观更新，无跨域边界 |
| URL navigation → Editor page        | Next.js 路由参数验证           |

## STRIDE Threat Register

| Threat ID  | Category               | Component              | Disposition | Mitigation Plan                                                   |
| ---------- | ---------------------- | ---------------------- | ----------- | ----------------------------------------------------------------- |
| T-19-V2-01 | Information Disclosure | URL 路由               | accept      | Pages 权限在 mock 阶段不做后端验证，未来真实 API 接入后由后端控制 |
| T-19-V2-SC | Tampering              | npm/pip/cargo installs | accept      | 无新增包依赖                                                      |

</threat_model>

<verification>
- [ ] TypeScript 编译通过
- [ ] 所有组件文件正确引入 `useStore()` 获取 PageStore
- [ ] API 调用走 `usePages`/`usePageMutations`（非直接操作 MOCK_PAGES）
- [ ] Tab 切换通过 PageStore.activeTab 控制
- [ ] 创建/删除弹窗通过 PageStore 控制 open/close
</verification>

<success_criteria>

1. 导航到 `/workspaces/ws-1/pages` 显示页面列表
2. 三个 Tab 正常工作，切换展示对应页面
3. 搜索输入框实时过滤页面名称（客户端过滤）
4. 排序下拉切换排序方式
5. 页面卡片显示 logo、名称、权限徽章、收藏星标、日期
6. 点击页面卡片导航到编辑器页面路由
7. 点击"创建页面"弹出 Modal，填写后创建
8. 点击收藏星标切换收藏状态
9. 删除页面弹出确认弹窗，确认后删除
10. 侧边栏显示"页面"导航链接
11. 不同 Tab 显示不同的 empty state 文案
    </success_criteria>

<output>
Create `.planning/phases/19-pages-views/19-02-SUMMARY.md` when done
</output>
