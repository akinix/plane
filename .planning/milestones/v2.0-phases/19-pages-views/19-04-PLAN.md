---
phase: 19-pages-views
plan: 04
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - yh-flow/clients/web/src/lib/mock-data.ts
  - yh-flow/clients/web/src/lib/hooks/use-views.ts
  - yh-flow/clients/web/src/lib/hooks/use-view-mutations.ts
  - yh-flow/clients/web/src/lib/hooks/index.ts
  - yh-flow/clients/web/app/store/view.store.ts
  - yh-flow/clients/web/app/store/root.store.ts
  - yh-flow/clients/web/app/store/types.ts
  - yh-flow/clients/web/app/lib/store-context.tsx
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/views/page.tsx
  - yh-flow/clients/web/app/components/views/views-list.tsx
  - yh-flow/clients/web/app/components/views/view-list-header.tsx
  - yh-flow/clients/web/app/components/views/view-list-item.tsx
  - yh-flow/clients/web/app/components/views/view-list-item-action.tsx
  - yh-flow/clients/web/app/components/views/modal.tsx
  - yh-flow/clients/web/app/components/views/form.tsx
  - yh-flow/clients/web/app/components/views/delete-view-modal.tsx
  - yh-flow/clients/web/app/components/views/quick-actions.tsx
  - yh-flow/clients/web/app/components/views/helper.tsx
  - yh-flow/clients/web/app/components/views/filters/filter-selection.tsx
  - yh-flow/clients/web/app/components/views/filters/order-by.tsx
  - yh-flow/clients/web/app/components/views/applied-filters/root.tsx
  - yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
user_setup: []

must_haves:
  truths:
    - "用户可以在 /workspaces/:wsId/projects/:projId/views 看到已保存的自定义视图列表"
    - "视图列表有 全部 / 我创建的 两个 Tab"
    - "用户可以在视图列表中搜索视图名称"
    - "用户可以在视图列表中切换排序方式"
    - "每个视图卡片显示名称、描述、权限徽章、收藏星标、创建人头像"
    - "用户可以点击'应用'按钮将视图配置应用到 Issue 列表"
    - "用户可以创建新视图（在视图列表页点击'新建视图'按钮）"
    - "用户可以从 Issue 保存视图（FilterSaveModal 保存后跳转到视图列表页）"
    - "用户可以编辑/删除已有的视图"
    - "用户可以收藏/取消收藏视图"
  artifacts:
    - path: "yh-flow/clients/web/src/lib/hooks/use-views.ts"
      provides: "useViews, useViewDetail TanStack Query hooks"
      exports:
        - "useViews"
        - "useViewDetail"
    - path: "yh-flow/clients/web/src/lib/hooks/use-view-mutations.ts"
      provides: "useViewMutations TanStack Query mutation hooks"
      exports:
        - "useViewMutations"
    - path: "yh-flow/clients/web/app/store/view.store.ts"
      provides: "ViewStore (MobX UI state)"
      exports:
        - "IViewStore"
        - "ViewStore"
    - path: "yh-flow/clients/web/app/store/root.store.ts"
      provides: "ViewStore registered on CoreRootStore"
      contains: "view: IViewStore"
    - path: "yh-flow/clients/web/app/routes.ts"
      provides: "Views 路由"
      contains: "views/:projectId/views"
    - path: "yh-flow/clients/web/app/components/views/views-list.tsx"
      provides: "视图列表主容器"
      min_lines: 30
    - path: "yh-flow/clients/web/app/components/views/view-list-item.tsx"
      provides: "视图卡片组件"
      min_lines: 50
    - path: "yh-flow/clients/web/app/components/views/modal.tsx"
      provides: "创建/编辑视图弹窗"
      min_lines: 30
  key_links:
    - from: "views-list.tsx"
      to: "use-views.ts"
      via: "useViews hook for view list data"
      pattern: "useViews\\(projectId\\)"
    - from: "view-list-item.tsx"
      to: "use-view-mutations.ts"
      via: "favoriteView mutation"
      pattern: "favoriteView"
    - from: "view-list-item.tsx"
      to: "react-router"
      via: "'应用' button navigates to issue list"
      pattern: "navigate.*/issues"
    - from: "filter-save-modal.tsx"
      to: "use-view-mutations.ts"
      via: "After save, navigate to views page (VIEW-02)"
      pattern: "createView|navigate.*/views"
    - from: "view.store.ts"
      to: "FilterSaveModal"
      via: "ViewStore.currentViewId tracks applied view"
      pattern: "appliedViewId"
---

<objective>
创建 Views（自定义视图）管理功能：视图列表页（全部/我创建的 Tab）、创建/编辑/删除视图、收藏视图、应用视图配置到 Issue 列表、从 Issue 保存视图流程。

Purpose: 实现 VIEW-01（视图列表）、VIEW-02（保存视图）、VIEW-03（应用视图）。
Output: ViewStore、useViews/useViewMutations hooks、视图列表页 UI 组件、FilterSaveModal 集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/19-pages-views/19-CONTEXT.md
@.planning/phases/19-pages-views/19-UI-SPEC.md

# Existing view service (Phase 17)

@yh-flow/clients/web/src/lib/services/issue-view.service.ts — 已有 mock 存储，需要扩展
@yh-flow/clients/web/src/lib/hooks/use-issue-views.ts — 已有 useIssueViews + useCreateIssueView

# Patterns to follow

@yh-flow/clients/web/app/store/page.store.ts — IPageStore pattern
@yh-flow/clients/web/src/lib/hooks/use-pages.ts — usePages pattern
@yh-flow/clients/web/src/lib/hooks/use-page-mutations.ts — usePageMutations pattern
@yh-flow/clients/web/app/routes.ts — 路由模式
@yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx — 已有保存视图弹窗

# Types

@yh-flow/clients/web/src/lib/types/views.ts — IProjectView, EViewAccess, TViewFilters 等

# Plane source files to fork

@d:/github/akinix-plane/apps/web/core/components/views/views-list.tsx
@d:/github/akinix-plane/apps/web/core/components/views/view-list-item.tsx
@d:/github/akinix-plane/apps/web/core/components/views/view-list-item-action.tsx
@d:/github/akinix-plane/apps/web/core/components/views/view-list-header.tsx
@d:/github/akinix-plane/apps/web/core/components/views/modal.tsx
@d:/github/akinix-plane/apps/web/core/components/views/form.tsx
@d:/github/akinix-plane/apps/web/core/components/views/delete-view-modal.tsx
@d:/github/akinix-plane/apps/web/core/components/views/quick-actions.tsx
@d:/github/akinix-plane/apps/web/core/components/views/helper.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 ViewStore + useViews + useViewMutations（Views 数据层）</name>
  <read_first>
    @yh-flow/clients/web/app/store/page.store.ts — MobX store 模式
    @yh-flow/clients/web/app/store/root.store.ts — Store 注册模式
    @yh-flow/clients/web/app/store/types.ts — 类型导出模式
    @yh-flow/clients/web/src/lib/hooks/use-pages.ts — usePages hook 模式（TanStack Query + mock array）
    @yh-flow/clients/web/src/lib/hooks/use-page-mutations.ts — usePageMutations 模式
    @yh-flow/clients/web/src/lib/hooks/use-issue-views.ts — 已有 useIssueViews + useCreateIssueView
    @yh-flow/clients/web/src/lib/services/issue-view.service.ts — 已有 issueViewService（mock 数据存储）
    @yh-flow/clients/web/src/lib/types/views.ts — IProjectView, EViewAccess
    @yh-flow/clients/web/src/lib/hooks/index.ts — hooks 导出模式
  </read_first>
  <files>
    yh-flow/clients/web/app/store/view.store.ts
    yh-flow/clients/web/app/store/root.store.ts
    yh-flow/clients/web/app/store/types.ts
    yh-flow/clients/web/src/lib/hooks/use-views.ts
    yh-flow/clients/web/src/lib/hooks/use-view-mutations.ts
    yh-flow/clients/web/src/lib/hooks/index.ts
    yh-flow/clients/web/src/lib/mock-data.ts
  </files>
  <action>
    创建 Views 数据层，参考 Page 模式（use-pages / PageStore / usePageMutations）及已有的 issue-view.service.ts。

    1. 创建 `view.store.ts`（ViewStore — MobX UI 状态，per D-P19-09）：

    `IViewStore` 接口：
    - `activeTab: "all" | "created"` — 全部 / 我创建的，默认 "all"
    - `selectedViewId: string | null`
    - `viewModalOpen: boolean`
    - `viewModalMode: "create" | "edit"`
    - `editViewData: IProjectView | null` — 编辑模式预填充数据
    - `deleteViewId: string | null`
    - `deleteViewName: string | null`
    - `viewDeleting: boolean`
    - `appliedViewId: string | null` — 当前应用的视图 ID（VIEW-03）
    - `filters: TViewFilters` — 搜索/排序
      - `searchQuery: string`
      - `sortKey: TViewFiltersSortKey` — "name" | "created_at" | "updated_at"
      - `sortBy: TViewFiltersSortBy` — "asc" | "desc"

    Actions: `setActiveTab`, `setSelectedViewId`, `openViewModal`, `closeViewModal`, `openEditModal(view)`, `openDeleteModal`, `closeDeleteModal`, `setViewDeleting`, `setAppliedViewId`, `setSearchQuery`, `setSortKey`, `setSortBy`, `reset`

    参照 `PageStore` 的模式实现：`makeObservable(this, {...})`, 箭头函数 actions。

    2. 创建 `use-views.ts`（TanStack Query hooks）：

    `useViews(projectId: string)`:
    - queryKey: `["issue-views", projectId]`  （与 Phase 17 useIssueViews 共享同一 cache key，保证 mutation 后双方都刷新）
    - queryFn: 返回 `issueViewService.getIssueViews(projectId)`（复用已有 service）
    - enabled: !!projectId, delay 250ms

    `useViewDetail(projectId: string, viewId: string)`:
    - queryKey: `["issue-views", projectId, viewId]`
    - queryFn: 返回 `issueViewService.getIssueView(viewId)`
    - enabled: !!projectId && !!viewId

    3. 创建 `use-view-mutations.ts`（TanStack Query mutations）：

    使用 `issueViewService`（已有的 service 类）进行 mock 操作：
    - `createView`: mutationFn 调用 `issueViewService.createIssueView(data)`
    - `updateView`: mutationFn 调用 service 的更新方法（需要：创建 `updateIssueView` 方法在 service 中）
    - `deleteView`: mutationFn 调用 `issueViewService.deleteIssueView(viewId)`
    - `favoriteView`: mutationFn 直接操作 service 内的 MOCK_VIEWS 数组 toggle is_favorite（需要在 service 中添加 `favoriteIssueView` 方法）

    所有 mutations 的 onSuccess: `queryClient.invalidateQueries({ queryKey: ["issue-views"] })`

    **需要在 `issue-view.service.ts` 中添加的方法：**
    - `updateIssueView(viewId, data)` — 查找更新
    - `favoriteIssueView(viewId, isFav)` — toggle is_favorite

    4. 所有 hooks 在 `hooks/index.ts` 中导出：
    ```
    export * from "./use-views";
    export * from "./use-view-mutations";
    ```

    5. 在 `root.store.ts` 中注册 ViewStore：
    - Import `ViewStore, IViewStore`
    - 在 ICoreRootStore 添加 `view: IViewStore`
    - 在 CoreRootStore 添加 `view: IViewStore` + `this.view = new ViewStore()`

    6. 在 `types.ts` 中 export IViewStore

    7. 如果 `issue-view.service.ts` 中的 MOCK_VIEWS 需要扩展（更多示例数据），添加 3-5 条额外 mock views。已有 view-1（"我的任务"），添加如 "高优先级 Issues"、"待审阅"、"本周计划" 等。使用中文名称。

    **关于已有 issue-view.service.ts 的注意事项：**
    - 已有 `createIssueView`, `getIssueViews`, `getIssueView`, `deleteIssueView` 方法
    - 需要补充 `updateIssueView`, `favoriteIssueView` 方法
    - MOCK_VIEWS 数组中现有样本视图已使用 TIssueView 接口（来自 `@/components/issues/filters/types`），Phase 19 需要保持兼容
    - 注意：TIssueView 接口有 `projectId` 字段（驼峰式），而 IProjectView 有 `project` 字段（snake_case）。Phase 17 的 service 使用 TIssueView（驼峰），Phase 19 视图列表页面也使用 TIssueView 保持兼容

  </action>
  <verify>
    <automated>
      grep -q "useViews" yh-flow/clients/web/src/lib/hooks/use-views.ts 2>/dev/null && echo "useViews created" || echo "checking..." && ls yh-flow/clients/web/app/store/view.store.ts && echo "ViewStore created"
    </automated>
  </verify>
  <acceptance_criteria>
    - `view.store.ts` 包含 IViewStore 和 ViewStore（activeTab, appliedViewId, filters 等）
    - `use-views.ts` 导出 useViews, useViewDetail
    - `use-view-mutations.ts` 导出 useViewMutations（createView, updateView, deleteView, favoriteView）
    - `issue-view.service.ts` 添加了 updateIssueView 和 favoriteIssueView 方法
    - ViewStore 在 root.store.ts 中注册
    - 所有文件头添加 `// FLOW:` 注释
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 Views 列表页 UI（路由 + 列表组件 + 弹窗 + 侧边栏更新）</name>
  <read_first>
    Plane 源文件（批量读取）：
    @d:/github/akinix-plane/apps/web/core/components/views/views-list.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/view-list-item.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/view-list-item-action.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/view-list-header.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/modal.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/form.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/delete-view-modal.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/quick-actions.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/helper.tsx
    @d:/github/akinix-plane/apps/web/core/components/views/filters/filter-selection.tsx (如果存在)
    @d:/github/akinix-plane/apps/web/core/components/views/filters/order-by.tsx (如果存在)
    @yh-flow/clients/web/app/routes.ts — 路由现有模式
    @yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx — 已有 FilterSaveModal
    @yh-flow/clients/web/app/components/issues/filters/types.ts — TIssueView 类型
  </read_first>
  <files>
    yh-flow/clients/web/app/routes.ts
    yh-flow/clients/web/app/workspaces/[workspaceId]/projects/[projectId]/views/page.tsx
    yh-flow/clients/web/app/components/views/views-list.tsx
    yh-flow/clients/web/app/components/views/view-list-header.tsx
    yh-flow/clients/web/app/components/views/view-list-item.tsx
    yh-flow/clients/web/app/components/views/view-list-item-action.tsx
    yh-flow/clients/web/app/components/views/modal.tsx
    yh-flow/clients/web/app/components/views/form.tsx
    yh-flow/clients/web/app/components/views/delete-view-modal.tsx
    yh-flow/clients/web/app/components/views/quick-actions.tsx
    yh-flow/clients/web/app/components/views/helper.tsx
    yh-flow/clients/web/app/components/views/filters/filter-selection.tsx
    yh-flow/clients/web/app/components/views/filters/order-by.tsx
    yh-flow/clients/web/app/components/views/applied-filters/root.tsx
    yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx
  </files>
  <action>
    创建 Views 列表页 UI。

    1. `routes.ts` — 添加 Views 路由（在 MODULE ROUTES 之后或之前）：
    ```
    // ====================================================================
    // VIEW ROUTES per D-P19-01 (Project-level: views belong to project)
    // ====================================================================
    route("workspaces/:workspaceId/projects/:projectId/views", "app/workspaces/[workspaceId]/projects/[projectId]/views/page.tsx"),
    ```

    2. `workspaces/[workspaceId]/projects/[projectId]/views/page.tsx` — 视图列表入口：
    - `"use client"` + `observer` + `useParams`
    - 从 URL 中获取 workspaceId + projectId
    - 渲染 ViewsList 组件

    3. Fork 视图组件（适配规则同 Pages）：

    **通用适配规则：**
    - 文件头 `// FLOW: Forked from Plane...`
    - `mobx-react-lite` → `mobx-react`
    - Plane store hooks → Flow `useStore()`
    - Plane data hooks → Flow `useViews`/`useViewMutations`
    - 所有文案为中文 per UI-SPEC

    `views-list.tsx` — 视图列表主容器：
    - 使用 `useViews(projectId)` 获取视图数据
    - 使用 ViewStore.activeTab 控制 "全部" / "我创建的" 切换
    - 使用 ViewStore.filters 搜索/排序
    - 创建按钮"新建视图"打开 ViewModal
    - 组合 ViewListHeader + 视图卡片列表
    - 空状态："暂无视图" + "在 Issue 视图中保存当前配置即可创建视图。"

    `view-list-header.tsx` — 视图列表头：
    - "新建视图" 按钮（primary CTA）
    - 搜索输入框 placeholder: "搜索视图..."
    - 排序下拉（按名称排序 / 按创建时间排序 / 按更新时间排序）

    `view-list-item.tsx` — 视图卡片：
    - 显示：名称、描述（截断）、权限徽章（公开/私人）、收藏星标、创建人头像
    - "应用"按钮（VIEW-03）
    - 点击应用调用 ViewStore.setAppliedViewId + 导航到 Issue 页
    - 导航目标：`/workspaces/${workspaceId}/projects/${projectId}/issues?view=${viewId}`

    `view-list-item-action.tsx` — 悬停操作：
    - "应用"按钮（调用 VIEW-03 逻辑）
    - 收藏按钮
    - "更多"下拉（编辑/删除）

    `modal.tsx` — 创建/编辑视图弹窗：
    - 使用 ModalCore from @plane/ui
    - 标题："保存为视图"（per UI-SPEC）
    - 包含 ViewForm
    - 保存调用 useViewMutations().createView 或 updateView

    `form.tsx` — 视图表单：
    - 名称输入 placeholder: "输入视图名称..."
    - 描述输入 placeholder: "添加描述（可选）"
    - 访问权限切换：公开 / 私人（per UI-SPEC 文案）

    `delete-view-modal.tsx` — 删除确认弹窗：
    - 确认文案："确定删除此视图？此操作不可撤销。"
    - 取消 / 删除按钮

    `quick-actions.tsx` — 快捷操作下拉：
    - 删除、复制链接、更改权限

    `helper.tsx` — 工具函数（复制到剪贴板等）

    `filters/filter-selection.tsx` — 视图列表筛选：
    - 筛选条件（创建者、视图类型：公开/私人）

    `filters/order-by.tsx` — 排序选择器

    `applied-filters/root.tsx` — 已应用筛选器显示

    4. **FilterSaveModal 集成（VIEW-02）：**
    - 修改 `filter-save-modal.tsx`：保存成功后，增加 post-save 导航
    - 在现有的 `handleSave` 中，调用 `createIssueView` 成功后，使用 `useNavigate` 导航到该项目的 views 页面
    - 或者更简洁：在 onClose 后，通过调用者的 navigate 跳转（更好，因为 modal 组件不需要知道导航逻辑）
    - **推荐方案**：FilterSaveModal 中添加 `onSave?: () => void` 可选回调，由调用者控制保存后的行为
    - 在 filter-save-modal.tsx 中添加 `onSave` prop，创建成功后调用

    5. **"应用视图" 流程（VIEW-03）：**
    - 在 view-list-item.tsx 中，"应用"按钮执行：
      1. `ViewStore.setAppliedViewId(viewId)`
      2. 使用 `useNavigate` 导航到 `issues` 页面
    - Issue 列表页面（Phase 16）需要读取 `ViewStore.appliedViewId` 并应用视图配置 — 此集成标记为 POINT 未来增强，在 Phase 19 中仅完成"导航到 Issue 页"部分

  </action>
  <verify>
    <automated>
      ls yh-flow/clients/web/app/components/views/views-list.tsx && ls yh-flow/clients/web/app/components/views/modal.tsx && grep -q "workspaces/.*/views" yh-flow/clients/web/app/routes.ts && echo "Views UI created"
    </automated>
  </verify>
  <acceptance_criteria>
    - routes.ts 包含 views 路由
    - views/page.tsx 入口文件存在
    - views-list.tsx 使用 useViews hook + ViewStore 控制状态
    - view-list-item.tsx 显示名称、描述、权限徽章、收藏星标
    - view-list-item.tsx 包含"应用"按钮，点击后导航到 Issue 页面
    - modal.tsx + form.tsx 实现创建/编辑视图
    - delete-view-modal.tsx 实现删除确认
    - filter-save-modal.tsx 添加 onSave 回调支持
    - 所有文案使用中文 per UI-SPEC Copywriting Contract
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description      |
| --------------------------------- | ---------------- |
| Mock view service → UI components | 纯客户端内存操作 |

## STRIDE Threat Register

| Threat ID  | Category               | Component                | Disposition | Mitigation Plan                            |
| ---------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------ |
| T-19-V4-01 | Information Disclosure | View list (access badge) | accept      | 权限控制未来在 API 层实现，mock 阶段仅展示 |
| T-19-V4-SC | Tampering              | npm/pip/cargo installs   | accept      | 无新增包依赖                               |

</threat_model>

<verification>
- [ ] TypeScript 编译通过（`npx tsc --noEmit`）
- [ ] ViewStore 在 root.store.ts 中注册为 `view` 属性
- [ ] useViews/useViewMutations 正确导出
- [ ] Views 列表页路由可用
- [ ] FilterSaveModal 已添加 onSave 回调
</verification>

<success_criteria>

1. 导航到 `/workspaces/ws-1/projects/proj-1/views` 显示视图列表
2. "全部" / "我创建的" Tab 切换正常
3. 搜索输入框实时过滤视图名称
4. 排序下拉切换排序方式
5. 视图卡片显示名称、描述、权限徽章、收藏星标
6. 点击"新建视图"按钮弹窗创建视图
7. 点击"应用"按钮导航到 Issue 列表页
8. 编辑/删除视图正常
9. 从 Issue 页面"保存为视图"后保存成功
10. 收藏视图星标切换状态
    </success_criteria>

<output>
Create `.planning/phases/19-pages-views/19-04-SUMMARY.md` when done
</output>
