---
phase: 18-cycle-module
plan: 04
type: execute
wave: 2
depends_on: [18-02, 18-03]
files_modified:
  - yh-flow/clients/web/app/routes.ts
  - yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx
  - yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx
  - yh-flow/clients/web/app/issues/page.tsx
files_created:
  - yh-flow/clients/web/app/cycles/page.tsx
  - yh-flow/clients/web/app/cycles/[cycleId]/page.tsx
  - yh-flow/clients/web/app/modules/page.tsx
  - yh-flow/clients/web/app/modules/[moduleId]/page.tsx
autonomous: true
requirements: [CYCLE-01, CYCLE-02, CYCLE-03, CYCLE-04, MODU-01, MODU-02, MODU-03]

must_haves:
  truths:
    - "4 个新路由注册到 routes.ts（cycles 列表 + cycles 详情 + modules 列表 + modules 详情）"
    - "侧边栏 PROJECT_VIEWS 中文标签改为 '周期' / '模块'"
    - "CyclesView + CyclesViewHeader 在 /cycles/ 页面渲染"
    - "Cycle 详情页包含双栏：左栏 Issue 列表（可切换看板）+ 右栏进度/Burndown"
    - "ModulesListView + ModuleViewHeader 在 /modules/ 页面渲染"
    - "Module 详情页双栏布局（D-P18-10）：左栏 Issue 列表 + Gantt 切换，右栏属性面板"
    - "Issue 详情侧栏中的 Cycle/Module 选择器导航到对应页面"
    - "npx tsc --noEmit 无新增错误"
  artifacts:
    - path: "yh-flow/clients/web/app/cycles/page.tsx"
      provides: "Cycle 列表页"
    - path: "yh-flow/clients/web/app/cycles/[cycleId]/page.tsx"
      provides: "Cycle 详情页"
    - path: "yh-flow/clients/web/app/modules/page.tsx"
      provides: "Module 列表页"
    - path: "yh-flow/clients/web/app/modules/[moduleId]/page.tsx"
      provides: "Module 详情页"
  key_links:
    - from: "routes.ts"
      to: "app/cycles/page.tsx, app/cycles/[cycleId]/page.tsx, app/modules/page.tsx, app/modules/[moduleId]/page.tsx"
      via: "route(...) calls"
    - from: "sidebar-tree.tsx"
      to: "../routes"
      via: "navigate with projectId path"
    - from: "cycles/page.tsx"
      to: "@/components/cycles/cycles-view, @tanstack/react-query, @/store/root.store"
      via: "imports"
    - from: "cycles/[cycleId]/page.tsx"
      to: "@/components/cycles/*, @/components/issues/list-view, @/components/issues/kanban-view, @/components/cycles/analytics-sidebar"
      via: "imports"
    - from: "modules/[moduleId]/page.tsx"
      to: "@/components/modules/*, @/components/issues/list-view, @/components/issues/gantt-view, @/components/modules/analytics-sidebar, @/components/modules/links"
      via: "imports"
---

<objective>
注册 Cycle/Module 的页面路由并创建 4 个页面组件（列表页 + 详情页），集成到项目侧边栏导航。将侧边栏英文标签改为中文。

目的：实现 D-P18-01 的独立路由架构和 D-P18-02 的自定义布局，将 Plan 18-02/03 的组件串联为完整页面。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/18-cycle-module/18-CONTEXT.md — D-P18-01, D-P18-02, D-P18-03, D-P18-09, D-P18-10, D-P18-11, D-P18-12
@.planning/phases/18-cycle-module/18-UI-SPEC.md — Page components, integration points, interaction contract

# Route system

@yh-flow/clients/web/app/routes.ts — Existing route structure
@yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx — Sidebar navigation (PROJECT_VIEWS has "Cycles"/"Modules")
@yh-flow/clients/web/app/issues/page.tsx — Issue list page pattern for reference
@yh-flow/clients/web/app/issues/[issueId]/page.tsx — Issue detail page pattern

# Components to compose

@yh-flow/clients/web/app/components/cycles/cycles-view.tsx — from Plan 18-02
@yh-flow/clients/web/app/components/cycles/cycles-view-header.tsx — from Plan 18-02
@yh-flow/clients/web/app/components/cycles/cycle-peek-overview.tsx — from Plan 18-02
@yh-flow/clients/web/app/components/cycles/form.tsx — from Plan 18-02
@yh-flow/clients/web/app/components/cycles/analytics-sidebar/ — from Plan 18-02
@yh-flow/clients/web/app/components/modules/modules-list-view.tsx — from Plan 18-03
@yh-flow/clients/web/app/components/modules/module-view-header.tsx — from Plan 18-03
@yh-flow/clients/web/app/components/modules/module-peek-overview.tsx — from Plan 18-03
@yh-flow/clients/web/app/components/modules/links/ — from Plan 18-03
@yh-flow/clients/web/app/components/modules/analytics-sidebar/ — from Plan 18-03

# Reused components

@yh-flow/clients/web/app/components/issues/list-view.tsx — Phase 16 IssueListView (filtered by cycleId/moduleId)
@yh-flow/clients/web/app/components/issues/kanban-view.tsx — Phase 16 KanbanView (as Cycle board per D-P18-09)
@yh-flow/clients/web/app/components/issues/gantt-view/ — Phase 17 GanttView (for Module gantt per D-P18-11)
@yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx — Phase 17 FilterBar (reused per D-P18-06)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 注册 4 个 Cycle/Module 路由 + 侧边栏中文标签</name>
  <files>
    - yh-flow/clients/web/app/routes.ts (MODIFY)
    - yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx (MODIFY)
  </files>
  <read_first>
    yh-flow/clients/web/app/routes.ts — Existing route structure (add after Issue routes)
    yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx — PROJECT_VIEWS array (lines 18-24)
  </read_first>
  <action>
    修改 routes.ts：
    在 Issue 路由之后（第 48 行之后）、layout 闭合之前添加 4 个 Cycle/Module 路由：

    ```typescript
    // ====================================================================
    // CYCLE ROUTES per D-P18-01
    // ====================================================================
    route("workspaces/:workspaceId/projects/:projectId/cycles", "app/cycles/page.tsx"),
    route("workspaces/:workspaceId/projects/:projectId/cycles/:cycleId", "app/cycles/[cycleId]/page.tsx"),

    // ====================================================================
    // MODULE ROUTES per D-P18-01
    // ====================================================================
    route("workspaces/:workspaceId/projects/:projectId/modules", "app/modules/page.tsx"),
    route("workspaces/:workspaceId/projects/:projectId/modules/:moduleId", "app/modules/[moduleId]/page.tsx"),
    ```

    修改 sidebar-tree.tsx：
    将 PROJECT_VIEWS 中的 label 从英文改为中文：
    - `{ key: "cycles", label: "Cycles", icon: Repeat }` → `{ key: "cycles", label: "周期", icon: Repeat }`
    - `{ key: "modules", label: "Modules", icon: Blocks }` → `{ key: "modules", label: "模块", icon: Blocks }`
    - 其他 key（issues/pages/views）保持不变

  </action>
  <acceptance_criteria>
    1. routes.ts 包含 4 个新路由（2 个 Cycle + 2 个 Module）
    2. 路由路径格式与现有 Issue 路由一致（workspaces/:workspaceId/projects/:projectId/...）
    3. 路由在 workspace-layout 内部（共享侧边栏）
    4. sidebar-tree.tsx 中 Cycles → 周期, Modules → 模块
    5. 侧边栏导航路径正确指向 `/workspaces/:wsId/projects/:projId/cycles`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "cycles" yh-flow/clients/web/app/routes.ts</automated>
    <automated>grep -c "modules" yh-flow/clients/web/app/routes.ts</automated>
    <automated>grep -q '"周期"' yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx</automated>
    <automated>grep -q '"模块"' yh-flow/clients/web/app/components/sidebar/sidebar-tree.tsx</automated>
  </verify>
  <done>
    * 4 个路由注册完成
    * 侧边栏标签改为中文（周期 / 模块）
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 Cycle 列表页 + 详情页</name>
  <files>
    - yh-flow/clients/web/app/cycles/page.tsx (CREATE)
    - yh-flow/clients/web/app/cycles/[cycleId]/page.tsx (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/app/issues/page.tsx — Issue list page pattern (useParams, useStore, CyclesView)
    yh-flow/clients/web/app/issues/[issueId]/page.tsx — Issue detail page pattern
  </read_first>
  <action>
    1. 创建 cycles/page.tsx（Cycle 列表页 — CYCLE-01, CYCLE-04）：
       - 使用 react-router 的 useParams 获取 workspaceId + projectId
       - 使用 useStore().cycle 管理 Tab/Modal 状态
       - 渲染 CyclesView 组件（该组件内部处理 Tab 切换 + 列表 + 加载/空/错误状态）
       - 渲染 CycleModal（创建/编辑弹窗，由 CycleStore.cycleModalOpen 控制）
       - 布局简单：全宽内容区 + CyclesView 占满

    2. 创建 cycles/[cycleId]/page.tsx（Cycle 详情页 — CYCLE-02, CYCLE-03）：
       - useParams 获取 workspaceId, projectId, cycleId
       - 使用 useCycleDetail(projectId, cycleId) + useCycleProgress(projectId, cycleId)
       - 使用 useStore().cycle 管理 viewLayout（list/board）
       - 布局：双栏
         - 左栏（flex-1）：
           - Cycle 名称（semibold 18px）+ 日期范围 + 编辑/删除按钮
           - 通过 CycleStore.viewLayout 切换：
             - "list" → IssueListView 组件（传入 cycleId 过滤 per D-P18-09）
             - "board" → KanbanView 组件（传入 cycleId 过滤，作为 Cycle 看板 per D-P18-09）
           - Issue 列表双栏切换按钮（List/Board）
         - 右栏（w-80, flex-shrink-0）：
           - ActiveCycleProgress（进度条 + 统计）
           - BurndownChart SVG（来自 analytics-sidebar/ per D-P18-08）
           - CycleStats（Issue 统计）
       - 编辑按钮 → CycleStore.openCycleModal("edit")
       - 删除按钮 → CycleDeleteModal
       - "添加 Issue 到 Cycle"按钮 → 弹出 Issue 选择器（搜索已有 Issue 并调用 useAddIssueToCycle）
       - 加载中：skeleton 双栏布局
       - 错误状态：重试按钮
       - 返回按钮 ← "返回周期列表" → navigate 到 `/workspaces/:wsId/projects/:projId/cycles`

    3. 确保页面组件没有被路由树中的 `projects/*` splat 吞掉：
       - 检查 routes.ts 中 `route("workspaces/:workspaceId/projects/*", ...)` 的顺序是否在 cycles/modules 路由之后
       - 如果是：已有 Issue 路由在 splat 之前，cycles/modules 同样在之前，因此安全

  </action>
  <acceptance_criteria>
    1. /cycles/ 页面显示 CyclesView + CycleModal
    2. /cycles/:cycleId 页面显示双栏布局（左栏 Issue 列表/看板 + 右栏进度/Burndown）
    3. Cycle 看板切换（list/board）通过 CycleStore.viewLayout
    4. "添加 Issue"功能：搜索已有 Issue → 调用 useAddIssueToCycle
    5. 编辑/删除操作正确路由到弹窗
    6. npx tsc --noEmit 无新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CyclesView" yh-flow/clients/web/app/cycles/page.tsx</automated>
    <automated>grep -q "ICycle\|useCycleDetail" yh-flow/clients/web/app/cycles/\[cycleId\]/page.tsx</automated>
    <automated>grep -q "IssueListView\|KanbanView" yh-flow/clients/web/app/cycles/\[cycleId\]/page.tsx</automated>
  </verify>
  <done>
    * Cycle 列表页（cycles/page.tsx）完成
    * Cycle 详情页（cycles/[cycleId]/page.tsx）完成 — 双栏 + 看板切换
    * Issue 添加/查看/编辑功能集成
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 创建 Module 列表页 + 详情页</name>
  <files>
    - yh-flow/clients/web/app/modules/page.tsx (CREATE)
    - yh-flow/clients/web/app/modules/[moduleId]/page.tsx (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/app/issues/page.tsx — Issue list page pattern
    yh-flow/clients/web/app/issues/[issueId]/page.tsx — Issue detail page pattern
  </read_first>
  <action>
    1. 创建 modules/page.tsx（Module 列表页 — MODU-01, MODU-03）：
       - useParams 获取 workspaceId + projectId
       - 使用 useStore().module 管理 UI 状态
       - 渲染 ModulesListView 组件（该组件处理列表 + 卡片/网格切换 + 空/加载/错误状态）
       - 渲染 ModuleModal（创建/编辑弹窗，由 ModuleStore.moduleModalOpen 控制）
       - 布局：全宽内容区 + ModulesListView

    2. 创建 modules/[moduleId]/page.tsx（Module 详情页 — MODU-02）：
       - useParams 获取 workspaceId, projectId, moduleId
       - 使用 useModuleDetail(projectId, moduleId) + useModuleIssues(projectId, moduleId)
       - 布局：双栏（D-P18-10）：
         - 左栏（flex-1，flex column）：
           - 顶部：Module 名称 + 状态徽章 + 编辑/删除按钮
           - 视图切换按钮（Issue 列表 / 甘特图）：
             - "list" → IssueListView（传入 moduleId 过滤 per D-P18-12）
             - "gantt" → GanttView（传入 moduleId 过滤 per D-P18-11，复用 Phase 17 组件）
           - 列表/甘特切换通过 ModuleStore.activeView
         - 右栏（w-80, flex-shrink-0，flex column gap-4）：
           - Module 属性面板（标题："属性"）：
             - 状态字段（ModuleStatusDropdown，含 4 状态）
             - 负责人字段（Lead 头像 + 名称）
             - 成员列表（Member 头像列表）
             - 开始日期 / 目标日期（只读显示）
           - Module 进度面板（标题："进度"）：
             - ProgressBar + 百分比
             - 已完成/总数 Issue 计数
             - AnalyticsSidebar（useModuleProgress 数据）
           - 描述面板（title: "描述"）：
             - Markdown 或纯文本显示 Module.description
             - 可点击编辑（内联编辑或弹窗）
           - 链接面板（title: "链接"）：
             - ModuleLinks 组件（links/list.tsx + links/list-item.tsx）
             - 添加/编辑/删除链接
       - 编辑按钮 → ModuleStore.openModuleModal("edit")
       - 删除按钮 → DeleteModuleModal
       - "添加 Issue 到 Module"按钮 → 搜索已有 Issue → useAddIssueToModule
       - 加载中：skeleton 双栏布局
       - 错误状态：重试按钮
       - 返回按钮 ← "返回模块列表" → navigate 到 `/workspaces/:wsId/projects/:projId/modules`

  </action>
  <acceptance_criteria>
    1. /modules/ 页面显示 ModulesListView + ModuleModal
    2. /modules/:moduleId 页面显示双栏布局（左栏 IssueList/Gantt + 右栏属性/进度/描述/链接）
    3. Issue 列表 / 甘特图切换（ModuleStore.activeView）
    4. 右栏包含状态、负责人、成员、日期、进度条、描述、链接管理
    5. 链接管理（ModuleLinks）可用
    6. npx tsc --noEmit 无新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ModulesListView" yh-flow/clients/web/app/modules/page.tsx</automated>
    <automated>grep -q "IModule\|useModuleDetail" yh-flow/clients/web/app/modules/\[moduleId\]/page.tsx</automated>
    <automated>grep -q "IssueListView\|GanttView" yh-flow/clients/web/app/modules/\[moduleId\]/page.tsx</automated>
    <automated>grep -q "ModuleLinks\|module/links" yh-flow/clients/web/app/modules/\[moduleId\]/page.tsx</automated>
  </verify>
  <done>
    * Module 列表页（modules/page.tsx）完成
    * Module 详情页（modules/[moduleId]/page.tsx）完成 — 双栏 + 甘特切换
    * 右栏属性面板（状态/进度/描述/链接）
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 将 Cycle/Module 选择器集成到 Issue 详情侧栏</name>
  <files>
    - yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx (MODIFY)
  </files>
  <read_first>
    yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx — Existing sidebar with cycle/module display
  </read_first>
  <action>
    修改 issue-detail-sidebar.tsx：
    查找现有 Cycle/Module 字段显示区域，添加以下能力：
    1. Cycle 字段：如果 Issue 有 cycle_id，显示 Cycle 名称（可从 useCycles 获取），并可点击导航到 Cycle 详情页
    2. Module 字段：如果 Issue 有 module_ids，显示 Module 名称列表，每个可点击导航到 Module 详情页
    3. 使用 `navigate(\`/workspaces/${wsId}/projects/${projId}/cycles/${cycleId}\`)` 和等价 modules 路径

    具体修改方式取决于现有 issue-detail-sidebar.tsx 的实现，若无 Cycle/Module 字段则只添加只读显示（选择器功能 deferred to Phase 19 per CONTEXT）。

    中文标签："周期"、"模块"

  </action>
  <acceptance_criteria>
    1. Issue 详情侧栏显示关联的 Cycle 名称（可点击导航）
    2. Issue 详情侧栏显示关联的 Module 名称列表（每个可点击导航）
    3. 无 cycle_id/module_ids 时不显示
    4. npx tsc --noEmit 无新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "周期\|cycle" yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx</automated>
    <automated>grep -q "模块\|module" yh-flow/clients/web/app/components/issues/issue-detail-sidebar.tsx</automated>
  </verify>
  <done>
    * Issue 详情侧栏集成 Cycle/Module 导航
    * Cycle/Module 名称可点击跳转
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID  | Category | Component                              | Disposition | Mitigation Plan                                        |
| ---------- | -------- | -------------------------------------- | ----------- | ------------------------------------------------------ |
| T-18-RT-01 | DoS      | Route order conflict                   | mitigate    | 确保 cycles/modules 路由在 `projects/*` splat 之前声明 |
| T-18-RT-02 | Spoofing | Issue detail sidebar cycle/module link | accept      | 仅导航到对应详情页，不操作数据                         |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
grep -c "cycles" app/routes.ts
grep -c "modules" app/routes.ts
grep -q "周期" app/components/sidebar/sidebar-tree.tsx
grep -q "模块" app/components/sidebar/sidebar-tree.tsx
ls app/cycles/page.tsx app/cycles/\[cycleId\]/page.tsx app/modules/page.tsx app/modules/\[moduleId\]/page.tsx
```
</verification>

<success_criteria>

- [ ] routes.ts 注册 4 个新路由
- [ ] 侧边栏导航改为中文（周期 / 模块）
- [ ] Cycle 列表页（cycles/page.tsx）— CyclesView + CycleModal
- [ ] Cycle 详情页（cycles/[cycleId]/page.tsx）— 双栏 + 看板切换 + Burndown
- [ ] Module 列表页（modules/page.tsx）— ModulesListView + ModuleModal
- [ ] Module 详情页（modules/[moduleId]/page.tsx）— 双栏 + 甘特切换 + 属性面板 + 链接管理
- [ ] Issue 详情侧栏 Cycle/Module 导航链接
- [ ] npx tsc --noEmit 无新增错误

</success_criteria>

<output>
创建 `.planning/phases/18-cycle-module/18-04-SUMMARY.md`
</output>
