---
phase: 16-issue
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - yh-flow/clients/web/app/issues/page.tsx
  - yh-flow/clients/web/app/components/issues/list-view.tsx
  - yh-flow/clients/web/app/components/issues/issue-row.tsx
  - yh-flow/clients/web/app/components/issues/quick-filter-bar.tsx
  - yh-flow/clients/web/app/components/issues/pagination.tsx
  - yh-flow/clients/web/app/components/issues/bulk-action-bar.tsx
  - yh-flow/clients/web/app/components/issues/issue-create-modal.tsx
  - yh-flow/clients/web/app/components/project/project-dashboard.tsx
autonomous: true
requirements:
  - ISSU-01
  - ISSU-02
  - ISSU-07
user_setup: []
must_haves:
  truths:
    - "用户可在项目下看到 Issue 列表视图"
    - "用户可通过状态/优先级/负责人快速筛选条件筛选 Issue"
    - "用户可按字段排序 Issue 列表"
    - "用户可通过分页控件浏览不同页的 Issue"
    - "用户可通过创建 Issue 按钮打开创建面板，填写标题/描述/优先级/负责人/标签后创建 Issue，创建后跳转到详情页"
    - "用户可批量选中 Issue 执行状态变更和删除操作"
    - "用户在项目仪表板点击 Issues 标签可导航到 Issue 列表页"
  artifacts:
    - path: "yh-flow/clients/web/app/issues/page.tsx"
      provides: "Issue 列表页面入口（view toggle + 条件渲染 list/kanban，本 plan 仅实现 list）"
    - path: "yh-flow/clients/web/app/components/issues/list-view.tsx"
      provides: "Issue 列表容器组件（顶栏筛选 + 数据行 + 分页）"
    - path: "yh-flow/clients/web/app/components/issues/issue-row.tsx"
      provides: "Issue 单行组件（checkbox + Issue ID + 标题 + 优先级 + 负责人 + 状态 + 更新时间）"
    - path: "yh-flow/clients/web/app/components/issues/quick-filter-bar.tsx"
      provides: "快速筛选条组件（状态/优先级/负责人筛选 chips + 展开面板）"
    - path: "yh-flow/clients/web/app/components/issues/pagination.tsx"
      provides: "分页控件（<< < 1 2 3 ... N > >>）"
    - path: "yh-flow/clients/web/app/components/issues/bulk-action-bar.tsx"
      provides: "批量操作浮动栏（状态变更/指派/删除）"
    - path: "yh-flow/clients/web/app/components/issues/issue-create-modal.tsx"
      provides: "创建 Issue 模态框（标题/描述/优先级/负责人/标签）"
  key_links:
    - from: "app/components/issues/list-view.tsx"
      to: "app/store/issue.store.ts"
      via: "useStore().issue 读取 filters/selectedIssueIds/currentPage"
    - from: "app/components/issues/list-view.tsx"
      to: "src/lib/hooks/use-issues.ts"
      via: "useIssues hook 获取 Issue 数据"
    - from: "app/components/issues/issue-create-modal.tsx"
      to: "src/lib/hooks/use-issues.ts"
      via: "useIssueMutations().createIssue 创建后 navigate 到详情页"
---

<objective>
实现 Issue 列表视图和 Issue 创建功能。创建 app/issues/page.tsx 作为 Issue 路由入口，实现 IssueListView（含 IssueRow、QuickFilterBar、Pagination）、BulkActionBar（批量操作）、IssueCreateModal（创建 Issue 表单）。更新 ProjectDashboard 的 Issues 标签导航到 Issue 列表页。

Purpose: 用户可在项目下查看分页/筛选/排序的 Issue 列表，创建 Issue，执行批量操作。
Output: 可工作的 Issue 列表页和创建 Issue 功能。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-issue/16-CONTEXT.md
@.planning/phases/16-issue/16-RESEARCH.md
@.planning/phases/16-issue/16-UI-SPEC.md
@yh-flow/clients/web/src/lib/mock-data.ts
@yh-flow/clients/web/src/lib/hooks/use-issues.ts
@yh-flow/clients/web/app/store/issue.store.ts
@yh-flow/clients/web/app/store/types.ts
@yh-flow/clients/web/app/routes.ts
@yh-flow/clients/web/app/components/project/project-dashboard.tsx
@yh-flow/clients/web/src/lib/types/issues/issue.ts
@yh-flow/clients/web/src/lib/types/issues.ts
@yh-flow/clients/web/src/lib/types/state.ts
@yh-flow/clients/web/src/lib/ui/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Issue 列表页面、列表视图容器、Issue 行和快速筛选条</name>
  <files>
    yh-flow/clients/web/app/issues/page.tsx
    yh-flow/clients/web/app/components/issues/list-view.tsx
    yh-flow/clients/web/app/components/issues/issue-row.tsx
    yh-flow/clients/web/app/components/issues/quick-filter-bar.tsx
    yh-flow/clients/web/app/components/issues/pagination.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts（useIssues hook 签名——入参 projectId + filters）
    yh-flow/clients/web/app/store/issue.store.ts（IssueStore 中 activeView、filters、sortBy 等 UI 状态）
    yh-flow/clients/web/src/lib/mock-data.ts（MOCK_ISSUES、MOCK_STATES——参考数据和用户）
    yh-flow/clients/web/src/lib/types/state.ts（IState 类型——状态颜色和组信息）
    yh-flow/clients/web/src/lib/types/issues.ts（TIssuePriorities——优先级枚举）
    yh-flow/clients/web/app/components/project/project-dashboard.tsx（UI 模式参考——Tailwind 类名和布局风格）
    yh-flow/clients/web/src/lib/ui/（Avatar, Badge, Button, Loader, EmptyState 等现有组件——使用，不改写）
  </read_first>
  <action>
    所有组件使用 observer() 包裹（import { observer } from "mobx-react"），使用 @/lib/store-context 的 useStore() 读取 IssueStore。
    所有组件使用 Tailwind CSS 类名（来自 @plane/tailwind-config 的语义 token，如 bg-custom-background-90、text-custom-text-100、border-custom-border-200）。
    使用 @plane/utils 的 cn() 函数条件拼接类名。使用 lucide-react 图标（Filter, ArrowUpDown, ChevronLeft, ChevronRight, List, Kanban, Search, X, Plus 等）。

    Step 1: 创建 app/issues/page.tsx（Issue 路由入口页面 per D-P16-01）:
    - import React, { observer, useStore, useParams, useNavigate } 等
    - import IssueListView from "@/components/issues/list-view"
    - import IssueCreateModal from "@/components/issues/issue-create-modal"
    - import { List, Kanban } from "lucide-react"（视图切换图标）
    - 获取 route params: workspaceId, projectId（从 useParams）
    - 用 useStore().issue 获取 IssueStore
    - 渲染：
      a) 页面顶部：标题 "Issues" + 视图切换按钮（List/Kanban 两个 tab 按钮，根据 IssueStore.activeView 高亮当前选中）。Kanban 标签目前设为不可用（opacity-50 cursor-not-allowed），等 Plan 16-04 启用。使用 D-P16-16 note 的共享筛选状态概念。
      b) 根据 activeView 条件渲染 IssueListView（activeView === "list" 时渲染）
      c) 浮动渲染 IssueCreateModal（通过 state 控制显示/隐藏）
    - 使用 useIssues(projectId, filters) 获取数据并传给 IssueListView
    - 默认函数式组件，export default

    Step 2: 创建 app/components/issues/list-view.tsx:
    - Props: { workspaceId: string; projectId: string }，通过 useStore().issue 读取 filters、sortBy 等
    - 使用 useIssues(projectId, filters) 获取数据
    - 按当前排序字段和方向在客户端排序（useMemo）
    - 分页：计算总页数（Math.ceil(sorted.length / pageSize)），取当前页数据（slice）
    - 渲染结构：
      a) QuickFilterBar 组件（传入 filters、onFiltersChange、store.issue.setFilters）
      b) 排序下拉：字段选择 + 方向切换
      c) 表格表头行（#、Issue ID、标题、状态、优先级、负责人、更新时间——按 D-P16-08 固定列）
      d) IssueRow 列表（map 当前页 Issue 数据）
      e) Pagination 分页控件
      f) BulkActionBar（当 selectedIssueIds.length > 0 时显示）
    - 加载状态：使用 Loader 组件显示 5 行骨架屏
    - 空状态：使用 EmptyState 组件显示"暂无 Issue" + 创建第一个 Issue CTA
    - 筛选无结果：显示"没有匹配的 Issue" + 清除筛选条件链接

    Step 3: 创建 app/components/issues/issue-row.tsx:
    - Props: { issue: TIssue; workspaceId: string; projectId: string; isSelected: boolean; onToggleSelect: () => void }
    - 使用 useNavigate() 导航
    - 渲染一行数据：
      a) Checkbox（选中状态绑定 isSelected，点击调用 onToggleSelect）
      b) Issue ID（格式：project.identifier + "-" + issue.sequence_id，如 "FF-12"）— 用 text-caption-md-regular 样式
      c) Issue 标题（issue.name，点击整行 navigate 到详情页）
      d) 优先级徽章（Badge 组件，颜色按 UI-SPEC.md Priority Color Palette——urgent=text-color-priority-urgent 等）
      e) 负责人头像（Avatar 组件，从 issue.assignee_ids 匹配当前 assignee 显示第一个）
      f) 状态徽章（Badge 组件，颜色从 MOCK_STATES 中匹配 state_id 获取）
      g) 更新时间（date-fns formatDistanceToNow，相对时间如"3 小时前"）
    - 整行点击导航到详情页（navigate），checkbox 区域点击不冒泡（event.stopPropagation）
    - 使用中等密度显示。行高 44px 紧凑式 per D-P16-05

    Step 4: 创建 app/components/issues/quick-filter-bar.tsx:
    - Props: { store: IIssueStore }
    - 显示快速筛选条 per D-P16-06：
      a) 三个快速筛选 chip 区域：状态、优先级、负责人
      b) 每个 chip 点击展开下拉面板显示选项列表（多选）
      c) 已选选项显示为 chip（可点击 X 移除）
      d) 搜索输入框（左侧放大镜图标，绑定 store.setFilters({ searchQuery })）
      e) "展开更多筛选"按钮（点击展开更多筛选项——当前仅三个快速筛选，扩展面板预留）
    - 使用 @plane/ui 的 Dropdown 组件（多选模式）或 lucide-react 图标 + 自定义弹出面板
    - 使用 observer() 包裹

    Step 5: 创建 app/components/issues/pagination.tsx:
    - Props: { currentPage: number; totalPages: number; onPageChange: (page: number) => void }
    - 显示分页控件 per D-P16-07：<< < 1 2 3 ... N > >>
    - 当前页高亮显示
    - 页码过多时用 ... 省略（最多显示 7 个页码）
    - 使用 lucide-react 的 ChevronLeft/ChevronRight 图标

  </action>
  <acceptance_criteria>
    - app/issues/page.tsx 存在，使用 useStore().issue.activeView 切换视图模式
    - app/issues/page.tsx 包含两个视图切换按钮（List 和 Kanban），Kanban 按钮初始为 disabled 状态
    - IssueListView 渲染时显示 QuickFilterBar + 表头 + IssueRow 列表 + Pagination
    - IssueRow checkbox 点击不触发导航
    - QuickFilterBar 包含状态/优先级/负责人三个快速筛选 chip
    - Pagination 组件显示页码控件
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    Issue 列表页面和所有列表视图组件已创建完成并通过类型检查
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 IssueCreateModal、BulkActionBar、更新 ProjectDashboard</name>
  <files>
    yh-flow/clients/web/app/components/issues/issue-create-modal.tsx
    yh-flow/clients/web/app/components/issues/bulk-action-bar.tsx
    yh-flow/clients/web/app/components/project/project-dashboard.tsx
  </files>
  <read_first>
    yh-flow/clients/web/src/lib/hooks/use-issues.ts（useIssueMutations hook——createIssue 和 bulkUpdateIssues 方法）
    yh-flow/clients/web/app/store/issue.store.ts（IssueStore 中 selectedIssueIds）
    yh-flow/clients/web/src/lib/mock-data.ts（MOCK_STATES、MOCK_LABELS、MOCK_MEMBERS——供下拉选项使用）
    yh-flow/clients/web/src/lib/types/issues/issue.ts（TIssue 类型）
    yh-flow/clients/web/src/lib/ui/（Modal, Button, Dropdown, Avatar 等现有组件）
    yh-flow/clients/web/src/lib/editor/（@plane/editor——用于 Issue 描述）
    yh-flow/clients/web/app/components/project/project-dashboard.tsx（当前 ProjectDashboard 代码——需修改 Issues tab 行为）
  </read_first>
  <action>
    Step 1: 创建 app/components/issues/issue-create-modal.tsx:
    - Props: { isOpen: boolean; onClose: () => void; workspaceId: string; projectId: string }
    - 使用 @plane/ui 的 Modal 组件作为容器（全屏/slide-over 样式的半屏面板 per CONTEXT.md specifics）
    - 表单字段：
      a) **标题**: input 元素，placeholder="Issue 标题"，必填，用 IssueStore 或 local state
      b) **描述**: @plane/editor 的 LiteTextEditorWithRef（轻量版本，提供基本格式化），placeholder="添加描述..."
      c) **优先级**: @plane/ui 的 Dropdown 单选，选项为 urgent/high/medium/low/none（中文标签：紧急/高/中/低/无），按 D-P16-05 优先级颜色显示
      d) **负责人**: @plane/ui 的 Dropdown 单选，选项从当前项目成员获取（从 mock-data.ts MOCK_MEMBERS），显示头像+名称
      e) **标签**: @plane/ui 的 Dropdown 多选，选项从 MOCK_LABELS 获取，显示颜色 dot + 名称
    - 提交按钮：使用 useIssueMutations().createIssue mutate，成功后在 onSuccess 回调中用 useNavigate 导航到新 Issue 详情页（/workspaces/${workspaceId}/projects/${projectId}/issues/${newIssue.id}）
    - 取消按钮：关闭 modal
    - 加载状态：提交中禁用按钮并显示 spinner
    - 使用 observer() 包裹

    Step 2: 创建 app/components/issues/bulk-action-bar.tsx:
    - Props: { workspaceId: string; projectId: string; selectedIds: string[]; onClearSelection: () => void }
    - 浮动显示在列表底部，fixed 定位
    - 显示选中数量："已选择 {N} 项"
    - 操作按钮组 per D-P16-07 note：
      a) "状态变更"：Dropdown 选择新状态（从 MOCK_STATES 中获取当前 project 的状态），选择后调用 useIssueMutations().bulkUpdateIssues
      b) "指派"：Dropdown 选择负责人，选择后调用 bulkUpdateIssues
      c) "删除"：红色按钮，点击弹出确认对话框（"确定删除选中的 N 个 Issue 吗？"），确认后调用软删除
    - 完成操作后自动清除选中（onClearSelection）
    - 使用 observer() 包裹

    Step 3: 修改 app/components/project/project-dashboard.tsx:
    - import { useNavigate } from "react-router"
    - 在函数组件内调用 const navigate = useNavigate()
    - 修改 Issues tab 的 onClick：不再 setActiveTab("issues")，而是 navigate 到 Issue 列表页面（/workspaces/${workspaceId}/projects/${projectId}/issues）
    - 其他 tab 保持原状（placeholder 文本）

  </action>
  <acceptance_criteria>
    - IssueCreateModal 渲染标题/描述/优先级/负责人/标签字段
    - IssueCreateModal 提交后调用 useIssueMutations().createIssue 并 navigate 到详情页
    - BulkActionBar 在 selectedIssueIds 非空时渲染，显示选中数量和三个操作（状态变更/指派/删除）
    - BulkActionBar 中的删除操作显示确认对话框
    - ProjectDashboard 的 Issues tab 点击后 navigate 到 /workspaces/:wsId/projects/:projId/issues
    - `npx tsc --noEmit` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd yh-flow/clients/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    IssueCreateModal、BulkActionBar 创建完毕，ProjectDashboard Issues tab 已更新导航行为，全部通过类型检查
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                           |
| --------------------------- | ------------------------------------------------------------------------------------- |
| IssueCreateModal form input | User-entered title/description — mock layer has no persistence, no server interaction |

## STRIDE Threat Register

| Threat ID  | Category  | Component                          | Disposition | Mitigation Plan                                                                                    |
| ---------- | --------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-16-02-01 | Tampering | IssueCreateModal form submission   | accept      | Mock data layer mutation is in-memory only; no real server-side injection surface                  |
| T-16-02-02 | DoS       | Bulk deletion with large selection | accept      | Mock layer operation is synchronous and instant; real API will need batch size limits in Phase 17+ |

</threat_model>

<verification>
1. `npx tsc --noEmit` — 类型检查通过
2. `grep -c "useIssueMutations" app/components/issues/issue-create-modal.tsx` — create modal 使用了 mutation hook
3. `grep -c "navigate" app/components/project/project-dashboard.tsx` — ProjectDashboard 使用了 navigate
4. 浏览器验证（checkpoint）：打开项目页面，点击 Issues 标签确认导航到 Issues 列表页
</verification>

<success_criteria>

- app/issues/page.tsx 是能正常渲染的 React Router 页面组件
- IssueListView 显示完整的 Issue 列表（含 QuickFilterBar + 分页）
- IssueCreateModal 可创建 Issue 并在创建后跳转详情页
- BulkActionBar 在选中 Issue 后浮动显示，支持状态变更/指派/删除
- 从 ProjectDashboard 点击 Issues 可导航到 Issue 列表
- `npx tsc --noEmit` 零错误
  </success_criteria>

<output>
Create `.planning/phases/16-issue/16-02-SUMMARY.md` when done
</output>
