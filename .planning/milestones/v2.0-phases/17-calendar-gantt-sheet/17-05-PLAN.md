---
phase: 17-calendar-gantt-sheet
plan: 05
type: execute
wave: 3
depends_on: [17-01, 17-02, 17-03, 17-04]
files_modified:
  - yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx
  - yh-flow/clients/web/app/components/issues/filters/column-selector.tsx
  - yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx
  - yh-flow/clients/web/src/lib/services/issue-view.service.ts
  - yh-flow/clients/web/src/lib/hooks/use-issue-views.ts
autonomous: true
requirements: [FILT-03, FILT-04]

must_haves:
  truths:
    - "FilterBar 组件提供快速筛选（state/priority/assignee）和展开面板（更多条件）"
    - "筛选条件以可移除 chip 形式展示"
    - "ColumnSelector 组件供用户选择可见列"
    - "用户可将当前筛选/排序/列配置保存为视图（FILT-04）"
    - "保存的视图数据通过 API 持久化（mock 层）"
  artifacts:
    - path: "yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx"
      provides: "Reusable FilterBar component"
      min_lines: 100
    - path: "yh-flow/clients/web/app/components/issues/filters/column-selector.tsx"
      provides: "Column visibility selector component"
      min_lines: 60
    - path: "yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx"
      provides: "Save view configuration modal"
      min_lines: 60
    - path: "yh-flow/clients/web/src/lib/services/issue-view.service.ts"
      provides: "API service for saving IssueView entities"
      min_lines: 30
  key_links:
    - from: "filter-bar.tsx"
      to: "use-filters.ts"
      via: "imports useFilters hook"
    - from: "filter-save-modal.tsx"
      to: "issue-view.service.ts"
      via: "calls createIssueView"
---

<objective>
构建筛选引擎 UI 组件：FilterBar 筛选条、ColumnSelector 列选择器、FilterSaveModal 保存视图模态框，以及 IssueView API 服务层。

目的: 实现 FILT-03（自定义列）和 FILT-04（保存视图），整合 Plan 01 的 hooks 为可用的 UI。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-calendar-gantt-sheet/17-CONTEXT.md — D-P17-09~15
@.planning/phases/17-calendar-gantt-sheet/17-UI-SPEC.md — Filter engine interaction contract

# Existing components

@yh-flow/clients/web/app/components/issues/filters/types.ts
@yh-flow/clients/web/app/components/issues/filters/use-filters.ts
@yh-flow/clients/web/app/components/issues/filters/use-sorting.ts
@yh-flow/clients/web/app/components/issues/quick-filter-bar.tsx — current quick filter to be replaced
@yh-flow/clients/web/app/components/issues/filters/use-group-by.ts
@yh-flow/clients/web/src/lib/services/ — existing service layer pattern
@yh-flow/clients/web/app/store/issue.store.ts — visibleColumnIds

# Plane reference

@apps/web/core/components/issues/issue-layouts/filters/header/filters/ — Plane filter components
@apps/web/core/components/views/form.tsx — Plane view save form reference
@apps/web/core/components/views/helper.tsx — Plane view helper functions
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 构建 FilterBar 组件</name>
  <files>
    - yh-flow/clients/web/app/components/issues/filters/filter-bar.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/quick-filter-bar.tsx (MODIFY — deprecate)
  </files>
  <read_first>
    yh-flow/clients/web/app/components/issues/quick-filter-bar.tsx — existing quick filter to draw from
    yh-flow/clients/web/app/components/issues/filters/use-filters.ts — filter hooks
    yh-flow/clients/web/app/components/issues/filters/use-sorting.ts — sorting hooks
    yh-flow/clients/web/app/components/issues/filters/types.ts — type definitions
    apps/web/core/components/issues/issue-layouts/filters/header/filters/ — Plane filter components reference
    yh-flow/clients/web/app/components/issues/list-view.tsx — current filter+sort usage in list-view
  </read_first>
  <action>
    创建 `app/components/issues/filters/filter-bar.tsx`（per D-P17-09, D-P17-10）：

    FilterBar 组件（所有 5 个视图共用）：
    1. Props: `{ workspaceId: string; projectId: string; viewType: TViewLayout }`
    2. 内部使用 `useFilters()` hook 获取筛选状态
    3. 内部使用 `useSorting()` hook 获取排序状态
    4. 快速筛选行（默认显示）:
       - 搜索输入框（左侧 Search 图标，placeholder "搜索 Issue..."）
       - 状态筛选 dropdown（原生 select，选项 = 该项目状态列表）
       - 优先级筛选 dropdown（原生 select，紧急/高/中/低/无）
       - 负责人筛选 dropdown（原生 select，选项 = 工作区成员）
       - "更多"按钮（展开扩展筛选面板 → 标签、创建日期范围等）
       - "清除"链接（仅当有活跃筛选时显示）
    5. 活跃筛选 chip 展示:
       - 已选条件以 chip 形式展示（带 X 移除按钮）
       - 状态 chip: 颜色圆点 + 状态名
       - 优先级 chip: 中文标签
       - 负责人 chip: 姓名
       - 标签 chip: 标签名
    6. 排序行（可选，通过 `showSorting` prop 控制）:
       - "排序" 标签
       - 排序字段按钮组（创建时间/更新时间/优先级/Issue ID）
       - 当前排序字段高亮 + ↑/↓ 箭头
    7. GroupBy 行（可选，通过 `showGroupBy` prop 控制）:
       - "分组" 标签
       - 分组选项按钮（按状态/按优先级/按负责人/按创建人/无）
    8. 扩展筛选面板:
       - "更多"按钮展开后显示标签筛选（多选 toggle）
       - 创建日期范围（开始日期 + 截止日期 input）
    9. 清空所有筛选后自动收起扩展面板

    样式遵循 UI-SPEC 设计：
    - 搜索框: border-custom-border-200, bg-custom-background-90, py-2 px-3
    - Filter chips: border-custom-border-200, bg-custom-background-80, rounded-full
    - 激活 chip: border-custom-primary

    在 quick-filter-bar.tsx 顶部添加 deprecation 注释：
    `// DEPRECATED: 此组件将被 filter-bar.tsx 替换。请在 Phase 17 完成前迁移。`

    **注意**：此组件不引用 Plane 代码——完全自主构建（per D-P17-09 可复用筛选引擎）。

  </action>
  <acceptance_criteria>
    1. FilterBar 组件导出 named export
    2. 搜索、状态、优先级、负责人四大快速筛选工作
    3. "更多"按钮展开标签和日期范围面板
    4. 活跃筛选以可移除 chip 展示
    5. "清除"链接清除所有筛选
    6. 排序和分组行可通过 showSorting/showGroupBy prop 控制显示
    7. `npx tsc --noEmit` 零新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "FilterBar" app/components/issues/filters/filter-bar.tsx</automated>
    <automated>grep -q "showSorting" app/components/issues/filters/filter-bar.tsx || grep -q "showGroupBy" app/components/issues/filters/filter-bar.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    * FilterBar 组件完成，支持快速筛选/扩展面板/chip 展示/排序/分组
    * 所有 5 个视图可共用
    * TypeScript 编译通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 ColumnSelector + FilterSaveModal + IssueView 服务</name>
  <files>
    - yh-flow/clients/web/app/components/issues/filters/column-selector.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/filters/filter-save-modal.tsx (CREATE)
    - yh-flow/clients/web/src/lib/services/issue-view.service.ts (CREATE)
    - yh-flow/clients/web/src/lib/hooks/use-issue-views.ts (CREATE)
  </files>
  <read_first>
    yh-flow/clients/web/app/components/issues/filters/types.ts — TIssueView, TColumnVisibility types
    yh-flow/clients/web/app/store/issue.store.ts — visibleColumnIds
    apps/web/core/components/views/form.tsx — Plane view save form reference
    yh-flow/clients/web/src/lib/services/ — existing service pattern (FlowApiService base class)
    yh-flow/clients/web/src/lib/hooks/use-issues.ts — existing TanStack Query hook pattern
    yh-flow/clients/web/src/lib/constants/issue/filter.ts — ISSUE_DISPLAY_PROPERTIES_KEYS
  </read_first>
  <action>
    创建 `column-selector.tsx`（per D-P17-13）：

    ColumnSelector 组件：
    1. Props: `{ isOpen: boolean; onClose: () => void; selectedColumns: string[]; onChange: (columns: string[]) => void }`
    2. 作为 overlay/弹出面板显示（不是全屏 modal）
    3. 显示所有可用列复选框列表：
       - state（状态）、priority（优先级）、assignee（负责人）
       - start_date（开始日期）、target_date（截止日期）
       - labels（标签）、cycle（周期）、module（模块）
       - estimate（估算）、created_at（创建时间）、updated_at（更新时间）
       - attachments（附件数）、links（链接数）、sub_issues（子 Issue 数）
    4. 每个复选框带中文标签和开关
    5. "确定"按钮关闭面板并应用选择
    6. "重置"按钮恢复默认列

    创建 `filter-save-modal.tsx`（per D-P17-14, FILT-04, D-P17-15）：

    FilterSaveModal 组件：
    1. Props: `{ isOpen: boolean; onClose: () => void; projectId: string; workspaceId: string; currentFilters: TFilterCriteria; currentSort: TSortConfig; currentColumns: string[]; currentLayout: TViewLayout }`
    2. 模态框内容：
       - 标题: "保存为视图"
       - 名称输入框（placeholder: "输入视图名称..."）
       - 布局类型（当前激活的布局，只读显示）
    3. "保存"按钮：
       - 调用 `createIssueView` mutation
       - 成功后关闭 modal 并显示 toast
    4. 使用 @plane/ui ModalCore 作为模态容器（同 Phase 16 模式）
    5. 将筛选配置序列化为 JSON 存储（per D-P17-15）

    创建 `issue-view.service.ts`：

    IssueView 服务层（per D-P17-14 保存到 API，当前为 mock 层）：
    1. `createIssueView(data: TIssueView): Promise<TIssueView>` — 创建视图
       - mock 实现：在内存数组中存储，返回带 ID 的 TIssueView
    2. `getIssueViews(projectId: string): Promise<TIssueView[]>` — 获取项目视图列表
       - mock 实现：从内存数组筛选
    3. 遵循现有 service 模式（参考其他 service 文件结构）

    创建 `use-issue-views.ts`：

    TanStack Query hooks：
    1. `useIssueViews(projectId: string)` — useQuery 获取视图列表
    2. `useCreateIssueView()` — useMutation 创建视图
       - onSuccess: invalidate query key ["issue-views"]

  </action>
  <acceptance_criteria>
    1. ColumnSelector 显示列复选列表
    2. FilterSaveModal 可输入名称并保存视图
    3. 保存的视图包含 filters/sort/columns/layout 配置
    4. issue-view.service.ts 有 createIssueView 和 getIssueViews 方法
    5. use-issue-views.ts 提供 useIssueViews 和 useCreateIssueView hooks
    6. `npx tsc --noEmit` 零新增错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ColumnSelector" app/components/issues/filters/column-selector.tsx</automated>
    <automated>grep -q "FilterSaveModal" app/components/issues/filters/filter-save-modal.tsx</automated>
    <automated>grep -q "createIssueView" src/lib/services/issue-view.service.ts</automated>
    <automated>grep -q "useCreateIssueView" src/lib/hooks/use-issue-views.ts</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    * ColumnSelector 组件完成
    * FilterSaveModal 保存视图完成（FILT-04）
    * IssueView mock 服务层完成
    * TanStack Query hooks 完成
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID   | Category               | Component              | Disposition | Mitigation Plan                                          |
| ----------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------------- |
| T-17-FIL-01 | Tampering              | Filter criteria to URL | mitigate    | useFilters 序列化时对 query params 做 encodeURIComponent |
| T-17-FIL-02 | Information Disclosure | Saved views            | accept      | Phase 17 mock 层无认证，Phase 19 管理 UI 时引入权限      |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
ls -la app/components/issues/filters/filter-bar.tsx
ls -la app/components/issues/filters/column-selector.tsx
ls -la app/components/issues/filters/filter-save-modal.tsx
ls -la src/lib/services/issue-view.service.ts
ls -la src/lib/hooks/use-issue-views.ts
```
</verification>

<success_criteria>

- [ ] FilterBar 组件完成（搜索/快速筛选 chip/扩展面板/排序/分组）—— 所有 5 个视图可共用
- [ ] ColumnSelector 组件完成（列复选/确定/重置）
- [ ] FilterSaveModal 组件完成（保存筛选/排序/列配置为视图）
- [ ] IssueView mock 服务层完成
- [ ] useIssueViews + useCreateIssueView hooks 完成
- [ ] `npx tsc --noEmit` 零新增错误

输出: filter-bar.tsx, column-selector.tsx, filter-save-modal.tsx, issue-view.service.ts, use-issue-views.ts
</success_criteria>

<output>
创建 `.planning/phases/17-calendar-gantt-sheet/17-05-SUMMARY.md`
</output>
