---
phase: 17-calendar-gantt-sheet
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yh-flow/clients/web/app/components/issues/calendar-view/calendar-view.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/calendar-header.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/day-tile.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/issue-block.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/issue-blocks.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/issue-block-root.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/week-days.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/week-header.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/quick-add-issue-actions.tsx
  - yh-flow/clients/web/app/components/issues/calendar-view/utils.ts
  - yh-flow/clients/web/app/components/issues/calendar-view/roots/base-calendar-root.tsx
autonomous: true
requirements: [CALN-01, CALN-02]

must_haves:
  truths:
    - "用户可以从 issues 页切换到日历视图"
    - "日历显示当前月份，issue 按日期显示在对应 day tile 上"
    - "用户可以上/下月导航，点击今天按钮跳回当前日期"
    - "Issue 带开始日期和截止日期时可跨多天显示（双日期 span）"
    - "用户可以通过拖拽调整 issue 日期（CALN-02）"
    - "空月份显示'该月份没有 Issue'"
  artifacts:
    - path: "yh-flow/clients/web/app/components/issues/calendar-view/calendar-view.tsx"
      provides: "Calendar view main container"
      contains: "export default CalendarView"
    - path: "yh-flow/clients/web/app/components/issues/calendar-view/calendar-header.tsx"
      provides: "Month navigation + today button"
    - path: "yh-flow/clients/web/app/components/issues/calendar-view/day-tile.tsx"
      provides: "Single day cell with issue blocks"
  key_links:
    - from: "calendar-view.tsx"
      to: "@plane/types, @plane/utils, @/../src/lib/mock-data, @/components/issues/filters"
      via: "imports"
---

<objective>
Fork Plane 日历视图组件到 yh-flow 项目中（per D-P17-01）。复制 apps/web/core/components/issues/issue-layouts/calendar/ 到 app/components/issues/calendar-view/，适配 yh-flow 的导入路径。

目的: 实现 CALN-01（日历视图展示）和 CALN-02（拖拽调整日期），保持 Plane UI 一致。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-calendar-gantt-sheet/17-CONTEXT.md — D-P17-01, D-P17-06
@.planning/phases/17-calendar-gantt-sheet/17-UI-SPEC.md — Calendar section interaction contract

# Plane source to fork

@apps/web/core/components/issues/issue-layouts/calendar/calendar.tsx
@apps/web/core/components/issues/issue-layouts/calendar/header.tsx
@apps/web/core/components/issues/issue-layouts/calendar/day-tile.tsx
@apps/web/core/components/issues/issue-layouts/calendar/issue-block.tsx
@apps/web/core/components/issues/issue-layouts/calendar/issue-blocks.tsx
@apps/web/core/components/issues/issue-layouts/calendar/issue-block-root.tsx
@apps/web/core/components/issues/issue-layouts/calendar/quick-add-issue-actions.tsx
@apps/web/core/components/issues/issue-layouts/calendar/week-days.tsx
@apps/web/core/components/issues/issue-layouts/calendar/week-header.tsx
@apps/web/core/components/issues/issue-layouts/calendar/utils.ts
@apps/web/core/components/issues/issue-layouts/calendar/roots/base-calendar-root.tsx

# Existing yh-flow patterns

@yh-flow/clients/web/app/components/issues/kanban-view.tsx — fork adaptation pattern
@yh-flow/clients/web/src/lib/hooks/use-issues.ts — issue data hook
@yh-flow/clients/web/src/lib/mock-data.ts — mock data
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fork Plane 日历视图核心组件</name>
  <files>
    - yh-flow/clients/web/app/components/issues/calendar-view/README.md (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/utils.ts (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/week-header.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/week-days.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/day-tile.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/header.tsx (CREATE, rename from Plane header.tsx → calendar-header.tsx)
    - yh-flow/clients/web/app/components/issues/calendar-view/issue-block.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/issue-blocks.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/issue-block-root.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/quick-add-issue-actions.tsx (CREATE)
    - yh-flow/clients/web/app/components/issues/calendar-view/roots/base-calendar-root.tsx (CREATE)
  </files>
  <read_first>
    apps/web/core/components/issues/issue-layouts/calendar/ — entire Plane calendar directory as reference
    yh-flow/clients/web/app/components/issues/kanban-view.tsx — yh-flow fork adaptation pattern (Plane→yh-flow conventions)
    yh-flow/clients/web/src/lib/hooks/use-issues.ts — issue data hooks for replacement
    yh-flow/clients/web/src/lib/mock-data.ts — mock data reference
  </read_first>
  <action>
    遵照 D-P17-01：从 Plane 的 apps/web/core/components/issues/issue-layouts/calendar/ 复制所有组件到 app/components/issues/calendar-view/。

    创建 README.md 标识 fork 来源（`// FLOW: Forked from Plane apps/web/core/components/issues/issue-layouts/calendar/`）。

    创建 utils.ts — 复制 Plane calendar/utils.ts，在文件头添加 `// FLOW: Forked from Plane calendar/utils.ts`。保持 date-fns 的日期工具函数不变（addMonths, subMonths, startOfMonth, endOfMonth, format, getDate, getDay, isSameMonth, isSameDay, isToday, startOfWeek, endOfWeek, eachDayOfInterval, parseISO 等 Plane 已有引入）。

    创建 week-header.tsx — 复制 Plane calendar/week-header.tsx，保持 WeekdayHeader 组件（SUN/MON/TUE/WED/THU/FRI/SAT 或中文化为 日/一/二/三/四/五/六），在文件头添加 fork标记。

    创建 week-days.tsx — 复制 Plane calendar/week-days.tsx，保持 WeekDays 渲染逻辑。

    创建 day-tile.tsx — 复制 Plane calendar/day-tile.tsx。这是关键组件，渲染单个 day cell 及其包含的 issue 列表。保持：
    - DayTile 组件，接收 date/issueBlocks/isToday/isCurrentMonth 等 props
    - 今天高亮（accent border indicator per UI-SPEC 设计）
    - issue 列表用 @hello-pangea/dnd Droppable

    创建 header.tsx （目标文件名 calendar-header.tsx） — 复制 Plane calendar/header.tsx。保持 CalendarHeader 组件：
    - 左/右箭头导航月份
    - "今天" 按钮
    - 当前月份 / 年份显示

    创建 issue-block.tsx — 复制 Plane calendar/issue-block.tsx。这是 issue 卡片组件：
    - 显示优先级颜色指示器
    - Issue 名称 (单行截断)
    - 状态颜色圆点
    - Draggable wrapper（per @hello-pangea/dnd）

    创建 issue-blocks.tsx — 复制 Plane calendar/issue-blocks.tsx。渲染多个 issue-block：
    - 在 Droppable 区域内渲染 issue-block 列表
    - 处理双日期 span（start_date 到 target_date 跨越多个 day tile 时显示颜色条）

    创建 issue-block-root.tsx — 复制 Plane calendar/issue-block-root.tsx。IssueBlockRoot 组件：
    - 整合 issue-block 渲染
    - 显示 issue 按键

    创建 quick-add-issue-actions.tsx — 复制 Plane calendar/quick-add-issue-actions.tsx。点击空 day tile 时的快速创建：
    - 内联输入框 + 提交按钮

    创建 roots/base-calendar-root.tsx — 复制 Plane calendar/roots/base-calendar-root.tsx。日历根容器：
    - 使用 useIssues hook 按月份范围懒加载 (per D-P17-06: date-fns 起止日期计算)
    - calendar month data 构建
    - DragDropContext 包装

    所有组件导入路径适配规则：
    - `@/components/issues/issue-layouts/calendar/` → `./`
    - `@/components/issues/issue-layouts/helpers/` → 移除或 replace with inline
    - `@/components/issues/` → `../../` 但尽量保持相对路径
    - `@/core/components/` → 评估是否在 yh-flow 中可用
    - `@plane/types` → 保持 `@plane/types`（已在 package.json）
    - `@plane/ui` → 保持 `@plane/ui`（已在 package.json）
    - `@plane/utils` → 保持 `@plane/utils`（已在 package.json）
    - Plane `useIssues` hook → 替换为 `@/../src/lib/hooks/use-issues` 的 `useIssues`
    - Plane store hooks → 替换为 yh-flow MobX `useStore` from `@/lib/store-context`

    **import 映射备忘（通用规则）**:
    - `@/store/issue/issue.store.ts` → `@/store/issue.store` (yh-flow 路径风格)
    - `@/hooks/use-issues` → `@/../src/lib/hooks/use-issues`
    - `@/hooks/use-project-states` → `@/../src/lib/hooks/use-issues` (已有 useProjectStates 导出)
    - `@/lib/mock-data` → `@/../src/lib/mock-data`
    - Plane UI 包导入保持原样

    在 Plane 组件中引用 `@/core/components/issues/issue-layouts/empty-states/` 的组件时，在线替换为空状态 HTML（参照 yh-flow list-view.tsx 的空状态模式）。

    所有修改过的 Plane 组件使用 `// FLOW:` 标记标识变化。

  </action>
  <acceptance_criteria>
    1. calendar-view/ 目录创建，包含所有 12 个组件文件 + README
    2. 所有组件不引用不存在的 Plane 内部路径
    3. `npx tsc --noEmit` 只报已经存在的 pre-existing 错误，不新增 calendar-view 相关的错误
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "calendar" || echo "no new calendar errors"</automated>
    <automated>ls -la app/components/issues/calendar-view/issue-block.tsx && echo "issue-block exists"</automated>
  </verify>
  <done>
    * 所有日历视图组件已 fork 到 app/components/issues/calendar-view/
    * 导入路径适配完成，无新增 tsc 错误
    * 组件保持 Plane UI 一致性
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 CalendarView 主容器 + 集成 mock 数据</name>
  <files>
    - yh-flow/clients/web/app/components/issues/calendar-view/calendar-view.tsx (CREATE)
    - yh-flow/clients/web/src/lib/mock-data.ts (MODIFY, add date fields to mock issues)
  </files>
  <read_first>
    apps/web/core/components/issues/issue-layouts/calendar/calendar.tsx — Plane CalendarView source
    yh-flow/clients/web/app/components/issues/kanban-view.tsx — yh-flow view container pattern
    yh-flow/clients/web/src/lib/mock-data.ts — existing mock issues (note: many already have start_date/target_date)
    yh-flow/clients/web/app/components/issues/filters/types.ts — TFilterCriteria for future filter integration
  </read_first>
  <action>
    创建 calendar-view.tsx — CalendarView 主容器组件（即 Plane calendar.tsx 的 yh-flow 适配版本）。

    CalendarView 组件：
    1. Props: `{ workspaceId: string; projectId: string }`
    2. 使用 `useIssues(projectId)` 获取当前项目的所有 issue
    3. 使用 `useStore().issue.activeView` 确认当前视图（兼容新 TViewLayout 类型）
    4. 使用 date-fns 计算当前可视月份范围（D-P17-06），按 start_date/target_date 筛选 issue
    5. 构建 calendar month 数据结构（ICalendarPayload / ICalendarMonth / ICalendarWeek 模式）
    6. 在 DragDropContext 中渲染：
       - CalendarHeader（月份导航 + 今天按钮）
       - WeekHeader（星期几）
       - WeekDays（实际日期网格）
       - DayTile 内渲染 issue-block
    7. 处理拖拽（per D-P17-CALN-02）：
       - DragDropContext onDragEnd → 计算新日期
       - 调用 updateIssue mutation（乐观更新 + 回滚）
    8. 状态处理：
       - 加载中: "加载中..." spinner
       - 空月份: "该月份没有 Issue" 居中显示（per UI-SPEC 文案）
       - 当前日: accent border 高亮

    mock-data.ts 更新：
    - 确认 MOCK_ISSUES 中已有 start_date / target_date 字段（大部分已有）
    - 为缺少日期的 issue-3（已取消）和 issue-6（已完成）等保持现有值
    - 检查是否有至少 5 个 issue 带有 start_date + target_date 以测试跨日期显示

    注意：所有文件添加 `// FLOW:` 标记标识 Plane fork 来源。

  </action>
  <acceptance_criteria>
    1. CalendarView 默认导出 named export 和 default export
    2. 导航到包含 start_date/target_date 的月份时可看到 issue 块
    3. 拖拽 issue 到新的 day tile 触发 updateIssue mutation
    4. 空月份显示"该月份没有 Issue"
    5. `npx tsc --noEmit` 不新增 calendar-view 相关错误
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CalendarView" app/components/issues/calendar-view/calendar-view.tsx</automated>
    <automated>npx tsc --noEmit 2>&1 | grep "calendar-view" | grep -c "error" || echo "no calendar-view type errors"</automated>
  </verify>
  <done>
    * CalendarView 主容器组件完成
    * 拖拽调整日期 (CALN-02) 功能实现
    * TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description             |
| ------------------------------------ | ----------------------- |
| Drag and drop → updateIssue mutation | 拖拽操作触发的 API 调用 |

## STRIDE Threat Register

| Threat ID   | Category  | Component          | Disposition | Mitigation Plan                                                                            |
| ----------- | --------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------ |
| T-17-CAL-01 | Tampering | DragDrop onDragEnd | mitigate    | 使用 StrictMode ref guard（同 kanban-view.tsx 模式），防止同一拖拽事件重复触发 updateIssue |

</threat_model>

<verification>
```bash
cd yh-flow/clients/web
npx tsc --noEmit
ls -la app/components/issues/calendar-view/ | wc -l
grep -c "FLOW:" app/components/issues/calendar-view/issue-block.tsx
grep -c "CalendarView" app/components/issues/calendar-view/calendar-view.tsx
```
</verification>

<success_criteria>

- [ ] 12 个 Plane 日历组件 fork 到 app/components/issues/calendar-view/
- [ ] 所有导入路径适配到 yh-flow 结构
- [ ] CalendarView 主容器完成，支持月份导航/今天按钮/拖拽调整日期
- [ ] issue 按日期展示在对应 day tile 上
- [ ] 双日期 span 展示（start_date → target_date）
- [ ] `npx tsc --noEmit` 不新增 calendar 错误

输出: app/components/issues/calendar-view/ 完整目录
</success_criteria>

<output>
创建 `.planning/phases/17-calendar-gantt-sheet/17-02-SUMMARY.md`
</output>
