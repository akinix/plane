---
phase: 02-workspace
plan: 06
type: execute
wave: 5
depends_on: [02-04, 02-05]
files_modified:
  - yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceLifecycleSmokeTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceRoleCapabilityTests.cs
  - .planning/phases/02-workspace/02-VERIFICATION.md
  - .planning/phases/02-workspace/02-VALIDATION.md
  - .planning/STATE.md
autonomous: false
requirements: [REQ-2.1, REQ-2.2, REQ-2.3, REQ-2.4, NFR-1, NFR-2, NFR-3, NFR-4]
user_setup: []

must_haves:
  truths:
    - "全量回归 dotnet test src/YH.Flow.slnx 全绿（含 Phase 1 Identity.Tests 412 + Phase 2 Workspace.Tests 全部新测试）"
    - "WorkspaceLifecycleSmokeTests 覆盖端到端生命周期：创建 → slug-check → 邀请 → 接受 → list members → 改 role → 删除工作区 → 验证 slug 释放"
    - "WorkspaceRoleCapabilityTests 验证 role→能力矩阵（Admin 全能/Member 读写非成员管理/Guest 只读）"
    - "Phase 2 跨 workspace 数据隔离通过（NFR-2）"
    - "Phase 1 MultitenancyModule 改动零回归（claim/header strategy 仍工作）"
    - "VALIDATION.md nyquist_compliant=true，所有 task 行 status=green"
    - "VERIFICATION.md 记录 Phase 2 完成 + 残留风险"
  artifacts:
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceLifecycleSmokeTests.cs"
      provides: "Phase 2 端到端生命周期 smoke test"
      contains: "WorkspaceLifecycle"
      min_lines: 80
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceRoleCapabilityTests.cs"
      provides: "role→能力矩阵测试（D-11）"
      contains: "RoleCapability"
    - path: ".planning/phases/02-workspace/02-VERIFICATION.md"
      provides: "Phase 2 验证报告"
      contains: "VERIFICATION"
  key_links:
    - from: "yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceLifecycleSmokeTests.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs"
      via: "全部端点串行调用验证生命周期"
      pattern: "CreateWorkspace.*InviteWorkspace.*AcceptInvitation.*ListMembers.*DeleteWorkspace"
---

<objective>
Wave 5：Phase 2 收尾 —— 全量回归 + 端到端生命周期 smoke + role 能力矩阵测试 + 验证报告。这是 Phase 2 从"功能完成"到"ship"的最后一公里。

Purpose: 前 5 个 plan 分散添加了 14+ unit/integration 测试覆盖单点行为，但缺少端到端串行验证（创建→邀请→接受→list→role→删除的全状态流），这是 VALIDATION §Manual-Only Verifications 列的 "端到端 workspace 生命周期" 的自动化部分 + 手工 smoke checkpoint 双重保障。Role 能力矩阵（D-11）需要在端点级别验证（Admin/Member/Guest 各能调哪些端点），避免单点测试漏检矩阵边界。Nyquist 采样连续性要求全量回归（VALIDATION §Sampling Rate "per wave merge"）。

Output: (1) WorkspaceLifecycleSmokeTests（自动化端到端，覆盖 D-08 slug 释放验证）；(2) WorkspaceRoleCapabilityTests（D-11 矩阵守护）；(3) `checkpoint:human-verify` 手工 smoke（用真实 JWT/API Key 跨端点跑流程）；(4) VERIFICATION.md + VALIDATION.md 更新 + STATE.md 标记 Phase 2 完成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-VALIDATION.md
@.planning/phases/02-workspace/02-04-SUMMARY.md
@.planning/phases/02-workspace/02-05-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md

@yh-flow/src/Tests/Architecture.Tests/TenantIsolationTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkspaceLifecycleSmoke + WorkspaceRoleCapability 集成测试 + 全量回归</name>
  <files>
    yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceLifecycleSmokeTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/WorkspaceRoleCapabilityTests.cs
  </files>
  <behavior>
    - WorkspaceLifecycleSmokeTests（端到端串行，10 步流程）：
      1. seed root user (owner) + 普通用户 (invitee)
      2. POST /api/v1/workspaces/ {name: "Acme Corp"} → 创建 workspace，验证返回 slug + 自动 Admin member（D-06）
      3. POST /api/v1/workspaces/slug-check/ {slug: "acme-corp"} → 返回 Exists=false（自身 slug 当然可用，或验证另一 slug "api" 拒绝）
      4. POST /api/v1/workspaces/{slug}/invitations/ {email, role: Member} → 返回 raw token
      5. POST /api/v1/workspaces/invitations/{token}/accept/ (作为 invitee) → 验证创建 member (role=Member)
      6. GET /api/v1/workspaces/{slug}/members/ → 返回 2 个 member（owner Admin + invitee Member），每个含 UserSummary（D-05 batch）
      7. PATCH /api/v1/workspaces/{slug}/members/{inviteeMemberId}/ {role: Admin} (作为 owner) → 成员变 Admin
      8. （验证 403）PATCH 同端点 (作为 invitee 改自己为 Admin) → 403 Forbidden（防 EOP，T-2-eop-self）
      9. DELETE /api/v1/workspaces/{slug}/ (作为 owner) → 软删除工作区
      10. POST /api/v1/workspaces/ {name: "Acme Corp", slug: "acme-corp"} → **成功创建**（D-08 slug 释放验证 —— 软删除后原 slug 可被新 workspace 复用）
    - WorkspaceRoleCapabilityTests（D-11 矩阵，每组 [Theory] case）：
      - Admin: 可调 Create/Update/Delete workspace、Create/List/Revoke invitation、List/Update/Remove member → 全 200/201
      - Member: 可调 List members、List invitations；**不可** Update workspace/Delete workspace/Create invitation/Update member role → 403
      - Guest: 可调 List members（只读）；**不可** 任何 mutation → 403
      - 非成员: 任何 workspace-scoped 端点 → 403
    - 全量回归 `dotnet test src/YH.Flow.slnx`：含 Phase 1 Identity.Tests 412 + Phase 2 全部新测试，0 fail
  </behavior>
  <action>
    **WorkspaceLifecycleSmokeTests.cs**：用 `WebApplicationFactory<Program>`（贴 Identity.Tests integration 风格）或直接构造 mediator + handler 调用链。**关键**：跨端点状态流需要同一 test fixture scope 内共享 DbContext + 缓存。10 步顺序执行（[Fact] 单方法，每步 assert 上一阶段状态）。**步骤 10 是 D-08 核心验证** —— 必须验证软删除后 slug 可复用，否则 D-08 未真正落地。
    **WorkspaceRoleCapabilityTests.cs**：用 [Theory] + [InlineData] 参数化 role × endpoint → 期望状态码。stub `ICurrentWorkspaceContext` 返回不同 role，调各 endpoint，assert 200/201 vs 403。**贴 Plane `app/permissions/workspace.py` 的 role 能力矩阵**：Admin=20 全能，Member=15 读写非管理，Guest=5 只读。
    **若 WebApplicationFactory 启动 API 太慢/复杂**，可降级为：直接注入 IMediator + 各 handler，构造 Command/Query 手动 Send，断言结果或异常。这种方式省去 HTTP 层但失去 middleware/authz 真实性。**推荐**：核心 smoke 用 mediator 直调（快），role capability 用 TestServer HTTP 层（验证 authz pipeline）。
    **执行 `dotnet test src/YH.Flow.slnx` 全量**：贴 Phase 1 Wave 5 验证流程。预期：Identity.Tests 412 全绿 + Workspace.Tests 全部新测试全绿（02-01 spike + 02-02 spike + 02-03 authz/isolation + 02-04 slug/create/softdelete/unique + 02-05 token/listmembers/invitation 三件套 + 02-06 smoke/rolecapability）。若有失败，定位 + 修复或回滚到对应 plan。
    **更新 VALIDATION.md**：把所有 task 行 status 改 ✅，frontmatter `nyquist_compliant: true` + `wave_0_complete: true`。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~WorkspaceLifecycleSmoke|FullyQualifiedName~WorkspaceRoleCapability" --nologo 2>&1 | tail -10 && dotnet test src/YH.Flow.slnx --nologo 2>&1 | tail -15</automated>
  </verify>
  <done>
    WorkspaceLifecycleSmokeTests 10 步全过（含 D-08 slug 释放验证）；WorkspaceRoleCapabilityTests role 矩阵 4 组全过；全量回归 dotnet test src/YH.Flow.slnx 0 fail（含 Phase 1 + Phase 2 全部测试）；VALIDATION.md 全部 status=green + nyquist_compliant=true。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 Workspace 模块完整功能：(1) Workspace 实体 + DbContext + Finbuckle slug 解析（D-01）；(2) WorkspaceMembershipMiddleware + `[RequireWorkspaceRole]` authz（D-02/D-11）；(3) EF 迁移应用 yhschema.Workspace schema；(4) Workspace CRUD + slug generator（D-06/D-07/D-08/D-09）；(5) Member 端点 + 批量 IUserIdentityService（D-04/D-05，无 N+1）；(6) Invitation 端点 + CSPRNG/SHA-256 token（D-10/D-12）。全量 dotnet test 已绿，集成 smoke + role 矩阵测试已过。
  </what-built>
  <how-to-verify>
    手工 smoke（自动化无法覆盖真实 auth + 跨端点状态流的最终验证）。**前置**：Aspire 栈运行（`cd yh-flow/src/Host/YH.Flow.AppHost && dotnet run`，等 PostgreSQL + Redis + API ready）+ 一个真实 root user 的 JWT/API Key（贴 Phase 1 的 `/auth/sign-in` 获取 token）。

    **步骤：**
    1. 用 root token 调 `POST https://localhost:7030/api/v1/workspaces/` body `{"name":"Acme Smoke Test"}` → 验证 201 + 返回 `{"id":"...","slug":"acme-smoke-test"}`（slug 自动 slugify）+ 自动创建 Admin member（D-06）。
    2. 调 `GET /api/v1/workspaces/acme-smoke-test/` → 验证 200 + 完整 WorkspaceDto 字段（id/slug/name/owner_id/logo/timezone/...）。
    3. 调 `POST /api/v1/workspaces/slug-check/` body `{"slug":"api"}` → 验证 400 或 returns Exists=false（受限词拒绝，D-09）。
    4. 调 `POST /api/v1/workspaces/acme-smoke-test/invitations/` body `{"email":"invitee@example.com","role":15}` → 验证 201 + 返回 raw token + slug（D-10/D-12）。
    5. 用 invitee token（先 sign-up invitee@example.com 拿 JWT）调 `POST /api/v1/workspaces/invitations/{token}/accept/` → 验证 200 + 返回 memberId + workspaceId。
    6. 用 root token 调 `GET /api/v1/workspaces/acme-smoke-test/members/` → 验证 200 + 返回 2 个 member（root Admin + invitee Member），每个含 `user: {id, display_name, email, avatar_url}`（D-05 batch 解析）。
    7. 用 invitee token 调 `PATCH /api/v1/workspaces/acme-smoke-test/members/{inviteeMemberId}/` body `{"role":20}` → 验证 **403 Forbidden**（防 EOP，T-2-eop-self，Member 不能自我提权 Admin）。
    8. 用 root token 调 `PATCH /api/v1/workspaces/acme-smoke-test/members/{inviteeMemberId}/` body `{"role":20}` → 验证 200（Admin 可改他人 role）。
    9. 用 root token 调 `DELETE /api/v1/workspaces/acme-smoke-test/` → 验证 204 或 200（软删除）。
    10. 用 root token 调 `POST /api/v1/workspaces/` body `{"name":"Acme Smoke Test","slug":"acme-smoke-test"}` → 验证 **201 成功**（D-08 软删除后 slug 释放可复用）。
    11. **隔离验证**：用 invitee token 调 `GET /api/v1/workspaces/another-workspace/members/`（一个 invitee 不属于的 workspace）→ 验证 **403**（D-02 非成员拒绝）。

    **预期**：所有步骤符合上述状态码 + 字段。任何不符 → 描述问题，回滚到对应 plan 修复。

  </how-to-verify>
  <resume-signal>Type "approved" 全部步骤通过，或描述具体问题让 Claude 回滚修复</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                     |
| -------------- | ----------------------------------------------- |
| 自动化测试覆盖 | 单点 + 端到端 + role 矩阵 + 手工 smoke 多层守护 |
| Phase 1 零回归 | MultitenancyModule 改动可能破坏 Phase 1         |

## STRIDE Threat Register

| Threat ID                | Category               | Component                                         | Disposition         | Mitigation Plan                                                                                              |
| ------------------------ | ---------------------- | ------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------ |
| T-2-regression [HIGH]    | Tampering              | MultitenancyModule 改动破坏 Phase 1               | mitigate [BLOCKING] | 全量 `dotnet test src/YH.Flow.slnx` 含 Identity.Tests 412 必须全绿；手工 smoke 仍能用 Phase 1 认证           |
| T-2-matrixgap [HIGH]     | Elevation of Privilege | role 矩阵漏洞（Guest 可写、Member 可改设置）      | mitigate [BLOCKING] | WorkspaceRoleCapabilityTests 4 组（Admin/Member/Guest/非成员）× 端点 全覆盖；手工 smoke 步骤 7 验证 EOP 守护 |
| T-2-e2egap               | Repudiation            | 端到端状态流漏洞（如 accept 后 list 不见 member） | mitigate            | WorkspaceLifecycleSmokeTests 10 步串行；手工 smoke 复测                                                      |
| T-2-slugreleasereal      | Tampering              | D-08 slug 释放在真实 DB 失效                      | mitigate [BLOCKING] | 手工 smoke 步骤 10 验证；测试步骤 10 验证                                                                    |
| T-2-isolationreal [HIGH] | Information Disclosure | 跨 workspace 数据隔离在生产配置失败               | mitigate [BLOCKING] | 手工 smoke 步骤 11 验证；02-03 TenantIsolationTests；02-06 RoleCapabilityTests 非成员 case                   |
| T-2-SC                   | Tampering              | NuGet                                             | accept              | 0 新包                                                                                                       |

## Threat Disposition Summary

- **mitigate [BLOCKING]**: T-2-regression（全量回归）、T-2-matrixgap（role 矩阵测试）、T-2-slugreleasereal（smoke 步骤 10）、T-2-isolationreal（smoke 步骤 11）—— Task 1 自动化 + Task 2 手工 smoke 双重
- **mitigate**: T-2-e2egap
- **accept**: T-2-SC
- **HIGH severity**: T-2-regression、T-2-matrixgap、T-2-isolationreal —— 手工 smoke 是最终人工 gate
  </threat_model>

<verification>
- [ ] `dotnet build src/YH.Flow.slnx` 0 warning / 0 error
- [ ] `dotnet test src/YH.Flow.slnx` 全绿（含 Phase 1 Identity.Tests 412 + Phase 2 全部测试，预估 440+ 总数）
- [ ] WorkspaceLifecycleSmokeTests 10 步全过（含 D-08 slug 释放）
- [ ] WorkspaceRoleCapabilityTests 4 组 role × 端点矩阵全过
- [ ] VALIDATION.md 全部 status=✅ + nyquist_compliant=true
- [ ] 手工 smoke 11 步全部通过（checkpoint:human-verify）
- [ ] VERIFICATION.md 完成报告写入
- [ ] STATE.md Phase 2 标记 ✅ COMPLETE
</verification>

<success_criteria>

- Phase 2 全量回归全绿（含 Phase 1 零回归）
- 端到端 workspace 生命周期（D-06/D-07/D-08/D-10/D-12）通过 smoke 验证
- role→能力矩阵（D-11）通过自动化 + 手工双重验证
- NFR-1（N+1 避免）/ NFR-2（隔离）/ NFR-3（软删除）/ NFR-4（模块化）全部验证
- Phase 2 可 ship，下游 Phase 3 可基于 `ICurrentWorkspaceContext` + `[RequireWorkspaceRole]` + Workspace DbContext 模式开发
  </success_criteria>

<output>
Create `.planning/phases/02-workspace/02-06-SUMMARY.md` when done. **关键**：(a) 全量回归测试总数 + 全绿证据；(b) 手工 smoke 11 步执行结果；(c) Phase 2 完成的最终状态；(d) 下游 Phase 3 的依赖接口清单（ICurrentWorkspaceContext/RequireWorkspaceRole/IUserIdentityService 用法）；(e) 残留风险（如 INotificationService Phase 11 待实现）。
</output>
