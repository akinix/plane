---
phase: 02-workspace
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/20260617120000_InitialWorkspace.cs
  - yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/20260617120000_InitialWorkspace.Designer.cs
  - yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/WorkspaceDbContextModelSnapshot.cs
  - yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj
  - yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
  - yh-flow/src/Host/YH.Flow.DbMigrator/YH.Flow.DbMigrator.csproj
  - yh-flow/src/Host/YH.Flow.Api/Program.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Middleware/WorkspaceMembershipMiddleware.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleAttribute.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleAuthorizationHandler.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleExtensions.cs
  - yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
  - yh-flow/src/Tests/Workspace.Tests/Authorization/RequireWorkspaceRoleHandlerTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/SlugTenantResolveTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/TenantIsolationTests.cs
  - yh-flow/src/Tests/Workspace.Tests/Integration/MembershipMiddlewareTests.cs
autonomous: true
requirements: [NFR-2]
user_setup: []

must_haves:
  truths:
    - "EF Core 迁移 InitialWorkspace 在 PostgreSQL 创建 yhschema.Workspace schema + Workspaces/WorkspaceMembers/WorkspaceInvitations 三张表"
    - "DbMigrator 启动执行迁移后，postgres 数据库存在 yhschema.Workspace.Workspaces 表（含 Slug unique 索引）"
    - "WorkspaceMembershipMiddleware 在认证之后、授权之前填充 ICurrentWorkspaceContext（D-02）；非成员 role=null"
    - "[RequireWorkspaceRole(Admin)] 装饰的端点：Member/Guest/非成员都返回 403；Admin 返回 200"
    - "DbMigrator Program.cs 的 mediator assemblies 数组 + module assemblies 数组都含 WorkspaceModule"
    - "Api Program.cs 的 moduleAssemblies 数组含 WorkspaceModule（让 FshModule 自动发现）"
    - "跨 workspace 数据隔离：workspace A 的 member 不出现在 workspace B 的 list 结果（NFR-2）"
  artifacts:
    - path: "yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/20260617120000_InitialWorkspace.cs"
      provides: "Workspace schema EF Core 迁移"
      contains: "yhschema.Workspace"
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Middleware/WorkspaceMembershipMiddleware.cs"
      provides: "D-02 成员资格填充中间件"
      contains: "ICurrentWorkspaceContext"
      min_lines: 30
    - path: "yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleAuthorizationHandler.cs"
      provides: "D-11 workspace-role authz handler"
      contains: "AuthorizationHandler"
    - path: "yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs"
      provides: "DbMigrator 注册 WorkspaceModule"
      contains: "WorkspaceModule"
    - path: "yh-flow/src/Tests/Workspace.Tests/Integration/TenantIsolationTests.cs"
      provides: "NFR-2 跨 workspace 隔离测试"
      contains: "TenantIsolation"
  key_links:
    - from: "yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs"
      to: "yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj"
      via: "迁移项目引用 + DbMigrator 启动执行迁移"
      pattern: "WorkspaceDbContext"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Middleware/WorkspaceMembershipMiddleware.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/CurrentWorkspaceContext.cs"
      via: "Scoped context 填充"
      pattern: "ICurrentWorkspaceContext"
    - from: "yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleAuthorizationHandler.cs"
      to: "yh-flow/src/Modules/Workspace/Modules.Workspace/Middleware/WorkspaceMembershipMiddleware.cs"
      via: "中间件填 context → handler 读 context（不查 DB）"
      pattern: "CurrentUserRole"
---

<objective>
Wave 2：执行 [BLOCKING] EF 迁移并落地 D-02 + D-11 的运行时栈 —— 成员中间件 + workspace-role authz + Host 项目全部 wiring。这是 Phase 2 从"领域骨架"到"可运行运行时"的转折点。

Purpose: schema_push_requirement 强制要求 EF 迁移必须应用到 PostgreSQL 并验证 yhschema.Workspace schema 存在（Phase 1 Wave 4 precedent）；D-02 成员中间件是 workspace-role authz 的前置（authz handler 不查 DB）；D-11 authz 是 02-04/02-05 端点的强制 gate；DbMigrator + Api Program.cs 必须注册 WorkspaceModule 才能让模块被发现和迁移被执行。

Output: (1) InitialWorkspace EF 迁移（生成 + 应用 + 验证 PostgreSQL schema）；(2) DbMigrator + Api Program.cs wiring；(3) WorkspaceMembershipMiddleware；(4) `[RequireWorkspaceRole]` attribute + handler + extensions；(5) 4 个新测试（authz handler unit + 3 个 integration：tenant 解析/隔离/成员中间件）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-workspace/02-CONTEXT.md
@.planning/phases/02-workspace/02-RESEARCH.md
@.planning/phases/02-workspace/02-PATTERNS.md
@.planning/phases/02-workspace/02-VALIDATION.md
@.planning/phases/02-workspace/02-02-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md

@yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Identity/20260617103113_AddAPITokenAndOAuthProviderSettings.cs
@yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
@yh-flow/src/Host/YH.Flow.DbMigrator/YH.Flow.DbMigrator.csproj
@yh-flow/src/Host/YH.Flow.Api/Program.cs
@yh-flow/src/Modules/Multitenancy/Modules.Multitenancy/MultitenancyModule.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Authorization/RequiredPermissionAuthorizationHandler.cs
@yh-flow/src/Modules/Identity/Modules.Identity/Authorization/RequiredPermissionAuthorizationExtensions.cs
@yh-flow/src/Modules/Auditing/Modules.Auditing/AuditingModule.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] 生成 EF Core 迁移 + DbMigrator/Api Program.cs 注册 + 应用迁移到 PostgreSQL + 验证 schema</name>
  <files>
    yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/20260617120000_InitialWorkspace.cs
    yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/20260617120000_InitialWorkspace.Designer.cs
    yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/WorkspaceDbContextModelSnapshot.cs
    yh-flow/src/Host/YH.Flow.Migrations.PostgreSQL/YH.Flow.Migrations.PostgreSQL.csproj
    yh-flow/src/Host/YH.Flow.DbMigrator/Program.cs
    yh-flow/src/Host/YH.Flow.DbMigrator/YH.Flow.DbMigrator.csproj
    yh-flow/src/Host/YH.Flow.Api/Program.cs
  </files>
  <behavior>
    - `dotnet ef migrations add InitialWorkspace -c WorkspaceDbContext -o Workspace -p src/Host/YH.Flow.Migrations.PostgreSQL -s src/Host/YH.Flow.DbMigrator` 生成迁移文件（含 Designer + ModelSnapshot）
    - 迁移 Up() 创建 schema `yhschema.Workspace` + 三张表 Workspaces/WorkspaceMembers/WorkspaceInvitations
    - Workspaces 表：Id/Name/Slug(unique index)/OwnerId/Logo/OrganizationSize/TimeZone/BackgroundColor/审计字段 —— **不**含 TenantId 列（IGlobalEntity）
    - WorkspaceMembers 表：含 TenantId 列（IsMultiTenant 自动加）+ Id/WorkspaceId/UserId/Role/IsActive/审计；unique composite (TenantId, UserId)
    - WorkspaceInvitations 表：含 TenantId 列 + Id/WorkspaceId/Email/TokenHash(unique)/Role/Accepted/RespondedAt/ExpiresAt/Message/审计；index (TenantId, Accepted)
    - DbMigrator Program.cs 的 mediator assemblies 数组追加 Workspace.Contracts 的 CreateWorkspaceCommand（占位，02-04 才真正建）+ WorkspaceModule；module assemblies 数组追加 WorkspaceModule.Assembly
    - Api Program.cs 的 moduleAssemblies 数组追加 WorkspaceModule.Assembly
    - 运行 DbMigrator 应用迁移；postgres 中 `\dn yhschema.Workspace` schema 存在；`SELECT to_regclass('yhschema.Workspace.Workspaces')` 返回非 null
  </behavior>
  <action>
    **步骤 1（Migrations csproj）**：编辑 `YH.Flow.Migrations.PostgreSQL.csproj`，追加 `<ProjectReference Include="..\..\Modules\Workspace\Modules.Workspace\Modules.Workspace.csproj" />`（参 csproj 现有 Identity 引用行 13-22）+ `<Folder Include="Workspace\" />`（csproj 26-34 模式）。
    **步骤 2（DbMigrator csproj）**：追加 `<ProjectReference Include="..\..\Modules\Workspace\Modules.Workspace\Modules.Workspace.csproj" />` + Workspace.Contracts 引用（csproj 45-49 模式）。
    **步骤 3（DbMigrator Program.cs）**：在 mediator assemblies 数组（`Program.cs:82-104`）追加两行：`typeof(YH.Modules.Workspace.WorkspaceModule)` 和 `typeof(YH.Modules.Workspace.Contracts.DTOs.WorkspaceDto)`（任何 Workspace.Contracts 公开类型即可触发 source-gen 扫描；CreateWorkspaceCommand 在 02-04 才有，这里先用 WorkspaceModule + 一个 Contracts 类型占位）。在 module assemblies 数组（`Program.cs:107-119`）追加 `typeof(YH.Modules.Workspace.WorkspaceModule).Assembly`。
    **步骤 4（Api Program.cs）**：在 moduleAssemblies 数组（`Program.cs:57-65`）追加 `typeof(YH.Modules.Workspace.WorkspaceModule).Assembly`（PATTERNS §Api Program.cs 给了完整模板）。这一行让 `AddModules` 自动发现 `[FshModule]` 注册 WorkspaceModule。
    **步骤 5（生成迁移）**：执行 `cd yh-flow && dotnet ef migrations add InitialWorkspace -c WorkspaceDbContext -o Workspace -p src/Host/YH.Flow.Migrations.PostgreSQL -s src/Host/YH.Flow.DbMigrator`。**注意**：EF CLI 必须能 build DbMigrator + Migrations.PostgreSQL 项目（前置 csproj 引用必须正确）。如果失败，检查：(a) Migrations csproj 是否引用 Workspace 模块；(b) DbMigrator 是否能 resolve WorkspaceDbContext（已通过 AddHeroDbContext 注册）；(c) Directory.Build.props 的 EF 版本对齐。
    **步骤 6（应用迁移）**：执行 `cd yh-flow && dotnet run --project src/Host/YH.Flow.DbMigrator`（贴 Phase 1 Wave 4 的 AddAPITokenAndOAuthProviderSettings 迁移流程，参考 01-05-SUMMARY）。或者直接 `dotnet ef database update -c WorkspaceDbContext -p src/Host/YH.Flow.Migrations.PostgreSQL -s src/Host/YH.Flow.DbMigrator`。**前置依赖**：Aspire 编排的 PostgreSQL 必须运行（`cd src/Host/YH.Flow.AppHost && dotnet run` 起栈，或单独 docker run postgres）。
    **步骤 7（验证 schema）**：用 psql 连接 postgres（Phase 1 connection string），执行：
    - `SELECT schema_name FROM information_schema.schemata WHERE schema_name = 'yhschema.Workspace';` —— 应返回 1 行
    - `SELECT to_regclass('yhschema.Workspace.Workspaces');` —— 应返回非 null
    - `SELECT to_regclass('yhschema.Workspace.WorkspaceMembers');` —— 应返回非 null
    - `SELECT to_regclass('yhschema.Workspace.WorkspaceInvitations');` —— 应返回非 null
    - `SELECT indexname FROM pg_indexes WHERE schemaname = 'yhschema.Workspace' AND tablename = 'Workspaces';` —— 应含 Slug unique 索引
    - `\d yhschema.Workspace.WorkspaceMembers` —— 应含 TenantId 列（**关键：隔离正确性**）
    - `\d yhschema.Workspace.Workspaces` —— 应**不**含 TenantId 列（**关键：Workspace=IGlobalEntity**）
    **若 schema 不符预期 → rollback 迁移 `dotnet ef migrations remove`，回到 02-02 检查实体配置，重新生成**。
    **autonomous 注意**：迁移生成/应用若有交互式 prompt（如 EF tool 第一次安装），flag `autonomous: false`；但 `dotnet ef migrations add` / `database update` 通常无 prompt，应保持 autonomous: true。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet build src/YH.Flow.slnx --nologo 2>&1 | tail -3 && ls src/Host/YH.Flow.Migrations.PostgreSQL/Workspace/ 2>&1 | head -5 && PGPASSWORD=postgres psql -h localhost -U postgres -d yhflow -tAc "SELECT to_regclass('yhschema.Workspace.Workspaces'), to_regclass('yhschema.Workspace.WorkspaceMembers'), to_regclass('yhschema.Workspace.WorkspaceInvitations');" 2>&1 | tail -3</automated>
  </verify>
  <done>
    InitialWorkspace 迁移文件就位（3 个：迁移 + Designer + Snapshot）；DbMigrator/Api Program.cs 注册 WorkspaceModule；postgres 中 yhschema.Workspace schema + 3 张表存在；WorkspaceMembers 表含 TenantId 列，Workspaces 表不含 TenantId 列；Slug unique 索引存在。
  </done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceMembershipMiddleware（D-02）+ WorkspaceModule.ConfigureMiddleware wiring</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Middleware/WorkspaceMembershipMiddleware.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/MembershipMiddlewareTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/SlugTenantResolveTests.cs
  </files>
  <behavior>
    - WorkspaceMembershipMiddleware 实现 IMiddleware（typed middleware，per PATTERNS §Middleware 推荐）
    - 读 IMultiTenantContextAccessor.AppTenantInfo.Id（= workspaceGuid 字符串）；Guid.TryParse 成功 + ICurrentUser.GetUserId() 成功时查 WorkspaceMember(workspaceId, userId, is_active=true)
    - 命中：调 ICurrentWorkspaceContext.Set(workspaceId, slug, member.Role)；未命中：不调 Set（role=null）
    - 顶层端点（无 slug → tenantInfo=null）跳过 Set（user-scoped）
    - WorkspaceModule.ConfigureMiddleware 注册 app.UseMiddleware&lt;WorkspaceMembershipMiddleware&gt;()
    - SlugTenantResolveTests：构造 HTTP request /api/v1/workspaces/{slug}/...，验证 IMultiTenantContextAccessor.MultiTenantContext.TenantInfo.Id = workspaceGuid（D-01 解析正确）
    - MembershipMiddlewareTests：构造 workspace + member + 非成员用户三种情况，验证 ICurrentWorkspaceContext.CurrentUserRole 分别为 Admin/null（D-02）
  </behavior>
  <action>
    **WorkspaceMembershipMiddleware.cs**（PATTERNS §WorkspaceMembershipMiddleware.cs 给了完整 inline `app.Use` 模板 + 推荐改成 typed middleware 类）：实现 `IMiddleware`，ctor 注入 `IMultiTenantContextAccessor<AppTenantInfo>`、`ICurrentUser`、`WorkspaceDbContext`、`ICurrentWorkspaceContext`。InvokeAsync(HttpContext, RequestDelegate)：
    ```
    var tenantInfo = _accessor.MultiTenantContext?.TenantInfo;
    if (tenantInfo is not null && Guid.TryParse(tenantInfo.Id, out var workspaceId))
    {
        var userId = _currentUser.GetUserId();  // extension from YH.Framework.Shared.Identity.Claims
        if (userId is { } uid)
        {
            var member = await _db.Members.AsNoTracking()
                .FirstOrDefaultAsync(m => m.UserId == uid.ToString() && m.IsActive, ctx.RequestAborted);
            _workspaceContext.Set(workspaceId, tenantInfo.Identifier, member is null ? null : (WorkspaceRole)member.Role);
        }
    }
    await next(ctx);
    ```
    **注意**: WorkspaceDbContext.Members 自动 tenant-filtered（ApplyTenantIsolationByDefault），所以 FirstOrDefaultAsync 自动按当前 workspaceId 过滤（双重保护：即使 ICurrentUser 或 accessor 异常，filter 兜底）。
    **WorkspaceModule.ConfigureMiddleware 更新**：把 02-02 留的占位 `// TODO 02-03` 替换为 `app.UseMiddleware<WorkspaceMembershipMiddleware>();`。**中间件顺序：在 Program.cs 的 UseAuthorization 之前注册**（PATTERNS §WorkspaceMembershipMiddleware 模板 + MultitenancyModule.cs:122-207 模式）。但 WorkspaceModule.ConfigureMiddleware 调用时机由 ModuleLoader 决定（按 FshModule Order 排序）—— 200 在 Identity/Multitenancy 之后、Auditing 之前，正确。
    **SlugTenantResolveTests.cs（integration，依赖 WorkspaceTestFixture）**：构造 `DefaultHttpContext` + 设置 RouteValue "slug"=fixture.SeededWorkspaceSlug + 跑 pipeline → 断言 `accessor.MultiTenantContext.TenantInfo.Id == fixture.SeededWorkspaceId.ToString()`。**这个测试间接验证 02-02 的 slug strategy wiring 正确**。若 spike 路径 A，验证 services.TryAddEnumerable 真的把 strategy 注入；若路径 B，验证 MultitenancyModule 调用 AddWorkspaceTenantResolution 正确链接。
    **MembershipMiddlewareTests.cs（integration）**：seed workspace A + member (Admin) + 另一个 user (非成员)；构造两组 HttpContext，分别模拟两个 user 跑 middleware → 断言 ICurrentWorkspaceContext.CurrentUserRole 分别为 WorkspaceRole.Admin 和 null。
    测试基础设施：WorkspaceTestFixture（02-01 Task 1 建的 InMemory 版）—— 若 InMemory 无法验证 tenant filter，可降级为直接构造 WorkspaceDbContext + 手动 add member + 用 NSubstitute stub IMultiTenantContextAccessor 返回固定 tenantInfo。**贴 Phase 1 Identity.Tests 的 integration test 风格**。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~SlugTenantResolve|FullyQualifiedName~MembershipMiddleware" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    WorkspaceMembershipMiddleware 实现 + 注册；SlugTenantResolveTests 通过（D-01 验证）；MembershipMiddlewareTests 通过（D-02 Admin vs 非成员 null 区分）。
  </done>
</task>

<task type="auto">
  <name>Task 3: [RequireWorkspaceRole] attribute + handler + extensions + 单元测试（D-11）</name>
  <files>
    yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleAttribute.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleAuthorizationHandler.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/Authorization/RequireWorkspaceRoleExtensions.cs
    yh-flow/src/Modules/Workspace/Modules.Workspace/WorkspaceModule.cs
    yh-flow/src/Tests/Workspace.Tests/Authorization/RequireWorkspaceRoleHandlerTests.cs
    yh-flow/src/Tests/Workspace.Tests/Integration/TenantIsolationTests.cs
  </files>
  <behavior>
    - RequireWorkspaceRoleAttribute : Attribute, IAuthorizationRequirement，构造接受 params WorkspaceRole[] Roles（D-11）
    - RequireWorkspaceRoleAuthorizationHandler : AuthorizationHandler&lt;RequireWorkspaceRoleAttribute&gt;，ctor 注入 ICurrentWorkspaceContext；从 context.CurrentUserRole 读，匹配 requirement.Roles 则 Succeed，否则 Fail
    - role=null（非成员）→ Fail；不查 DB（PATTERNS §Authorization）
    - RequireWorkspaceRoleExtensions: 静态类含 `RequireWorkspaceRole(this RouteHandlerBuilder builder, params WorkspaceRole[] roles)` 扩展方法 → 等价 `.RequireAuthorization(new RequireWorkspaceRoleAttribute(roles))`
    - WorkspaceModule.ConfigureServices 注册 handler：`builder.Services.TryAddEnumerable(ServiceDescriptor.Scoped<IAuthorizationHandler, RequireWorkspaceRoleAuthorizationHandler>())`
    - RequireWorkspaceRoleHandlerTests：单元测试 4 个 case：Admin role + requirement[Admin] → Succeed；Member role + requirement[Admin] → Fail；null role + requirement[Member] → Fail；Guest role + requirement[Member/Guest] → Succeed
    - TenantIsolationTests（NFR-2）：seed workspace A + workspace B + 各自一个 member；查 list members A 不应包含 B 的 member（隔离正确）
  </behavior>
  <action>
    **RequireWorkspaceRoleAttribute.cs**（PATTERNS §RequireWorkspaceRoleAuthorizationHandler.cs 给了完整模板）：`[AttributeUsage(AttributeTargets.Class | AttributeTargets.Method)] public sealed class RequireWorkspaceRoleAttribute : Attribute, IAuthorizationRequirement { public WorkspaceRole[] Roles { get; } public RequireWorkspaceRoleAttribute(params WorkspaceRole[] roles) => Roles = roles; }`。
    **RequireWorkspaceRoleAuthorizationHandler.cs**（贴 `RequiredPermissionAuthorizationHandler.cs:9-41` 结构 + PATTERNS §Authorization 模板）：ctor 注入 `ICurrentWorkspaceContext`（**关键差异：identity handler 注入 IUserService 查 DB；workspace handler 注入 context 不查 DB**）。HandleRequirementAsync 实现：role=null → context.Fail()；roles.Contains(role) → context.Succeed；否则 Fail。
    **RequireWorkspaceRoleExtensions.cs**（贴 `RequiredPermissionAuthorizationExtensions.cs:21-35` 模式）：`public static RouteHandlerBuilder RequireWorkspaceRole(this RouteHandlerBuilder b, params WorkspaceRole[] roles) => b.RequireAuthorization(new RequireWorkspaceRoleAttribute(roles));`。**这个扩展方法在 02-04/02-05 端点上用 `.RequireWorkspaceRole(WorkspaceRole.Admin)` 替代 verbose `.RequireAuthorization(new RequireWorkspaceRoleAttribute(...))`**。
    **WorkspaceModule.ConfigureServices 注册**：在 02-02 已有的注册基础上追加 handler DI 注册（上述 TryAddEnumerable）。注意 IAuthorizationHandler 是 multi-registered（不冲突 with Identity 的 PermissionAuthorizationHandler）。
    **RequireWorkspaceRoleHandlerTests.cs（unit，用 NSubstitute）**：4 个 [Theory] case，stub `ICurrentWorkspaceContext`，构造 `AuthorizationHandlerContext(requirement, user: new ClaimsPrincipal(), resource: null)`，调 `handler.HandleAsync(context)`，断言 `context.HasSucceeded` 或 `context.HasFailed`。**贴 Identity.Tests/Authorization/ 测试风格**（若该目录无现有测试，参 `PathAwareAuthorizationHandler` 模式）。
    **TenantIsolationTests.cs（integration，NFR-2 关键守护）**：seed workspace A（含 member1）+ workspace B（含 member2）；构造两个 HttpContext 分别设 TenantId=A/B，跑 `db.Members.ToListAsync()` → 断言 A 上下文只看到 member1，B 上下文只看到 member2。**这是验证 ApplyTenantIsolationByDefault 正确性的核心测试**，若失败说明 02-02 实体配置或 DbContext 派生有误，需回查。
  </action>
  <verify>
    <automated>cd yh-flow && dotnet test src/Tests/Workspace.Tests --filter "FullyQualifiedName~RequireWorkspaceRole|FullyQualifiedName~TenantIsolation" --nologo 2>&1 | tail -10</automated>
  </verify>
  <done>
    [RequireWorkspaceRole] 三件套（attribute + handler + extensions）就位；handler 注册到 DI；4 个单元测试 case 全过；TenantIsolationTests 通过（隔离正确，member 不跨 workspace 泄露）。
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                    |
| --------------------------- | ---------------------------------------------- |
| HTTP → TenantId → DB filter | slug 解析 → TenantId → DbContext 自动过滤      |
| AuthN → Membership          | 认证后的 user 经中间件解析 workspace role      |
| Role → Capability           | `[RequireWorkspaceRole]` gate 在 endpoint 入口 |

## STRIDE Threat Register

| Threat ID        | Category               | Component                                          | Disposition         | Mitigation Plan                                                                                                                                     |
| ---------------- | ---------------------- | -------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-2-idor [HIGH]  | Information Disclosure | TenantIsolation（跨 workspace member 泄露）        | mitigate [BLOCKING] | ApplyTenantIsolationByDefault 自动 filter；TenantIsolationTests 守护（本 plan Task 3）；02-06 smoke 复测                                            |
| T-2-eop [HIGH]   | Elevation of Privilege | RequireWorkspaceRole handler 未正确拒绝            | mitigate [BLOCKING] | handler 显式 role=null → Fail；单元测试 4 case 守护（Admin/Member/Guest/null）；default-deny                                                        |
| T-2-memberskip   | Elevation of Privilege | WorkspaceMembershipMiddleware 未在 authz 之前跑    | mitigate            | WorkspaceModule Order=200 保证在 Authorization 之后注册 middleware；代码注释引用 D-02 + RESEARCH A6                                                 |
| T-2-migration    | Tampering              | 迁移漏加 TenantId 列 → 隔离失败                    | mitigate [BLOCKING] | 应用迁移后 `\d WorkspaceMembers` 验证含 TenantId；Workspaces 验证不含；本 plan Task 1 verify                                                        |
| T-2-migration2   | Tampering              | 迁移 Slug unique 索引漏 → 软删除后 slug 未释放失败 | mitigate            | 应用迁移后 `pg_indexes` 验证 Slug unique；本 plan Task 1 verify                                                                                     |
| T-2-sqlinject    | Tampering              | EF Core 迁移手写 SQL 注入风险                      | accept              | EF Core 生成的迁移 SQL 参数化；不手写拼接                                                                                                           |
| T-2-cacheinvalid | Information Disclosure | workspace 改名后 Redis slug→tenant 缓存陈旧        | mitigate            | 02-04 UpdateWorkspace handler 显式失效 cache key `ws:slug:{oldSlug}`（D-01 决策）；本 plan 不实现但 Task 2 中间件注释提示后续 handler 需 invalidate |
| T-2-SC           | Tampering              | NuGet EF tools                                     | accept              | EF Core 已在 Phase 1 锁定，dotnet-ef tool 信任 chain                                                                                                |

## Threat Disposition Summary

- **mitigate [BLOCKING]**: T-2-idor（隔离）、T-2-eop（authz handler）、T-2-migration（迁移 schema 正确）—— 必须在 Task 1/3 验证通过
- **mitigate**: T-2-memberskip、T-2-migration2、T-2-cacheinvalid（02-04 handler invalidate）
- **accept**: T-2-sqlinject、T-2-SC
- **HIGH severity**: T-2-idor、T-2-eop、T-2-migration —— 本 plan Task 1/3 自动化测试 + 02-06 smoke 双重守护
  </threat_model>

<verification>
- [ ] `dotnet build src/YH.Flow.slnx` 0 warning / 0 error（含迁移文件 + middleware + authz + 测试）
- [ ] InitialWorkspace 迁移应用到 postgres，3 张表存在，TenantId 列正确（Workspaces 不含，Members/Invitations 含）
- [ ] DbMigrator/Api Program.cs 数组含 WorkspaceModule
- [ ] `dotnet test src/Tests/Workspace.Tests --filter "SlugTenantResolve|MembershipMiddleware|RequireWorkspaceRole|TenantIsolation"` 全过
- [ ] Phase 1 Identity.Tests 412/412 仍全绿（MultitenancyModule 改动零回归）
</verification>

<success_criteria>

- EF 迁移应用 + schema 验证完成（schema_push_requirement 解除阻塞）
- D-02 中间件 + D-11 authz 在运行时工作（unit + integration test 守护）
- 跨 workspace 隔离验证通过（NFR-2 关键守护）
- Host 项目 wiring 完成，02-04/05 的端点 MapEndpoints 自动被发现
  </success_criteria>

<output>
Create `.planning/phases/02-workspace/02-03-SUMMARY.md` when done. 记录：(a) 迁移应用 timestamp + 验证查询结果（3 张表存在性）；(b) MultitenancyModule 改动行（确认零回归）；(c) WorkspaceMembershipMiddleware 在 Program.cs pipeline 的注册位置；(d) TenantIsolationTests 通过证据（截图或测试输出尾部）。
</output>
